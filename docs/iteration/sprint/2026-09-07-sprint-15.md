# Sprint 15（2026-09-07）—— F2 验收修复批次（交互信息类）+ TG-3

> 归属：`docs/iteration/sprint/`。启动依据：用户 2026-09-07 拍板排期（选项 1：Sprint-15 验收修复交互类 → Sprint-16 数据+成本 → 发版讨论）；问题台账见 `2026-08-31-f2-acceptance-findings.MD`。
> 关联卡：F-AC1/2/3/4/5/6/7/9（refactor-analysis backlog）+ TG-3（testing-governance backlog）。

## 1. Sprint Planning

- **Goal**：F2 验收 8 项问题中的**交互信息类**全部修复——字体统一、hints 限高+标题影响聚合、local 路径+条件隐藏、完成后收起、subcanvas 响应式、失败复制+报错详情、状态作用域、反向联动；TG-3（ATDD case-first 卡模板）随本 sprint 落地（与验收卡撰写天然配合）。
- **团队与产能**：1 人（AI 开发者），预估 **16 点 / ~2 个工作日**（2026-09-07 网络时间 UTC+8）。
- **验收演示**：逐卡跑 `gui_check_config_schema.mjs` / 新增 GUI 检查（复制按钮、收起行为、联动、条件隐藏）+ 截图留证；前端 build 通过；TG-1 规则自证（每卡带 verify case）。
- **备注**：F-AC8（provider 一文件+定时调研）、F-AC10（文献级 checkpoint）、TG-5（cron 回归）留 Sprint-16（数据准确性+成本韧性，13 点）。

## 2. Backlog（卡 + 验收标准）

| 卡号 | 内容 | 验收标准 | 点 |
|---|---|---|---|
| F-AC1 | 1.1 标题字号收敛 + 卡片内字体类型统一（token 化），排查全部卡片批量修 | ① 全部卡片标题/正文字体统一；② gui_check_config_schema 截图对比；③ 无其他卡片遗漏 | 2 |
| F-AC2 | 1.2 hints 限高滚动 + 影响上浮标题聚合（"LLM（6项，3项被改动）"改动数标红） | ① hints 区限高+滚动；② 标题聚合计数且改动数标红；③ GUI 检查断言 | 2 |
| F-AC3 | 1.3 新增 `local_dir` 字段（默认 data/pdf）+ 后端接线 + 数据源条件隐藏无关参数（schema show-if 语义）+ 排查其他卡片交互 | ① local 路径字段可改且生效；② local 模式隐藏 urls/arxiv/dois；③ 其他卡片同类条件隐藏排查完成 | 4 |
| F-AC4 | 2 节点运行完成后自动收起全部展开项，仅留 output_snapshot | ① 完成后仅 output_snapshot 展开（Q4 口径）；② GUI 断言 | 1 |
| F-AC5 | 3 subcanvas 桌面分辨率响应式自适应（手机暂不考虑） | ① 常见桌面分辨率下右侧卡片完整可见；② 截图留证 | 1 |
| F-AC6 | 4 失败一键复制 + 任意状态框选复制已完成卡片 + 主画布报错摘要+可展开完整堆栈 | ① 失败态复制按钮；② 框选复制可用；③ 主画布错误详情可展开（Q5 口径） | 3 |
| F-AC7 | 5 执行状态信息仅展示在执行节点下（作用域修复） | ① 切换节点后状态信息不残留；② GUI 断言 | 1 |
| F-AC9 | 7 subcanvas 手动切换节点 → 主画布联动定位（双向联动） | ① subcanvas 切节点主画布同步高亮；② GUI 断言 | 1 |
| TG-3 | P4 ATDD case-first：功能卡模板增"验收 case"节（实现前先写步骤+断言），本 sprint 各卡即按此撰写 | ① backlog 卡模板含验收 case 节；② 本 sprint 卡均先写 case 后实现 | 1 |

合计 **16 点**。

## 2.1 验收 case（TG-3：实现前先写步骤+断言；每卡实现后跑绿留证据）

> **TG-3 补充（走查 2026-09-10）**：每条验收 case 必须覆盖 **完整测试链路（正常路径全步骤）+ 边界情况（≥1 条异常/边界输入）**——单个 case 不足以证明功能正确；边界项不适用时须注明"无边界场景"并说明理由。

| 卡 | 验收 case（步骤 → 断言） |
|---|---|
| F-AC1 | ① 打开页面逐卡目检；② 断言：所有卡片标题字号一致（且 ≤ 当前最大值的收敛值）；所有卡片标题/正文字体族一致；③ 新增 `gui_check_s15_typography.mjs`：Playwright 遍历卡片标题/正文 computed style，断言 font-size/font-family 集合唯一性 |
| F-AC2 | ① 构造 ≥8 条 hints（dashscope 场景）；② 断言：hints 容器有 max-height 且内部滚动、卡片总高受限；③ 标题出现"LLM（6项，3项被改动）"且改动数 3 为红色；④ gui 断言 |
| F-AC3 | ① 切 local 模式；② 断言：出现 local_dir 字段（默认 data/pdf）；③ 断言：source_urls/arxiv/dois 字段不可见；④ 改路径为临时目录 → config 步骤实际索引该目录；⑤ 切 remote → 三字段恢复可见；⑥ 排查其余卡片 show-if 场景清单 |
| F-AC4 | ① 运行一节点至完成；② 断言：此前展开的所有小节自动收起，仅 output_snapshot 展开 |
| F-AC5 | ① 1280×720 与 1920×1080 两分辨率下打开 subcanvas；② 断言：右侧卡片完整可见无溢出（截图 ×2） |
| F-AC6 | ① 构造节点失败；② 断言：失败态显示"复制报错"按钮，点击后剪贴板含完整错误文本；③ 断言：完成态卡片任意状态可鼠标框选复制；④ 主画布节点错误提示 = 摘要，点击展开完整堆栈 |
| F-AC7 | ① 节点 A 运行失败产生状态行；② 切换到节点 B；③ 断言：状态行展示**B 的状态**（B 有状态→B 的完成/失败文案；B 从未运行→"B idle"），不残留 A 文案（走查 2026-09-10 语义修正） |
| F-AC9 | ① 在 subcanvas 手动切换到节点 X；② 断言：主画布对应节点高亮/定位同步 |
| TG-3 | 本表即验收 case 先行证据；实现后逐卡回填 §7 证据 |

## 3. 任务看板（Kanban）

| 卡片 | 状态 | 点 |
|---|---|---|
| F-AC1 字体统一 | ✅ DONE | 2 |
| F-AC2 hints+标题聚合 | ✅ DONE | 2 |
| F-AC3 local 路径+条件隐藏 | ✅ DONE | 4 |
| F-AC4 完成后收起 | ✅ DONE | 1 |
| F-AC5 subcanvas 响应式 | ✅ DONE | 1 |
| F-AC6 复制+报错详情 | ✅ DONE | 3 |
| F-AC7 状态作用域 | ✅ DONE | 1 |
| F-AC9 反向联动 | ✅ DONE | 1 |
| TG-3 ATDD case-first | ✅ DONE | 1 |

## 4. 燃尽图（Burn-down）

总点数 16。随每张卡推进更新。

## 5. 执行记录（Standup 日志）

- 2026-09-07 Sprint Planning（用户排期拍板）；发版实践预研并行落盘 `pre-research/tech/2026-09-07-release-practice.MD`。
- 2026-09-07 TG-3 DONE（§2.1 验收 case 全卡先行）；F-AC1 DONE：styles.css 排版 token 化（--font-ui/--font-mono/--fs-* 六级字号变量；node-title 14px→13px、block 标题统一 12px、全部标题字体族统一）；新增 `gui_check_s15_typography.mjs`（VERIFY_META 登记，3 断言全 PASS）+ 截图 f2-typography.png；回归 gui_check_config_schema/s4 PASS、build 324KB、矩阵 25 脚本 CHECK OK。
- 2026-09-07 F-AC2 DONE：分组 summary 升级为"LLM（6项，M项被改动）"且 M>0 标红（schema-changed-count）；hints/warnings 汇总限高 96px + 内部滚动；新增 `gui_check_s15_hints_title.mjs`（3 断言全 PASS）+ 截图 f2-hints-title.png；回归 typography/config_schema PASS、build 324KB、矩阵 26 脚本 CHECK OK。
- 2026-09-08 F-AC3 DONE：根因=paper_directory 字段在 Index 组（可改但入口反直觉）→ 移入数据源组（label 本地论文目录）+ schema 新增 **show_if 条件可见语义**（remote 三字段 `show_if: {data_source: remote}`）+ get_config_schema 透传 show_if + SchemaForm 过滤渲染（隐藏≠删除参数，summary 显示 可见/总数）；新增 `gui_check_s15_local_dir.mjs`（5 断言全 PASS）+ `verify_local_dir.py` 引擎接线实证（唯一文件名 LocalDirProbe.pdf 证明候选来自临时目录，3 断言 PASS）；回归 114 断言/typography/hints/config_schema PASS、build 324KB、矩阵 28 脚本 CHECK OK。**排查清单（⑥）**：数据源组 remote 三字段为唯一显式条件隐藏场景；manifest_file 两模式均可见（remote 下载后同样可增强）；provider 联动为值联动（删除/带出）非隐藏——无其他待隐藏场景。
- 2026-09-08 F-AC4 DONE：FlowNode 增加 running→success/failed 完成信号（collapseSignal），完成后 SchemaForm 分组全部收起、input_snapshot/function_trace 的 JsonTree 收起、**output_snapshot 保持展开**（collapsed={false}）；JsonTree/SchemaForm 增 collapsed prop；新增 `gui_check_s15_collapse.mjs`（4 断言全 PASS + 前后截图）；回归 local_dir/hints/typography PASS、build 325KB、矩阵 29 脚本 CHECK OK。
- 2026-09-09 F-AC7 DONE：步骤计时 tick 改造——运行中节点全展示（保留 M4 并发显示）、**冻结文案只在当前 activeStep 下展示**、空态写回清空（切到无状态节点不残留旧文案）；新增 `gui_check_s15_status_scope.mjs`（6 断言全 PASS：config/load_index 切换互不残留 + 切回复原）；回归 collapse/local_dir/typography/hints PASS、build 325KB、矩阵 30 脚本 CHECK OK。
- 2026-09-09 F-AC5 DONE：分栏溢出根因=`55%+8px+45%` 未减分隔条与 padding（1280 下溢出 8~18px 截断右卡）→ `minmax(320px, calc(pct% - 8px)) 8px minmax(340px, calc(pct% - 8px))` + fn-node-card max-width:100%；新增 `gui_check_s15_responsive.mjs`（1280/1366/1920 三档 × 分栏无溢出 + 5 张 fn 卡片完整可见，6 断言全 PASS + 三截图）；回归 status/collapse/local_dir PASS、build 325KB、矩阵 31 脚本 CHECK OK。
- 2026-09-09 F-AC9 DONE：主画布 ReactFlow 加 mainFlowRef + activeStepId 变化时选中对应节点并 fitView 居中（subcanvas 按钮切换 → 主画布联动定位）；新增 `gui_check_s15_bidi_link.mjs`（3 断言全 PASS：Retrieve/Answer 两次切换主画布选中跟随）；回归 status/responsive/collapse PASS、build 325KB、矩阵 32 脚本 CHECK OK。
- 2026-09-09 F-AC6 DONE：① 后端 StepResponse 增 `error_detail`（完整 traceback，含 run_records 存档）；主画布报错 = 摘要 + 可展开完整堆栈（Q5 口径），复制按钮带堆栈；② fn 卡错误全文不再截断 + 一键复制按钮（含降级路径）；③ fn-node-card `user-select:text` 允许框选复制；新增 `gui_check_s15_error_copy.mjs`（9 断言全 PASS：parse 坏候选构造失败 → 主卡/复制含 Traceback/fn 卡全文不截断/user-select）；全量回归 10 项 GUI + 114 断言 + smoke 8/8 + agentops 42 全绿、build 326KB、矩阵 33 脚本 CHECK OK。
- 2026-09-10 文档时间记录修正（六轮反馈）：§10 走查记录与 §8 工作日志日期误记——走查实为 09-10、F-AC3/4 实为 09-08、F-AC5/6/7/9 实为 09-09（以 git 提交时间为据）；全库排查同步修正（backlog/代码注释）；doc-audit spec 升级 v1.3.0 新增 timestamps 维度（时间记录准确性入文档审计）；3-LEARNED 1.51 回填。

## 6. Definition of Done

- [x] 每张卡有验收证据（截图 + 命令输出 + verify case 断言，§7）
- [x] 结论来自可复现证据
- [x] 记录关键权衡/理由/局限
- [x] 未破坏既有功能（10 项 GUI 检查 + 114 断言 + smoke 8/8 + agentops 42 全绿）
- [ ] 三查完成且发现项闭环（§9）——待用户走查验收后执行
- [ ] windows 已 PR 同步 main（不含 docs/iteration/）；main CI 绿

## 7. 项目管理与证据（Sprint Review 时填实）

| 卡 | 证据 | 通过情况 |
|---|---|---|
| TG-3 | §2.1 全卡验收 case 先行（步骤+断言）；本 sprint 各卡按此撰写 | ✅ |
| F-AC1 | `gui_check_s15_typography.mjs` 3 断言全 PASS（node-title 13px 一致 / block 标题 12px / 字体族唯一）；截图 `verify/f2-typography.png`；回归 gui_check_config_schema、gui_check_s4 PASS；build 324KB；矩阵 25 脚本 CHECK OK | ✅ |
| F-AC2 | `gui_check_s15_hints_title.mjs` 3 断言全 PASS（summary 形态"LLM（6项，1项被改动）"/改动数 rgb(220,38,38) 标红/hints 限高 96px+auto）；截图 `verify/f2-hints-title.png`；回归 typography、config_schema PASS；build 324KB；矩阵 26 脚本 CHECK OK | ✅ |
| F-AC3 | `gui_check_s15_local_dir.mjs` 5 断言全 PASS（local 显示本地论文目录/隐藏远程三字段/Index 组移除/remote 恢复/切回再隐藏）；`verify_local_dir.py` 3 断言 PASS（临时目录唯一文件名候选命中）；截图 `verify/f2-local-dir.png`；回归 114 断言 + typography/hints/config_schema PASS；build 324KB；矩阵 28 脚本 CHECK OK | ✅ | 实现中修一处：get_config_schema 未透传 show_if → 补透传（GUI 首跑抓到）；verify_local_dir 首跑候选为相对路径且与默认目录同名无法区分 → 改唯一文件名 |
| F-AC4 | `gui_check_s15_collapse.mjs` 4 断言全 PASS（运行前分组展开 2/完成后 0/output_snapshot 保持展开/input_snapshot 收起）+ 前后截图 f2-collapse-before/after.png；回归 local_dir/hints/typography PASS；build 325KB；矩阵 29 脚本 CHECK OK | ✅ | GUI 脚本改 DOM 级 click（按钮在视口外，Playwright 滚动不可靠）；build 与 dev server 并行会冲突 → 先停服务再 build（记入操作习惯） |
| F-AC7 | `gui_check_s15_status_scope.mjs` 6 断言全 PASS（config 完成显示/load_index 完成显示且无 config 残留/切回 config 换回且无 load_index 残留）；截图 f2-status-scope.png；回归 collapse/local_dir/typography/hints PASS；build 325KB；矩阵 30 脚本 CHECK OK | ✅ | 首跑 load_index 60s 超时（索引首次构建）→ waitDone 改双终态+150s；waitForFunction 返回字符串比较类型问题 → String() 化 |
| F-AC5 | `gui_check_s15_responsive.mjs` 6 断言全 PASS（1280/1366/1920 分栏右缘=视口宽 + 5 张 fn 卡片完整可见）；截图 f2-responsive-{1280x720,1366x768,1920x1080}.png；回归 status/collapse/local_dir PASS；build 325KB；矩阵 31 脚本 CHECK OK | ✅ | 探针实测定位根因（1280 溢出 18px）；fn 卡片需先点卡片切换 activeStep 且 load_index 依赖 config 先行（新会话直接跑会失败）→ 脚本顺序 config→load_index |
| F-AC9 | `gui_check_s15_bidi_link.mjs` 3 断言全 PASS（subcanvas 切 Retrieve/Answer → 主画布 .react-flow__node.selected 跟随 + active 按钮一致）；截图 f2-bidi-link.png；回归 status/responsive/collapse PASS；build 325KB；矩阵 32 脚本 CHECK OK | ✅ | |
| F-AC6 | `gui_check_s15_error_copy.mjs` 9 断言全 PASS（parse 坏候选 → 主卡摘要+可展开堆栈+复制含 Traceback；fn 卡全文不截断+复制完整；user-select:text）；截图 f2-error-copy.png；全量回归 10 项 GUI + 114 断言 + smoke 8/8 + agentops 42 全绿；build 326KB；矩阵 33 脚本 CHECK OK | ✅ | 定位两处：后端 error_detail 已返回但 FlowNode 解构 errorDetail（驼峰）与 data.error_detail（蛇形）错配 → 改 error_detail: errorDetail；parse 失败路径经单文件 except 再 raise → 外层 err_resp 统一携带堆栈 ✓ |

## 8. 回顾 Retro（Sprint Review 后填）

**做得好（Keep）** / **可改进（Improve）** / **行动项（Action）**

## 9. 评审/同步记录（三查结论 + PR，Sprint 关闭必填）

- **统一 PR**：待三查通过后创建（按 2026-08-31 用户决策：Sprint 内不逐卡 PR）。

## 10. 用户走查验收记录（2026-09-10）

> 走查会话于 2026-09-09 深夜启动、跨日至 09-10 凌晨（用户口径：2026-09-10 开始走查）。此前误记 09-07 已按 git 提交时间全库修正（六轮反馈）。

| # | 项 | 结果 |
|---|---|---|
| 1 | 字体统一 | ✅ PASS |
| 2 | 标题聚合+hints | ❌ 返工 → **已修复（三轮）**：① isChanged 无默认值字段计数；② JSON 编辑区 + SchemaForm 文本/密码/数字输入全部非受控（机制杜绝光标跳，hooks 置于早退前）；`gui_check_s15_cursor.mjs` 6 断言全 PASS |
| 3 | local 路径+条件隐藏 | ✅ PASS |
| 4 | 完成后收起 | ✅ PASS |
| 5 | 响应式 | ✅ PASS |
| 6 | 复制+报错详情 | ⚠️ 原逻辑通过；**追加需求已做（两轮）**：报错后 fn 画布定位报错卡——第二轮改 fitView 节点过滤 + DOM 实测坐标兜底（旧 setCenter 按存储 position 偏移到相邻卡），**实测 centerDelta=0 精确居中**；`gui_check_s15_error_copy.mjs` 12 断言全 PASS |
| 7 | 状态作用域 | ⚠️ 语义修正 → **已修（两轮）**：切换后展示切换后节点状态行，未运行显示 idle；**第二轮追加**：切回报错节点同样定位到 error 卡、切回正常节点恢复正常视图（fitFnView 按 active 状态分流，12 断言含"切回报错节点再次定位"） |
| 8 | 反向联动 | ❌ 返工 → **已修**：根因 = ReactFlow v11 实例须经 `onInit` 获取（我用 ref= 导致 fitView not a function 抛错）；改为 onInit + setCenter（节点 position 定位）；`gui_check_s15_bidi_link.mjs` 增 pageerror 守卫断言（4/4 PASS，零页面异常）；subcanvas 空白 = 未运行节点的预期空态（配合 7 的 idle 展示已可辨识） |
| 9 | ATDD case 先行 | ✅ PASS + **补充已落盘**：§2.1 增注"验收 case 须覆盖完整测试链路 + 边界情况（≥1 异常/边界；不适用须注明理由）"；TG-3 卡同步更新 |
| N1 | output 完整查看 | ❌ 新需求 → **已做（F-AC11，四轮定稿）**：output 复制全文 + answer 全文块 + 复制答案；**tooltip 按用户要求移除**（四轮："不好用"） |
| N2 | 论文截图清晰度 | ❌ 新需求 → **已做（F-AC12）**：runtime_trace 预览 0.45→1.0 缩放 + 尺寸护栏 0.4MB→1.2MB；`verify_f12_preview_res.py` 实证 612×792（旧 ~268×379，2.3×）；更深优化留 embed-optimization 预分割卡 |
| 10 | 三轮新逻辑/新问题（2026-09-10） | ✅ **全部已做**：① 点击运行节点自动定位卡片顶部；② 回车删卡禁用（deleteKeyCode 显式）；③ 输入控件 nodrag/nopan；④ 运行中切换计时 + 切回恢复；⑤ 冻结文案跨轮保留 | 实测全绿 |
| 11 | 四轮反馈（2026-09-10） | ✅ **全部已做**：① tooltip 移除；② **Backspace 也禁用**（deleteKeyCode=null，键盘删除整体关闭）；③ output/input_snapshot/function_trace 区域 **nodrag nopan + user-select:text**（不再显示抓手，可框选文字）；④ **fn 每步缓存直铺**（fnNodesByStepRef：切回瞬间还原、零重计，实测切回 300ms 内 74 张卡即时在场；新 run 重置缓存）；⑤ 报错定位复验（fitView 节点过滤 + DOM 兜底，centerDelta=0，进入/切回均正确） | 实测：cursor 7/7（含 Backspace 不删卡、output 框选）、status_scope 10/10（含零重计）、error_copy 12/12、bidi/collapse/typography、smoke 8/8、矩阵 36 全绿 |
| 12 | 五轮反馈（2026-09-10）：**多报错卡定位** | ⚠️ 返工 → **已做**：单次失败可能产生多张 error 卡（runtime_trace 对每个抛异常的追踪帧各记一条 error 事件）。自动定位改为按 call_id 升序取**最新一张**（errLocateIndexRef 记录下标，切换步骤重置）；新增 **"定位下一处报错（N）"** 按钮——报错卡 ≥2 张时显示，按时间逆序循环定位上一处（fitView 节点过滤 padding 0.35 + DOM 实测兜底，整卡居中）。单卡场景按钮隐藏（error_copy 脚本增 1 断言）；多卡循环已由用户走查实测，多卡自动化复现难以稳定构造（parse 首个坏文件即抛错中止），以手动走查记录为准 | 待复验 |
| 13 | 六轮反馈（2026-09-10）：**文档时间记录准确性** | ⚠️ 返工 → **已修**：§10 走查记录误标 09-07（实际 09-10 开始走查）→ 修正；§8 工作日志 6 条日期与 git 提交不符（F-AC3/4=09-08、F-AC5/6/7/9=09-09）→ 逐条修正；全库排查（refactor-analysis/testing-governance backlog、代码注释 6 处）→ 同步修正；**机制固化**：doc-audit spec v1.3.0 新增 timestamps 维度（时间记录准确性入文档审计）+ 3-LEARNED 1.51 | 待复验 |
