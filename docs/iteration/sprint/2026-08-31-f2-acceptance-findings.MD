# F2 验收问题台账（2026-08-31 用户验收反馈）

> 状态：**归类完成，待用户补充细节后开卡**。依据 `docs/iteration/sprint/2026-08-31-f2-acceptance-guide.MD` 验收流程。
> 判定（`1-WORKFLOW.MD` §4.4）：用户实测驱动的成批修复（8 项、合计约 18~23 点）→ **插队 Sprint**。

## 1. 问题归类（6 类）

| 类 | 问题 | 现状归因（已排查） | 初步方案方向 | 点估 |
|---|---|---|---|---|
| A UI 一致性与布局 | 1.1 标题字号偏大、卡片内字体不统一 | 各卡片样式局部定义，无统一排版规范 | 统一字号/字体 token + 排查全部卡片批量修 | 1~2 |
| A | 1.3b 数据源=local 时未自动隐藏无关参数（urls/arxiv/dois 等） | SchemaForm 无字段级"条件可见"规则 | schema 增加 show-if 语义 + 排查其他卡片同类问题（provider 联动已有先例） | 2~3 |
| A | 1.3a local 模式无本地路径配置入口 | **已确认**：数据源组只有 `data_source` enum + 3 个远程源字段，无 local 目录字段（实际写死 data/pdf） | 新增 `local_dir` 字段（默认 data/pdf）+ 后端接线 + 路径安全校验 | 并入 1.3（+1） |
| A | 3. subcanvas 右侧卡片分辨率下显示不完整 | 布局固定宽度未自适应 | 桌面分辨率响应式（flex/grid 自适应，手机暂不考虑） | 1~2 |
| B 信息架构与状态 | 1.2a 提示行过多卡片被拉长 | hints 列表无高度上限 | 限高 + 内部滚动 | 2 |
| B | 1.2b 影响内容未上浮到标题（示例"LLM（6项，3项被改动）"数字3标红） | impacts 仅以行内提示呈现 | 标题聚合计数 + 改动数标红 | 并入 1.2 |
| B | 2. 节点运行完成后收起所有展开项，仅展开 output_snapshot | 完成后 UI 状态保持原样 | 完成回调统一收敛（口径待用户确认） | 1 |
| B | 5. 执行状态信息（"load_index 失败 46.6s…"）切换到其他节点仍展示 | 状态文本挂载/清除作用域 bug | 状态信息仅绑定执行节点，切换即清 | 1 |
| C 复制与错误信息 | 4. 失败无法复制报错 + 报错展示不全 + 主画布报错无参考价值 | subcanvas 失败态无复制控件；主画布只透传简略信息 | 失败态一键复制按钮 + 任意状态允许框选复制已完成卡片 + 主画布报错透传详情（程度待用户确认） | 2~3 |
| D Provider 数据准确性 | 6. 切换 provider 的模型/embedding 未严格依据官网最新文档（dashscope embedding 官方最新 v3 而自动配 v4；LLM 自动选 25 年 qwen-omni-turbo） | **已确认**：`provider_config.py` 手写策展（注释"最新策展（2026-08）"失实） | **用户方案（Q1）**：provider_config 拆为**一 provider 一文件**，切换 provider 加载对应文件；tech-research 调研官网后更新文件，**定时更新默认每两周一次**（主动跑调研子代理阅读官网并更新）——与 M9 定时更新机制共用调度底座 | 6（拆文件 2~3 + 首轮调研 2 + 定时机制 1~2） |
| E 画布联动 | 7. subcanvas 手动切换节点时主画布不联动 | 仅主画布→subcanvas 单向联动 | 双向联动（subcanvas 切节点 → 主画布高亮定位） | 1 |
| F 成本韧性 | 8. 文献超 embedding 额度致节点失败：需已完成 embedding 本地留存 + 断点续跑省成本；并作为持续迭代主题 | parse_chunk_embed 中断后已完成部分未持久化复用 | **文献级 checkpoint（Q2 已定）**：每篇文献 embed 完成即落盘，失败后只重跑未完成文献；持续迭代主题 | 4~5（首版） |

合计：**约 24~26 点** → 拆 2~3 个插队 Sprint（交互信息类 F-AC1/2/3/4/7/9 先行，数据准确性+成本韧性 F-AC8/10 与响应式/复制报错 F-AC5/6 其后；开工时按依赖与产能定拆分）。

## 2. 用户补充细节（2026-08-31 已答复）

- **Q1**：tech-research 调研后更新，provider_config 改**一 provider 一文件**，切换即加载对应文件；**定时更新默认每两周一次**（主动跑调研子代理阅读官网并更新）
- **Q2**：**文献级 checkpoint**
- **Q3**：**底座批次优先**（底座批次开工后，验收修复批次紧随）
- **Q4**：运行完成后**全部收起**，仅留 output_snapshot
- **Q5**：主画布报错 = **摘要 + 可展开完整堆栈**

## 3. 排期（Q3 后确定）

1. **Sprint-14（底座批次，13 点，即刻开工）**：TG-1 → M15 → TG-2 → TG-4 → M10 → M14 → M9
2. **验收修复批次（拆 2~3 个插队 sprint，约 24~26 点，Sprint-15 起）**：交互信息类（F-AC1/2/3/4/7/9）→ 数据准确性+成本韧性（F-AC8/10）+ 响应式/复制报错（F-AC5/6）；开工时按依赖与产能定拆分
3. 问题 6（F-AC8）的定时更新机制与 M9 共用调度底座

## 4. 决策记录

| 时间 | 决策 |
|---|---|
| 2026-08-31 | 用户完成 F2 验收，提交 8 项问题；指示先分析归类、补充细节后开卡开发迭代 |
| 2026-08-31 | Q1~Q5 答复（见 §2）；排期=底座批次优先（Sprint-14 即刻开工，验收修复批次其后） |
