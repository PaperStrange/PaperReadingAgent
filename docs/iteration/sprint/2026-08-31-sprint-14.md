# Sprint 14（2026-08-31）—— 底座提前批次（领域壳 / 测试矩阵 / 账本锁 / 价表抓取）

> 归属：`docs/iteration/sprint/`。启动依据：用户拍板"底座批次优先"（F2 验收 Q3，2026-08-31）；F2 验收问题另立台账 `2026-08-31-f2-acceptance-findings.MD`（验收修复批次紧随其后）。
> 关联卡：TG-1/TG-2/TG-4（testing-governance）、M9/M10/M14/M15（refactor-analysis）；批次依据 `pre-research/2026-08-31-domain-governance.MD` §6。

## 1. Sprint Planning

- **Goal**：底座提前批次 ①~⑦ 全部完成——① 测试义务规则（TG-1）→ ② 领域治理壳（M15）→ ③ 覆盖矩阵 SSOT（TG-2）→ ④ provider e2e 去漂移（TG-4）→ ⑤ 账本并发锁（M10）→ ⑥ 上线评审 agent（M14）→ ⑦ 价表自动抓取（M9）。
- **团队与产能**：1 人（AI 开发者），预估 **13 点 / ~13h**。
- **周期（时间盒，必填）**：**2026-08-31 起（网络时间 UTC+8，约 1~2 个工作日）**；超时预案：与用户确认后拆批（三选一）。
- **验收演示**：① 反例演练（缺 verify case 的改动被三查拦下）；② `verify_matrix.py` check CI 断言；③ 去漂移后 dashscope+deepseek 双跑绿；④ 账本并发锁测试；⑤ M14 spec 上线 + validate；⑥ M9 抓取实测更新 prices.json。

## 2. Backlog（卡 + 验收标准）

| 卡号 | 内容 | 验收标准 | 点 |
|---|---|---|---|
| TG-1 | P1 成文规则：DoD+三查增补"用户可见功能必须同 PR 交付 verify case + 矩阵登记" | ① 规则文本入 1-WORKFLOW §4.2/§4.4；② 反例演练：构造缺 case 的改动被三查拦下并留证据 | 1 |
| M15 | 领域治理壳：product/marketing 笔记目录 + 追溯索引登记 + fanout 路由预留 | ① 目录/索引就绪；② fanout research 步支持领域路由参数（预留，不建空 agent） | 1 |
| TG-2 | 覆盖矩阵 SSOT：脚本头部元数据 + verify_matrix.py 生成/校验 + TEST-MATRIX.MD + CI 离线断言 + 全量登记 | ① 矩阵生成且 check 模式 CI 断言"矩阵==脚本元数据"；② 全部 verify 脚本登记（tier/provider/估时） | 4 |
| TG-4 | provider e2e 三脚本去漂移：参数化共享基座，修复 verify_e2e_openai 潜在 1.46 bug | ① 三脚本共享基座；② dashscope+deepseek 双跑绿（openai 因 billing 仅静态断言） | 2 |
| M10 | fetch-spec 成功路径测试（sha256 命中/失配）+ 账本并发锁 | ① fetch-spec 用例断言；② 并发写账本 last-writer-wins 有测试覆盖 | 2 |
| M14 | 新 agent 上线评审 agent（spec 合规/权限/模板一致性） | ① spec 落地 + validate-spec；② 对现有 7 个 spec 跑一遍评审出报告 | 2 |
| M9 | 价表自动抓取：provider 官网固定 URL + prices.json 更新 + 定时刷新机制 | ① 抓取脚本实测产出正确单价；② prices-derive 兼容；③ 定时机制（与 F-AC8 共用调度底座，本卡先落地手动/脚本入口） | 1 |

合计 **13 点**。

## 3. 任务看板（Kanban）

| 卡片 | 状态 | 点 | 开始 | 完成 |
|---|---|---|---|---|
| TG-1 测试义务规则 | ✅ DONE | 1 | 08-31 | 08-31 |
| M15 领域治理壳 | ✅ DONE | 1 | 08-31 | 08-31 |
| TG-2 覆盖矩阵 SSOT | ✅ DONE | 4 | 08-31 | 08-31 |
| TG-4 provider 去漂移 | ✅ DONE | 2 | 08-31 | 08-31 |
| M10 账本并发锁 | ✅ DONE | 2 | 08-31 | 08-31 |
| M14 上线评审 agent | ⬜ 待办 | 2 | — | — |
| M9 价表自动抓取 | ⬜ 待办 | 1 | — | — |

## 4. 燃尽图（Burn-down）

总点数 13。随每张卡推进更新。

## 5. 执行记录（Standup 日志）

- 2026-08-31 Sprint Planning；开卡 TG-1。
- 2026-08-31 TG-1 DONE：规则入 1-WORKFLOW §4.2（二查内容 + 证据 checklist + Self-check），反例演练完成（§7）。
- 2026-08-31 M15 DONE：product/marketing 壳目录（README 注明启用时机）+ 追溯索引更新 + fanout v4 domain 路由预留。
- 2026-08-31 用户决策（PR 节奏）：改"逐卡 PR"为 **Sprint 关闭（三查通过）后统一 PR**；已建 PR #46/#47 关闭并入关闭时统一 PR；1-WORKFLOW §3 增补「同步节奏」规则（Blocker/Critical 例外可即时同步）。
- 2026-08-31 同步流程定稿（含 push windows 节奏 + CI 口径 + concurrency 去重），入 1-WORKFLOW §3。
- 2026-08-31 TG-2 DONE：24 个 verify 脚本注入 `VERIFY_META` + `verify_matrix.py`（derive/check，compile 级校验）+ `TEST-MATRIX.MD` 生成 + CI 离线套件接入 + verify/README 登记。
- 2026-08-31 TG-4 DONE：新增共享基座 `verify/e2e_common.py`（服务生命周期 + config 参数构造恒显式 provider/vision_model + 6 步驱动器），三脚本重写为薄入口；实测 dashscope 双 phase PASS + deepseek 单脚本 PASS；openai 脚本静态编译通过（1.46 由构造杜绝）。
- 2026-08-31 M10 DONE：agent-ops.py 增账本互斥锁（msvcrt/fcntl 文件锁 + 写命令装饰器，load→mutate→save 全程持锁）；sha256 比对抽为 `_match_sha256` 纯函数；verify_agentops 增 UC-11（sha256 命中/失配）+ UC-12（6 进程并发 register 无丢失更新），35 断言全 PASS。

## 6. Definition of Done

- [ ] 每张卡有验收证据（命令输出 + 截图，§7）
- [ ] 结论来自可复现证据
- [ ] 记录关键权衡/理由/局限
- [ ] 未破坏既有功能（三查按 fanout 五步）
- [ ] 三查完成且发现项闭环（§9）
- [ ] windows 已 PR 同步 main（不含 docs/iteration/）；main CI 绿

## 7. 项目管理与证据（Sprint Review 时填实）

| 卡 | 证据 | 通过情况 | 理由/说明 |
|---|---|---|---|
| TG-1 | 规则文本：1-WORKFLOW §4.2 二查内容列 + 证据 checklist 新增"功能-测试配对"行 + Self-check 新增 TG-1 条目（缺失不得关闭，豁免须用户确认） | ✅ | 规则即既有实践的制度化 |
| TG-1 反例演练 | **反例**：合成场景"PR 新增 `/api/config_schema` 端点但未附任何 verify case" → 按新规则核查：用户可见功能（新端点）→ 未交付 verify case 且无豁免 → **拦截，不得关闭**。**正例对照**：远程数据源→verify_remote_e2e.py、provider 切换→verify_provider_switch.py、F2 schema→verify_config_schema.py、看板 fanout→gui_check_dashboard_fanout.mjs、dashscope→verify_e2e_dashscope.py 全部成对 | ✅ | 规则对既有实践零新增负担，只堵缺口 |
| M15 | `pre-research/product/README.md`、`pre-research/marketing/README.md` 壳目录（启用时机=对应领域讨论启动）；追溯索引状态更新；`agents/fanout.json` v4——research 步新增 `domain` 路由预留（默认 tech，product/marketing 经用户批准切换，不建空 agent） | ✅ | 壳就位、血肉不提前；fanout v4 JSON 解析通过 |
| TG-2 | 24 个脚本注入 `VERIFY_META`（13 py + 11 mjs）；`verify/verify_matrix.py` derive/check 双模式（compile 级校验 + `newline="\n"` 显式 LF 写盘）；`verify/TEST-MATRIX.MD` 生成入库；CI 离线套件加 `verify_matrix.py check`；verify/README 登记 | ✅ | 实测：derive 24/24、check OK、verify_smoke 8/8、verify_config_schema 114 断言、gui_check.mjs node --check OK；中途修三处缺陷：__future__ 放置、gui_check.mjs 前缀漏匹配、**Windows 文本模式写盘换行翻译致整文件重排（3-LEARNED 1.47，改二进制写盘 + git diff --stat 核对变更量）** |
| TG-4 | 共享基座 `verify/e2e_common.py`：`build_config_params`（恒显式 provider/api_base/model/vision_model/embedding——1.46 由构造杜绝）、`start_backend/wait_healthy/stop_backend`、`step/full_pipeline`（6 步）、二进制 `write_results`；三脚本重写为薄入口（deepseek 单 phase / dashscope 双 phase / openai full+mixed-switch） | ✅ | 实测：py_compile ×4 OK；`verify_e2e_dashscope.py` 双 phase PASS（answer 1697/1684 chars）；`verify_e2e.py` PASS（answer 1482 chars，10 contexts）；openai 因 billing 静态断言（`_cfg`+`build_config_params` 保证 vision_model 显式） |
| M10 | agent-ops.py 账本互斥锁：`_registry_lock`（msvcrt/fcntl 文件锁，`agents/runtime/.registry.lock` gitignore）+ `_with_registry_lock` 装饰器包装 register/update/finish（异常自动释放）；`_match_sha256` 纯函数（成功路径受 SSRF 防护离线不可达，抽纯函数供进程内断言）；verify_agentops 增 UC-11/UC-12 | ✅ | 实测：verify_agentops **35 断言 ALL PASS**（UC-12 六进程并发 register 6/6 在账 + 完整性有效）；verify_smoke 8/8 回归；矩阵 check OK |

## 8. 回顾 Retro（Sprint Review 后填）

**做得好（Keep）** / **可改进（Improve）** / **行动项（Action）**

## 9. 评审/同步记录（三查结论 + PR，Sprint 关闭必填）

- **统一 PR**：待三查通过后创建（含本 Sprint 全部共享变更：1-WORKFLOW §3 同步节奏 + §4.2 TG-1 规则、fanout v4、后续 TG-2/4、M10/14、M9 的共享文件）；按 2026-08-31 用户决策，Sprint 内不逐卡 PR。
