# Sprint 2（2026-08-29）—— 前置重构：解耦与地基（行为不变）

> 归属：`docs/iteration/sprint/`。输入：`../refactor-analysis.MD`。
> 范围：windows 分支迭代；最终同步 main 的 windows 部分；**MAC 验证跳过**。
> 完成标准：**结构解耦、行为不变**（以回归证据证明）。

## 1. Sprint Planning

- **Goal**：在不改变任何可观察行为的前提下，把后端从"单文件+全局状态"解耦为 表现/API/编排/引擎适配/基础设施 五层，并建立配置唯一真源（策展 schema）与统一事件模型，为后续功能 Sprint 打地基。
- **团队与产能**：团队 = 1（AI 开发者）。工作量以**故事点 + 预估工时**双轨估算；本 Sprint 预估 **52 点 / ~33h**，按任务增量推进并逐日更新燃尽。
- **Sprint 周期**：2026-08-29 起，直至 Backlog 全部 DONE 且回归证据齐备（不设硬性日历截止，以 DoD 为准）。

## 2. Backlog（用户故事 + 验收标准 + 估算）

| ID | 用户故事 | 验收标准 | 点数 | 预估工时 |
|---|---|---|---|---|
| US-2.1 | 配置唯一真源：从 paperqa `Settings` 自动抽取 + 策展覆盖生成 schema（分组/标签/影响提示） | ① `config_schema.json` 可生成/校验；② 分组（LLM/Embedding/Parsing/Agent/Answer/Index）与 `impacts` 元数据存在；③ 失配/非法配置给出清晰报错；④ 现有 `build_settings` 行为不变 | 13 | 8h |
| US-2.2 | 编排层解耦：`backend/main.py` 拆分 routes / orchestration，6 节点逻辑迁移不改行为 | ① 目录分层清晰（API 路由、编排、领域类型分离）；② 6 节点输入输出与现在完全一致 | 13 | 8h |
| US-2.3 | 事件模型：pydantic `StepEvent`/`AgentEvent` 统一抽象（普通流水线/Agent 共用），本期完成类型与最小接线 | ① 类型定义+文档；② 现有 SSE 走新事件类型（字段兼容）；③ 回归通过 | 8 | 5h |
| US-2.4 | SessionStore：接口 + `MemorySessionStore` 替代 `SESSIONS` 全局字典，DI 注入 | ① 接口（get_or_create/get/reset）；② 内存实现；③ 行为不变 | 5 | 3h |
| US-2.5 | EngineAdapter：接口（config/index/query/add_doc/evidence/answer/run_agent/version）+ `LocalVendorAdapter` 包住现有 paperqa 调用 | ① 接口定义；② 现有调用经 adapter 透传，行为不变 | 8 | 5h |
| US-2.6 | 回归验收 + 文档（3-LEARNED 更新、项目管理与证据、Retro） | ① smoke / embed_load / e2e 证据；② burn-down 收尾；③ Retro 完成 | 5 | 4h |

## 3. 任务看板（Kanban：开卡/推进/完成）

| 卡片 | 状态 | 点 | 开始 | 完成 |
|---|---|---|---|---|
| US-2.4 SessionStore | ✅ DONE | 5 | 08-29 | 08-29 |
| US-2.3 事件模型 | ✅ DONE（类型+文档；SSE 接线并入 US-2.2） | 8 | 08-29 | 08-29 |
| US-2.1 配置 SSOT | ✅ DONE | 13 | 08-29 | 08-29 |
| US-2.5 EngineAdapter | ✅ DONE | 8 | 08-29 | 08-29 |
| US-2.2 编排层解耦 | ✅ DONE | 13 | 08-29 | 08-29 |
| US-2.6 验收+文档 | ⬜ TODO | 5 | | |

> 开卡规则：一次只开一张卡并推进到 DONE（或明确 BLOCKED + 理由）；每张卡完成后跑对应回归并记录证据。

## 4. 燃尽图（Burn-down，随进展更新）

总点数 52。剩余点数趋势（任务完成即更新）：

| 里程碑 | 剩余点 | 说明 |
|---|---|---|
| Sprint 开始 | 52 | 2026-08-29 |
| US-2.4 DONE | 47 | SessionStore 迁移，行为不变 |
| US-2.3 DONE | 39 | 事件模型类型 + 文档 |
| US-2.1 DONE | 26 | 配置 SSOT schema + validate_config 接线（追加 config_notes） |
| US-2.5 DONE | 18 | EngineAdapter + LocalVendorAdapter 透传 |
| US-2.2 DONE | 5 | 编排层解耦：main.py 纯路由 + app/orchestration.py |

```text
剩余
52 ┤●
47 ┤ ●
39 ┤  ●
26 ┤   ●
18 ┤    ●
 5 ┤     ●
   │
 0 └───────────────────────── 时间
     开始 → US-2.4 → US-2.3 → US-2.1 → US-2.5 → US-2.2 →（US-2.6 收尾）
```

## 5. 执行记录（Standup 日志）

- 2026-08-29 Sprint Planning 完成；开卡 US-2.4 + US-2.3。
- 2026-08-29 US-2.4 DONE：`app/session_store.py`（SessionStore 接口 + MemorySessionStore，dict 风格兼容）替换 main.py 全局 `SESSIONS` 字典；回归证据见 §7。
- 2026-08-29 US-2.3 DONE（类型）：`app/events.py` 定义 `StepEvent/AgentActionEvent/AgentToolEvent/FunctionTraceEvent/ErrorEvent` 统一事件模型；SSE 协议接线随 US-2.2 编排解耦完成。
- 2026-08-29 US-2.1 DONE：`app/config_schema.py` 策展六组 schema（LLM/Embedding/Parsing/Agent/Answer/Index）+ 从 `Settings.model_fields` 自动提取默认值 + `validate_config`（errors/warnings/hints 中文文案与影响提示）；`main.py` config 步骤输出追加 `config_notes`（只增不减）；产物 `docs/iteration/sprint/config_schema.json`；回归证据见 §7。
- 2026-08-29 US-2.5 DONE：`app/engine.py` `EngineAdapter` 接口（config/index/query/add_doc/evidence/answer/run_agent/version）+ `LocalVendorAdapter` 原样透传；`main.py` 全部引擎函数调用改经 `ENGINE`（`build_settings` 保留为薄包装供 verify 脚本）；行为不变，回归证据见 §7。
- 2026-08-29 US-2.2 DONE：`app/orchestration.py` `PipelineOrchestrator`（StepRequest/StepResponse + 六步逻辑 + embed 缓存 + RuntimeTracer 接线）接管编排；`main.py` 瘦身为纯路由层（app/CORS/RunEventBroker/SESSIONS/8 路由）；SSE 事件改经 `app/events` 模型构造（`FunctionTraceEvent`/`StepEvent`），线字段与拆分前逐一比对一致；回归证据见 §7。

## 6. Definition of Done（每张卡与整个 Sprint）

- [ ] 行为不变：对应回归证据（smoke 8/8、verify_embed_load、关键 API 快照）
- [ ] 分层/命名符合 `refactor-analysis.MD` 目标架构
- [ ] 新边界类型化（pydantic），无复制粘贴的配置/常量
- [ ] 文档同步：`3-LEARNED.MD`（经验教训）、本 Sprint 文档
- [ ] 无密钥/安全回归；提交规范（`type: 说明`）

## 7. 项目管理与证据（Sprint Review 时填实 —— 图文 + 理由）

按 `docs/1-WORKFLOW.MD` §4.2：每条 US 需截图/命令输出 + 理由，放入本板块；Self-check 全勾才可关闭。

### Backlog 验收情况

| US | 证据 | 通过情况 | 理由/说明 |
|---|---|---|---|
| US-2.4 | 命令输出：`py_compile OK`；`verify_smoke 8/8`；`verify_embed_load` 三步与重构前一致 | 通过 | `SessionStore` 接口+内存实现，dict 风格兼容使 main.py 最小迁移；`get_or_create_session`/`reset_session` 走 store 方法 |
| US-2.3 | 代码/文档：`app/events.py`（5 类事件 pydantic 模型 + `EventKind` 枚举 + 文档） | 通过（类型部分） | 普通流水线/Agent 共用事件头 `BaseEvent`（session_id/run_id/trace_id）；`ErrorEvent.detail` 承载原始 traceback 作为一手证据；SSE 接线并入 US-2.2 |
| US-2.1 | 产物：`docs/iteration/sprint/config_schema.json`（生成+一致性检查通过）；命令输出：`py_compile OK`、`verify_smoke 8/8`、config 步骤 `config_notes` 校验样例（errors/warnings/hints） | 通过 | 六组 schema 满足 DoD②；自动提取：`multimodal=1`、`evidence_k=10`、`answer_length` 均来自 `Settings.model_fields`；`config_notes` 为追加字段，`build_settings` 原输出不变（DoD④） |
| US-2.5 | 命令输出：`py_compile OK`；`verify_smoke 8/8`；`verify_provider_switch` 三服务商 `build_settings` 输出一致 + deepseek 实际连通 SUCCESS；`verify_embed_load` 三步 run/load/cache-load 与基线一致（30.5s/0.0s/0.0s，texts=23） | 通过 | `app/engine.py` 接口含 DoD①全部方法；引擎函数调用（index/query/add_doc/evidence/answer）全部经 `ENGINE` 透传，`main.build_settings` 保留为薄包装（verify 脚本兼容）；`SearchIndex` 的 `index_files`/`index_name` 为返回对象上的属性读取，保持原位 |
| US-2.2 | 命令输出：`py_compile OK`；`verify_smoke 8/8`（8 路由完全一致）；`verify_e2e` 全六步（config 0.1s → load_index 0.0s → retrieve 0.0s → parse_chunk_embed 32.6s/60 funcs → evidence 15.4s/10 contexts → answer 13.0s/1661 chars）；`verify_embed_load` 三步（32.0s/0.0s/0.0s，texts=23）；SSE 线字段单元比对（function_trace 17 键、step_done ok/fail 键集与拆分前一致） | 通过 | `main.py` 557 行 → 纯路由层 219 行；六步逻辑逐字迁移 `app/orchestration.py`（StepRequest/StepResponse/embed 缓存/RuntimeTracer 全部随迁）；US-2.3 的 SSE 接线在本次完成：事件经 `FunctionTraceEvent`/`StepEvent` 构造，`model_dump` 排除 ts/trace_id（/output/error 视成功与否）后与线上协议逐字段一致；编排层通过 `on_event` 回调发布、`RunEventBroker` 留在路由层（传输职责） |

### 权衡与理由 / 边界与回归 / 关闭 Self-check

- 行为不变证据：冒烟 8/8；embed_load 三步（run 68.4s / load 0.0s / 新会话 cache 0.0s）与重构前一致。
- 理由：US-2.3 的 SSE 接线推迟到 US-2.2（编排拆分层），避免在单文件内再叠协议改动——符合 Strangler Fig 渐进切分的节奏。
- US-2.1 设计取舍：`Settings.model_json_schema()` 因 `AsyncContextSerializer` 抛 `PydanticInvalidForJsonSchema` 而不可用 → 改用 `model_fields` 逐段导航提取默认值（失败回落策展 default）；策展 `default` 优先于框架默认（app 有效默认如 `temperature=0.1`、`use_doc_details=False` 与 `build_settings` 一致）；`readonly` 字段（multimodal/evidence_k/answer_length/rebuild_index/manifest_file）如实标注"表单暂不可改"，避免误导为可用控件。
- 边界：`MemorySessionStore` 为单机实现，多 worker 共享需换 Redis 等实现（接口已预留）。

（关闭 Self-check 在 Sprint Review 时统一勾选）

## 8. 回顾 Retro（Sprint Review 后填）
