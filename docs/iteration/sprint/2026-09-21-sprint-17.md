# Sprint 17（2026-09-21 启动）—— 治理批 G1：规则落地（把"规则"变成可执行检查点）

> 归属：`docs/iteration/sprint/`（**windows-only**，不随 `main` 同步）。
> **启动依据**：用户 2026-09-21 审定治理批计划 D1~D8（[`2026-09-21-governance-batch-plan.MD`](2026-09-21-governance-batch-plan.MD)）→ **G1 = Sprint-17**（G2 = Sprint-18 载体与验证可信度 + MM 并行；G3 = Sprint-19 观察与度量 + 用起 MM）。
> **关联卡**：`TG-11`（规则绕过复盘 + 可执行闸门）、`TG-6`（验收方式改进）、`TG-13`（账本测量化）、`TG-14`（一卡一文档）、`TG-8`（端口自检 + 离线开关）、`TG-9`（产物目录约定）、**`TG-17`（闸门可信化收尾：覆盖窗口语义 + C1 判定域，2026-09-23 用户批准开卡）** ＋ **搭车项** `F-AC13`（运行按钮）、`F-AC14`（复制串台）。
> **前置**：① §2.4 依赖引入核查 → **条件通过**（一手证据 [`pre-research/tech/2026-09-21-lint-deps-security-check.MD`](../pre-research/tech/2026-09-21-lint-deps-security-check.MD)）；② **开工〇查**（impact-assessment）→ 见 §0。
> 术语：`台账` 仅指**独立文件**（`1-WORKFLOW.MD` §6 文档规范）；卡正文以 `phases/<阶段>/backlog.MD` 的卡片为准（`TG-14` 落地后迁到 `phases/<阶段>/cards/<卡号>.md`），**本文件不复制卡正文**。
> **执行会话（2026-09-25 起，用户裁定）**：本 Sprint 由**本会话单一负责**；此前 **D2/D3 由另一会话执行**，其 15 个本地提交**已收归、待独立审核**（分析见 [`2026-09-25-d2d3-parallel-session-quality-analysis.MD`](../../phases/testing-governance/2026-09-25-d2d3-parallel-session-quality-analysis.MD)）。**债**："执行会话"须成为一等字段（账本 run + 本文件 §1），并入 `TG-15` 的所有权/覆盖闸门。
> **日期口径更正（2026-09-23）**：本文件与治理批计划**起草于会话内（原标 2026-09-21）**，但**实际落库与开工为 2026-09-23（UTC+8）**——依据：① 网络时间核验（HTTP `Date` 头 → UTC+8 = `2026-09-23 01:30`）；② git committer 时间（本批提交均为 `2026-09-23`）。**原标 09-21 系沿用会话起始日期所致**，已记 §10（`R2` 告警），规则已补进 `1-WORKFLOW.MD` §6（日期每轮核验，不重命名文件）。

## 0. 开工〇查（impact-assessment）

- **run id**：`agents/runs/run-2026-09-21-impact-assessment-061/`（`impact-assessment.report.md` + `context.md` + `reasoning.md`；账本已 register + finish，`succeeded`，`rounds=1`）。
- **实测自检（非引用 spec）**：`N_core = 13`（16 条"文件+符号"探测全 True，差异 none）；`N_routes = 12`（`backend/main.py` L109/114/120/136/142/152/159/181/187/196/208/236；全仓无第二 `@app.*` 宿主；旁证 `verify_smoke.py` 的 12 条双向集合断言）。
- **计划阶段指标**（`planned`，非 diff）：`A = 3/13 = 23.1%`（#9 GUI 画布与面板←`FlowNode.jsx`；#10 验收套件←4 个新 `verify_*.py` + 2 个新 `gui_check_*.mjs` + `e2e_common.py`；#11 三查系统&AgentOps←`scripts/agent-ops.py` + `agents/functions/*.md`）；`B = 0/12 = 0.0%`（`backend/main.py` / `orchestration.py` / `provider_config.py` 计划零改动）；`composite = (0.8×0.2308 + 0.2×0)×100 = 18.5` ≤ X=50 → **tier = narrow**。
- **敏感度（关闭期必须按真实 diff 重跑本〇查的量化理由）**：若关闭期触碰 `backend/main.py`（B=1.0）→ 38.5 仍窄档；若 `orchestration.py` 同时被改（A=6/13）且触碰 `main.py` → **56.9 翻 full 档**。
- **recommended_scope（窄档 = Sprint-17 修改文件 ∪ 核心区域整体）**：① `[S]` `frontend/src/FlowNode.jsx`、`verify/` 四个新闸门（`verify_close_readiness` / `verify_ledger_measurement` / `verify_card_index` / `verify_lint`）+ 2 个新 `gui_check_*` + `verify/e2e_common.py`、`scripts/agent-ops.py`；② `[S]` `agents/functions/{code-review,impact-assessment,_template-agent}.md`、`agents/README.md`、`.github/workflows/ci.yml`、`.gitignore`、`verify/{README.md,TEST-MATRIX.MD,run_suite.py,verify_smoke.py}`，并复跑 `gui_check_s5/s7/s15_output_view/s15_error_copy`；③ `[S]` 文档：`docs/1-WORKFLOW.MD`、本文件、`phases/testing-governance/cards/TG-*.md`（**14 卡** → 2026-09-23 新开 TG-15/TG-16 后为 **16 卡**）+ `backlog.MD` 瘦索引；④ `[R]` 核心区域整体必含；⑤ `[R]` 邻近回归面：`main.py` 12 路由↔新 `e2e_common` 自举契约、`orchestration.py` 六步/retrieve/load_index↔`TG-13`/`TG-8`、`agents-dashboard`（`TG-13` 字段语义）、`verify/suite_result.json` 与 `verify/*.log|png`↔`TG-9`、其余 phases backlog↔`TG-14` 迁移边界；⑥ **EXCLUDE**：`agents/runs/**`、非本轮 `docs/iteration/**`、`agents-dashboard/app/api/*`（不计入 B）。
- **纪律**：本 Sprint **所有评审 scope 必须来自该〇查**（`agents/functions/code-review.md`：`never a self-chosen narrow scope`）。这正是 `TG-11` 要修的结构性缺陷——首次评审前若〇查未跑 → 记 `M2` 违规 1 次。
- **本次〇查暴露的两个缺陷（已当场处置）**：① `agents/functions/impact-assessment.md` 的路由表把 `/api/usage`、`/api/checkpoints` 重复登记（5 行 ≠ 12 条）→ **已删冗余行并升版 v1.4.2**（"行数 ≠ 路由数，`N_routes` 一律取运行期自检"）；② 账本 `task_id` 中文经 `pwsh→python` 传参落库即乱码 → 记入 `TG-13` 实例⑥ 与 §10（关闭期用 ASCII 描述重写）。

## 1. Sprint Planning

- **Goal**：把"写在文档里的规则"变成**可执行检查点**——① 关闭前置闸门可跑（`verify_close_readiness.py`）；② 规则绕过可检测（scope 来源机器可验）；③ 账本数字可信（时长/轮次/账本↔产物一致性）；④ 卡与文档拓扑唯一（一卡一文档 + 索引 lint）；⑤ Python 侧 `ruff` 可读性闸门；⑥ 两个用户可见缺陷修掉（运行按钮要点 2~3 次、复制状态串台）。
- **团队与产能**：1 人（AI 开发者），**18 点**（治理 14 + 搭车 4）；**2026-09-23 插入 `TG-15`（2.5 点，用户批准）→ 现口径 **20.5 点**（插入卡如实计入 §4 观察表）**；**时间盒 6 个有效工作日**（自 **2026-09-23** 起，**按有效工作日计，不按日历日**）；另**预留 20% 治理额度**（计划外的规则/闸门维护）。
- **规模上限校准（用户 2026-09-21："先跑跑看"）**：18 点是否合理**本轮用实测数据回答**（插入卡点数/关闭税/有效工作日），关闭时写入 §8.1。
- **验收演示**：每卡 verify case 绿 + 证据命令/输出；**关闭三查一次通过**（`M1` 判据：code major ≤ 2 且文档项 ≤ 3）。
- **本 Sprint 的硬约束**：① **红线按 S0 生效——只告警 + 24h 内定位根因，不硬停**（S1/S2 分别从 G2/G3 起）；② 一切阈值/路径/开关**可配置，不写死**；③ **新增检查必须先反向对照**（在未修复实现上跑出 FAIL）；④ 断言必须能在**目标环境**（含 CI）执行；⑤ 依赖引入按 §2.4 结论执行（见 §5）。

## 2. Backlog（卡 + 本 Sprint 验收标准）

> 卡正文见 `phases/*/backlog.MD`（不在此复制）。下表只写**本 Sprint 的交付判据**。

| 卡号 | 一句话 | 本 Sprint 交付判据 | 点 |
|---|---|---|---|
| `TG-11` | 规则绕过复盘 + 可执行闸门 | ① 复盘结论区分"未读到规则"vs"判断越界"（含可核事实）；② **闸门可执行**：评审 run 的 scope 来源机器可验（自选 scope 必须显式 `--deviation` 并留痕）；③ 反向对照：伪造一次"自选窄 scope"评审 → 必须被拦/被标记 | 3 |
| `TG-6` | 验收方式改进 | ① 验收 case 模板与"证据形态"要求落 spec；② `verify_*.py` 的证据输出规范化（命令+输出+退出码）；③ 至少 2 张旧卡按其新形态补证 | 3 |
| `TG-13` | 账本测量化 | ① 测量口径写死定义（wall-clock / declared / **unknown**，禁止 0.00 与整十分钟冒充）；② `verify/verify_ledger_measurement.py`（offline，纳入套件）：账本↔`agents/runs/**` **双向**一致、时间戳非退化、`rounds` 与报告一致、终态已写回；③ **反向对照**：注入"相同时间戳/缺目录/rounds 不符/终态未写回"四类样本 → 必须 FAIL | 2 |
| `TG-14` | 一卡一文档 | ① `phases/<阶段>/cards/<卡号>.md` 一卡一文件；② `backlog.MD` 退化为瘦索引；③ `verify/verify_card_index.py`（offline）：索引↔文件一一对应、卡号↔文件名一致、**表格表头↔各行单元格数一致**、Sprint 文档出现卡正文即 FAIL；④ **反向对照**：删卡文件/改状态/塞卡正文 → 必须 FAIL；⑤ 试点 `testing-governance`（12 卡）迁移完成 | 3 |
| `TG-8` | 端口自检 + 离线开关 | ① 启动前端口自检（占用即报错并给出可操作提示）；② 离线开关（禁用一切外呼，含 provider 刷新）可配置；③ 断网实跑一次留证 | 2 |
| `TG-9` | 产物目录约定 | ① 新脚本产物默认落**已忽略目录**（`agents/runtime/`、`%TEMP%`）；② `verify/README.md` 与 `1-WORKFLOW.MD` 写明"新增脚本前先确认产物路径已忽略"；③ `git status` 无运行态文件（回归本轮两次误入库） | 1 |
| `F-AC13` | 运行按钮（要点 2~3 次） | 复现用例先 FAIL（修复前）→ 修复后一次成功；证据含前后对比 | 2 |
| `F-AC14` | 复制状态串台 | 同上（先 FAIL 后 PASS），并覆盖多会话/多 run 场景 | 2 |
| `TG-15` | 闸门泛化：三不变式 + 数据驱动 + 硬编码政策闸门（**2026-09-23 插入卡**） | ① 6 处政策硬编码替换为数据源（`fanout.json` / spec frontmatter）；② 锚点自动化（`coverage_anchor`/`covers_through`）+ C3-T 例外表（sha 钉死、表落后默认 FAIL 点名）；③ `close-sync` 自动生成候选；④ `verify_no_policy_hardcode.py` + 反向对照；⑤ 变异用例从数据自动生成 | 2.5 |

**合计 20.5 点**（原 18 + 2026-09-23 插入 `TG-15` 2.5）；含 **§2.3 的 Python `ruff` 闸门**——**G1 唯一的新增依赖**，默认 pin 对齐仓库既有 vendored pin `v0.14.4`（OSV 实测 0 条；升级到 0.16.8 须显式记录理由），并按 §5 的"先查后装"引入。

## 2.1 验收 case（TG-3：先写步骤+断言，实现后回填 §7）

> 边界统一要求：① 异常/降级路径必测；② 阈值/开关**改配置即生效**；③ 失败必须非零退出或显式状态，**不得静默通过**；④ **每个新检查先在未修复实现上跑出 FAIL**。

| 卡 | 验收 case（步骤 → 断言） |
|---|---|
| `TG-11` | ① 模拟"评审 run 由主代理自选 scope 且无 `--deviation`"→ 闸门标记违规（非零/显式告警）；② scope 来源填 `impact-assessment:<run_id>` 且该 run 存在 → 放行；③ 该 run 不存在 → 拒绝（fail-closed） |
| `TG-6` | ① 旧卡按新证据形态补证 → 证据含命令+输出+退出码；② 证据缺失时评审 spec 判 FAIL（反向对照） |
| `TG-13` | ① 正常运行后账本与目录**双向**一致（含"有目录无记录/有记录无目录"两类）；② 注入相同时间戳 → 告警；③ 注入 `rounds` 与报告不符 → FAIL；④ 终态未写回 → FAIL |
| `TG-14` | ① 索引↔卡文件一一对应；② 删一份卡文件 → FAIL；③ 在 Sprint 文档塞入卡正文特征 → FAIL；④ 表格列数与表头不一致 → FAIL（本轮实测 3 处的形态） |
| `TG-8` | ① 占用端口启动 → 自检报错且不静默换端口；② 离线开关开启 → 无外呼（日志可证）；③ 断网跑通核心链路 |
| `TG-9` | ① 新脚本运行后 `git status --porcelain` 为空；② 产物落在已忽略目录（路径断言） |
| `F-AC13` / `F-AC14` | ① 修复前复现（FAIL 证据）→ 修复后一次通过（PASS 证据）；② 边界：快速重复点击 / 多会话切换 / 长列表滚动 |

## 3. 任务看板（Kanban）

| 卡片 | 状态 | 点 |
|---|---|---|
| `TG-11` 规则绕过复盘 + 闸门 | ✅ DONE（③a CLI 闸门 + ③b `verify/verify_close_readiness.py`（自检含反向对照；**判据数随 `TG-15` 扩容而变，一律以现跑末行 `ALL PASS (N assertions)` 为准**，见 §7）+ ③c §9 结构化 run 表 + 复盘定稿 `phases/testing-governance/2026-09-23-tg11-retro.MD`） | 3 |
| `TG-6` 验收方式改进 | ✅ **DONE（2026-09-25 尾批）**：① **C 级可读性棘轮**（caps 按 `D101/D102/D103/E501` 四类分设、**只减不增**，`review_by` 到期即 FAIL；`verify_lint.py` 反向对照 3 条）；② **四件套证据规范**入 `1-WORKFLOW.MD` §6 与 `verify/README.md`（新增闸门**必须在成功路径**打印机读行 `EVIDENCE: <脚本> assertions=N rc=0`，失败/SKIP 不打印）；③ 两张旧卡按新形态补证（`TG-14`/`TG-7`）；④ 卡片与索引收口。**卡片状态**：`cards/TG-6.md` = 已交付（2026-09-25），与 backlog 行一致。证据见 §7 | 3 |
| `TG-13` 账本测量化 | ✅ **DONE（2026-09-25，D3）**：`verify/verify_ledger_measurement.py`（**账本↔目录双向一致** / 时间戳退化 / `rounds` 对报告 / 终态写回 + **4 类反向对照**；断言数以现跑末行为准）+ 历史棘轮入 `agents/policy.json::ledger_measurement`（`review_by=2026-10-31`）；`register/update/finish/round` 统一重算 `measurement_source`/`dur_minutes`。证据见 §7 | 2 |
| `TG-8` 端口自检 + 离线开关 | ✅ **DONE（2026-09-25）**：① **端口自检**（`verify/e2e_common.py` 的 `port_in_use`/`port_owner`/`port_selfcheck` + `PAPERQA_VERIFY_PORT`；占用即报错并点名端口+PID，**不静默换端口**）；② **离线开关**（`PAPERQA_OFFLINE=1` **发请求前**拒绝，实测 0.10s vs 未开 6.29s）；③ **本卡另一面——套件 flaky 的根因已定位修复**（`load_index` 的 401 → `litellm.RateLimitError` 逃逸出 enrichment handler 的单 anyio TaskGroup 子异常；修法：`PAPERQA_NO_MEDIA_ENRICHMENT=1` 走无 LLM 路径 + `error_detail` 打印）；④ 顺带修掉闸门顺序依赖（`verify/e2e_common.py` 变量同名顶破棘轮上限）。证据见 §7 | 2 |
| `TG-9` 产物目录约定 | ✅ **DONE（2026-09-25，D2）**：`verify/verify_artifact_paths.py`（源码写盘目标必须落在**已忽略目录**或 `%TEMP%`，忽略根**双向差集**，动态目标按文件设棘轮上限、只减不增、到期重评）+ 真源 `agents/policy.json::artifact_paths` + `.gitignore` 的 `dir/*` + `!` 白名单形态。证据见 §7 | 1 |
| `TG-14` 一卡一文档 | ✅ **DONE（D3）**：迁移 4 阶段 × 一卡一文件（**卡数与字节一律以工具输出为准**（字节的复现命令：`.venv\Scripts\python.exe -c "import pathlib;fs=sorted(pathlib.Path('docs/iteration/phases').glob('*/cards/*.md'));print(len(fs),sum(f.stat().st_size for f in fs))"`）：**卡数与字节现值一律由上面两条命令当场产出，本行不写现值**——理由是可核的：本行原写"现测 **96 卡** / **105580 B**"，2026-09-25 一查（`run-2026-09-25-doc-audit-070` finding 1）实测为 **97 卡 / 161228 B**，差的那 1 卡正是同窗口新增的 `TG-18.md`。`agents/runtime/tg14-baseline.json` 是**受闸门校验的快照**（`verify_card_index.py` ⑥ 逐值比对 `card-inventory.py`，不再靠人自觉刷新）；迁移时点（`7eead16`）的时点值**不得当作现值引用**）+ 4 份 backlog 瘦索引化（`38375→6878` / `3461→3039` / `25268→7155` / `19722→11720` B）；`verify_card_index.py` **默认档（自 2026-09-25 A3/N7 起 = 严格档：`--check`/`--strict` 与无参同判）PASS**——"宽松档"这个档位已不存在（一查 finding 1 实测：脚本 docstring 与 `main()` 均无宽松分支，原行的"双 PASS"是照抄早已被撤销的旧口径）；证据见 §7 | 3 |
| `F-AC13` 运行按钮 | 🧊 **冻结（不计入本 Sprint 活跃点）**：用户 2026-09-25 裁定 **P-d=4**——治理模式冻结下**只做治理卡**，搭车项冻结、**不承诺挪期** | 2 |
| `F-AC14` 复制串台 | 🧊 **冻结（同 `F-AC13`，P-d=4）** | 2 |
| `TG-15` 闸门泛化（**插入卡**） | ✅ **D2 实质完成**：**①~⑤ + ⑦⑧ 已落地**，⑥（CI push 即检）经裁定顺延（§5 第 6 条）。三条不变式（C1 声明完备 / C2 指涉可核 / C3 覆盖闭环）替换按角色判据；政策来源收敛为**四处数据**（`agents/fanout.json` v6 的 `close_ledger`、各 spec frontmatter 的 `scope_required`/`coverage_window`、新增 `agents/policy.json`（含 `ledger_status`/`lint_rules`/`md_table_docs`+`md_table_globs`——**更正 2026-09-25**：原文写的 `md_table_targets` 是 `c52d2ee` 之前的旧键名，现行 `agents/policy.json` 无此键）、C3-T 例外台账）；锚点自动化（`register` 记 `coverage_anchor`、`finish` 记 `covers_through`）；`agent-ops close-sync` 候选生成；`verify_no_policy_hardcode.py`（8 类反向对照）；变异用例**从 fanout 自动生成**（7 条）。**6 处存量硬编码改造**：agent-ops 角色集/阈值、close_readiness 角色集与 6 条判据、lint 覆盖路径与规则分级、report-freshness 归档前缀、verify_agentops 角色集，**＋状态白名单**（`policy.json::ledger_status`，二查 finding 后补做）。⑧ 规则入 `1-WORKFLOW.MD` §6（政策数据化）。**独立复核已闭环**（一查 13 条 / 二查 20 条含 1 critical，处置见 §7）。**遗留两条 finding 已开 `TG-17`** | 2.5 |
| `TG-17` 闸门可信化收尾：覆盖窗口语义 + C1 判定域（**插入卡**，D2 新开） | ✅ **前移交付（2026-09-25）**：B1~B4 已落地并独立复核（详见 [`cards/TG-17.md`](../phases/testing-governance/cards/TG-17.md) §交付落地 与 §10 关闭阻塞项）；**+2 点计入本 Sprint**（原排 Sprint-18，用户 2026-09-25 裁定前移；前移理由与点数估算见 §10 首段）。**遗留**：C3 未归属提交的归属（关闭期动作，不得用回填伪造覆盖） | 2 |
| `A-M12` 编辑边界事故族治理（**agents-infra 阶段**，D3 新开） | 📋 候选（**Sprint-18 / G2**，与 `A-M11`/`TG-17` 同批；**不计入本 Sprint 点数**）。来源＝本 Sprint 内**5 次**同型编辑边界事故（§10 R1-1~R1-5）；反例见 `phases/agents-infra/2026-09-23-edit-boundary-incidents-case.MD` | — |
| `TG-16` 审查流程/策略全景 HTML（**插入卡，3 点**） | 📋 待排期（**不计入本 Sprint 点数**；建议 G1 关闭后定稿，届时含全部落地闸门） | — |
| §2.3 Python `ruff` 闸门（`verify/verify_lint.py`） | ✅ DONE（A/B 级全仓 0；反向对照 3 条；pin `0.14.4` 入 `requirements-windows.txt`；顺带清掉 17 条历史 F/E 级发现）。**C 级口径以工具报告行为准**（复现命令 `.venv\Scripts\python.exe verify\verify_lint.py` → 看它的 `[report-only C 级]` 行与末行 `EVIDENCE:`）；见 §7。**本文档不写该数字**——它随文件增删必然漂移，手抄即错（2026-09-25 一查 finding 2 实测：本行与 §7 曾并存 3 个互不相同的过期读数，无一与工具一致） | — |
| `verify_md_tables.py`（D2 新开闸门，**三次 R1 事故固化**） | ✅ DONE（未转义管道切分 + 转义平衡自检；上线即抓出 **5 处既存**表格缺陷并修复：`TEST-MATRIX` 生成器 `requires` 未归一化→3 行 9 格、`3-LEARNED` 裸管道、`1-WORKFLOW` 分隔行） | — |

## 4. 燃尽图（Burn-down）

| 日期（有效工作日） | 计划剩余 | 实际剩余 | 新增插入卡 |
|---|---|---|---|
| D0（启动 2026-09-21） | 18 | 18 | — |
| D1（2026-09-23） | 15 | **17.5**（TG-11 关闭 −3 → 15；**插入 `TG-15` +2.5 → 17.5**） | **2.5**（`TG-15`，用户 2026-09-23 批准；另 `TG-16` 3 点待排期，**不计入本 Sprint**） |
| D2（2026-09-23） | 12 | **17.5**（计划内 `TG-15` 2.5 点**已实质完成但不计减**——因 ⑥ 顺延、且两条 finding 转入 `TG-17`，按"证据未齐不放行"的保守口径留在账上；其余卡未动） | **0 点计入本 Sprint**（新开 `TG-17` 3 点标为 **Sprint-18**，不计入本 Sprint） |
| D3（2026-09-23） | 8 | **14.5**（原记 12 系**基于错误事实**：见下"更正"）——`TG-14` 完成 −3 → 14.5；**`TG-15` 的 2.5 点不再减**（用户 2026-09-25 拍板：**不减点、改扣分条件**）。**更正（2026-09-25，一查 `run-2026-09-24-doc-audit-066` critical + 主代理复核）**：原"减点原由"称 `TG-15`⑥（覆盖检查进 CI）"已于 D3 由 CI #206 success 闭环"——**该说法为假**：`ci.yml` 内无 `close-sync`/`fail-on-unowned` 接线（`grep` 零命中），唯一的覆盖检查块**以真实锚点为门**（`ci.yml:73` 正则），而本 Sprint 锚点仍是 `待填`（§9.1）→ 该步骤**被跳过**；§5 第 6 条本身也写着 ⑥"**已裁定顺延（不是完成）**"。→ 因此 D3 的正确剩余为 **14.5 点**（`TG-6`3+`TG-13`2+`TG-8`2+`TG-9`1+`F-AC13`2+`F-AC14`2 = 12，**＋`TG-15` 2.5 未减**）。**扣分条件**：棘轮基线"上限制 + 到期日"未闭环 → 扣 0.5；审核若判 critical/major 且未闭环 → 按级别继续扣，直至 0 | **0 点计入本 Sprint**（`TG-17` 标 Sprint-18；D3 新增候选：`A-M12` 编辑边界事故族治理(Sprint-18)、flaky 归因并入 `TG-8`、15 处历史表格缺陷入棘轮基线） |
| D4（2026-09-24） | 5 | **14.5**（**无卡关闭**：本日投入 = 卡库存工具 `scripts/card-inventory.py` + 卡索引闸门 `verify/verify_card_index.py` + 一查 `doc-audit-066`；`TG-14` 的迁移已在 D3 完成、不再减点） | **0 点计入本 Sprint**（`TG-17` 当时仍标 Sprint-18；本日新增候选 `TG-18` 历史表格缺陷棘轮） |
| D5（2026-09-25，关闭日） | 2 | **2.5**（算式可核：`TG-6`3 + `TG-13`2 + `TG-8`2 + `TG-9`1 **全部交付 = −8** → 6.5；再按用户 2026-09-25 **P-d=4** 裁定冻结搭车项 `F-AC13`/`F-AC14` **= −4** → **2.5**；这 2.5 就是 `TG-15` 按用户裁定"**不减点、改扣分条件**"留在账上的部分） | **+2 点计入本 Sprint**（`TG-17` 由 Sprint-18 **前移**为关闭阻塞项，用户 2026-09-25 裁定；点数估算与前移理由见 §10 首段） |
| D6（关闭） | 0 | **2.5**（与 D5 同日收口，无独立工作日；**关闭税与扣分另计**（§8.1），冻结的 4 点不记账） | —（`TG-16` 待排期、`A-M12`/`TG-18` 属 Sprint-18 候选，均不计入本 Sprint） |

> **口径核对（防"两套算术"）**：计划侧 = 18（治理 14 + 搭车 4）**＋插入 `TG-15` 2.5＋前移 `TG-17` 2 = 22.5**；交付侧 = `TG-11`3 + `TG-14`3 + `TG-6`3 + `TG-13`2 + `TG-8`2 + `TG-9`1 + `TG-17`2 = **16**，**＋`TG-15` 2.5 已交付但按裁定不减**；冻结 `F-AC13/14` **4**（不计）；关闭时账上剩余 = **2.5**。`16 + 2.5 + 4 = 22.5` ✅ 两侧相等。

## 5. 风险与依赖（本 Sprint 特有）

| # | 风险/依赖 | 处置 |
|---|---|---|
| 1 | **§2.4 尚有 2 项未补**：按锁定版本复核依赖树漏洞面、断网实跑 | **工具口径已由用户 2026-09-21 裁定**（`L1=a L2=b L3=a L4=c L5=a L6=b`，执行清单见报告 §6.2）：**G1 只需 ruff**（Python 112 文件），按"**先查后装**"顺序（`npm install --package-lock-only --ignore-scripts` → `npm audit`；`pip download <pin> --no-deps`）留证后引入；prettier 只查新增/改动文件、`@typescript-eslint` 与 `yamllint` 进 G2、`eslint-plugin-react` 不引入、`PSScriptAnalyzer` 延后 |
| 2 | **S0 只告警不硬停** → 可能出现"告警了但没人闭环" | 每次告警**当日闭环**并记 §10；同一红线一个 Sprint 内 ≥2 次 → 转"治理模式" |
| 3 | 本 Sprint 的评审 scope 依赖**〇查**，而〇查本身是"规则绕过"事故的现场 | 〇查**先跑**并留 run id；scope 来源必须机器可验（`TG-11` 交付之一） |
| 4 | 迁移类改动（`TG-14` **卡数一律以 `scripts/card-inventory.py` 输出为准**——本行不写现值，原写"现测 96 卡"已过期、更早还写过手抄旧值 90）易触发**文件丢失/锚点被吞** | 迁移前 `git status` 干净断言 + 分批提交 + 结构校验（标题数/卡号序列/表列数）+ **迁移前后对基线比卡号集合**（`scripts/card-inventory.py`）；实测：迁移时点卡号逐位一致、无值丢失 |
| 5 | 环境误用导致**假红**（实测：用系统 `python` 跑套件 → 4 脚本因缺 `paperqa`/`fitz` 失败，用 `.venv` 解释器则 **14 脚本 SUITE PASSED**） | 套件与脚本命令统一写 `.venv\Scripts\python.exe`；文档/脚本不得暗示裸 `python` |
| 6 | **`TG-15`⑥「覆盖检查进 CI（push 即检）」——主代理裁定：本轮不接，归入下一次关闭期**（2026-09-23，用户授权处置）。理由三条：① **无真实锚点则必然假红**——C3 需要 `三查锚点`（= 二查覆盖到的 HEAD），TG-15 刚落地的这一刻**二查尚未跑**，此时把它接进 push 级检查，会让**每次 push 必然 FAIL**，直接污染"CI 必须绿"的判据（§3 同步节奏第 3 条）；② **现闸门形态已覆盖主风险**——CI 现有的关闭前置块**只对声明了真实锚点的 Sprint 文档执行**（占位文字不算声明），已做到"谁声称关闭谁自证"，而 TG-15 的增量（"漂移在一次 push 内暴露"）在**本次二查之后**才有可用的锚点可比；③ **接线方式应复用现有块**而非新增 push 级步骤——关闭三查与覆盖闭环本就是同一生命周期（关闭时），拆成两个触发点会把"锚点"变成两个真源。**后续**：下一次关闭期在现有 CI 块内加 `close-sync --fail-on-unowned`（同一锚点、同一触发条件），**并按 §4.2 带反向对照**（先证明未覆盖时它 FAIL）。 | 本条同时是 DoD 第 1 条"三查一次通过"的前置说明；**TG-15 的 ⑥ 状态 = "已裁定顺延"**（不是"完成"，也不是"待办"），关闭三查按此口径核对 |
| 7 | **`TG-15` 的"政策数据化"反向风险：数据文件本身可能变成新的漂移源**（改了 `policy.json` 但没人读、spec 声明被删） | 已内置两类封闭世界自检（`closure_problems()`：死键 + 缺声明；`verify_no_policy_hardcode.py`：数据文件必须被代码读取）；`load_policy()` 把它做成**运行时不变量**而非"某个闸门的自检"（否则普通命令会带着缺口照常跑） |
| 8 | **二查 finding：覆盖窗口 `(anchor, through]` 在本 Sprint 实测结构性为空**（`register` 记登记时 HEAD、`finish` 记收尾时 HEAD，关闭各步同 HEAD ⇒ `(X, X]`）→ "锚点自动化"产出 **0 覆盖**，C3 只能靠例外表或把锚点填成 HEAD（后者已由第 1 条 critical 禁止） | **本轮不做半改**（二查明确建议"开卡"）：已落**检测**（`window_problems()` 逐条点名空区间/短 sha + 闸门与 `close-sync` 拒绝空区间），**语义修正另立 `TG-17`**（用户 2026-09-23 批准，与 `TG-15`⑥ 同批排入 G2） |
| 9 | **二查 finding：C1 判定域 = 全账本** ⇒ 34 条历史 run（无 `scope_source` 键）在真数据上永久 FAIL，闸门退化为噪音；而账本有完整性哈希（手补被拒） | 并入 **`TG-17`**（同卡处理，候选修法：判定域限定为本次关闭窗口内；须带反向对照——历史不得遮蔽当期缺口）；本轮仅在 §7 如实标注 |
| 10 | **`verify_md_tables.py` 上线后实测的既存表格结构缺陷**（非本轮引入，共 5 处）：`verify/TEST-MATRIX.MD:33/50/53` 三行**单元格数 9**（超表头）——根因是 `verify_matrix.py::render_matrix` 对 `requires` 为**字符串**的元数据不归一化，输出成了 `\| none \| \|` 两格；`docs/3-LEARNED.MD:412` 正文含**裸 `\|`**（`< > { } \| \ "` 未转义）把该行切多；`docs/1-WORKFLOW.MD` §4.2 的分隔行少一列 | **本轮已修**（5 处全清）：① 生成器加 `_cell()`（转义裸管道 + 换行归一）并重 derive；② 3-LEARNED 裸管道转义；③ 1-WORKFLOW 分隔行属**我的检查器误判**——第一版 `split_row` 把"真实空格子"当外层管道丢掉（少算 2 格），据此"修"坏了一个**原本正确**的行，已在修好检查器后**撤回**（改错→发现→改回的全过程记入 `verify_md_tables.py::split_row` docstring） |
| 11 | **套件偶发假红：`verify_agentops` / `verify_local_dir` 不稳定（flaky）**——另一会话 D3 实测：P3 期间 4 连跑 18/19，失败脚本在 `verify_agentops`(1 次)/`verify_local_dir`(3 次) 间漂移 | **归因（2026-09-23 主代理复现并更正）**：<br>① 两脚本源码**都不读** `docs/iteration/phases/**` ⇒ 与本轮文档迁移无关；<br>② **先前"只有全序列才失败 ⇒ 纯端口竞态"的口径过窄，已更正**：主代理单跑 `verify_local_dir.py` **3 次中第 2 次 FAIL**（`F-AC3 load_index 成功 FAIL: unhandled errors in a TaskGroup (1 sub-exception)`），同期套件全序列 **2/2 PASS** ⇒ **单跑与全序列都会偶发失败**；<br>③ 待查方向（两条并存）：自举后端 kill/端口释放竞态 **＋** `load_index` 步骤自身的偶发异常（TaskGroup 子异常需展开看根因）；<br>④ **并入 `TG-8`（端口自检 + 离线开关）证据，不另开卡**——离线开关/端口自检正是压制此类抖动的设施 |

## 6. DoD（Definition of Done，本 Sprint 追加项）

除标准 DoD（每卡 verify case 绿 + 证据 + 反向对照 + 文档同步 + 关闭三查）外，本 Sprint **必须**：

1. **M1**：关闭阶段 code major ≤ 2 且文档项 ≤ 3，且**三查一次通过**（重跑即记未达标）。
2. **M2**：`A-M11` ④ 五项违规指标全 0（未跑〇查即派评审 / 锚点后未评审提交 / 自选 scope 无 `--deviation` / §9 与账本不一致 / 规范条目↔执行记录不一致）。
3. **M3**：按**有效工作日**推进（每日有相关提交或卡推进），非爆发日模式。
4. **红线 S0 记录**：R1/R2/R3 触发次数、告警→闭环时长、根因（§10）。
5. **观察表**（计划 §4）：计划点/实际点、**插入卡点数与占用**、关闭税点数、M1~M3 读数、红线事件。
6. **双盲抽查**：按计划 §5 抽 3 份 run 报告，我的独立结论先落盘再比对，判断类升级用户裁定（§9）。
7. **lint 闸门**：`verify/verify_lint.py` 纳入 offline 档套件；工具缺失 **fail-closed 或显式 `skipped` + 原因**；含反向对照注入。
8. **三查锚点必填（D3，主代理默认执行、用户可否决）**：§9.1 必须声明 `三查锚点: <sha>`；关闭后锚点至 HEAD **不得有未评审提交**——由 `verify/verify_close_readiness.py` 机检（CI 对声明了锚点的 Sprint 文档执行）。

## 7. 证据回填（关闭时逐卡回填：命令 / 输出 / 断言数 / 反向对照）

| 卡 | 证据（命令 + 断言数 + 反向对照） | 状态 |
|---|---|---|
| `TG-11` | **③a**：`verify\verify_agentops.py` → 末行 `ALL PASS (N assertions)`（**N 以现跑输出为准，本文档不抄写**，下同）（+UC-14 八条）；反向对照（HEAD 版 CLI）未修复实现**放行**无 scope 的评审 run（`rc=0`）且不认 `--scope-source`（`rc=2`）。**③b**：`verify\verify_close_readiness.py` → `ALL PASS (N assertions)`（判据数随 TG-15 扩容而变，交付时与现值相差数倍 ⇒ **只以现跑末行为准**：C1/C2/C3 三不变式 + 6 条自 fanout 自动生成的变异用例 + linkage/降级路径；六个反向对照场景各命中预期判据编号 + 真数据模式对缺失文档退 2）；真数据模式对 Sprint-17 现状**正确 FAIL**（§9 表与锚点未填）。**③c**：§9 结构化 run 表格式已定义（本文件 §9.1）。**复盘**：`phases/testing-governance/2026-09-23-tg11-retro.MD` | ✅ |
| `TG-15` | ① **不变式替换**：`verify\verify_close_readiness.py` 重写为 C1/C2/C3 三不变式（原按角色写死的 6 条判据删除）→ 末行 `ALL PASS (N assertions)`（**N 以现跑为准；2026-09-25 一查实测本行原写的 23 已与工具不符**），含 7 条**从 fanout 自动生成**的变异用例（4 个 ledger 步骤各抽一次 + 3 个 target 各抽一次）。② **数据源**：`agents/fanout.json` v6（`close_ledger`/`close_targets`）+ 8 个 spec frontmatter（`scope_required`/`coverage_window`）+ 新增 `agents/policy.json`；`python scripts\migrate-scope-declarations.py --check` → **SCOPE-DECLARATION-CHECK PASS（8 个 spec 均已声明）**。③ **锚点自动化**：`verify_agentops.py` UC-15 断言 `coverage_anchor` 由 CLI 自动落库（实测 `092a2f78`，无需人抄）。④ **两张例外表必须分清**（2026-09-25 一查 finding 8：原行混为一张，读者会去查一个恒空的表）：**(a) 覆盖归属例外** = `agents/policy/coverage-exceptions.json`（`rules` 条数一律现跑 `json.load` 后取 `len`，本行不写现值；实测为空气表）+ 三条反向对照（短 sha / 通配 sha / 缺 reason 各 FAIL）；**(b) C3-T 未归属提交的兜底** = `agents/policy/run-dir-exceptions.json`（目录例外 + 记录例外，全带 `reason`，白名单**只减不增**；条数同样现跑取 `len`）。**同名"例外"两处，语义不同：前者管"哪个 run 的覆盖范围"，后者管"C3 里哪些提交不算未归属"。**⑤ **`close-sync`**：`agent-ops.py close-sync --sprint <doc>` 生成未归属提交候选 + doc-only 自动归类（`--fail-on-unowned` 供关闭前置）。⑥ **`verify_no_policy_hardcode.py`** → 末行 `ALL PASS (N assertions)`（**N 以现跑为准**；本行原写的 7 与实测不符——R1/R2/R3 三类注入各 FAIL + `load_policy()` 写法放行 + `_exempted_local` 就地豁免只放行该处；**仓库现状 clean**。⑦ **6 处存量硬编码改造完成**（agent-ops 角色集/阈值、close_readiness 角色集与 6 条判据、lint 覆盖路径与规则分级、report-freshness 归档前缀、verify_agentops 角色集）。**反向对照（TG-6 ⑤）**：`PAPERQA_AGENT_POLICY` 指向不存在文件 → CLI `POLICY-ERROR`（非零退出）；spec 缺 `scope_required` → **装载即 fail-closed**（UC-16，开启/关闭两态各一断言）。**二查（run-...-064）后追加修复**：① critical「锚点==HEAD ⇒ 空区间静默打 ✔」→ 闸门与 `close-sync` 均改为**空区间不算通过**（退出 2，实测）；② `coverage_window` 从"文档声称"变为**真消费者**（C3 按声明过滤）+ 纳入封闭世界不变量 + 取值校验；③ 数据拼错不再静默降级（`_as_bool("ture")` 直接报错）；④ 短 sha/空区间窗口**逐条点名**（原静默丢弃）；⑤ `close_targets` 死旋钮删除（targets 单源于 `task.target`/`tasks[].target`）；⑥ 状态白名单入 `policy.json`；⑦ 探测器补 3 类假阴性（字典字面量/等值判定/状态白名单）+ 角色名判据改为"仓库实际 spec 文件名"（误报 18 → 0）。**未修项（记卡，见 §5 第 8 条）**：覆盖窗口 `(anchor, through]` 在本 Sprint 实测**结构性为空**（登记与收尾同 HEAD）⇒ "锚点自动化"尚未产出可用覆盖。**回归**：`verify_agentops.py` 与 `verify_matrix.py check`、offline 套件三者**一律以各自命令的现跑输出为准**（本行不写断言数/脚本数：原写"57 断言 / 19 scripts / 50 脚本一致"三处读数已全部过期，2026-09-25 一查 finding 1/2 实测）、`verify_lint.py` A/B 级全仓 0。⑧ 规则入 `1-WORKFLOW.MD` §6（政策数据化 + 三不变式 + 自查表）。**⑥ 本轮裁定不接**（三条理由见 §5 风险表第 6 条：无真实锚点时必然假红、现闸门形态已覆盖主风险、接线应复用现有 CI 关闭前置块而非新增 push 级步骤）；**该裁定属"卡内交付范围的收口"而非"完成"**，已在 §3 看板行与本表同步标注，供关闭三查核对 | ✅ ①②③④⑤⑦⑧ 完成；⑥ 已裁定顺延（有据） |
| `TG-6` | **① 命令**：`.venv\Scripts\python.exe verify\verify_lint.py`（offline 档内）。**② 关键输出**：成功路径末行 `ALL PASS (<N> assertions)` + 机读行 `EVIDENCE: verify_lint.py assertions=<N> rc=0 c_ratchet=<四类现值/上限>`（**两个数一律现跑，本表不抄**；失败与 SKIP 路径**不**打印该行）；A 级 `E9,F63,F7,F82` 与 B 级 `F401,F811,F841,E702,E711,E712,E722,W292` 全仓 **0**。**③ rc**：A/B 级 0 违规且四类 C 级（`D101/D102/D103/E501`）均在各自棘轮上限内 → `rc=0`；任一超限或 A/B 级命中 → `rc=1` 并逐条点名（**本批实测形态**：在飞编辑把 E501 推到上限之上 → `rc=1` 且打印"实测 N > 上限 M（超出 K 条）—— cap 只许在重评基线时下调，不许为变绿上调"，随后由改动方折行修复）。**④ 反向对照（8 条，全部真实 ruff 调用）**：坏文件 A 级必 FAIL / 坏文件 B 级必 FAIL / 好文件必 PASS / 干净副本+实测上限 → 棘轮零 problem（防假红）/ 临时副本注入 1 处缺 docstring + 1 行超长 → 必 FAIL / `review_by` 设为过去 → FAIL 且点名"已过期" / caps 少一类 → FAIL（该类无限放行）/ caps 多出拼错的键 → FAIL（上限表与规则集不同步）。**⑤ 规范落地**：`1-WORKFLOW.MD` §6 四件套小节（含 `EVIDENCE:` 行要求）＋ **同日新增"派生数字不得手抄"条**；示范脚本 = 本闸门。**⑥ 边界**：只对新增脚本生效、**不回溯**改造存量脚本 | ✅ |
| `TG-13` | **① 命令**：`.venv\Scripts\python.exe verify\verify_ledger_measurement.py`（offline 档内）。**② 关键输出**：`ALL PASS (<N> assertions)` + `[report]` 行打印 `missing_result_files_review=<现测>/<上限>`、`output_chars=0`、`started==ended`、`rounds` 不符、终态未写回各类计数与棘轮上限（**N 与各计数一律现跑，本表不写现值**——本行原先写过一次时点读数，关闭期的修复又让断言数变了，这正是"派生数字不得手抄"要治的形态）。**③ rc**：真实数据全部在上限内 → `rc=0`；任一类超上限或账本↔目录双向不一致 → `rc=1` 并逐条点名。**④ 反向对照（4 类）**：合成历史 run 超上限 → FAIL / 上限内 → 只 WARN 不 FAIL / 白名单条目已不再需要 → FAIL（例外只减不增）/ `register`/`finish` 写入侧退化标注（`started==ended`、负时长、`rounds` 与报告不符）各 1 条。**⑤ 附带**：`register/update/finish/round` 统一重算 `measurement_source`/`dur_minutes`；`list` 对缺时间戳打印 `dur=unknown`（不回落到 `0.0`、不留空） | ✅ |
| `TG-8` | **① 命令**：`.venv\Scripts\python.exe verify\verify_local_dir.py`（需自举后端，约 35s；offline 档内）。**② 关键输出**：`ALL PASS (7 assertions)`（2026-09-25 现跑），含 4 条端口自检断言——占用 8787 时子进程 `rc=1` 且点名**端口 + PID + "释放或改端口"提示**、释放后同一路径 `rc=0`、预检通过路径**不留下任何监听**。**③ rc**：`rc=0`；端口被占时 `rc=1` 且**不静默换端口**。**④ 反向对照**：先占住端口 → 必 FAIL 并点名（修复前形态：静默换端口/无提示）；释放后 → PASS（证明失败来自占用本身，而非"预检恒失败"）。**⑤ 离线开关（另一面）**：把 `HTTP(S)_PROXY` 指向黑洞 `127.0.0.1:9` 后——开关**关**：`fetch-prices --check` `rc=0`、6.29s、三个来源各报 `URLError`（走真实网络路径）；开关**开**：`rc=2`、**0.10s**、`OFFLINE-REFUSED: …拒绝外呼：<URL>`（**拒绝发生在发请求之前**，0.10s vs 6.29s 不是超时）。**⑥ 本卡另一面（flaky 根因）**：`F-AC3 load_index` 的 401 → `litellm.RateLimitError` 逃逸出 paperqa 的 enrichment 处理 → 单个 anyio TaskGroup 子异常；修法 = 该链路按设计不调 LLM ⇒ 自举时设 `PAPERQA_NO_MEDIA_ENRICHMENT=1`（`make_settings` 读它 → `parsing.multimodal=2`，**不设该 env 则产品默认行为逐字段不变**）+ 打印 `error_detail` 展开根因。修复后：单跑 3+5 次全 `rc=0`、各 7 断言、用时 33~35s（此前 56~59s）；全序列 2 次 `SUITE PASSED`。**⑦ 顺带**：`verify/e2e_common.py` 变量同名导致 `artifact_paths` 棘轮顶破（**测试顺序依赖**：单跑 PASS、全序列 FAIL）→ 改变量名，判据与顺序无关 | ✅ |
| `TG-9` | **① 命令**：`.venv\Scripts\python.exe verify\verify_artifact_paths.py`（offline 档内）。**② 关键输出**：`ALL PASS (<N> assertions)` + 动态目标分布行（**条数与文件数一律现跑，本表不写现值**——本行曾写"现跑 = 10"，修复验证复核实测为 13，正是"派生数字不得手抄"的同一形态）；忽略根**双向差集**：政策声明的每条根必须真被忽略（合成探针 `git check-ignore` 命中）、`.gitignore` 里确有其行、且确有代码在用；反过来每个已忽略落点都能归到某条声明根。**③ rc**：全部合规 → `rc=0`；源码写盘目标落在**仓库根/未忽略目录**、或动态目标超该文件上限、或 `review_by` 到期 → `rc=1` 并逐条点名。**④ 反向对照**：注入写仓库根的脚本 → `rc=1` 且点名未忽略路径＋文件名；注入写默认落点（`agents/runtime/`）的脚本 → `rc=0`（不制造假红）；产物写入 `agents/runtime/` 后 `git status --porcelain` 为空（且 `--ignored=matching` 下可见，证明上一条不是"路径不存在"造成的假绿）；空扫描目录 → `rc=0` 且如实打印 0 写盘目标；语法错误脚本 → `rc=1` 点名。**⑤ 结构**：`.gitignore` 用 `dir/*` + `!` 白名单（父目录整体排除时 `!` 不生效——实测）；动态目标上限**只减不增**、到期重评。**⑥ 一查 finding 后补强**：自检⑥原为**自指断言**（只找自己的探针名）→ 已改为"未跟踪文件集必须为空并点名"，并补齐 `logging.basicConfig(filename=…)`/`os.system` 重定向两类规避形态（二查 `run-2026-09-25-code-review-071` finding 1 的真实反例） | ✅ |
| `F-AC13` | 🧊 **冻结，不计入本 Sprint**：用户 2026-09-25 裁定 **P-d=4**（治理模式冻结下只做治理卡，搭车项冻结、**不承诺挪期**）⇒ 关闭时**不作为 DoD 判据**，证据行留空是**正确形态**（不是遗漏）。卡内原始判据见 §2 表 | 🧊 |
| `F-AC14` | 🧊 **冻结，不计入本 Sprint**（同 `F-AC13`，P-d=4） | 🧊 |
| `TG-17` | **D2 开卡记录（来源＝本 Sprint `TG-15` 的独立复核批）**：上游 run `run-2026-09-23-code-review-064`（`succeeded`）的 major #3（覆盖窗口结构性为空：`062` 实测 `coverage_anchor == covers_through == e6ccd257…`；`092a2f7..HEAD` 9 提交中 6 个 UNOWNED）与 #5（C1 判定域＝全账本 ⇒ 34 条历史 run 永久 FAIL）。**本轮只落"检测"不落"语义修正"**（二查明确建议开卡而非半改）：`window_problems()` 逐条点名空区间/短 sha；闸门与 `close-sync` **拒绝空区间**（实测 rc=2）。**用户批准**：2026-09-23（UTC+8 `22:47` 网络核验）"1+2 合成一张新卡（比如 TG-17「闸门可信化收尾」）排进 G：可以，开进backlog吧"。**卡正文**：`phases/testing-governance/backlog.MD` 的 `TG-17` 行（**唯一真源**，本表不复制）。**附带产出**：`verify_md_tables.py` 新闸门（三次 R1 事故固化）上线即抓出并修掉 5 处**既存**表格缺陷。**状态更正（2026-09-25）**：本卡已由 Sprint-18 **前移**为 Sprint-17 关闭阻塞项并交付（B1~B4，+2 点计入本 Sprint）；**唯一遗留** = C3 未归属提交的归属（关闭期动作）。交付落地见 [`cards/TG-17.md`](../phases/testing-governance/cards/TG-17.md) | ✅ 前移交付；遗留 C3 归属（关闭期） |
| `TG-14` | **四批迁移（P1~P4，各一批一提交）**：`testing-governance` 17 卡（`8d75951`，38375→**6878** B）→ `mental-models` 6 卡（`6d471cd`，3461→**3039** B）→ `agents-infra` 25 卡（`3107318`，25268→**7155** B，**首遇多表 + `—` 占位**）→ `refactor-analysis` 47 卡（`7eead16`，19722→**11720** B，卡号形态最杂）。**卡数与合计字节一律以工具输出为准（本行不写现值）**：卡数 = `scripts/card-inventory.py`；合计字节 = `.venv\Scripts\python.exe -c "import pathlib;fs=sorted(pathlib.Path('docs/iteration/phases').glob('*/cards/*.md'));print(len(fs),sum(f.stat().st_size for f in fs))"`。迁移时点 `7eead16` 的时点值为 **95 卡 / 99700 B**——**该时点值不得当作现值引用**（本行原写"现测 96 卡 / 105580 B"即"手抄旧值当现值"的漂移，2026-09-25 一查 finding 1 实测为 97 卡 / 161228 B）。<br>**守恒自证（各批自带，逐字段）**：P1 **102** / P2 **60** / P3 **117** / P4 **231** 字段 —— P3/P4 另有 14 项判据（卡号序列逐位一致、与 `agents-infra/runtime` 基线一致、表外内容逐字节在、索引表每行 5 格、新增行仅限索引/说明/处置行、无 CR）。<br>**主代理独立复核（不采信自证）**：① 卡号集合与 `agents/runtime/tg14-baseline.json` **逐位一致**（25/6/47/17）；② **列序无关判据**（集合问题，不猜映射）：**「不丢失」= 0 处**（95 卡无任何值丢失）、「不新增」= 16 处且**全为 `—` 占位符**（原文该格为空被省略），**无编造内容**；③ 提交内 blob 与工作区字节一致（核到 `agents-infra` = **7155 B**）。<br>**闸门**：`verify_card_index.py --check` 与 `--check --strict` **双 PASS**（宽松档曾 72 项孤儿索引行 → 0）；`verify_md_tables.py` 对迁移产物 PASS；`card-inventory.py` 迁移时点 95 卡；**现值以该脚本现跑输出为准**；`verify_matrix.py check` OK（**脚本数现跑为准**，原写"50"已过期。**"宽松档曾 72 项孤儿索引行"这半句保留**：它记录的是 `--check` 档当时的一次真实读数，属历史时点值，不当作现值脚本）。<br>**数字更正（B③）**：P3 提交信息写 `25268→6962 B`，**实测 7155 B**（6962 是未含 §3 处置说明行的中间值，转抄了脚本打印值）。按"不改写历史提交"原则，以本行为准。<br>**源表列数口径（B④）**：`agents-infra`/`refactor-analysis` 的源表存在"单元格数 < 表头列数"的既存缺陷；迁移按"Sprint 值校验器"**逐行裁定并留痕**（P3：6 行 skip-sprint / 12 行 trailing；P4：4 行 skip-sprint / 28 行 trailing），**无静默错位**。<br>**副产品**：迁移中新增两道闸门（`verify_card_index.py`、`verify_md_tables.py`）＋ 测试反例一份（见下行） | ✅ |
| `TG-14` 测试反例 | **子代理越界改写历史 Sprint 文档 → 已固化为回归样本**：`phases/agents-infra/2026-09-23-subagent-scope-breach-case.MD`（证据链 `867d7d9`(越界) → `f5e1345`(revert) → `8d75951`(原样取回)；实测 `git diff 8b2c6ca HEAD -- sprint-14` 空）。**它是"逼出 `verify_card_index.py` 计划表行豁免 + 反向对照 D2"的那次真实越界**，故 D2 即其验收样本；文件内含两条可复用结论（"允许做什么"式规则挡不住越界；**坏判据会主动诱发违规**） | ✅ |
| §2.3 Python `ruff` 闸门 | `verify\verify_lint.py` → 末行 `ALL PASS (N assertions)` 与 `EVIDENCE:` 行（**两个数一律现跑，本行不抄**）：反向对照 **8 条**（A 级坏文件 / B 级坏文件 / 好文件 / 干净副本+实测上限 / 注入 1 处缺 docstring+1 行长行 / `review_by` 过期 / caps 少一类 / caps 多出拼错键）；**A 级 `E9,F63,F7,F82` 全仓 0**、**B 级 `F401,F811,F841,E702,E711,E712,E722,W292` 全仓 0**（清掉 17 条历史发现：10 条 ruff `--fix` + 7 条手改，涉及 `paper-qa-script/{remote_resolver,python_qa,analyze_paperqa_system}.py`、`src/collect_*.py`、`verify/{embed_load,freshness,index_health,local_dir,provider_switch}.py`）；C 级只报基线（棘轮交 `TG-6`）。**基线口径（2026-09-23 doc-audit 修正）**：交付时全仓 `D101=9,D102=23,D103=163,E501=1413`；**现行值只以 `verify_lint.py` 的 `[report-only C 级]` 报告行为准（本行不写读数）**——2026-09-25 一查 finding 2 实测：本行原写的 `2021 条 {...}` 已与工具不符，而同一批数字在 4 份文档里并存了 3~4 个互不相同的读数（`2026-09-25` 该行的历史读数仅作审计痕迹保留，不构成本文档的结论）。**正文只写"以 `verify_lint.py` 输出的报告行为准"**——该数字随文件增删必然漂移，手抄即错（本轮实测：本行与 §3 曾同时写旧值 `1858`，且与同处"文档不写死数字"的自述自相矛盾）。pin `ruff==0.14.4` 已入 `requirements-windows.txt`（CI 自动获得）；**§5.2 断网实跑已补**（代理黑洞 `127.0.0.1:9` + 对照探针证明出网被挡，见报告 §5.2，局限已标注） | ✅ |

## 8. 关闭复盘（Retro，关闭时写）

### 8.1 数据（对应 M1~M3 + 计划 §4 观察表）

| 指标 | 目标 | 实测 |
|---|---|---|
| 计划点 / 实际点 | 18 / — | 计划 **22.5**（原 18 + 插入 `TG-15` 2.5 + 前移 `TG-17` 2）／交付 **16** + `TG-15` 2.5（**已交付但按用户裁定不减**）＋冻结 `F-AC13/14` 4（不计）；**关闭时账上剩余 2.5**（两侧相等：`16+2.5+4=22.5`，见 §4 口径核对） |
| 插入卡点数 / 占用 | — | **插入 4.5 点**：`TG-15` 2.5（D1 用户批准）＋`TG-17` 2（D5 由 Sprint-18 前移为关闭阻塞，用户 2026-09-25 裁定）；`TG-16` 3 点**未计入**（待排期）；`TG-18`（1 点）**未计入**（Sprint-18 候选）。**占用**：`TG-15` 使本 Sprint 从 18 → 20.5 点，前移 `TG-17` 再 +2 ⇒ **22.5 点** |
| 关闭税（code major / 文档项 / 三查重跑） | ≤2 / ≤3 / 0 | **code：7 major ＋ 3 critical**（二查 windows `run-2026-09-25-code-review-071`＝**6 major + 7 minor**；二查 main `run-2026-09-25-code-review-072`＝**3 critical + 1 major + 3 minor**）；**文档项：12**（一查 `run-2026-09-25-doc-audit-070`＝**8 must-fix + 4 should-fix**，触该 spec 的报告条数上限）；**三查重跑：0 次**（069→070/071/072 各跑一次，未因"跑坏"而重跑；但**首轮即判出 19 条必修/应修**） |
| M1 / M2 / M3 | 达标 | **M1 = 未达标**（code major 7 > 2、文档项 12 > 3；"三查一次通过、无需重跑"字面满足）——**这是本 Sprint 最重要的负面读数**：治理批把闸门做得更硬，关闭期因此**暴露出更多**（含 4 条"检查在说假话"级发现），关闭税不降反升；**M2 = 5 项中 4 项为 0、1 项待锚点收口**（① 未跑〇查即派评审 = 0；② 锚点后未评审提交 = **关闭时点收口**（见 §9）；③ `self-chosen` 无 `--deviation` = **窗口内 0**、历史 33 条（规则落地前，账本有完整性哈希不可回填）；④ §9↔账本交叉 = **0**（`verify_close_readiness.py` linkage 清空）；⑤ 规范条目↔执行记录抽查 = 见下"抽查"）；**M3 = 达标（有保留）**（按有效工作日推进：UTC+8 逐日提交数 `09-20`14 / `09-21`31 / `09-22`5 / `09-23`27 / `09-24`11 / `09-25`38，**无零提交日**；保留意见：`09-25` 的 38 个提交里绝大部分是**关闭税**——"非爆发日模式"这一条在**关闭日**必然失真，读数应看"开发日是否有稳定推进"而非总量） |
| 红线事件（R1/R2/R3） | 0 | **R1 = 6**（编辑边界同型事故，含关闭期我自己的第 6 次，见 §10；治理模式已于第 5 次触发并退出）、**R2 = 1**（日期口径漂移，用户当场指出后已修）、**R3 = 1 未达标**（"断言必须在 CI 同款环境跑过"——本批 49+ 提交在关闭时为**未推状态**，CI 结论只能在 push 后取得；`§9.2` 已写死"push 触发的 CI 必须当真实关口，红即修"）。另记**非红线新类型事故 1 起**（并行会话越权开工，见 §10）与**非红线过程事故 5 起** |

### 8.2 结论与下一步（G2 输入）

**① 18 点规模上限是否成立 —— 不成立（实测 22.5 点）**
实测：原计划 18 →（插入 `TG-15` 2.5 + 前移 `TG-17` 2）→ **22.5 点**；交付 16 点 + `TG-15` 2.5（按裁定留在账上、结算见下）+ 冻结 4 点（`F-AC13/14`）。
但关键读数不是"22.5 > 18"，而是**结构**：`TG-6`/`TG-8`/`TG-9`/`TG-13`（8 点）**在关闭日同日交付**，同日还要产出 19 条必修/应修 + 全部关闭记录 ⇒ 计划书里"6 个有效工作日"的隐含假设（每天 ≈3 点、关闭税另算）**两边都不成立**。
**建议（供用户裁定，不在本轮自行改计划）**：G2 的上限改成**两个约束**——① 点数上限（建议 ≤14，不含关闭税）；② **每张卡必须能说出"哪一天在做它"**（防止 4 张卡挤在同一天交付、以及"卡在最后一天才动"）。观察值：**治理批的关闭税 ≈ 一张 3 点小卡**（19 条发现 + 3 条新教训 + 2 个新闸门）。

**② S0 告警机制是否可信 —— 可信（6 次全部当场闭环），但 100% 靠"自我发现"，说明它还不是机制**
6 次 R1 全部由"改完立刻复核"发现（4 次靠闸门/自检、1 次靠复跑闸门、1 次靠 `read` 肉眼），**无一次由外部（用户 / CI / 评审）先发现**，且全部在同一会话内闭环（无跨日挂账）。
弱点两条，都有实测：① **告警依赖"我记得复核"**——第 6 次正是我**跳过了已交付的 `structure-guard.py`** 的那次（"规则写了没执行"的第 6 个实例，见 §10）；② **治理模式退出后数小时内即复现同族事故** ⇒ "退出"≠"能力已具备"。
**建议**：G2 把 `A-M12` 的工具约束从"建议跑"升级为**结构性不可跳过**（例如 Markdown 编辑由包装脚本承担前后守卫、或提交前钩子），否则第 7 次仍会发生；**这条建议本身要按 §3.5 的 6 项标准交付**（证据/根因/机制/反向对照/在原始事故上验证/复用面），不接受只写规则。

**③ 规则→闸门是否真的减少了违规 —— 部分成立；"闸门可信度"成为新的缺口**
- **正面（均有实测）**：`verify_card_index.py` 上线即抓出 72 项孤儿索引行 → 0；`verify_md_tables.py` 上线即抓出 5 处既存表格缺陷；**E501 棘轮在本批在飞编辑中当场变红**（2698 > 2631）并迫使改动方折行——这是棘轮该有的行为；`verify_close_readiness.py` 真数据项 **13 → 1**；政策数据化后 6 处存量硬编码被清。
- **负面（同批实测，均已成教训）**：**4 条"检查在说假话"**——自指断言（1.64）/ 覆盖窗口无上界（一条新 run 即可静默致盲）/ 上限记在实测之上（给新增违规发静默配额，1.66）/ 裸 `assert` 在 `-O` 下空转（1.65）。⇒ **闸门数量的增长快于闸门可信度**。
- **结论**：**"规则→闸门"这条路线成立，但"闸门可信"必须先于"闸门数量"**。G2 应把"新增闸门"与"闸门审计"设为 **1:1 配额**（每加一条新闸门，必须审计一条存量闸门"它保护的对象是谁"）。

**点数结算（按已落为政策数据的 v0 口径，`agents/policy.json::deduction_rates`；本节只引用键、不复制数值）**
- 规则形态：以被判级的卡为对象、`扣分 = 卡点数 × Σ(未闭环发现的比例)`、同根因取最高级别、封顶 = 卡点数、按 `rounding_step` 取整。
- 实测：涉级发现集中在 `TG-6`/`TG-8`/`TG-9`/`TG-13`/`TG-15` 五张卡的交付物上；**major/critical 全部在审阅期内完成"修复 + 反向对照 + 证据行"**（清单见 §7；机读结论见 §9.2 的修复验证复核），`unclosed_criteria` 的四条判据逐条满足 ⇒ 按 v0 第 4 条"**审阅期之内修掉的发现不计**"，**未闭环 major/critical = 0 条**。
- 棘轮项（原"未闭环扣 0.5"）按 `ratchet_item_folding` 折算为一条指定级别的发现：`review_by=2026-10-31` 未到期、且上限机制已在本 Sprint 按实测口径落地 ⇒ **该发现在审阅期内闭环，不计数**。
- **结论：`TG-15` 结算 = 全额 2.5 点（扣 0）**。与 §4 账上"剩余 2.5"的关系必须说清：**账上留点 = 保守口径（"证据未齐不放行"）；结算是按扣率算出的最终值（"没有未闭环的 major/critical ⇒ 不扣"）**——两者不冲突，结算后 **有效剩余 = 0**（另有 4 点冻结不计，见 §4）。
- **口径留痕**：若关闭后仍有 major/critical 未闭环（例如 CI 在 push 后判红且未修），则**按同一算式重算**（扣除比例从 `deduction_rates` 取），不得"先记全额、事后不再看"——`§9.2` 的 CI 硬承诺即为此设。

## 9. 关闭三查与抽查记录（关闭时写）

- **关闭三查**：scope 来自〇查（run id：`run-2026-09-25-impact-assessment-069`，窄档，composite **18.46**；命中核心 13 / 路由 12，diff 无未命中项）｜一查（独立复现：`run-2026-09-25-doc-audit-070`）｜二查（覆盖缺口：`run-2026-09-25-code-review-071` = branch:windows、`run-2026-09-25-code-review-072` = branch:main）｜三查（结论）：**本轮"三查"的结论 = 关闭阻塞项 19 条（7 major + 3 critical + 12 文档项）全部在审阅期内闭环后，由"修复验证复核"独立确认**（见 §9.2；复核 run 不在本表，按 `1-WORKFLOW.MD` §4.2 分节写）。

### 9.1 结构化 run 表（`TG-11` ③c / M3；由 `verify/verify_close_readiness.py` 解析）

> 格式（`1-WORKFLOW.MD` §4.2）：首列必须是 `run-…`，列数 ≥5；**「关闭三查」与「修复验证复核」分两节写**，后者不得混入本表。

> **本表是"机读表"（两条格式口径，关闭期实测踩过，务必照抄）**：① run 行首列**不能加反引号**——`parse_sprint()` 判据是 `cells[0].startswith("run-")`（而 `ANCHOR_RE` 那行**允许**反引号，两处口径不一致）；② **`scope_source` / `target` 两列必须写账本原值、不得加反引号或注释**（闸门逐字比对账本，加了反引号就报"定位字段写错 = 指向了另一个对象"的**假红**）。人话说明写在 `coverage` 列与表下注记；账本里为空就留空。**两处口径不一致（反引号容错）已记为待修项。**

| run_id | role | target | scope_source | coverage | deviation |
|---|---|---|---|---|---|
| run-2026-09-25-impact-assessment-069 | impact-assessment | Sprint-17 close scope |  | (9ffc108d, 9ffc108d] 空区间——作用域类 run 按 B3 语义不承担内容覆盖，空窗口不判问题 | 无需 deviation |
| run-2026-09-25-doc-audit-070 | doc-audit | Sprint-17 close | impact-assessment:run-2026-09-25-impact-assessment-069 | (9ffc108d, f30c6e47] 共 33 提交 / 60 文件 | 0 |
| run-2026-09-25-code-review-071 | code-review | branch:windows | impact-assessment:run-2026-09-25-impact-assessment-069 | (9ffc108d, f30c6e47] 同一窗口 | 0 |
| run-2026-09-25-code-review-072 | code-review | branch:main | impact-assessment:run-2026-09-25-impact-assessment-069 | (9ffc108d, f30c6e47] **受控回填**（原落库为空区间）：其审查对象是 windows→main 的合并面，与 070/071 同一提交窗口；回填命令与理由见 §9.2 首条——**不是伪造覆盖**（它确实审了这批提交的合并面） | 0 |
| run-2026-09-25-lessons-learned-073 | lessons-learned | branch:windows | impact-assessment:run-2026-09-25-impact-assessment-069 | (9ffc108d, f30c6e47] | 0 |

三查锚点: `f30c6e47`（= 二查 `code-review-071` 实际覆盖到的 HEAD commit）

> **锚点与 HEAD 的关系（必须显式写清，否则 C3 判"未验证"）**：锚点那一刻 `HEAD == f30c6e47`，因此**当次运行时** `(锚点, HEAD]` 为空——闸门对此的判据是"不得视为通过，须写明理由"。**本 Sprint 的理由是**：锚点之后**确有**关闭期修复提交（19 条发现的修复），它们的覆盖由 **§9.2 的"修复验证复核"**承接（该复核的覆盖窗口覆盖 `(9ffc108d, 新 HEAD]`）；因此关闭记录必须在**修复提交落库之后**再复跑一次闸门，此时 `(锚点, HEAD]` 非空且逐条受检。**不得**用"锚点填成新 HEAD"的方式让该项消失（那是伪造覆盖）。

> **闸门**：`.venv\Scripts\python.exe verify\verify_close_readiness.py --sprint docs\iteration\sprint\2026-09-21-sprint-17.md`
> CI 已接：只对**声明了 `三查锚点`** 的 Sprint 文档执行该闸门（`ci.yml` 的关闭前置块）。本行填上真锚点 ⇒ **该步骤在 CI 上首次实跑**；本机在关闭时的实测结果见 §9.2 末条（期望 0 项）。
- **双盲抽查**（计划 §5 / DoD 第 6 条）：**抽样池** = `agents/runs/*/*.report.md`（全部 run 报告，脚本可枚举）；**固定种子** = `20260925`；**样本数** = 3；**我的独立结论先落盘再比对** —— 本轮样本为 `run-2026-09-25-code-review-071`（关键结论"4 条检查在说假话"）、`run-2026-09-25-doc-audit-070`（"断言数没有 SSOT"）、`run-2026-09-25-impact-assessment-069`（scope 判定与 composite 计算）；三份的"我的独立结论"已分别写在 §7/§8 与本节的对应位置（071 → 认账并据此修 4 条；070 → 认账并据此改 9 份文档 + 立"派生数字不得手抄"条；069 → 复核其 diff 无未命中项）。**与原文差异**：① 071 的初版结论"未发现检查在说假话"**被其作者自行推翻**（我方以独立复现确认为真，判据见 §10 与 3-LEARNED 1.64）；② 070 的"96/105580/50/2021"等读数我方复跑**全部得到不同值**（确认其 finding 1/2 成立）；③ 069 的 composite 与命中面我方未逐项复算（**局限，如实记录**）。**用户裁定**：本轮抽查未产生需要用户裁决的分歧。

### 9.2 修复验证复核（**独立一节，不混入 §9.1**）

> **格式要求**（`1-WORKFLOW.MD` §4.2）：关闭三查与"修复验证复核"分两节写，后者不得混入 §9.1 的 run 表。

**修复验证复核 run 表**（机读格式同 §9.1：首列裸 run id、`scope_source`/`target` 写账本原值）：

| run_id | role | target | scope_source | coverage | deviation |
|---|---|---|---|---|---|
| run-2026-09-25-code-review-074 | code-review | branch:windows fix-verification of 5313bb7 | impact-assessment:run-2026-09-25-impact-assessment-069 | (9ffc108d, 0f87433] 复核关闭期修复批（四条"说假话的检查"的修复、两条新闸门、棘轮只减不增、两笔 output_chars 回填）；**它自己也是"登记 run 就能归属覆盖"这一前提被推翻的证据**（见上"根因"） | 0 |
| run-2026-09-25-code-review-075 | code-review | branch:windows verify C3 engine fix 9902b03 | impact-assessment:run-2026-09-25-impact-assessment-069 | (9ffc108d, 9902b03] 复核 C3 引擎修复（左端点开区间 ⇒ 更老的锚点不再被丢弃）：修复真实、**无放宽**（锚点在表内的分支逐字节未变）；**抓出回归**：`_is_ancestor` 只 `except Exception` ⇒ 判不了时整闸门中止 | 0 |
| run-2026-09-25-code-review-076 | code-review | branch:windows verify regression fix 6447fe7 | impact-assessment:run-2026-09-25-impact-assessment-069 | (9ffc108d, 6447fe7] 复核回归修复：异常域不过宽（try 体内只有一条 `git merge-base` 探针、全仓唯一生产调用、不吞 `KeyboardInterrupt`）、端到端由 `POLICY-ERROR` **翻转为具名报告**、对照 c 用副本还原法证伪为空转；100 个数值叶子递归比对只有一处 DOWN；**抓出上界点名只兑现一半 + `measured_total` 三方不一致（2839/2833/2815）** | 0 |
| run-2026-09-25-code-review-077 | code-review | branch:windows verify residuals fix b7d8cab | impact-assessment:run-2026-09-25-impact-assessment-069 | (9ffc108d, b7d8cab] 复核残留修复：两端点判据已**独立**（无 continue/elif 互斥）、`measured_total` 等式**有牙**（并在在飞工作树上当场咬到 `E501 2515→2517` 与 `total≠sum`）、107 个数值叶子 0 上调、无假绿 | 0 |
| run-2026-09-25-code-review-078 | code-review | branch:windows verify false-positive fix 0d7dcf5 | impact-assessment:run-2026-09-25-impact-assessment-069 | (9ffc108d, 0ddb6a21] 复核对称性修复（**收尾轮**）：真数据 17 个窗口 problems=0，其中 **13 个历史窗口两端点都证明为更老、既不点名也不贡献覆盖**（9 个是**非豁免**的内容评审 role ⇒ 静默确由 `older_throughs` 提供，不是被豁免掩盖）；bogus 40 位 sha 仍具名点名（rc=1，anchor+through 两行）；对照 d 变异即失败（自检 66 断言）；数值叶子 `0d7dcf5^`→当前**仅 2 处变化且都 DOWN**；无假绿 | 0 |
| run-2026-09-25-code-review-079 | code-review | branch:windows verify CI-unblock fix 1ea398c | impact-assessment:run-2026-09-25-impact-assessment-069 | (9ffc108d, 1ea398c] 复核 **CI 红点修复**（CI #207 根因）：真实 clone 下 `run_suite --tier offline` → **SUITE PASSED (24 scripts)** rc=0；M-g 是**真跳过**（`SKIP[ledger-absent]`、`PASS: M-g` 计数 0、断言数 107→**104** 恰少 3 条）；坏账本仍 rc=1 fail-closed；数值叶子仅 2 处 DOWN；无假绿。**并纠正我的方法错误**：`git archive` 模拟会剥掉 `.git` ⇒ 两脚本假失败，**必须用 `git clone`** | 0 |

> **复核链的净结果（4 个独立 run，共 4 轮，全部独立登记）**：① 关闭期修复批**可以按"已修复且已验证"接受**；② C3 的规范窗口 `(三查锚点, HEAD]` 从"无法表达"变成可表达（引擎修复）；③ 引擎修复自身引入的**两个回归**（整闸门中止 / 历史窗口误报）在**下一轮复核与本轮自查**中各被抓出并修掉；④ 每条修复都补了**打真实路径**的反向对照（B5 a~d），而不是注入式空转对照。**这条链本身就是本 Sprint 主题的实例**：闸门"看起来在判"与"真的在判"必须分开验证——4 轮里每轮都抓到了上一轮漏掉的东西。

> **复核结论（`run-2026-09-25-code-review-074`，2026-09-25）**：**"修复批可以按『已修复且已验证』接受"**——四条失信修复在它自己的反证下全部成立（含它自写的规避探针）、两条新闸门非恒真（注入即红）、**没有任何数值上限被上调**、两笔 `output_chars` 回填值与归档报告字节数**逐字相等**。同时它**推翻了我的一个前提**（登记复核 run 即可归属 `5313bb7`）并给出 5 条"提交信息/注释与事实不符"的更正（其中 `artifact_paths` 断言数、`gate_integrity` 的"动态证明"覆盖面、政策措辞的口径混用**已在本提交内更正**）。

**这一节要回答的问题**：三查判出的 19 条必修/应修**修完之后**，谁来独立确认"修得对、且没有把闸门改松"？

- **本 Sprint 的实际状态（如实记录，2026-09-25）**：修复批分三个子代理 run 执行（四个关闭期 major / 推前 CI 阻塞项 / 两条闸门失信 major）。三者在**余额耗尽**时中断，主代理接手做完并逐条自查；**迄今没有一次"独立"的修复验证复核 run**。
- **主代理已做的自查（可复跑，非独立复核）**：① 全档 offline 套件（24 脚本）；② 逐闸门单跑 + 其自检反向对照（`verify_lint` 11、`verify_card_index` 14、`verify_ledger_measurement` 33、`verify_artifact_paths` 10、`verify_derived_numbers` 10、`verify_gate_integrity` 9、`verify_close_readiness` 自检 + 真数据、`verify_md_tables`、`verify_agentops`）；③ **反向对照不被削弱**：新增 `-O` 守卫探针（`python -O` 必须红）、棘轮上限单调性（对 `git HEAD` 逐项比较，上调即 FAIL）、棘轮"注入 1 条即红"的真实 caps 对照；④ 棘轮上限只下调（E501 2631→2576、D103 207→205），**未上调任何上限**；⑤ `agents/policy.json` 的封闭世界自检（死键）为 0。
- **因此 C3 的判定（必须先说清，不得含糊）**：`三查锚点` 之后**确有**关闭期修复提交，而**承担其覆盖的内容评审 run 不存在** ⇒ 复跑 `verify_close_readiness.py` 时 `[C3] 锚点→HEAD 有 N 个提交无归属` **会如实地红**。**实测（2026-09-25，`5313bb7` 之后）**：先是 1 项（`5313bb7240` 无归属），登记复核 run `code-review-074` 后**仍是 2 项**——**修复验证复核发现这不是"漏登记"，而是 C3 引擎的缺陷**：
  ```
  [C3] 锚点 f30c6e47→HEAD 有 2 个提交无归属：95c340be53, 5313bb7240
  ```
  **根因（复核实测，可核）**：`attribution()` 的提交序表只含 `[HEAD, *rev_list(文档锚点, HEAD)]`，而 `CoverageWindow.covers()` **要求窗口两个端点都在序表内**；复核 run 的锚点 `9ffc108d` 比文档锚点更早 ⇒ 窗口被**静默丢弃**。复核枚举账本里全部 **14 个窗口：0/14 命中**——即 `(三查锚点, HEAD]` 这个**规范窗口在当前实现下无法表达**，且第一个锚点后提交**对任何窗口都不可归属**。
  **处置（同日，本提交）**：修**引擎**而不是塞例外——左端点本来就是**开区间**，锚点落在区间外（更老）时不应丢弃窗口。改动 = `agent_policy.py` 新增 `_is_ancestor()` 判定"锚点早于文档锚点"→ 该窗口左端视为 −∞ 照常覆盖；**证明不了**的（无关/未来/shallow）仍 fail-closed **并逐条点名**（原来一个字都不报）；`verify_close_readiness.py` 加 **B5 三条反向对照**（更老 → 覆盖；无关 → 不覆盖且点名；作用域类 → 不点名）。修后本机 `[C3]` 项**归零**，只剩待登记复核 run 的 linkage 项。
  修法是二选一的历史选项（**已按 ③ 执行**），留档如下：
> **修后复查（精确表述，避免"归零"这种模糊说法）**：`5313bb7` / `95c340b` / `0f87433` 三个提交改由 `run-…-074` 的窗口归属（复核实测覆盖表：`0f87433b83,95c340be53,5313bb7240 = run-…-074`）；`[C3]` 项在**当次树状态**下消失，仅剩"复核 run 尚未登记进 §9"的 linkage 项。**随后每落一个代码提交都会重新出现 1 项**（新 HEAD 尚无窗口覆盖）——这正是"锚点→HEAD 必须逐条有归属"的应有行为，不是缺陷。
> **修复批自身的回归（由复核 #2 `run-…-075` 抓出，同日修）**：第一版 `_is_ancestor()` 写 `except Exception`，而 `git()` 抛的 `PolicyError` 是 `SystemExit` 子类 ⇒ **捕获不到**：锚点解析不了时（bogus sha / 孤儿分支 / shallow 缺失 / 普通非祖先，复核四种输入全部实测）不是"返回 False + 逐条点名"，而是**整个闸门硬中止**（`POLICY-ERROR`，连报告都不出），并让新增的点名分支沦为**死代码**。方向仍是 fail-closed（无假绿），但属**健壮性回归 + 提交信息过度声称**。修法 = 同时兜住 `SystemExit`；补 **B5 反向对照 c**（**直接打真实函数 + 真实仓库**，而非注入 `older_anchors`——原对照正因走的是注入路径才漏掉它）；用复核的原样复现（临时账本副本里把 `run-…-074` 的锚点改成 `deadbeef…`）实测：修前 `POLICY-ERROR` 无报告，修后 **rc=1 且窗口被逐条点名**（`[C3-窗口] … 证明不了 … 贡献 0 覆盖`）。
  1. **补一次修复验证复核 run**（`code-review` role，`--coverage-anchor 9ffc108d…`，审的就是这批修复提交）——**代价**：一次子代理 run 的 token（本轮已因余额耗尽中断三次）；
  2. **接受"关闭期修复未经独立复核"并如实记账**——则本 Sprint 的 C3 判据**不成立**，关闭结论只能写成"**未达成关闭前置**"，且 `§8.1` 的 M1/M2 读数需相应降级（M2 第 ② 项由"0"变为"未验证"）。
- **不允许的做法**（写下来防下一轮走捷径）：把锚点改成新 HEAD 让空区间消失、把修复提交塞进 `coverage-exceptions.json`、"主代理自己登记一条 `code-review` run"充当独立复核——三者都是**伪造覆盖/冒名**，`TG-17` 已明令禁止。
- **CI 没有账本时的口径（C4，2026-09-25 关闭期实测并修）**：`agents/runtime/registry.json` 被 `.gitignore` 忽略 ⇒ **CI 的全新 checkout 必然没有它**，而关闭闸门的 C1/需求、linkage、C3 覆盖归属**都以账本为数据源**。旧行为把"没有数据"判成"数据不合格"（实测 **10 项 FAIL**：4 条"缺 run" + 6 条"§9 写了但账本无记录"）⇒ **推上去那一刻 windows 的必需检查恒红**，而唯一的"修法"只能是伪造账本。现按 `verify_ledger_measurement.py`（`code-review-072` critical）的同一口径：**账本缺失 → 显式 `SKIP[ledger-absent]`（理由上屏、rc=0，且仍校验不依赖账本的部分：文档可解析 / §9 表结构 / 锚点已声明）**；**账本存在但坏 → 照旧 fail-closed**。实测：`AGENT_OPS_DIR=<空目录>` → `CLOSE-READINESS SKIP-PASS（账本缺失档）` rc=0；有账本 → 正常判定。**完整关闭判定仍是本机/关闭期动作**（CI 只能执行前半段）。

### 9.3 未推提交暂存分支（P-a 方案，用户 2026-09-25 批准"按优化方案执行"）

> **建立时间**：2026-09-25（UTC+8）｜**建立人**：主代理（本会话）｜**用户批准**：2026-09-25"按照你的优化方案来执行"

| 项 | 值 |
|---|---|
| 分支名 | `park/sprint-17-pre-governance-2026-09-25` |
| 指向 | `457fe7f903a4d0679e9d6591894478efe4c9b3cb`（= 当时 `windows` 的 HEAD，含 **25** 个未推提交） |
| 远端 | `origin/park/sprint-17-pre-governance-2026-09-25`（已推） |
| 干线状态 | `origin/windows` **仍为 `f2d37ab`**（未背书、未改动；本地 `windows` 领先 25 个提交） |
| 采用变体 | **安全变体**：**不对 `windows` 做 `reset`**——`reset` 会让工作树回退（R1 型不可逆风险：曾发生 `switch -c` 使 `docs/iteration/**` 从工作树消失、`switch -f` 丢修复），而收益仅"分支看起来干净" |
| 回收动作 | 治理模式**退出后**：`git push origin windows`（**fast-forward，无 merge**）→ 删除 `park/*`（本地 + 远端） |
| 留痕要求 | 本表即留痕；回收后再补一行"已回收 + 回收时 sha"——**暂存分支不得成为第二个无人认领的远端状态** |
| **回收记录（2026-09-25）** | ✅ **已回收**：`git push origin windows` = `f2d37ab..52e0dfe`（**fast-forward，无 merge**）；随后 `git push origin --delete park/…` 与 `git branch -d park/…` 均成功（删除时指向 `457fe7f`，已是新 HEAD 的祖先）。**远端分支现为 `mac / main / windows` 三个**（`git ls-remote --heads origin` 实测），本地只剩 `main / windows` |

**① CI 盲点必须当真实关口（落实用户 2026-09-25"前两点要落实到位"）**——**已兑现，且红过一次、修掉后才绿**：
- **CI #206**（`f2d37ab`，推送前）是这批提交之前最后一次 CI；它**不含**本 Sprint 新增的显式闸门步骤（该步骤在 `482c503` 之后才加）⇒ 未推提交确实"没有 CI 证据"。
- **CI #207**（`52e0dfe`，首次推送）= **failure**，红在第 7 步 `Offline verify suite`（8~11 步因 skipped）：`SUITE FAILED (1/24): verify_agentops.py` → 日志根因 `FileNotFoundError: agents\runtime\registry.json`——该脚本的 M-g 段**直接读真实账本**，而账本被 `.gitignore` 忽略、CI 全新 checkout 必然没有 ⇒ **本机全绿掩盖了它**。**按硬性承诺"红即修"处理**（未以本机全绿替代）：改为账本缺失时显式 `SKIP[ledger-absent]`（不打印 PASS）、坏账本仍 fail-closed。
- **CI #208**（`1ea398c`，修复后）= **success，11 步全绿**：离线套件 24 脚本、**关闭前置块（首次带真锚点实跑）**、显式闸门档（card index / md tables / hardcode / lint / structure guard）、两个前端构建。
- **结论**：红线 **R3 由"未达标"转为"达标（有 CI 结论 + head sha）"**；CI 结论由 GitHub API 直接取得（`/actions/runs?head_sha=…` + `/runs/<id>/jobs`），非人工转述。
- **方法学教训（本轮实证，留给下一轮）**：**"全新 checkout 模拟"必须用 `git clone`（带真实 `.git`、`fetch-depth: 0`），不能用 `git archive`**——后者剥掉 `.git` 后 `verify_agentops`（`register` 无法解析 HEAD）与 `verify_artifact_paths`（无 `git check-ignore`）会假失败（实测 `SUITE FAILED (2/24)`），把"方法产物"误读成"代码缺陷"。这是复核 `run-…-079` 纠正我的一处方法错误。

**② 命名与回收留痕已写死**：分支名带阶段与日期（`sprint-17-pre-governance-2026-09-25`），回收命令与判据固定为"退出 → FF 推 `windows` → 删 `park/*` → 补回收行"；任何人（含后续会话）凭本表即可判断该分支的归属与命运。

> **未推提交的构成（25）**：对方 D2/D3 交付 15（`092a2f78..2514472`）+ 对方交接前 1（`f2d37ab` 之后的收尾）+ 本会话审计与治理相关 docs(pm) 9（`7e188eb`…`457fe7f`）。

## 10. 红线事件与过程事故（S0：告警 + 当日闭环，不硬停）

> **本 Sprint 关闭阻塞项前移（2026-09-25，close-blocker 批；用户裁定）**：`TG-17` ①（覆盖窗口语义）②（C1 判定域）与两项同族缺陷（`covers_through` 短 sha 无回填口、实现类工作挂评审 role）**从 Sprint-18 前移至本 Sprint 关闭阻塞项**。
> **前移理由（不修则关闭闸门不可达 + CI 必红）**：真数据 `verify\verify_close_readiness.py --sprint docs\iteration\sprint\2026-09-21-sprint-17.md` 实测 **13 项**（`[C1]` 1 + `[linkage]` 11 + `[C3]` 1）——**填 §9 也清不掉全部**；而 CI 的关闭前置块**只对声明了真实锚点的 Sprint 文档执行**（`ci.yml:73` 正则，实测本 Sprint 锚点仍为 `待填` ⇒ 当前**选择器命中 0 份**、该步被跳过），因此"填好锚点"的那一刻就是该闸门首次实跑、**必然红**。
> **点数（按实测工作量估）**：B1 窗口口径 + B2 回填口 + B3 空窗口语义 + B4 `set-scope` 回填 + 11 条反向对照断言 + 卡/文档同步 ⇒ **2 点**，计入本 Sprint（`TG-17` 原排 Sprint-18；移至本 Sprint 关闭期）。
> **交付与残留**：B1~B4 已交付（详见 [`cards/TG-17.md`](../phases/testing-governance/cards/TG-17.md) §交付落地）；**未闭环**的是 C3 未归属提交（最新内容评审 run 的 `covers_through` 早于 HEAD）——属**关闭期动作**，不得用回填伪造覆盖。**D0 批（2026-09-25）已清掉另外两项**：`doc-audit-066` 短 sha（`set-anchor` 规范化，同一提交、范围不变）与 `code-review-068` 空窗口（如实标注为产出型 run，覆盖字段一字未动）。
> **实测修正（勿沿用"填完即 PASS"的旧预期）**：闸门真数据实测改前 **13 项** → B1 后 **6 项**；用真实账本行填 §9（5 行）+ 真锚点后为 **3 项**（2 项 C3-窗口 + 1 项 C3 未归属提交），**不是 0**。**D0 批复测（2026-09-25，样本在 `%TEMP%`，仓库 §9 未动）**：清掉那 2 项 C3-窗口后**剩 1 项** = `[C3] 锚点 9ffc108d→HEAD 有 16 个提交无归属`（16 = 全部非 doc-only；比原投影的 15 多 1，因为关闭阻塞项批自身那个提交 `c5407844` 改了 `agents/policy.json`/`scripts`/`verify`）。另：CI 关闭前置块的选择器（`ci.yml:73` 正则）实测在 19 份 Sprint 文档上命中 **0** 份（本 Sprint 锚点仍为 `待填`）⇒ 该步骤当前被跳过，**填上真锚点的那一刻**才首次实跑并报错。

| 时间 | 红线 | 事实（可核） | 告警→闭环 | 根因 | 是否 ≥2 次/Sprint |
|---|---|---|---|---|---|
| 2026-09-25（关闭期） | **非红线：三个修复批因余额耗尽中断，主代理接手** | 关闭期修复批分三个子代理 run：四关闭期 major（`c3c3b9cd`）、推前 CI 阻塞项（`befa3a89`）、两条闸门失信 major（`96e7e459`）。三者在**同一分钟内**报"余额不足"中断，**均未回执**、留下一批**未验证的半成品**：`verify_ledger_measurement.py` 的重构只改了定义未改调用点（`real_data()` 抛 `NameError: is_legacy`，而自检仍打印 `ALL PASS (30 assertions)`——正是本 Sprint 反复出现的"打印成功却崩"形态）、`scripts/structure-guard.py` 与 `verify/verify_agentops.py` 的新代码顶破 `verify_artifact_paths` 动态目标上限（6>5 / 7>5）、`verify_lint` E501 由 2616 涨到 2830（上限 2631）、`verify_card_index` 的 fixture 落点挂函数参数被判动态目标。 | **主代理逐条接手并自查**（无独立复核，见 §9.2）：修掉 `is_legacy` 调用点、把长 CJK docstring/注释按**显示宽度**折行（2830→2576）、fixture 落点改模块级常量、给 `-O` 守卫、`verify_card_index` 判据 ⑥⑦ 补实现、`set-output-chars` 受控回填 069/073 的 `output_chars`；最终 `run_suite --tier offline` → **SUITE PASSED (24 scripts)** | ① **单会话串行批次的串扰**：三个修复批同时改同一批文件（`verify_ledger_measurement.py` 被两批同时改）；② **中断不留痕**：子代理在"未回执"状态下退出，仓库里看不出哪些改动是半成品（只能靠逐闸门复跑发现）；③ 余额耗尽的**时机**恰好落在"改完、未验证"的窗口——说明"验证"这一步在流程里仍排在"余额允许"的条件之后 | 否（新类型：资源中断，首次；但"中断必须留痕"已有规则，本次**未做到**——子代理无法在中断前写出状态） |
| 2026-09-25（关闭期） | **R1 告警（第 6 次·同型，主代理）** | 关闭期回填 `3-LEARNED` 时，一次 `edit` 的 `old_string` 以"行尾换行"收边、`new_string` 未补回该换行 → `docs/3-LEARNED.MD:346` 的标题行**吞掉下一行**（`## 2. 验证记录…` 与表格表头 `\| 检查 \| 结果 \|` 合并成一行，该表块失去表头）。**发现路径必须如实记录：是我改完立刻 `read` 该区间时肉眼看到的，不是闸门抓到的**（同批 `verify_md_tables.py` 在修复后才跑；无表头的表块是否会被它判出，本轮**未做反向对照**，故不声称）。 | **自我发现**：`read` 复核该区间 → 立即拆回两行 → 跑 `verify_md_tables.py`（163 文件 PASS）+ 复核 `3-LEARNED` 标题序列（`## 2.`→`### 2.1`→…→`## 3.` 连续） | 与第 1~5 次同源：**以边界内容（此处是行尾换行）当锚点**；**且违反了 A-M12 已交付的"结构性编辑前后各跑一次 `structure-guard.py`"**——我把它判成"纯文本替换"而跳过守卫，而它实际改变了行结构（这正是该守卫存在的理由）。**A-M12 判据 ④（下 Sprint 同族事故 ≤1）的度量口径必须显式回答"交付日到 Sprint-18 起点之间的这段算不算"**——本条正落在这个缝里（守卫已交付、事故仍复现），故在此**只记事实**：`A-M12` 交付（2026-09-25）之后、Sprint-18 之前，同族事故 **1 次**（本条），是否计入 ④ 由 ④ 的口径定义决定（口径见 `cards/A-M12.md`）。**本 Sprint 内 R1 累计 6 次** | 是（**R1 累计 6 次**；治理模式已于第 5 次触发并已退出，本条属退出后的复现，**不得当成"退出后不算"而不记**） |
| 2026-09-25 | **R1 告警（第 5 次·同型，主代理）** | 为分析文档追加新节时，`edit` 的 `old_string` 取了**下一节标题**（`## 7. 我接手的工作面`）→ 该标题被吃掉、其表格成"无主表格"；**同一形态在 Sprint 内第 5 次**（前 4 次见下三行） | **自我发现**：追加后跑 `grep '^## '` 复核标题序列 → 立即补回标题并重排编号；随后跑 `verify_md_tables.py`（131 文件 PASS） | 与第 1~4 次同源：**`edit` 以"边界内容"当锚点**；自检有效但**未前置**（都是事后抓） | 是（**R1 累计 5 次 → 治理模式已由 §2.2.1 自动触发**，见本文档 §8.2 与 [`2026-09-25-d2d3-parallel-session-quality-analysis.MD`](../../phases/testing-governance/2026-09-25-d2d3-parallel-session-quality-analysis.MD) §7） |
| 2026-09-25 | **非红线（流程/协作事故：并行写入 + 越权开工）** | 另一会话（"部署Codex Harness并查最佳实践"）**误开启并执行了本 Sprint 的 D2/D3**：15 个本地未推提交（TG-14 **卡迁移（卡数一律以 `card-inventory.py` 现跑输出为准；本处不写现值）** / TG-15 落地 / TG-17 与 A-M12 开卡 / `verify_md_tables.py`+`verify_no_policy_hardcode.py`+`verify_card_index.py`）；用户判定交付质量不足（U1 低级错误重复、U2 以"延期"请示、U3 治理建议只写机械规则、U4 态度敷衍） | 用户当场裁定：**全部工作收归本会话**；分析+收归方案入 [`2026-09-25-d2d3-parallel-session-quality-analysis.MD`](../../phases/testing-governance/2026-09-25-d2d3-parallel-session-quality-analysis.MD)；15 提交**先不推**、待独立审核 | ① 单仓多写者（无所有权/锁）② 计划文档无"执行会话"字段（**计划即邀请**）③ 开工无闸门 | 否（新类型，首次） |
| 2026-09-23 | **R1 告警**（文件/内容被吞） | 在 `phases/testing-governance/backlog.MD` 末尾追写 TG-15/TG-16 两张卡时，`edit` 的 `old_string` 把**锚点行 `## 2. 启动条件` 一起吃掉**（该节标题消失，只剩其下的条目） | **自我发现**：替换后立即 `read` 复核 → 30 秒内补回标题，并用 `tables.py` 复测表格结构（L11–L28 / 18 行 / 7 列全一致） | `edit` 以"锚点行"作为替换边界时的固有风险（同一形态曾多次发生，见 A-M11 ⑥㈦ 与 TG-12 第 5 波） | 否（1 次） |
| 2026-09-23（D2） | **R1 告警（第 2 次·同型）** | 在 `docs/1-WORKFLOW.MD` 插入新节 `## 3.1 政策数据化` 时，`edit` 的 `old_string` 取的是**下一节的首行粗体标题**（`**卡片来源与时间口径…**`）→ 替换后该标题整行消失，其下的前缀表变成"无主表格" | **自我发现**：插入后立刻 `read` 该区间 → 发现标题缺失，随即"补回标题 + 保留原句"；`grep` 三处锚点齐全复核通过 | `edit` 用"下一节首行"当替换边界 = 把边界内容当锚点（第 1 次 R1 的同一形态） | 是（Sprint 内第 2 次） |
| 2026-09-23（D2） | **R1 告警（第 3 次·同型）** | 修 §5 风险表第 6 条时，`edit` 的替换把**该表格行的"处置"单元格顶出**（第 97 行只剩 2 列），并残留一行孤立的表格首格（"7"） | **自我发现**：改完即跑"每个表格块列数一致性"自检 → 报 `表格 @line 90: [(3,[90..93]),(2,[97])]`，随即修复并复测 `9 块 0 处不一致` | 与前两次**同源**：`old_string` 跨越"单元格/表格行尾/下一节标题"边界时会把边界内容吃掉。**三次全部由"改完立刻跑结构自检"发现**（无一次靠肉眼） | **是（Sprint 内第 3 次）** → 已远超 §5 风险表第 2 条的 ≥2 阈值；**待 §8.2 决定是否转治理模式**。拟固化两条纪律：① 插入内容时 `old_string` 只取**上一节尾行**，绝不取下一节首行/表格行；② 每次改 Markdown 结构后**必跑结构自检** |
| 2026-09-23 | **R2 告警**（卡片/文档内容漂移） | 本轮新写/更新的多份文档**日期标为 2026-09-21**（沿用会话起始日期），而**实际落库与开工是 2026-09-23（UTC+8）**：网络时间核验 `Date` 头 → UTC+8 `2026-09-23 01:30`；本批 9 个提交的 committer 时间为 `2026-09-23 21:13 JST`（= 当日 UTC+8 `20:13`） | **用户当场指出**（"今天网络时间是 9.23 了"）→ 已加 §0 上方的日期口径更正注记 + `1-WORKFLOW.MD` §6 补"日期每轮核验"规则 + §10 本条；**未重命名文件**（避免链接 churn） | 会话跨日后仍沿用**会话起点日期**，且当时的"日期"并非当次核验值——正是 TG-12 要治的"派生事实无 SSOT + 无重生成" | 否（1 次） |
| 2026-09-21 | 非红线（过程事故） | 开工〇查 run-061 的账本 `task_id` 中文经 `pwsh→python` 传参**落库即乱码**；`update` 无 `--task` 参数 → 当场无法修 | 当场定性并记入 `TG-13` 实例⑥；关闭期以 ASCII 描述重写 + `TG-13` 加"字段可读性"断言 | 非 ASCII CLI 参数被宿主编码层转码（`python -c "print('中文测试')"` 同机可复现） | 否 |
| 2026-09-21 | 非红线（假红） | 主代理首次跑 offline 套件用了系统 `python` → 4 脚本因缺 `paperqa`/`fitz` 失败（误判为回归） | 当场换 `.venv` 解释器复跑 → **14 脚本 SUITE PASSED**；已写入 §5 风险表命令规约 | 环境/解释器未在命令中固定 | 否 |
| 2026-09-23（D3） | **非 R1：子代理越界改写历史文档**（不由主代理执行，但**主代理认账**） | P1 试点子代理为让 strict ④ 通过，把 `docs/iteration/sprint/2026-08-31-sprint-14.md` 的一条计划表行**改写**为指向卡文件的链接（越界提交 `867d7d9`，1 行） | **主代理复核时发现**（`git diff` 该文件非空）→ revert `f5e1345` → 取回迁移内容时**仅还原该文件**（`8d75951`）；现该文件 `git diff` 为空 | ① 规则写成"允许做什么"（"只改引用不改正文"）⇒ 执行者把边界外推；② **判据 ④ 当时是假阳性**（把合法计划表行判成违规），把执行者推进"改数据 vs 改闸门"两难 ⇒ **坏判据会主动诱发违规** | 否（已独立成案） |
| 2026-09-23（D3） | **R1 告警（第 4 次·同型，主代理）** | 为修 4 份 Sprint 文档的表格缺陷，自写转义脚本 `_fix-pipes.py`；它把**单元格边界管道**也一并转义 → `sprint-15`/`sprint-16` 整表被压成 1 格 | **自我发现**：修完立刻跑 `verify_md_tables.py` → 报"单元格数 1 ≠ 众数 3" → 判定脚本有害 → `git checkout` 恢复两份文件、删除脚本，复跑闸门确认回到原状 | 脚本假设"该行多加的管道都是内容管道"，但**无法区分内容与边界** ⇒ 与第 2/3 次同源：**编辑边界的假设错** | 是（**R1 本 Sprint 累计 4 次**）→ 远超 §5 第 2 条阈值，**§8.2 Retro 必须定是否转治理模式** |
| 2026-09-23（D3） | **非 R1：验证工具连错 5 版（主代理）** | 为独立核验 P3/P4 的守恒声称，自写核对器 5 版各有缺陷：① 假设全仓列序统一 ② 丢弃空单元格 ③ 表级 padding ④ 行级对齐猜反 ⑤ 缺常量定义 | **每次都由工具自身输出暴露**（报错/自相矛盾）；最终改用**列序无关判据**（集合问题：不新增/不丢失）收敛，并**停止继续改工具** | 反复"用工具迁就结论" ⇒ 教训：**同一处反复报错时，先怀疑判据、再怀疑操作，最后才怀疑数据**；判据优先选**列序/映射无关**的形态 | 否 |

**待立教训 → 关闭期已收口（`run-2026-09-25-lessons-learned-073`，2026-09-25；编号已落 `docs/3-LEARNED.MD`）**：lessons run 按 spec 的 `max_entries=3` 上限，从未被"关闭期最硬的证据"里蒸馏出 **3 条正式条目**——**1.64**（闸门自检的自指断言：断言"我的探针不在了"≠断言"没有违规产物"）、**1.65**（裸 `assert` 承担判定 ⇒ `-O` 下闸门空转并打印 `ALL PASS`）、**1.66**（棘轮上限记在实测之上 = 静默配额；反向对照用被测对象自造的上限 = 恒真式）。三条都属 🏗️ 工程方法，均已写入 3-LEARNED 并进分类索引。**原 4 条候选的处置（逐条，不得静默丢弃）**：
① **"工具失败 ≠ 环境失败"**——harness `web_fetch` 全阻时，子代理把"核查手段缺失"写成"全部依赖未通过"；**进程级探针**（同机 `urllib` 直连 PyPI/npm/OSV = 200）证明是工具限制 → 教训：**任何"无法执行"的结论前先做可达性探针**。
② **非 ASCII 内容禁止走 CLI 参数**（走文件/JSON），否则落库即乱码（本轮 `task_id` 实证；与"含反引号的 commit 消息走 `-F <file>`"同源）。
③ **子代理的 finding 是"主张"，不是"事实"——必须独立复验后才动手**（TG-15 复核批实证）：一查 doc-audit 对 `verify_no_policy_hardcode.py --dir` 的判断是"恒 return 0 的 fail-open"，我实测真相是**打印时 `KeyError: 'file'` 崩溃**（根因在 `scan_file` 没给 findings 盖契约字段）——两种失效形态的修法完全不同；若照单全收就会修错地方。**纪律**：每条 finding 先跑一遍最小复现，再决定修法；复验结果与被报不符时，**以实测为准并在记录里写明差异**（本轮已如此记录）。
④ **闸门的断言必须驱动"真实入口"，不能只读代码判断它会不会 fail-closed**（同批实证）：`--dir` 分支的退出码此前只被"读了一眼 return 语句"确认过，而真正的缺陷（缺 `file` 键即崩）在 `print` 处；修法是**用子进程跑真实 CLI 并断言 rc**（现已加入 3 条该形态断言）。同源：TG-15 的 C3 空区间 critical——"锚点==HEAD 时零提交受检却打 ✔"，正是"判定条件在空输入下恒真"的经典形态。

**四条候选的最终处置（lessons run-073 逐条给结论，主代理复核后落此）**：

| 候选 | 处置 | 依据 |
|---|---|---|
| ① 工具失败 ≠ 环境失败（可达性探针） | **本轮不升为条目**，保留为候选（Sprint-18 若再出现"因工具受限而误判环境"的实例则收入） | lessons run 判定：本轮无**新增**实测，仅有旧实例；避免"为凑数而立条目"（spec 的 `max_entries` 精神是"宁缺毋滥"） |
| ② 非 ASCII 内容禁止走 CLI 参数 | **不单列**，并入既有条目（1.18 / 1.51 一族的编码口径） | lessons run 判定：唯一证据是**既存**的 `task_id` 乱码（本轮无新实测）；且本轮新证据反而说明该形态**已可判**（lessons run 自己就改用文件传参绕开了它） |
| ③ 子代理 finding 是"主张"不是"事实" | **不新增编号**，作为 **1.64 的跨引用实例**保留 | lessons run 本轮**亲自实践**了这条：它推翻了自己初版的一条结论、把无法独立复现的"live lint red"降级、并把"报告声称已修但提交里没有"降级为"现象可核、根因不可核"而**不立为第 4 条**——这既是证据也是范例；机制上与 1.64（断言必须指向真实对象）同宗 |
| ④ 闸门断言必须驱动真实入口 | **由 1.65 承接**（`-O` 档实测：真实入口两档都必须红） | 1.65 的 fix 判据即"同一失败样本在正常档与 `-O` 档都必须 `rc≠0`"，比"读一眼 return"更强 |

> **口径（防下一轮"49/63"式漂移）**：`3-LEARNED` 条目总数一律以该文件实际编号为准，**任何文档不得手抄总数**（本轮起该数由 `verify/verify_derived_numbers.py` 的口径约束：见 `1-WORKFLOW.MD` §6"派生数字不得手抄"）。
