# Sprint 3（2026-08-29）—— 数据源抽象 B：本地目录/URL/manifest/论文ID 多源 + 前端切换 UI

> 归属：`docs/iteration/sprint/`。输入：`../phases/refactor-analysis/refactor-analysis.MD` 主题 B（前端2：load_index 数据源写死本地绝对路径）。
> 范围：windows 分支迭代；完成后同步 main 的 windows 部分并对两分支做 code review；MAC 验证跳过。
  原则延续 Sprint-2：**默认本地目录行为不变**（回归为证），新增能力只做加法。

## 1. Sprint Planning

- **Goal**：在行为不变的底座上，把"数据源"从单一 `paper_directory` 假设解耦为 **local / remote(urls·arxiv·doi) 多源 + manifest 元数据增强**，前端 Config 节点提供切换 UI；本地目录默认路径完全保持现状。
- **勘察结论（paperqa v2026.1.6.dev10+g36348d0ca 能力边界）**：
  - `get_directory_index(settings, build)` 只读单一 `IndexSettings.paper_directory`；`manifest_file` 仅做元数据增强（`maybe_get_manifest`）。
  - `Docs.aadd_url(url)` 可下载全文解析；无内建"论文ID→全文"解析。
  - arXiv 可用公开 API（export.arxiv.org，免 key）解析 PDF 链接；DOI 可经 `clients.unpaywall`（免 key，仅需 email 参数）取 OA PDF。
  - 结论：**远程源统一"下载到 staging 目录 → 复用现有索引/解析管线"**（Strangler 风格，不另起检索分支）。
- **团队与产能**：1 人（AI 开发者），预估 **31 点 / ~24h**。
- **周期**：2026-08-29 起，Backlog 全 DONE 且回归证据齐备为止；**后续 Sprint（4~7+）全部 hold**；双分支 code review 已完成（§9），评审修复已合入 windows 并经 PR #3 同步 main。

## 2. Backlog（用户故事 + 验收标准 + 估算）

| ID | 用户故事 | 验收标准 | 点数 | 预估工时 |
|---|---|---|---|---|
| US-3.1 | 数据源领域模型 + 配置 SSOT 扩展 | ① `app/data_sources.py`：`SourceKind`（local_dir/url/arxiv_id/doi）+ `SourceSpec` + `RemoteSourceConfig` pydantic 模型与解析/校验函数；② `config_schema.py` 新增「数据源」组（`data_source` 模式、`source_urls`/`source_arxiv_ids`/`source_dois`、`manifest_file` 解锁为可配置）+ 影响提示；③ `validate_config` 覆盖新字段（非法模式/空列表提示） | 5 | 4h |
| US-3.2 | 远程源解析器 | ① arXiv ID → PDF 链接（export.arxiv.org API）；DOI → Unpaywall OA PDF 链接（免 key）；直链 URL 直接下载；② 统一下载到 `data/remote/<index_name /`，幂等（同名且非空跳过）、文件名校验（防路径穿越）、逐个源失败明细（不影响其它源）；③ 全部失败给出清晰中文报错 | 8 | 6h |
| US-3.3 | 引擎+编排接线 | ① `make_settings`：`data_source="remote"` 时 `paper_directory` 指向 staging 目录；`manifest_file` 参数生效（默认 None 行为不变）；② `load_index` 步骤：远程模式先解析下载（输出追加 `remote_sources` 明细），再建索引；③ **`data_source` 缺省（local）时与 Sprint-2 行为逐字段一致** | 8 | 6h |
| US-3.4 | 前端数据源切换 UI | ① Config 节点新增「数据源」面板（local/remote 模式切换、URLs/arXiv/DOI 逐行列表、manifest 路径），写入同一 params JSON；② 默认 params 增加新字段（local 默认值）；③ 不破坏现有 JSON 编辑与 Run 流程 | 5 | 4h |
| US-3.5 | 回归验收 + 文档 + Retro | ① local 回归：smoke 8/8、e2e 六步、embed_load 与 Sprint-2 基线一致；② remote 实测：arXiv + 直链各至少一篇下载→索引→解析成功，GUI 截图；③ 3-LEARNED 更新、燃尽收尾、Retro；④ windows/main code review 完成 | 5 | 4h |

## 3. 任务看板（Kanban：开卡/推进/完成）

| 卡片 | 状态 | 点 | 开始 | 完成 |
|---|---|---|---|---|
| US-3.1 数据源模型+SSOT | ✅ DONE | 5 | 08-29 | 08-29 |
| US-3.2 远程源解析器 | ✅ DONE | 8 | 08-29 | 08-29 |
| US-3.3 引擎+编排接线 | ✅ DONE | 8 | 08-29 | 08-29 |
| US-3.4 前端切换 UI | ✅ DONE | 5 | 08-29 | 08-29 |
| US-3.5 验收+文档+Retro+review | ✅ DONE | 5 | 08-29 | 08-29 |

  开卡规则：一次开一张卡并推进到 DONE（或明确 BLOCKED + 理由）；每卡完成后跑对应回归并记录证据。

## 4. 燃尽图（Burn-down，随进展更新）

总点数 31。

| 里程碑 | 剩余点 | 说明 |
|---|---|---|
| Sprint 开始 | 31 | 2026-08-29 |
| US-3.1 DONE | 26 | 数据源领域模型 + SSOT 扩展 |
| US-3.2 DONE | 18 | 远程源解析器（arXiv/DOI/URL → staging） |
| US-3.3 DONE | 10 | 引擎+编排接线（remote 模式 + manifest + 会话级源参数） |
| US-3.4 DONE | 5 | 前端 Config 数据源面板 |
| US-3.5 DONE | 0 | 验收+文档+Retro+双分支 code review 完成 |

```text
剩余
31 ┤●
26 ┤ ●
18 ┤  ●
10 ┤   ●
 5 ┤    ●
 0 └─────●─────────────────── 时间
      开始 → US-3.1 → US-3.2 → US-3.3 → US-3.4 → US-3.5 ✅
```

## 5. 执行记录（Standup 日志）

- 2026-08-29 Sprint Planning 完成；勘察 paperqa 数据源能力边界（见 §1）。
- 2026-08-29 US-3.1 DONE：`app/data_sources.py`（`SourceKind`/`SourceSpec`/`RemoteSourceConfig` + 解析/校验/暂存目录/arXiv 提取）；`config_schema.py` 新增「数据源」组（data_source 模式、source_urls/arxiv_ids/dois、manifest_file 解锁）与 string_list 校验；`config_schema.json` 重新生成；证据见 §7。
- 2026-08-29 US-3.2 DONE：`app/remote_resolver.py`（arXiv API → PDF 链接、Unpaywall DOI → OA PDF 链接、直链下载；幂等缓存、逐源失败明细、文件名净化、PDF/HTML 内容嗅探）；实测 arXiv/URL 下载成功、幂等 cached、坏 arXiv ID 与未配邮箱的 DOI 给出清晰中文报错；证据见 §7。
- 2026-08-29 US-3.3 DONE：`make_settings` 支持 `data_source=remote`（论文目录 → `data/remote/<index_name /`）与 `manifest_file` 生效；config 步骤把数据源参数存入会话（`SessionState.data_source_params`），load_index 读取会话参数解析下载（输出追加 `remote_sources` 明细）；local 缺省行为不变（local e2e 全绿）；证据见 §7。
- 2026-08-29 US-3.4 DONE：`DataSourcePanel.jsx`（Config 节点数据源面板：local/remote 切换、URL/arXiv/DOI 逐行列表、manifest 路径），写入同一 params JSON；App.jsx 默认参数补数据源字段；样式 `styles.css` 增加 ds-* 样式；证据见 §7。

## 6. Definition of Done（每张卡与整个 Sprint）

- [x] local 默认行为不变：smoke 8/8、e2e 六步、embed_load 与 Sprint-2 基线一致
- [x] remote 实测：arXiv + 直链各至少一篇 下载→索引→解析→证据 全通
- [x] 新边界 pydantic 类型化；错误文案用户友好（含影响提示）
- [x] 文档同步：`3-LEARNED.MD`、本 Sprint 文档、架构文档（数据源模块）
- [x] 无密钥/安全回归；提交规范；windows/main code review 完成

## 7. 项目管理与证据（Sprint Review 时填实）

| US | 证据 | 通过情况 | 理由/说明 |
|---|---|---|---|
| US-3.1 | 命令输出：`py_compile OK`；schema 七组（含 datasource）、一致性检查通过；`parse_remote_sources` 解析 str/list、`validate_source_specs` 识别坏 arXiv ID；`config_schema.json` 重新生成 | 通过 | `app/data_sources.py` 纯模型/校验（无 IO），供 SSOT、编排、前端三处引用；`manifest_file` 从 readonly 解锁为可配置 |
| US-3.2 | 实测：arXiv 2409.13740 → downloaded 1.78MB；直链 URL → downloaded；复跑 → cached（幂等）；坏 arXiv ID → failed 明细；DOI 未配邮箱 → 清晰中文指引；文件名净化/内容嗅探生效 | 通过 | 逐源失败隔离 + 全失败才报错（带明细）；Unpaywall 要求真实邮箱（`UNPAYWALL_EMAIL`），示例邮箱 422 被拒 → 改为读环境变量并给出指引 |
| US-3.3 | `verify_remote_e2e`：config(remote+arXiv) → load_index（indexed 1 file + `remote_sources` 明细）→ retrieve 1 candidate → parse 22.1s → evidence 8 contexts → answer 1821 chars；local 回归：smoke 8/8 + `verify_e2e` 六步（candidates=['PaperQA2.pdf']、9 contexts、1824 chars）与基线一致 | 通过 | 关键修正：Run All 各节点参数独立 → config 步骤把数据源参数存入 `SessionState.data_source_params`，load_index 从会话读取（显式传参可覆盖）；`make_settings` remote 指向 staging、manifest_file 生效、local 缺省逐字段不变 |
| US-3.4 | GUI 截图 `us3-remote.png`（116KB/1718×1000）：面板切 remote + arXiv 填 2409.13740 → Run All → 答案含 PaperQA2；local GUI `us2-06.png` 重跑通过（面板随 Config 节点渲染） | 通过 | `DataSourcePanel.jsx` 写回同一 params JSON（复用 onChangeParams），不破坏 JSON 编辑区与 Run 流程；默认参数增加数据源字段（local 默认值） |
| US-3.5 | 本表 + Retro + `3-LEARNED` 1.16 + 架构/verify 文档同步；windows/main code review 报告见 §9 | 通过 | 回归矩阵：offline smoke / local e2e / remote e2e / GUI×2（local+remote）全绿 |

### 关闭 Self-check

- [x] 行为不变：local 四层回归与 Sprint-2 基线一致
- [x] 新能力只做加法：`remote_sources`/`config_notes` 为追加字段，旧输出键不变
- [x] 文档同步完成；无密钥回归；提交规范（`feat(US-3.x)`）

## 8. 回顾 Retro（2026-08-29 完成）

**做得好（Keep）**
1. 勘察先行：先弄清 paperqa 对 URL/manifest/DOI 的实际能力边界（`aadd_url`、`manifest_file` 仅元数据、Unpaywall 免 key），避免设计出框架不支持的抽象。
2. 下载归一化：远程源统一落到 staging 目录复用索引/解析管线，零新检索分支，行为兼容成本最低。
3. 会话级数据源参数：Run All 场景暴露"节点参数相互独立"问题后，用 `SessionState.data_source_params` 一次修正，回归即刻转绿。

**可改进（Improve）**
1. 远程源失败诊断（httpx `str()` 为空）在 GUI 实测才暴露——解析器的错误路径应从一开始就有 repr 兜底 + 超时语义。
2. staging 目录生命周期未管理（长期累积下载文件）；后续 Sprint 可加"清空暂存"按钮/自动清理策略。
3. `data/remote/` 与索引同名互串风险存在（同名 index 不同 paper_directory 指向同一 Tantivy 目录）；测试用独立 index_name 规避，未在代码层提示。

**行动项（Action）**
1. **（随 Sprint-4+ 一起 hold）**：给 load_index 增加"remote 模式下若索引名与本地常用名冲突"的提示（config_notes）。
2. 后续：staging 清理 UI + DOI 源接入实测（需真实 UNPAYWALL_EMAIL）。
3. 后续：`resolve_one` 增加下载进度/大小上限（防超大文件拖垮内存）。

## 9. windows / main Code Review（Sprint-3 完成后，需求方要求）

两分支由独立评审代理并行评审（2026-08-29），结论与处置如下。

### 9.1 windows 评审结论（分层/正确性/安全）

**必须修复（已全部修复并回归通过）**

| # | 发现 | 处置 |
|---|---|---|
| M1 | 远程下载 SSRF：URL 仅校验 scheme，可访问本机/内网地址，重定向未复检 | `_validate_url`（仅 http/https + 拒绝私有/回环/链路本地/保留地址；沙箱透明代理段 198.18.0.0/15 豁免并注释）+ 重定向逐跳复检（≤5 跳）+ arXiv/DOI 解析出的链接同样校验 |
| M2 | 下载无大小上限且全量缓冲内存 | 200MB 上限（Content-Length 预检 + 流式计数硬限）+ 流式写 `.part` 临时文件后原子改名 |
| M3 | api_key 明文进入 input_snapshot/run_records（`/api/session_records` 可读） | 编排层 `_redact_secrets` 对 snapshot/记录递归脱敏（端到端验证：响应与 records 均为 `***`） |
| M4 | DataSourcePanel 对字符串型 source_* 调用 `.join` 崩溃 | `toText()` 归一化（数组/字符串均安全） |

**已采纳的优化（同提交）**：m2 校验/解析契约统一（string_list 接受字符串）；m3 URL 兜底文件名改 sha1（跨进程幂等）；m5 make_settings 数值强转友好报错；m6 数据源参数以会话为唯一来源（去掉覆盖漂移）；m8 事件历史 500/键上限 + 明确单进程前提；m9 删 `suggest_arxiv_id` 死代码（`main.build_settings` 保留——verify 脚本仍引用）；m11 verify_remote_e2e 失败即止。

**记录在案的欠账（后续 Sprint）**：m1 配置 SSOT 未驱动前端（三处默认值漂移）→ 计划暴露 `/api/config_schema` 并收敛；m4/m7 内容嗅探严格化、manifest 路径基准文档化（已核实 paperqa 以 paper_directory 为基准，hint 正确）；m10 前端 JSON 编辑区与面板的受控输入冲突；DOI 源需真实 `UNPAYWALL_EMAIL` 实测。

### 9.2 main 评审结论（合并完整性）

- **Critical**：Sprint-3 尚未合入 main（PR #2 仅 Sprint-2）→ 本轮评审后经 **PR #3 同步**（见下）。
- **Major 已修复**：`gui_check.mjs` 截图路径写死 docs/iteration（main 已删该目录）→ 改为 `verify/` 默认 + `GUI_SHOT` 覆盖；main README 两处 docs/iteration 死链 → 同步时删除。
- **Minor 记录**：3-LEARNED/1-WORKFLOW/app docstring 中指向 refactor-analysis.MD 的引用为注释级（保留，避免为文档指代做无谓改动）；`2-ARCHITECTURE.MD` 跨平台定位、reactflow README 按钮名等列为后续文档清理项。
- 核验通过：main 导入闭合、无 Sprint-3 悬空引用（整体未合并而非半合并）、docs/iteration 确认不存在。

### 9.3 评审后同步（PR #3）

- **PR #3 已创建并合并**（`sync/sprint3-datasource` → main，merge `f284dc7`）：Sprint-3 代码 + 双分支评审修复 + main README 死链删除；**main 无 docs/iteration（已验证）**；远程 feature 分支已删除（本地同名分支待 prune）。
- 合并前在 feature 分支验证 smoke 8/8；local/remote 行为与 windows 一致（同一代码树）。
