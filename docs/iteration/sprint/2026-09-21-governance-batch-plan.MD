# 治理批正式计划（G1 / G2 / G3）—— 先立规则、再清欠债、稳定后才做功能

> **状态：正式计划（2026-09-21，用户审定 D1~D8）**。依据：Sprint-16 PM 复盘（`sprint/2026-09-12-sprint-16.md` §8.1/§8.2）、A-M11 两路穷举（`phases/agents-infra/2026-09-21-a-m11-from-{learned,runs}.MD`）、用户 2026-09-21 的口径。
> **批次 → Sprint 映射**：**G1 = Sprint-17**（规则落地，文档：[`2026-09-21-sprint-17.md`](2026-09-21-sprint-17.md)）｜**G2 = Sprint-18**（载体与验证可信度 + MM 并行）｜**G3 = Sprint-19**（观察与度量 + 用起 MM）。
> 生成时间：**2026-09-23（UTC+8；文件名的 `2026-09-21` 为会话起始日期，按 `1-WORKFLOW.MD` §3 新规保留原名并加此更正）**｜术语：`台账` 仅指独立文件（`1-WORKFLOW.MD` §3）。

## 1. 为什么先治理（数据，不是感觉）

| 事实 | 数据 |
|---|---|
| 节拍不可预测 | Sprint-16 日历 **9 天**，有提交的只有 **3 天**（23 / 41 / 3 提交） |
| 规模承诺失效 | 计划 **19 点** → 实际含追加 ≈ **44 点** |
| 收尾成本高（"关闭税"） | 关闭阶段产出 **7 个代码 major + 12 条文档项 + 3 轮修复复核** |
| 规则执行力低 | 同一形态"规则写了没执行"在 3-LEARNED 中**≥5 次复发**（1.39→1.49→1.57→1.61→1.62，跨 S11→S16） |
| 教训指向流程 | **49/63 条**属"agent 行为/流程"域（C1 16 / C2 32 / C5 12 / C6 22 / C4 7…） |
| 治理欠债积压 | 治理地基四项 **TG-6 / TG-11 / TG-12 / A-M11 全为"候选未落地"** |
| 缺陷存活久 | **F-AC13 存活 4 个 sprint**；现有 GUI 检查**结构上抓不到** |
| 产物质量其实不差 | 16 个 Sprint 全部关闭；四卡技术路线零返工；用户发现缺陷占比 **3/23 ≈ 13%**；CI 绿 |

→ 结论：**问题在过程质量，不在产物质量**。在高关闭税的地基上继续做功能 = 加楼层。

## 2. 达标判据（用户 2026-09-21 拍板：压缩为三指标 + 红线即死）

### 2.1 三个指标（"稳定可持久化的开发模式"）

| # | 指标 | 判据（草案，待定阈值） |
|---|---|---|
| **M1 关闭税** | 一个 Sprint 的关闭阶段产出 | **code major ≤ 2 且文档项 ≤ 3**，且 **三查一次通过、无需重跑** |
| **M2 违规** | `A-M11` ④ 的可观测指标 | 未跑〇查即派评审 = **0**；三查锚点后未评审提交 = **0**；self-chosen scope 无 `--deviation` 声明 = **0**；§9 与账本交叉不一致 = **0**；规范条目↔执行记录抽查不一致 = **0**（全 0，或每项 ≤1 且**当场闭环**） |
| **M3 节拍** | 迭代节奏 | 按**有效工作日**推进（每日有相关提交/卡推进），非"爆发日"模式 |

**达标定义（草案）**：**连续 2 个 Sprint 同时满足 M1+M2+M3** → 视为"稳定可持久化"，可开始功能性开发。观察期长度待用户确认。

### 2.2 红线机制（**触碰即死**）

> 用户 2026-09-21："额外增加红线触碰即死机制（硬核红线举例：**文件丢失、卡片内容飘移、测试结果假绿**）"。

| 红线 | 定义 | **可检测判据**（否则"即死"无法触发） |
|---|---|---|
| **R1 文件丢失** | 任何文件/内容被静默删除、截断、少写（含"锚点行被吞"型） | ① 编辑后**结构校验**（标题数/标题层级序列、表格行数、卡号序列无跳号）② 提交前 `git status` + `git diff --stat` 与预期比对 ③ 破坏性操作（`switch -f`/`reset --hard`/`clean -fd`/`rm -rf`/`branch -D`）**前置断言"工作树干净或已提交/stash"**，否则拒绝 ④ 恢复演练：`git reflog` / `stash list` / 操作前补丁快照 |
| **R2 卡片内容漂移** | 同一事实在多处不一致，或卡片状态与事实不符 | ① `TG-14` 的 `verify/verify_card_index.py`（索引↔文件↔状态）② `TG-12` 的派生事实 SSOT + 漂移闸门（数字/状态一致性）③ 反向对照：人为改一处 → 必须 FAIL |
| **R3 测试结果假绿** | 断言在旧实现/目标环境上**不会** FAIL，或断言从未在 CI 执行 | ① **反向对照**（新检查必须先在旧实现上跑出 FAIL）② 断言必须在 **CI 同款环境**跑过（本轮 `RUNNER~1` 事件实证）③ 关键闸门做**变异测试**（去掉修复 → 断言应 FAIL） |

**即死后的动作（草案）**：**立即停止本 Sprint 的功能性推进** → 冻结 → 定位 + 修复 + 反向对照 → 由用户二次确认 → 才恢复。红线事故记入 Sprint §9 与 `A-M11` 卡内实例清单。

#### 2.2.1 红线**分阶段生效**（主代理建议 + 用户 2026-09-21 同意"分阶段执行"）

**为什么分阶段**：红线选得对（都属"会污染全部后续工作"的地基级失效，不该放松），但 ① **检测机制在 G1/G2 尚未校准**（结构校验可能误报、`verify_card_index.py` 与漂移闸门还没写、断言环境依赖刚暴露过）；② **误杀会诱发绕过**（机制不可信 → 被绕开，正是 1.58/TG-11 的教训）。故"先有可靠检测，再有即死"。

| 阶段 | 窗口 | R1 文件丢失 | R2 卡片内容漂移 | R3 测试假绿 | 该阶段目的 |
|---|---|---|---|---|---|
| **S0 观测 + 告警** | **G1 期间** | 告警 + **24h 内定位根因** | 告警 + 当日闭环 | 告警 + 当日闭环 | 校准检测机制的误报/漏报（不硬停） |
| **S1 部分即死** | **G2 期间** | **立即硬停** | 告警 + 当日闭环 | 告警 + 当日闭环 | R1 不可逆、破坏面最大 → 先硬起来 |
| **S2 全量即死** | **G3 起** | 硬停 | 硬停 | 硬停 | 此时检测机制已被 G1/G2 的闸门验证（反向对照 + CI 执行） |

**配套三条**：① **灰区规则**——检测机制自身故障 / 无法判定时按"**告警**"处理并当日复检，**不硬停**（避免机制故障造成停摆）；② **升级条款**——同一红线在一个 Sprint 内触发 **≥2 次** → 该 Sprint 立即转入"治理模式"（停功能推进）；**治理模式的提案必须满足 6 项合格标准**（证据／根因／机制／反向对照／**在原始事故上验证**／复用面，见 [`2026-09-25-d2d3-parallel-session-quality-analysis.MD`](../phases/testing-governance/2026-09-25-d2d3-parallel-session-quality-analysis.MD) §3.5），**缺项即退回；不接受"只写机械规则"**（用户 2026-09-25 U3 判定）；③ **恢复判据**——修复 + 反向对照 + **用户二次确认**，才解除冻结。

#### 2.2.2 红线判据（分阶段生效的共同判据）

### 2.3 "人类读得懂"硬标准（用户 2026-09-21 追加）

> `code major` 的判定需**增加"写的代码必须人类读得懂"这一硬性标准**，可参考大公司代码规范（如 **Google Python Style Guide**）。
> **用户 2026-09-21 追加口径（D3 回复）**："C 可以，但是**不仅仅是 python 语言，需要覆盖该项目所用到的所有编程语言**"。

**落地形式：C（spec 判据 + 工具闸门）**，并按语言全覆盖（文件量为 2026-09-21 `git ls-files` 实测）：

| 语言/文件类型 | 量 | 工具闸门 | 规范依据 | 落地批次 |
|---|---|---|---|---|
| **Python** | **112** | `ruff`（启用 pycodestyle/pyflakes/**pydocstyle `D`**/命名 `N` 等规则集）+ 既有 `verify_*.py` 结构约定 | [Google Python Style Guide](https://google.github.io/styleguide/pyguide.html) | **G1** |
| **JavaScript / JSX** | 26 mjs + 7 jsx（含 GUI 检查） | `eslint`（recommended + react 插件）+ `prettier --check` | [Google JavaScript Style Guide](https://google.github.io/styleguide/jsguide.html) | **G2** |
| **TypeScript / TSX** | 17（Next.js 看板） | `@typescript-eslint` + `prettier --check` | [Google TypeScript Style Guide](https://google.github.io/styleguide/tsguide.html) | **G2** |
| **JSON / YAML** | 26 / 53（`providers/*.json`、CI workflow、`fanout.json` 等） | JSON：**既有 schema/结构校验**（`validateFanout`、`VERIFY_META` 校验）统一登记；YAML：`yamllint` + workflow 结构校验 | 项目自身 schema + GitHub Actions 语法 | **G2** |
| **PowerShell** | 7 | **`PSScriptAnalyzer`**（`Invoke-ScriptAnalyzer -Severity Warning,Error`）+ 项目约定（纯 ASCII/带 BOM、`$PSNativeCommandUseErrorActionPreference`，见 1.18 与 CI pip 静默失败） | PSScriptAnalyzer 默认规则集 | **G2** |
| **Batch** | 4 | 无成熟 linter → **人工评审 + 轻量断言**（编码、换行、`chcp`/`setlocal` 约定） | 项目约定 | **G2** |
| **Markdown（文档/规范）** | **82** | 结构校验（`TG-14` 的 `verify_card_index.py`：标题层级、表格连续性、**表头↔各行单元格数一致**、卡号无跳号）＋ `markdownlint`（**MD056** `table-column-count`） | `1-WORKFLOW.MD` §3 文档规范 | **G1**（结构）/ **G2**（style） |
| CSS / TOML | 2 / 5 | 可选（`stylelint` / `taplo`） | — | 后续 |

**闸门要求**：新增 `verify/verify_lint.py`（**offline 档，纳入套件**）——① 逐语言调用上表工具（工具缺失时 **fail-closed** 或显式 `skipped` 并记原因，**不得静默跳过**）；② **反向对照**：注入一处违规（如未使用变量/缺 docstring/超长函数）→ 必须 FAIL；③ **spec 侧**：`agents/functions/code-review.md` 的 `focus` 增 `readability`，判据引用上表规范，`strictness=strict` 时每条可读性问题必须给"为什么读不懂 + 怎么改"；④ 工具能自动抓的交给工具，抓不到的（命名语义、注释质量、隐式假设）由评审按 spec 判。

**§2.3 工具口径（用户 2026-09-21 裁定，详见 [`pre-research/tech/2026-09-21-lint-deps-security-check.MD`](../pre-research/tech/2026-09-21-lint-deps-security-check.MD) §6.2）**：**G1 仅 `ruff`**；`prettier` **只对新增/改动文件** `--check`（不重排历史）；`tsc --noEmit` **与** `@typescript-eslint` 都上；`yamllint` 引入（GPL-3.0 由用户接受，**仅开发期/CI 工具**，若改为分发给第三方须重新评估）；**不引入** `eslint-plugin-react`；`PSScriptAnalyzer` 延后 G2。所有新依赖按"**先查后装**"（先按锁定版本查漏洞，再安装）并留原始输出。

**§2.3 首批实测实例（2026-09-21，主代理按 `(?<!\\)\|` 分列实测，同日已修）**：4 份 `phases/*/backlog.MD` 实测到**表头列数 ≠ 行内列数**的结构缺陷——① `testing-governance` 主表：表头 **6** 列、`TG-7`~`TG-14` 行为 **7** 列，末尾"卡内待产出"整列**无表头**；② `refactor-analysis` 主表：表头 6 / `F-AC13`~`F-AC16` 行 7（同形）；③ `agents-infra` A-PM/A-M 表：表头 **5** 列而行内为 **4~7** 列，并含两类**真错位**——(a) `A-PM5`/`A-M2`/`A-M5`/`A-M6` 四行**少一个"规模"格** → 其 **状态** 渲染在"规模(点)"列下、**来源** 渲染在"状态"列下；(b) `A-M9` 行的 **Sprint** 落在"来源"列下，`A-FANOUT`/`A-M1`/`A-M3`/`A-M4`/`A-M8`/`A-M11` 行的 **来源 / 卡内待产出** 落在表头之外（无对应列）。**处置**：三处表头统一为 **7 列**（`卡号｜主题｜规模(点)｜状态｜所属 Sprint｜来源/备注｜证据/卡内待产出`），4 行补"规模"`—`、3 行（`A-M7`/`A-RS`/`A-M10`）补"所属 Sprint"`—`。**判定依据**：行多于表头属**结构非法**（`markdownlint` **MD056** / ESLint markdown `table-column-count` 专治此形态），多余单元格**无表头对应、渲染行为不可保证**；缺格只有**末尾补空**兜底，**中间缺格不会被任何渲染器察觉** → 必须靠 lint。本段只以"结构非法"为处置依据，不宣称具体渲染器的取舍行为。→ 该形态是 `TG-14` 索引 lint 的**首个真实用例**：列数一致性断言必须能抓出 ①②③ 中任意一处（反向对照）。

## 3. G1 / G2 / G3 拆分（多 Sprint 实现）

| 批次 | 目标 | 卡（点数） | 合计 | 关键交付 |
|---|---|---|---|---|
| **G1 规则落地** | 让"规则"变成**可执行检查点** | `TG-11` 规则绕过复盘+闸门(3)、`TG-6` 验收方式改进(3)、`TG-13` 账本测量化(2)、`TG-14` 一卡一文档(3)、`TG-8` 端口自检+离线开关(2)、`TG-9` 产物目录约定(1) ＋ **搭车项（用户 2026-09-21 批准）**：`F-AC13` 运行按钮(2)、`F-AC14` 复制串台(2) | **14 + 4 搭车 = 18** | 关闭前置闸门 `verify_close_readiness.py`；反向对照规则落地；账本测量化；卡↔文档结构 + 索引 lint；**Python 侧 `ruff` 闸门（§2.3 G1 行）**；两个用户可见 bug 修复 |
| **G2 载体与验证可信度**（**MM 并行**） | 把纪律挂到**动作**语境 + 治"检查在骗人" | `A-M11` ① 分层提示词、② 四列映射表、⑧ 验证可信度（C2b 24 条）、⑨ 接管判据、⑩ 报告↔run 绑定(合计 3+)、`TG-12` 派生事实机制(3) ＋ **并行线**：`MM-1`(2)、`MM-2`(3)、`MM-3`(2) | **≈12（治理）+ 7（MM 并行）** | 分层提示词（把规则挂动作）；四列映射表；验证可信度通则；接管留痕与边界继承；漂移闸门；**多语言 lint 闸门（§2.3 G2 行）**；**MM schema/生成器/种子模型首跑（与 G2 并行推进）** |
| **G3 观察与度量（并用起 MM）** | 让"是否稳定"**可判定** | 观察期度量（插入卡额外点数/工时、M1~M3 基线）+ **双盲抽查机制**；**MM 进入实用**（用 G2 已交付的 MM 框架做本轮复盘/决策的思维实验） | **≈8 + 复用 MM** | 三指标基线 + 红线检测；抽查协议首跑；**MM 从 G3 起实际用于"复盘/决策/验收"场景（用户 2026-09-21 口径：MM 与 G2 并行，这样 G3 正好用起来）** |

**批次与红线的对应（§2.2.1）**：G1 = **S0 观测+告警**｜G2 = **S1 部分即死（R1 硬停）**｜G3 起 = **S2 全量即死**。

**共同约束（按 `1-WORKFLOW.MD` §4.1 新规）**：每批声明**有效工作日时间盒** + **规模上限** + **预留 20% 治理额度**；**开工时先跑一次〇查**（关闭时再跑，档位差异记 §9）。

**产品侧插入项（待用户权衡）**：`F-AC13`(2) + `F-AC14`(2) 是**用户可见缺陷**（运行要点 2–3 次 / 复制状态串台），建议作为 **G1 搭车项**修掉；若延迟到治理批之后，需明确接受"日常使用继续受影响"。

## 4. 观察期要记的东西（用户 2026-09-21 要求）

> "同时观察期间**插入卡造成的额外故事点/开发时间**的情况"

每 Sprint 记录一张表（关闭时填）：

| 字段 | 说明 |
|---|---|
| 计划点 / 实际点 | 承诺 vs 交付（含追加） |
| **插入卡点数** | 走查/评审/用户插入的卡合计点数（Sprint-16 实证：+25 点） |
| **插入卡占用** | 插入卡消耗的有效工作日（半日粒度）与占比 |
| 关闭税点数 | 三查/复核/文档同步/复盘消耗 |
| M1/M2/M3 读数 | 三指标实测值 |
| 红线事件 | R1/R2/R3 触发次数与处置 |

→ 目的：判定"插入卡"是**常态需求变化**还是**过程缺陷的补偿动作**（若插入卡长期占 >30%，说明计划与验收面本身有系统性问题）。

**变更／延期协议（2026-09-25 补，用户 U2 判定）**：插入卡占比越阈值（>30%）或预计延期时，执行方**不得只提交"能否延期"的请示**——必须提交**三选一方案**（① 降范围：砍哪几张卡、少几点；② 延时间：延几个有效工作日、代价是什么；③ 换批次：哪些卡挪到下批）+ 各方案的点数与风险对比，**由用户裁定**；执行方负责把选项和代价给足，**决定权始终在用户**。每次插入卡同时登记 §4 观察表（点数 + 占用 + 触发原因）。

## 5. 抽查机制（用户 2026-09-21：我做抽查、**按双盲**；判断类由用户二次检查）

**双盲协议（草案）**：
1. **样本空间预先登记**：抽查对象 = 本期全部 `agents/runs/**` 报告（run_id 清单在抽样前落盘，作为"抽样池"）；
2. **抽样规则先定后抽**：`Get-Random -SetSeed <固定种子>` 从抽样池抽 **3 份**（规则与种子在抽样前写入 Sprint 文档，**抽样后不得更换样本**）；
3. **单盲（对子代理）**：被抽查的子代理**不知道**自己会被抽查，也不知道抽查判据；
4. **双盲（对我）**：**不预先看结论**——我先按原始 spec 独立复核抽取样本的关键断言（含必要时按原命令复现），**得出我自己的结论后**再与报告原文比对，记录"一致 / 不一致 / 无法判定"；
5. **判断类二次检查**：凡涉及"是否算违规""是否算假绿""是否算红线"的**判断**，一律**升级给用户裁定**，我不自行结案；
6. **留痕**：抽查记录（种子、样本、我的独立结论、与原文差异、用户裁定）写入 Sprint §9。

**已知局限（须如实写入报告）**：样本量 3 份、以"关键断言复核"为主（非全文重审）；抽查频率每 Sprint 一次；**抽查本身也受"验证可信度"约束**（抽查脚本/命令须可复现）。

## 6. 审定状态（用户 2026-09-21 回复）

| # | 问题 | 用户决定 |
|---|---|---|
| **D1** | 三指标阈值 + "连续 2 个 Sprint 达标" | ✅ **接受** |
| **D2** | 红线三条 + 即死动作 **是否过苛** | ✅ **同意"分阶段执行"** → 已落为 **§2.2.1 三段式（S0 观测+告警 / S1 部分即死（R1 硬停）/ S2 全量即死）** + 灰区规则 + 升级条款（同一红线 ≥2 次/Sprint → 转治理模式）+ 恢复判据（用户二次确认） |
| **D3** | "人类读得懂"落地形式 | ✅ **C（spec 判据 + 工具闸门）**，且**须覆盖项目用到的所有语言**（非仅 Python）→ 已落为 §2.3 的**逐语言闸门表**（Python 112 / JS-JSX 34 / TS-TSX 17 / JSON 26 / YAML 53 / PS1 7 / BAT 4 / Markdown 82 / 可选 CSS-TOML），新增 `verify/verify_lint.py`（offline、反向对照、工具缺失 fail-closed 或显式 skipped） |
| **D4** | G1/G2/G3 划分与点数；F-AC13/14 | ✅ **接受搭车** → G1 = 14 + **4 搭车 = 18**（已标注：可能触顶，需按 §4.1 规模上限与用户确认） |
| **D5** | MM 位置 | ✅ **MM 与 G2 并行**（不放在 G3）→ 这样 **G3 时正好把 MM 用起来**（G3 = 观察与度量 + 用 MM 做复盘/决策/验收场景） |

**尚待确认（新增）**：
- **D6** G1 = **18 点**（含搭车项）是否超出你的规模上限？→ ✅ **用户 2026-09-21："先跑跑看，对数字我也没有具体感受"** → **G1 按 18 点执行，G1 关闭时用实测数据（插入卡点数/关闭税/有效工作日）回头校准规模上限**。
- **D7** §2.3 的工具闸门是否允许**引入新依赖**？→ ✅ **用户同意，但附加前提**："**引入前提要查清楚依赖有无已暴露的安全风险、数据隐患**" → 已落为 **§2.4 前置核查（G1 第一项任务，未通过不得引入）**。
- **D8** G1/G2/G3 的有效工作日时间盒？→ ✅ **按建议：G1 6 / G2 8 / G3 5 个有效工作日**。

## 2.4 依赖引入前置：安全与数据隐患核查（用户 2026-09-21 D7 附加前提）

**规则**：**任何**为 §2.3 闸门引入的新依赖（`ruff` / `eslint` / `prettier` / `@typescript-eslint` / `yamllint` / `PSScriptAnalyzer` 及其插件），在引入前必须完成下列核查，**任一项不通过 → 拒绝引入或换等价替代**；结果记入 Sprint §9（含核查命令与原始输出）。

| # | 核查项 | 具体做法 | 不通过的处理 |
|---|---|---|---|
| 1 | **已知漏洞** | `pip-audit` / `npm audit` / **OSV API**（`https://api.osv.dev/v1/query`）逐包按版本查已知 CVE/OSV | 有 high/critical → 拒绝或换替代 |
| 2 | **安装脚本与构建期执行** | 检查是否带 `postinstall`/`setup.py` 执行、是否需要 node-gyp/编译工具链 | 需构建期执行的 → 需用户批准 |
| 3 | **数据外发 / 遥测** | 是否有 telemetry / 上传代码或路径 / 默认联网抓取；**离线运行验证**（断网跑一遍闸门） | 有联网行为且不可关 → 拒绝 |
| 4 | **许可证** | 许可证类型（MIT/Apache-2.0/PSF 等）与本项目分发兼容性 | 不兼容 → 拒绝 |
| 5 | **维护活跃度与来源** | 官方仓库 / 下载量 / 最近发布日期（供应链风险）；**锁版本**进锁文件 | 冷门或长期不维护 → 换替代 |
| 6 | **最小化** | 能用既有工具（`verify_*.py` 自研断言、`json`/schema 校验）解决的**不引入新依赖** | — |

> **注**：`PSScriptAnalyzer` 为 PowerShell 官方模块（微软）、`ruff`/`eslint`/`prettier`/`yamllint` 均为高流行度开源工具，但**上述六项仍逐条执行并留证**（用户要求"查清楚"，不接受"看起来没问题"）。

> **§2.4 首轮执行结果（2026-09-21，一手证据）**：见 [`pre-research/tech/2026-09-21-lint-deps-security-check.MD`](../pre-research/tech/2026-09-21-lint-deps-security-check.MD) —— 判定 **条件通过**（无一包"六项全绿"）：**直接拒绝** `eslint-config-prettier`/`eslint-plugin-prettier`（`CVE-2025-54313` = `GHSA-f29h-pxvx-f335` HIGH，OSV 确证内嵌恶意代码）；**建议不引入** `yamllint`（GPL-3.0 + 自有 YAML 仅 1 个文件）、`eslint-plugin-react`（17 个月未发版）；**条件引入** `ruff`/`eslint`/`prettier`/`@typescript-eslint`（待补"按锁定版本复核依赖树 + 断网实跑"）；`PSScriptAnalyzer` 延后。**原始数值不在本文件复制**（TG-12 派生事实纪律）。
