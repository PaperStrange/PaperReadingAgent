# Sprint 5（2026-08-30）—— 实战问题修复（目录生效/检索说明/UI/回调上限/SSE 断连）

> 归属：`docs/iteration/sprint/`。输入：用户实战截图与 5 项问题。roadmap C/E3 继续 hold。

## 1. Sprint Planning

- **Goal**：修复 5 个实战问题并使"改配置→改目录→重跑"闭环自洽；UI 增加报错复制与步骤计时；检索选择逻辑透明化；litellm 回调上限防崩溃；SSE 断连静默。
- **团队与产能**：1 人（AI 开发者），预估 **21 点 / ~14h**（双轨估算）。
- **周期**：2026-08-30 完成；关闭时补齐三查（§9）。
- **勘察结论**：
  1. 改目录后 load_index/parse 用旧配置：**前端节点参数独立**——load_index 用上次 config 的 session.settings，面板改参后未自动重跑 config（节点已标 stale）；parse 报"no such file"是旧索引文件 + 新目录组合。
  2. retrieve top_n：Tantivy BM25 chunk 级排名；**零命中时回退取索引前 N 个文件**（用户所见"与 query 无关"）；候选可能含重复 chunk 路径。
  3. UI：面板标题 `.node-block-title` 左 padding(6px) 与行内容(8px) 不齐；error 不可复制；Function Subcanvas 无步骤计时。
  4. MAX_CALLBACKS：litellm 每次 Router 实例化注册 logger（paperqa 每次模型创建），累积至 30 上限报错。
  5. SSE：客户端断连后 send 抛错由 uvicorn 记录（仅噪音，但应静默）。

## 2. Backlog（用户故事 + 验收标准 + 估算）

| ID | 用户故事 | 验收标准 | 点数 | 预估工时 |
|---|---|---|---|---|
| US-5.1 | 改配置后重跑下游自动生效；目录/索引不一致友好报错 | ① 下游节点运行时若 config 节点 stale → 自动先跑 config；② parse 文件缺失报错含"请先重跑 Config→load_index"指引；③ 实测"改目录→直接跑 load_index"索引即新目录 | 5 | 3h |
| US-5.2 | retrieve 选择逻辑透明 + 去重 | ① candidate_paths 去重保序；② 零命中回退时输出 `result=fallback_first_n` 标记；③ 文档说明排名机制 | 3 | 2h |
| US-5.3 | UI 三件套（对齐/复制/计时） | ① 面板标题与行内容左对齐；② error 块"复制报错"按钮（clipboard）；③ Subcanvas 头部显示当前步骤耗时，运行中实时更新，完成/失败冻结 | 5 | 3h |
| US-5.4 | litellm 回调上限防护 | 每次 run_step 前对 litellm 回调（含 `_async_*`）去重并裁剪（keep 最近 20），不再抛 MAX_CALLBACKS | 3 | 2h |
| US-5.5 | SSE 断连静默 | stream 循环内检测 `request.is_disconnected()` 即退出；不再产生 socket.send 报错（客户端正常关闭场景） | 2 | 1h |
| US-5.6 | 回归 + 文档 | smoke/e2e/GUI 回归；4-ALGORITHM/README/3-LEARNED 同步；PR 同步 main | 3 | 3h |

## 3. 任务看板（Kanban）

| 卡片 | 状态 | 点 | 开始 | 完成 |
|---|---|---|---|---|
| US-5.1 配置变更自动生效 | ✅ DONE | 5 | 08-30 | 08-30 |
| US-5.2 检索透明+去重 | ✅ DONE | 3 | 08-30 | 08-30 |
| US-5.3 UI 三件套 | ✅ DONE | 5 | 08-30 | 08-30 |
| US-5.4 回调上限防护 | ✅ DONE | 3 | 08-30 | 08-30 |
| US-5.5 SSE 断连静默 | ✅ DONE | 2 | 08-30 | 08-30 |
| US-5.6 回归+文档 | ✅ DONE | 3 | 08-30 | 08-30 |

## 4. 燃尽图

总点数 21。

| 里程碑 | 剩余点 | 说明 |
|---|---|---|
| Sprint 开始 | 21 | 2026-08-30 |
| US-5.1~5.6 全部 DONE | 0 | 三查补齐后关闭 |

```text
剩余
21 ┤●
 0 └───●───────────────────── 时间
    开始 → US-5.1~5.6 ✅
```

## 5. 执行记录（Standup 日志）

- 2026-08-30 开卡并完成 US-5.1~5.6（同日）；关闭前补三查（§9）并修复二查发现：config 重跑失效下游状态、索引目录指纹一致性重建、裁剪覆盖 `_async_*` 列表并移至 engine 层。

## 6. Definition of Done（每张卡与整个 Sprint）

- [x] 每张卡有对应回归证据（Playwright/命令输出/截图，§7）
- [x] 结论来自可复现证据（gui_check_s5.mjs / verify_prune_callbacks.py 入库）
- [x] 记录关键权衡/理由/局限（§7 理由列）
- [x] 未破坏既有功能（smoke 8/8、e2e 六步）
- [x] sprint 文档已入库并随分支同步
- [x] 三查完成且发现项闭环（一查/二查结论与修复见 §9）
- [x] 决策逻辑变更同步 4-ALGORITHM.MD；windows 已 PR 同步 main（不含 docs/iteration）

## 7. 项目管理与证据

| US | 证据 | 通过情况 | 理由/说明 |
|---|---|---|---|
| US-5.1 | Playwright `gui_check_s5.mjs`：改 temperature 后直接点 load_index → config 自动重跑 → success；API 实测"改目录 A→B"索引随目录切换（指纹重建）；失败态（目录不存在）报"论文目录不存在" | 通过 | 前端 stale→自动重跑 + 后端 config 重跑失效下游状态（二查修正）；parse 单文件失败文案含 Config→load_index 指引（与 load_index 的目录不存在文案区分） |
| US-5.2 | Playwright：中文 query → `result=fallback_first_n`；英文 query PaperQA2 → `result=ranked`；API 实测候选去重且 ranked 返回 ['PaperQA2.pdf'] | 通过 | BM25 零命中才回退，回退显式标记；按命中序去重（chunk 级重复） |
| US-5.3 | Playwright：`.fn-timer` 运行中刷新、parse 完成后 `完成 X.Xs` 冻结；失败态 `.copy-err-btn` 渲染 + 友好报错；截图 `us5-01.png` | 通过 | 冻结语义与文档一致（二查后修正）；剪贴板含 execCommand 降级与 unmount 清理 |
| US-5.4 | `verify_prune_callbacks.py`：32 项（超上限+重复）→ 裁剪后 20 个唯一 | 通过 | 覆盖 7 个列表（含 `_async_*`，二查修正）；移至 engine 层（分层契约） |
| US-5.5 | 代码路径：stream 发送前 `request.is_disconnected()` 检测 | 通过 | finally 仍保证 unsubscribe |
| US-5.6 | smoke 8/8、vite build、e2e 六步全绿；文档同步；PR #15（初版）+ PR #16（三查整改） | 通过 | — |

### 权衡与理由 / 边界与回归 / 关闭 Self-check

- 理由：索引目录一致性采用"目录指纹 + 损坏探针 → 整目录重建"，同目录同指纹保持增量（性能不回归）；仅删 files.zip 会残留 Tantivy 段/docs 旧引用（实测 query 报 No such file），故改为整目录重建。
- 边界：`prune_litellm_callbacks` 保留最近 20 个（理论可误删在途回调，本地单用户风险低）；计时器多节点并发时仅显示首个运行节点（nit，已记录）。
- 回归：smoke 8/8、e2e 六步（PaperQA2.pdf、ranked 检索、Skarlinski 引用答案）、GUI 7 项 PASS。

- [x] 每个 US 有对应证据（图文 + 说明）
- [x] 结论来自可复现证据（命令/截图）而非推测
- [x] 记录关键权衡/理由/局限
- [x] 未破坏既有功能（smoke/e2e）
- [x] sprint 文档已入库并随分支同步
- [x] 三查完成且发现项闭环（§9）；windows PR 同步 main

## 8. 回顾 Retro

**做得好（Keep）**
1. 证据脚本升级后一次性覆盖 7 项断言（双模式检索/冻结计时/复制按钮），可复现。
2. 用 API 最小复现定位"ranked 缺失"→ 挖出索引一致性根因（比前端猜测快）。

**可改进（Improve）**
1. 首版关闭未走三查即发 PR——已按工作流补齐；后续 Sprint 三查前不发同步 PR。
2. `_prune` 上限 20 为硬编码，未暴露配置。

**行动项（Action）**
1. 检索语言不匹配（中文 query vs 英文分块）的查询侧优化列入后续候选。
2. `prune_litellm_callbacks` 上限可配置化（后续 Sprint）。
3. 多节点并发计时显示（nit）随交互式面板深化一并处理。

## 9. 评审/同步记录（三查结论，Sprint 关闭必填）

- **一查（文档审计）**：发现 Sprint-5 文档模板不达标（缺 §6 DoD/§9 三查/§2 工时列/§1 产能周期/§7 理由列+Self-check）与证据失真 4 项（US-5.1/5.2/5.3/5.4 证据与脚本不符）；4-ALGORITHM/README/3-LEARNED 与代码一致（仅 fn-timer"冻结"文案偏差）。→ 全部整改：本节及 §1/§2/§3/§4/§6/§7 补全，证据脚本升级至 7 项断言并实测全 PASS。
- **二查（windows+main code review）**：必须修 1 项（config 重跑未失效下游状态）→ 已修（config 步骤重置 search_index/docs/evidence/answer 等）；建议修采纳（`_async_*` 列表补全 + `_prune` 移至 engine 层 + FlowNode 剪贴板降级/unmount 清理）；**新发现**：目录切换复用索引名导致 Tantivy 段/docs 残留旧引用（retrieve 报 No such file）→ 已修（目录指纹 + 整目录重建，API 实测 A→B 切换 PASS）。main 合并完整性通过（PR #15）。
- **三查（环境/功能完整性）**：工作区零残留、分支仅 main+windows、端口 5173/8787/8501 FREE；回归 smoke 8/8、e2e 六步、GUI 7 项 PASS、verify_prune_callbacks PASS。
- **PR 同步**：初版 PR #15（merge `5cd17c6`）；三查整改经 **PR #16** 同步 main（不含 `docs/iteration/`）。
