# Sprint 11（2026-08-31）—— F2 配置面板深化 + SSOT 驱动前端（阶段 A：SSOT 地基）

> 归属：`docs/iteration/sprint/`。启动依据：用户拍板下一阶段方向 = F2（配置面板深化 + SSOT 驱动前端），要求**分阶段分 Sprint 执行**（2026-08-31）；`1-WORKFLOW.MD` §4.4 判定：命中问题 1（新端点/协议变更）+ 问题 3（跨层）+ 问题 5（roadmap 主题 F2）→ 新 Sprint。
> 关联卡：F2（refactor-analysis backlog，本 Sprint 起拆为阶段 A/B/C）、M7（默认值漂移收敛，并入 F2）。
> 执行前环境预检（2026-08-31 已做）：git 树干净（main+windows）、5173/8787/8501/8600 零监听、无项目残留进程。

## 0. F2 分阶段总计划（三 Sprint，随进度滚动细化）

| 阶段 | Sprint | 内容 | 预估 |
|---|---|---|---|
| A SSOT 地基 | **Sprint-11（本 Sprint）** | `/api/config_schema` + `/api/config/validate` 端点；M7 默认值漂移收敛（4 处 → SSOT）；前端 schema 接入层（拉取 + 分组字段清单只读渲染 + 单一受控状态接线） | 5 点 |
| B 全字段交互 | Sprint-12 | schema 驱动表单（类型控件/分组/中文标签/readonly 展示）；字段级校验 + 友好报错；impacts 影响提示切换前可见 + provider 联动并入 | 6 点 |
| C 完全 SSOT + 收口 | Sprint-13 | App.jsx 字段清单不再手写（完全 schema 驱动）；Settings 升级同步脚本 + CI diff 检查；三查 + PR + F2 用户验收 | 5 点 |

> 设计决策已冻结（refactor-analysis.MD §4 Q2 方案 B：手工策展 schema 唯一真源 + 自动渲染；`app/config_schema.py` 已实现 GROUPS/get_config_schema/validate_config/assert_schema_consistency）——本轮为纯实现，无新选型，不触发 tech-research。

## 1. Sprint Planning

- **Goal**：阶段 A——把已冻结的 SSOT（config_schema.py）**暴露为 API 并接入前端**：① 后端新增 `/api/config_schema`（GET）与 `/api/config/validate`（POST）② 前端 Config 节点从 API 拉取 schema、以分组字段清单（只读）渲染并保留现状手写面板，③ **M7 收敛**：App.jsx 配置默认值对象与 SSOT 默认值逐项比对，重复处改为引用/校验锁定。
- **团队与产能**：1 人（AI 开发者），预估 **5 点 / ~5h**。
- **周期（时间盒，必填）**：**2026-08-31（网络时间 UTC+8，1 个工作日）**；超时预案：与用户确认后把 US-11.3 推到 Sprint-12（三选一：续期/拆卡/砍范围）。
- **验收演示**：后端启动后 `GET /api/config_schema` 返回 7 分组完整 schema（JSON）；`POST /api/config/validate` 对非法值返回字段级 errors；前端 Config 节点展示分组字段清单截图；`verify_config_schema.py` 全 PASS。

## 2. Backlog（用户故事 + 验收标准 + 估算）

| 卡号 | ID | 用户故事 | 验收标准 | 点数 | 预估工时 |
|---|---|---|---|---|---|
| F2 | US-11.1 | 后端 SSOT 端点：`GET /api/config_schema` 返回策展 schema（7 分组/字段元数据/默认值/impacts）；`POST /api/config/validate` 复用 validate_config 返回 {errors,warnings,hints} | ① 离线 verify 断言 get_config_schema 分组/字段/默认值；② 端点实测（后端启动后 curl）返回 200 与预期 JSON；③ validate 对非法 enum/范围外数值返回 errors | 2 | 2h |
| M7 | US-11.2 | 默认值漂移收敛：App.jsx 配置默认值对象与 SSOT 默认值逐项比对（chunk_chars/temperature/embedding_model 等），重复处收敛为"前端仅保留展示缺省，权威值以 schema 为准"+ verify 断言锁定 | ① 比对清单（4 处来源）写入 §7；② 前端与 SSOT 不一致处全部修复或标注理由；③ verify_config_schema 断言"前端默认值 ⊆ 且 = SSOT" | 2 | 2h |
| F2 | US-11.3 | 前端 schema 接入层：Config 节点启动时拉取 /api/config_schema，以分组折叠面板渲染字段清单（只读视图：标签/类型/默认值/impacts 高亮当前值）；保留现有手写面板不变（阶段 B 再替换交互） | ① 截图证据（分组清单可见）；② 拉取失败降级提示不阻塞现有面板；③ 单一受控状态（草稿态）不受影响 | 1 | 1h |

合计 **5 点 / ~5h**。

## 3. 任务看板（Kanban：开卡/推进/完成）

| 卡片 | 状态 | 点 | 开始 | 完成 |
|---|---|---|---|---|
| US-11.1 后端 SSOT 端点 | ✅ DONE | 2 | 08-31 | 08-31 |
| US-11.2 M7 默认值收敛 | ✅ DONE | 2 | 08-31 | 08-31 |
| US-11.3 前端 schema 接入层 | ✅ DONE | 1 | 08-31 | 08-31 |

> 开卡规则：一次开一张卡并推进到 DONE（或明确 BLOCKED + 理由）；自主开卡制（1-WORKFLOW §4.1）：主代理自行开卡/推进/关卡，里程碑摘要汇报。

## 4. 燃尽图（Burn-down，随每张卡推进更新）

总点数 5。

| 里程碑 | 剩余点 | 说明 |
|---|---|---|
| Sprint 开始 | 5 | 2026-08-31（网络时间 UTC+8） |
| US-11.1 DONE | 3 | GET /api/config_schema + POST /api/config/validate（复用 config_schema 真源）；Node 实测 UTF-8 正常、errors 字段级 |
| US-11.2 DONE | 1 | verify_config_schema.py **101 断言**：schema 结构/默认值/pydantic_path/validate 行为 + **M7 收敛锁**（App.jsx 字面量 == SSOT == provider_config；实际零漂移，风险被断言锁死） |
| US-11.3 DONE | 0 | SchemaFieldList.jsx 分组只读清单接入 Config 节点（拉取失败降级不影响现有面板）；前端 build 通过；截图 f2-config-schema*.png ×2 |

```text
剩余
 5 ┤●
 3 ┤ ●
 1 ┤  ●
 0 ┤   ●
 0 └─────────────────────────────── 时间
    08-31 开始 → US-11.1 → 11.2 → 11.3（阶段 A 完成，进入三查）
```

## 5. 执行记录（Standup 日志）

- 2026-08-31 环境预检通过（零监听/树干净）后完成 Sprint Planning；开卡 US-11.1。
- 2026-08-31 US-11.1/11.2/11.3 全部 DONE（详见 §7）；路由 8→10 → impact spec v1.4.0 + 全库"8 条"引用同步（1.34 教训执行）；冒烟 8/8、verify_config_schema 101 断言、verify_agentops 30/30。

## 6. Definition of Done

- [x] 每张卡有验收证据（截图 + 命令输出，§7）
- [x] 结论来自可复现证据
- [x] 记录关键权衡/理由/局限
- [x] 未破坏既有功能（三查按 fanout 五步：impact → doc-audit → code-review ×2 → lessons → workspace-check）
- [x] 三查完成且发现项闭环（§9）
- [x] windows 已 PR 同步 main（不含 docs/iteration/）；main CI 绿

## 7. 项目管理与证据（Sprint Review 时填实）

| US | 证据 | 通过情况 | 理由/说明 |
|---|---|---|---|
| US-11.1 | 端点实测：`GET /api/config_schema` 200（version=1、7 分组、23 字段）；`POST /api/config/validate` 非法值返回 2 errors（Node UTF-8 客户端验证中文正常）；`verify_config_schema.py` 101 断言 PASS（含端点依赖的纯函数全链） | ✅ | 端点是 config_schema 真源的薄暴露；validate 复用 validate_config，行为不变原则 |
| US-11.2 | `verify_config_schema.py` 的 M7 断言组：App.jsx n1 节点 12 个默认值字面量（正则抽取、剔除注释）逐项 == SSOT 默认值；api_base 前端预填 == provider_config deepseek；provider deepseek model == schema | ✅ | 实测**零漂移**（历史风险为潜在漂移，现由断言锁死）；注释行中的 macOS 原始值不参与比对 |
| US-11.3 | 前端 build 成功（325KB bundle）；截图 `verify/f2-config-schema.png` + `f2-config-schema-embedding.png`（分组清单 + 当前值高亮）；拉取失败降级文案（schema-error 分支）| ✅ | 只读清单不改变现有交互（阶段 B 再替换）；单一受控状态（草稿态）不受影响 |

### 权衡与理由 / 边界与回归 / 关闭 Self-check

- <填实>

- [ ] 每个 US 有对应证据
- [ ] 结论来自可复现证据
- [ ] 记录关键权衡/理由/局限
- [ ] 未破坏既有功能
- [ ] 三查完成且发现项闭环
- [ ] PR 同步 main

## 8. 回顾 Retro（Sprint Review 后填）

**做得好（Keep）** / **可改进（Improve）** / **行动项（Action）**

- **Keep**：① **F2 分阶段拆解**（A/B/C 三 Sprint）——阶段 A"只读清单先行"（只读渲染分组清单、保留现手写面板，阶段 B 再替换交互），低回归面、渐进交付；② tech-research 教训纠偏机制再度验证（1.31 由调研改写，本轮 1.34/1.36 由 doc-audit 021 复验深化）；③ 三查联动快速闭环（022/023 发现项当日修复复验）。
- **Improve**：① 数字/常量级联同步的 grep 覆盖不全（漏英文 docstring 与裸数字公式）→ 沉淀 1.38；② M7 收敛锁首版漏判面（source_* 空列表）→ 沉淀 1.39；③ 临时手动启动 vite 未显式 cwd → 1.7 第二根因。
- **Action**：① 级联同步 grep 三覆盖（双语/大小写/注释裸数字）固化为固定动作；② 收敛锁单一 key 列表驱动；③ 临时 dev server 启动一律显式 cwd 或走 start 脚本。

## 9. 评审/同步记录（三查结论 + PR，Sprint 关闭必填）

- **〇查范围（impact-assessment 020，run-2026-08-31-impact-assessment-020）**：composite=44.62（A=4/13、B=10/10——触及 main.py 保守计 1.0）→ **窄档**（19 个变更文件 ∪ 全量核心区域）；自检 N_core=13、N_routes=10 与 spec v1.4.0 无差异。
- **一查（doc-audit 021）**：2 must-fix（verify_smoke 英文 docstring 8→10、agents-infra architecture B 公式分母 8→10）+ 5 should-fix（示例重算、SchemaFieldList 入 4 处文件清单、ROADMAP F2 Next→Now、5-VERSIONS 全景）→ 全部回填（30ce5cd）。
- **二查（code-review 022 windows）**：无 critical/major；1 minor 必修（verify_smoke docstring，与 021 同源）+ 1 minor（M7 漏 3 个 source_* 空列表断言）+ 3 nit（死代码/类型守卫 pre-existing/截图 try-finally）→ 修复 4 项，104 断言复验全过。
- **二查（code-review 023 main）**：合并后复核 6 项视角全过（合并完整性仅 4 个 mac .sh 有意分叉、跨平台零泄漏、docs/iteration 为空、远程分支卫生、锁文件隐私、10 路由与 spec 一致）；2 处 pre-existing nit 不阻塞（docstring 反斜杠、1-WORKFLOW:124 注记，下轮 doc-audit 收尾）。
- **lessons-learned 024（fan-out 第 4 步）**：固化 1.38（grep 三覆盖）与 1.39（断言/提取同源）已回填 3-LEARNED + 分类索引；vite cwd 问题并入 1.7（第二根因）；分层校验边界与 F2 拆解分别记"已知限制/§8 Keep"。
- **三查 workspace-check（主代理）**：`git status` 零残留；本地分支 main/windows；远程仅 mac/main/windows；端口 5173/8787/8501/8600 全部 FREE；回归：verify_config_schema 104 断言、verify_smoke 8/8、verify_agentops 30/30、validate-spec 7/7、前端 npm build 通过；CI（离线套件含新 verify_config_schema）绿。
- **PR 同步 main**：PR #37 已合并 main（`9ba8680`，不含 docs/iteration/）；3-LEARNED 1.38/1.39 回填随 PR #38 <待填>。
