# Sprint-18 / 治理批 G2 执行计划（规划稿；D1~D6，规模上限 ≤14 点）

- `plan`：本文件 `docs/iteration/sprint/2026-09-25-governance-batch-plan-G2.MD`（Sprint-18 的**计划书**；Sprint 文档在 D1 开工时另建）
- **口径真源**：[`2026-09-21-governance-batch-plan.MD` §6](2026-09-21-governance-batch-plan.MD)（用户 2026-09-25 裁定 B1①/ B2①/ B3①/ B4② / A1~A3）
- **范围依据**：〇查 `run-2026-09-25-impact-assessment-081`（windows 单血统，开区间 `b6198f51…`；卡集提案见其 §3，机制提案见其 §4）
- **状态**：规划稿。**§8 的五项待用户裁定**——裁定前不建 Sprint 文档、不派卡。

> 起草时间以网络时间戳为准：2026-09-25（UTC+8，周五）。本 Sprint（G1）已于同日关闭并合并入 `main`。

## 0. 口径与取代关系（读前必看）

- **本文件的卡集与规模上限是 G2 的唯一有效口径**：`2026-09-21-governance-batch-plan.MD`（G1 计划书）的 **§3"G2 行"与 §2.3"落地批次 = G2"已被取代**（依据＝用户 2026-09-25 的 B3①（≤14 点＋每张卡说得出哪一天）/ B4②（TG-18＋机制缺口排进 G2）＋本文件 §8.2 的 D4 裁定）。
- **取代后的去向（没有任何一项被静默丢弃）**：`TG-12`(3 点) 与多语言 lint(≈3~5 点) → **G3 候选**（`TG-12` 需先与 `TG-19` 的 M-E 去重再估点）；`MM-1/2/3`(7 点) → **保持"独立并行线"定位**（G2 不含、不占 G2 点数；是否启动由用户另定）。`F-AC13/14` 继续冻结；`TG-16` 等 G2 关闭后定稿。
- **就地标注（取代必须看得见）**：G1 计划书 §3 的 G2 行已标 `[已被 G2 计划书 §0/§8.2 取代]`、§2.3 的"落地批次"列（JS/TS/JSON/YAML/PS1/BAT 行）已改为 G3；`TG-12` 与 `MM-1/2/3` 的 `## Sprint` 与 backlog 索引行已同步改齐（三处一致由闸门 ⑥ 强制）。
- **取代规则（本批新增，防再发）**：任何裁定若改变"某 Sprint 包含什么"，必须**就地**在旧行标注 `[已被 §X 取代（日期）]` 并写明**取代原因**；**不得**只在文末追加"校准节"而让旧行留活。判据（prose-only，暂无闸门）：文档里同一事实只允许存在**一个未被标注作废**的版本。

## 1. 目标与头号指标

> **开工理由（用户 2026-09-25 补充，逐字口径）**："**我们需要赶上因治理耽误的进度**，按照我的目标这个项目最终应能**对外包装为一个 agent 供使用/调用**，现在来看**远远不达标**。"⇒ G2 的两条纪律：① 治理只做**能提高"检查可信度"的机制**（判定域、反空转、棘轮完整性、读数派生），不做规则堆积；② 规模受 **≤14 点**硬约束，**不得挤占**后续功能开发的额度。按计划书 §2.1 的组合门槛（连续 2 个 Sprint 满足 M1~M3）恢复功能开发，G2 的关闭质量直接决定这个时点。
> **机制缺口留档（用户 2026-09-25 追加定规）**：闸门/机制类缺陷同样作为 `agents-infra` 反例留档 ⇒ [`2026-09-25-gate-mechanism-failure-modes-case.MD`](../phases/agents-infra/2026-09-25-gate-mechanism-failure-modes-case.MD)（建立时 10 案例，每条 6 要素：现象/证据/根因/机制/反向对照/复用面；**同日中途独立复核及其后续修复后追加至 **21 例**（现例数以该文件小节数为准）**——现例数以该文件小节数为准）；规则条文见 `1-WORKFLOW.MD` §6 第 8 条。`TG-19` 治的四个族即该文件的前 9 条案例。

G2 照原计划做治理（用户 B1①），**头号复测指标 = M1 关闭税**，门槛不变：`code major ≤ 2` 且 `文档项 ≤ 3`，且三查一次通过、无需重跑。

| 项 | 值 | 来源 |
|---|---|---|
| G1 关闭税读数（输入） | `code major 7 / 文档项 11` ⇒ **未达标** | [`sprint-17.md` §8.1](2026-09-21-sprint-17.md)、更正见其 §9.5 |
| G1 关闭阻塞项合计 | 7 major + 3 critical + 8 must-fix + 3 should-fix = **21 条** | 同上（§9.5 口径） |
| G2 的判定 | 同一算式在 G2 关闭期重测；**读数由工具派生**（`TG-19` M-E），不得手抄 | 本文件 §5 |

**为什么 G2 必须先修"产生读数的闸门"**：M1 是关闭期产出，若闸门本身在假红/空转/自证上限，G2 的 M1 读数不可比——G1 末期一天内实测到四类失真（详见 [`TG-19`](../phases/testing-governance/cards/TG-19.md) 正文表）。因此 `TG-19` 的 M-A/M-B 排在 **D1**。

## 2. 范围与判定域（开工基线）

| 项 | 内容 |
|---|---|
| 纳入 | `windows` 单血统；窗口 = `(b6198f51…, G2 关闭锚点]`；G2 期间新开的卡与其落地提交 |
| 排除 | `mac` 血统（顶端无 `scripts/`）、`main` 血统（重建血统，同步由 PR 承担）、`F-AC13/14`（继续冻结）、`TG-16`（等 G2 关闭后定稿）、main 上缺席的 `docs/iteration/**` |
| 卡集（本计划内） | `TG-19` 新开、`TG-18`、`TG-17` 剩余条目、`A-M11`、`A-M12`、`A-M13` 新开 |
| 明文 out-of-scope | **已写死（2026-09-25 D4 裁定，见 §0/§8.2）**：`TG-12`(3)、`MM-1/2/3`(7)、多语言 lint(≈3~5) **均不计入 G2**；`F-AC13/14` 继续冻结、`TG-16` 等 G2 关闭后定稿 |

> **窗口纪律（`TG-17` 条目 8 修好之前的临时兜底，已写进 `TG-17` 卡）**：G2 的**关闭〇查登记之后，关闭期内不得再登记任何 scope 步 run**（否则 G2 自己的关闭窗口会被 G3 的 kickoff 顶掉——这正是 2026-09-25 让 Sprint-17 读数从 1 项变 99 项的机制）。

## 3. 卡集与点数

| 卡 | 点 | 来源 / 立项依据 | 交付物（一句话） | 哪一天 |
|---|---|---|---|---|
| **`TG-19` 关闭税机制化**（新） | 2 | 用户 B4② ＋ 〇查 §4 ＋ 本日实测事故 | M-A 判定域绑定、M-B 反空转、M-C 棘轮完整性、M-E 关闭税读数派生 | D1（M-A/M-B）、D3（M-C/M-E） |
| **`A-M13` 实现类 role**（新） | 1 | `TG-17` 证据⑤ ＋ 〇查 §2.7 | `implementation` spec ＋ C2 role↔spec 断言 ＋ §9 在飞宽限 | D2（**必须先于** `TG-17` 的 C1 定稿） |
| **`TG-18` 闸门债** | 1 | 棘轮基线 ＋ 二查 R3 | 历史表格缺陷逐处清零，棘轮键随之收敛 | D2 |
| **`TG-17` 剩余条目** | 1 | 用户 2026-09-25 前移裁定后的剩余 | 条目 3/6/7/8 与 1/2/5 两类：空转与横幅（3/6）、死键放宽（7）、跨 Sprint 窗口翻滚（8）、倒挂窗口（1）、棘轮单调性（2）、clone 模拟方法（5） | D4、D5 |
| **`A-M12` 编辑边界事故族** | 2 | 用户 2026-09-23 成卡 | 结构守卫从"建议跑"升级为**结构性不可跳过** ＋ 判据④口径 | D4、D5 |
| **`A-M11` workflow/提示词治理** | 3 | 用户 2026-09-21 成卡（本轮才实现；**前置调研已裁定先跑**，见 §8.1 D3） | ①外部实践调研 ②规则 vs 闸门清单 ③选项 A/B/C/D；④ 判据接线、读数顺延 G3 | D6（① 可前移 D5） |
| **`TG-20` 决策登记册与提问纪律**（新，用户插入） | 2 | 用户 2026-09-25 当场要求（原文见 [`docs/6-DECISIONS.md` §0](../../6-DECISIONS.md)） | `docs/6-DECISIONS.md`（原文逐字／生效状态／证据／验证时间戳）＋ 提问纪律条文（`1-WORKFLOW.MD` §6 第 9 条）＋ 机检判据与反向对照 | D1（登记册 v1）、D6（机检判据） |
| ~~预留插入~~（**已兑现为 `TG-20`**） | ~~2~~ → 0 | B3① 的插入纪律 | 原意是给"用户插入 / 复核新增发现"留缓冲；已被本卡用掉 | — |
| **合计（规划内）** | **12** | 上限 14 ⇒ **余量 2** | 再插入须用该余量或走"降范围/延时/换批次"三选一 | — |

**点数口径（已裁定，见 §8.1 D1）**：`TG-17` 按"原 3 点 − G1 关闭期已支付 2 点 = **剩余 1 点**"计（那 2 点已记在 Sprint-17 的插入账上，见 [`sprint-17.md` §9.4](2026-09-21-sprint-17.md) 前移块）。若按"剩余工作量从零重估"则 `TG-17` 记 2 点 ⇒ 合计 13，仍在 14 以内。

**关闭税不进这张账**：三查 + 独立复核 + 复盘 + Retro 属关闭税（G1 实测 ≈ 一张 3 点小卡），另计。

## 4. 日程（D1~D6）

**已裁定（用户 2026-09-25，回答 D2）**：**D1 = 2026-09-25（今天，周五）**；**09-26 / 09-27 是休息日**（用户口径），**10-01~10-07（国庆）不计入有效工作日** ⇒ 六个有效工作日 = `09-25 / 09-28 / 09-29 / 09-30 / 10-08 / 10-09`，**D6 = 2026-10-09（周五）**。

| 日 | 卡（点） | 当日交付物 |
|---|---|---|
| D1 09-25 | `TG-19` ①（1）＋ `TG-20` ①（1） | M-A 判定域绑定 ＋ M-B 反空转（**同时清掉本日的 98 项假红**）；`TG-20` 交付**登记册 v1**（原文逐字／生效状态／证据／验证时间戳）＋ 提问纪律条文（`1-WORKFLOW.MD` §6 第 9 条）；建 Sprint 文档 §9.1 骨架与看板（关闭税，不记点） |
| D2 09-28 | `A-M13`（1）＋ `TG-18`（1）→ **实际只完成 `A-M13`** | `implementation` spec ＋ `C2` 的 role↔spec 校验 ＋ 在飞宽限（**先于** `TG-17` 的 C1 定稿）**已交付**；`TG-18` **改期到 D3**——理由＝硬次序第 3 条（`TG-18` 与 M-C 同改 `agents/policy.json` 的棘轮键，**先 M-C 再 TG-18**，否则要改两遍） |
| D3 09-29 | `TG-19` ②（1）＋ `TG-18`（1） | M-C 棘轮完整性（`measured_value`/`measured_at`/`rebased_at`）＋ M-E 关闭税读数派生；随后 `TG-18` 把历史表格缺陷逐处清零并收敛（删）棘轮键 |
| D4 09-30 | `TG-17` ①（0.5）＋ `A-M12` ①（1） | `TG-17` 条目 1/2/5（含 C1 判定域定稿，在 `A-M13` 之后）；`A-M12` 结构守卫"不可跳过" |
| D5 10-08 | `TG-17` ②（0.5）＋ `A-M12` ②（1） | `TG-17` 条目 3/6/7/8；`A-M12` 判据④口径与读数方式 |
| D6 10-09 | `A-M11`（3）＋ `TG-20` ②（1） | `A-M11` ①②③④（④ 判据接线、读数顺延 G3）；`TG-20` 机检判据 ＋ 反向对照（交付时 9 条，整改后现跑 12 条）＋ 实战复算（**余量 2 点仍留作缓冲、不预先分配**） |

> D6 的 3 点是**同一张卡**（`A-M11` 的四个交付物同日收口），预留 2 点是缓冲、不预先绑定任何卡。**09-26/09-27 与国庆期间照常休息**；若 D2~D6 中某日实际未产出，按 M3 的"有效工作日"口径**顺延**，不记为零提交缺陷日（时间盒以有效工作日为准，日历跨度另列）。

**满足 B3①**：每张卡都能说出"哪一天在做它"，且不存在"4 张卡挤在同一天交付"（同日最多 2 张、合计 ≤2 点）。

## 5. M1 关闭税计数口径（**已定**，G2 直接沿用）

| 项 | 口径（机器可复跑，`TG-19` M-E 将把它做成工具） |
|---|---|
| `code major` / `code critical` | 二查报告 `## Graded findings` 节内、以 `- **<级别>**` 开头的 **graded 行数**（合并项算 1 行；"本轮未发现 critical"这类说明行不计） |
| `文档项` | 一查报告 `## Must fix` ＋ `## Should fix` 两节内 `^\d+\.` 的**编号条数**（不是文件数、不是"处"数） |
| 达标门槛 | `code major ≤ 2` 且 `文档项 ≤ 3`（一查 spec 的 Finding cap = 12 是**报告上限**，与门槛无关） |

**G1 读数的两处更正（已落 [`sprint-17.md` §9.5](2026-09-21-sprint-17.md)）**：文档项 `12 → 11`；首轮合计 `19 → 21`。根因是**把上一个 Sprint 的关闭税读数抄进了本行**（Sprint-16 的一查恰为 8+4=12、代码 major 亦为 7），因此 `1-WORKFLOW.MD:79` 与计划书 §1 里那两处"7 major + 12 文档项"是**正确的 Sprint-16 读数**，不要顺手改错。

> 〇查报告（`run-…-081`）的 §4 与附录 A 仍写"19 项 / 12 文档项"，且其"更正"小节把方向判反了（说 `1-WORKFLOW.MD:79` 记错）。**报告是冻结产物、按 `output_chars` 计量，不改**；以本文件 §5 与 `sprint-17.md` §9.5 为准（该报告的 §6 未知项 2 与 8 已如实标注"构成存疑/口径未定"，本条即对它们的收口）。

## 6. DoD（G2）

1. **每张卡**：交付物落地 ＋ 反向对照实测 ＋ 在**原始事故**上验证过（6 项标准：证据／根因／机制／反向对照／原始事故验证／复用面，**缺项即退回**）。
2. **M1 关闭税**：`code major ≤ 2` 且 `文档项 ≤ 3`，三查一次通过、无需重跑；读数**由工具派生**并与关闭记录逐字一致。
3. **M2 / M3**：沿用 [`sprint-17.md` §6](2026-09-21-sprint-17.md) 的五项违规指标与有效工作日口径（逐日提交、无零提交日）。
4. **`A-M11` / `A-M12` 的判据 ④ 是跨 Sprint 读数** ⇒ G2 的 DoD 对这两张卡改判为"**交付物交付 ＋ ④ 判据可执行并接线**"，**读数顺延 G3**（这不是放宽，是把一个在 G2 内结构性不成立的要求改成可核形态）。若不放宽，G2 会把 5 个**无法闭环**的点算进自己的关闭税。
5. **关闭税不进 ≤14**；但关闭税本身仍是 M1 的读数对象。
6. **`TG-19` 的 M-A/M-B 必须在 G2 第一次评审 run 之前落地**（否则 G2 的关闭读数被同一形态假红淹没）。
7. **关闭标准 = 零未闭环项（2026-09-27 00:06 UTC+8 用户裁定，登记 `D-250926-06`）**：G2 与 G3 **均按"零未闭环项"执行**——即关闭时**不允许存在任何未处置的发现项**，不以"过闸门 + 未达标项留待下批"结案。
   **时效（必须随引用一起写）**：本口径**自裁定时刻起对仍在进行中的 G2 生效**，并适用于 G3；**不回溯**——**G1（Sprint-17）是在"按声明关闭"的旧形态下结案的（其 M1 为 7/11 未达标、且当时本闸门体系尚不存在），本口径不用于重开 G1**；G1↔G2 的时长/达标对照均须标注"两批关闭形态不同"。
   **未闭环的计数口径**：复盘 §5 的行**全部计入**，除非该行已由用户**裁定处置**（含"单次豁免"，如行 14）；**"已由用户裁定处置"与"已修"是两件事**，前者计入已闭环、后者是修复动作——**此读法为现行采用，若与用户本意不符以用户更正为准**。

## 7. 关闭流水线与闸门（顺序与角色逐字）

| 步 | role | 说明 |
|---|---|---|
| 1 | `impact-assessment` | G2 关闭〇查；其 `recommended_scope` 是下游 scope 的唯一合法来源（缺它不得派评审） |
| 2 | `doc-audit` | 一查；scope 取自〇查；`Finding cap = 12` |
| 3 | `code-review` ×2 | 二查：`target=branch:windows` ＋ `target=branch:main`，`scope_source=impact-assessment:<关闭〇查 run_id>` |
| 4 | `lessons-learned` | 在 workspace-check 之前；不可跳过（可写"本轮无需新增"） |
| 5 | `workspace-check` | 三查环境；**主代理自跑**，不子代理化 |

**修复验证复核与关闭三查分节记录**（复核 run **不计入**三查覆盖）。关闭前必须全绿：`verify_close_readiness.py --sprint <G2 文档>`、`run_suite.py --tier offline`、`verify_gate_integrity.py`、`verify_derived_numbers.py`、`verify_md_tables.py`、`verify_card_index.py`、`scripts/structure-guard.py --replay`，以及 push 触发的 CI（"全新 checkout 模拟"必须用 `git clone`，`git archive` 会剥掉 `.git` 而假失败）。

> **CI 读法（2026-09-25 实测，避免把取消当失败）**：以**分支头**那次 run 的结论为准；被后续 push 取代的中间 run 会被并发组标成 `cancelled`——它**既不是失败也不是"已验过"**（同一棵树的内容由头 run 覆盖）。查 run 时按**机器取的完整 40 位 sha** 过滤（短 sha 会静默查不到）。

**Sprint-17 关闸读数的恢复时点（回答"多久后才能重启 sprint17 的关闭判定"）**：`TG-19` 的 M-A 一落地即可重跑（无需等 G2 关闭）。M-A 的反向对照 ① 就是本日事故的复现，**其通过即等于 Sprint-17 的关闭读数恢复**——但通过标准是"恢复后的项数 = 该 Sprint 的真实缺口"，**不是**"项数变少"：域的左端取该 Sprint 的作用域 run、**右端取该 Sprint 关闭期的最后一条覆盖 run**（G2 期间的提交属于 G2，不得算进 Sprint-17）。因此 M-A 实施的第一步是**打印恢复后的逐项清单**并逐条判"真缺口 / 假红"。在那之前，没有任何**合法**手段能恢复该读数：账本 `agents/runtime/registry.json` 被 `.gitignore:38` 忽略、有完整性哈希，删/改 `run-…-081` 属"改数据以换绿"。

## 8. 裁定记录

### 8.1 已裁定（用户 2026-09-25）

| 编号 | 裁定 | 要点 |
|---|---|---|
| **D1** | **12 点，含 `A-M11`** | 关闭税卡 2 ＋ `TG-18` 1 ＋ `TG-17` 剩余 1 ＋ `A-M11` 3 ＋ `A-M12` 2 ＋ `A-M13` 1，另留插入缓冲 2（上限 14 ⇒ 余量 2）。裁定时已知备选：砍 `A-M11` ⇒ 9 点；`TG-17` 按 2 点重估 ⇒ 13 点。**后续变更（同日）**：插入卡 `TG-20`（2 点，见 D-250925-11）兑现了该缓冲 ⇒ 规划内 **12 点**、距上限 14 **余 2 点** |
| **D2** | **D1 = 2026-09-25（今天）** | 用户口径："**25-27 都是休息日**……**从今天开始开工**"；⇒ 09-26/09-27 休息、国庆 10-01~10-07 不计有效工作日，六个有效工作日见 §4，**D6 = 10-09**。理由（用户同日补充）："**需要赶上因治理耽误的进度**" |
| **D3** | **先跑一次 `tech-research`** 再进 `A-M11` ① | 卡内原文要求"需继续调研后再确认"；用旧笔记替代 = 把"未核实"当输入 |
| **D5** | `TG-10` ③ **不并入 G2、也不现在排 G3** | 用户口径："暂不用管也不需要现在就排G3" ⇒ 维持 backlog"待排期"，不改其 Sprint 列 |
| **D6** | **已被 D2 吸收** | D1 就是今天 ⇒ M-A/M-B 即今日交付物，不再单列时点问题 |
| **D-250925-12（插入）** | **D4 之前先插一次"中途独立复核"** | 用户口径（本会话）："先插一次'中途独立复核'" ⇒ 复核对象 = D1~D3 已落地的代码＋文档＋闸门，**不等 Sprint 关闭**；**已执行**：`run-2026-09-25-code-review-083`（critical 0 / major 5 / minor 4 / nit 1，5 条 major 全部本轮整改）。详见 [`sprint-18` §5/§7](2026-09-25-sprint-18.md) 与 [`TG-19` 卡「独立复核整改」节](../phases/testing-governance/cards/TG-19.md)；登记册条目 = `docs/6-DECISIONS.md` 的 `D-250925-12`。**不占卡点**（它不是卡，是流程插入） |
| — | **机制缺口同样留档** | 用户同日追加定规 ⇒ 落 [`2026-09-25-gate-mechanism-failure-modes-case.MD`](../phases/agents-infra/2026-09-25-gate-mechanism-failure-modes-case.MD)（10 案例）；规则条文 = `1-WORKFLOW.MD` §6 第 8 条。**2026-09-25 追加**：中途复核又派生 5 例（案例 15~19），文档现 19 例 |

### 8.2 D4 裁定：**`§6` 优先**（`§3` 的 G2 行已被取代，2026-09-25 主代理按 §0 的取代规则落地）

> **裁定**：G2 的范围以 §6（最新裁定）为准 ⇒ **纯治理 12 点**；`TG-12`、多语言 lint 记 **G3 候选**；`MM-1/2/3` 保持**独立并行线**定位（G2 不含、不占点数）。**用户对"为什么会有冲突"的追问本身是有效输入**——冲突的根因是"同一事实写两处只更新一处"（族 E），处置见 §0 的取代规则。
> **可翻性**：若用户认为"非 Python 代码的可读性"是当务之急，改判成本＝把 §2.3 的"落地批次"列改回 G2 并从 G2 卡集里换出等量点数（备选见下方选项 (b)）。

**冲突本身**：G1 计划书 §3 的 G2 行写着"治理 ≈12 ＋ **MM 并行 7**"，其中包含 `TG-12`(3)、`MM-1/2/3`(7) 与 §2.3 的多语言 lint 行；而 §6（用户 2026-09-25 最新裁定）给的是"≤14 点 ＋ 卡集"，**没说这些项怎么办**。若两者同时生效，G2 会从 12 撑到约 22 点。

**决策所需的背景（逐项是什么、多大、与 G2 主题的关系）**：

| 项 | 点 | 是什么 | 与 G2 主题（"检查可信度"）的关系 | 若顺延 G3 |
|---|---|---|---|---|
| `TG-12` 文档内容漂移复盘 ＋ **派生事实机制** | 3 | 复盘历次"数字/状态副本过期"波次，并给出派生事实机制；卡内状态 `候选`，backlog 的 Sprint 列**已写 Sprint-18** | **部分重叠**：G1 已交付 `verify_derived_numbers.py`（手抄数字闸门）与三处棘轮；`TG-19` 的 **M-E** 又覆盖"关闭税读数派生" ⇒ G2 之后 TG-12 的剩余价值需重新界定 | 需要在 backlog 与卡文件里同步改 Sprint 列（三处状态一致性由闸门 ⑥ 强制），并把"与 M-E 重叠部分"去重后再估点 |
| `MM-1`(2) / `MM-2`(3) / `MM-3`(2) | **7** | 思维模型库：模型 schema 与库结构 / 半自动生成器（模型 → spec 骨架）/ 种子模型实例化首跑 | **无关**：§3 明写它是"**与 G2 并行**"的独立线（治理批之外的路线），不属"检查可信度" | 需在 §3 或 roadmap 里显式标注"并行线延后"，否则会与 G2 的 ≤14 混淆 |
| §2.3 的多语言 lint（G2 行） | ≈3~5（§2.3 **未估点**） | JS/JSX：eslint＋prettier；TS/TSX：`tsc --noEmit`＋`@typescript-eslint`；JSON/YAML：schema＋`yamllint`；PowerShell：`PSScriptAnalyzer`；Batch：人工＋轻量断言；Markdown style：`markdownlint` MD056。§2.3 已裁定 **G1 只上 `ruff`（已交付）**，其余标 G2 | **是机制**（"代码必须人类读得懂"是 `code major` 的硬标准），但**每个语言一套工具链＋一条反向对照**，与 G2 的四个机制族不同源 | 需在 §2.3 的"落地批次"列改成 G3，并说明"`code major` 的可读性判据在 G3 前**只有 Python 侧**有工具支撑，其余靠评审人工判" |

**选项**：(a) 全部 de-scope 到 G3（并在本文件 §2 与本计划书的 out-of-scope 里写死）；(b) **用多语言 lint（≈3）换掉 `A-M11`（3）**——G2 仍 12 点，但把"决策产出"换成"机制产出"；(c) `TG-12` ＋ 多语言 lint 并入 ⇒ 约 18 点（**超上限**，须同时改 B3① 的口径）；(d) 连 `MM-1/2/3` 也纳入 ⇒ 约 25 点（等于放弃 ≤14 与"每张卡说得出哪一天"）。

**建议**：**(a)**，理由三条——① §6 是你 09-25 的最新裁定，§3 是旧版且从未声明被 §6 取代；② 你的目标是"赶上进度并对外可用"，`MM` 线延后不伤工程可信度，而 `TG-12` 与 `M-E` 有重叠，**先去重再估点**比现在硬塞更省；③ 多语言 lint 的"人工评审兜底"在离线套件（`verify/run_suite.py --tier offline`，脚本数以其现跑输出为准）里仍然存在（可读性未失去全部防线），把它整批排进 G3 更可控。若你认为"非 Python 代码的可读性"是当务之急，则选 **(b)**。
