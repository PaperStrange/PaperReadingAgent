# Sprint 6（2026-08-30）—— F1 检索体验与查询质量（多语检索 v1 + 轻量评测）

  归属：`docs/iteration/sprint/`。启动依据：`1-WORKFLOW.MD` §4.4 判定标准（roadmap 新增候选 F1，用户选定方向）。
  关联：`../refactor-analysis.MD` §8.1 F1/F4。

## 1. Sprint Planning

- **Goal**：解决"中文 query 在英文分块上 BM25 零命中 → 回退前 N"的体验痛点——零命中时对 query 做**确定性关键词提取重试**（多语检索 v1，零成本无 LLM 调用），并在输出中给出 `query_used` / `query_strategy` / 命中理由；同步起步 F4 轻量评测基准（小样本 hit@1 前后对比）作为证据。
- **团队与产能**：1 人（AI 开发者），预估 **13 点 / ~8h**。
- **周期**：2026-08-30 完成；关闭前执行三查（§9）。

## 2. Backlog（用户故事 + 验收标准 + 估算）

| ID | 用户故事 | 验收标准 | 点数 | 预估工时 |
|---|---|---|---|---|
| US-6.1 | retrieve 多语检索 v1：零命中时提取 query 中的英文关键词重试 | ① 含 CJK 的 query 零命中 → 提取 `[A-Za-z][A-Za-z0-9-]{2,}` 关键词重试；② 输出 `query_used`（实际检索串）与 `query_strategy`（`direct` / `keyword_retry` / `fallback_first_n`）；③ 命中后 `result=ranked` 且候选与关键词相关 | 5 | 3h |
| US-6.2 | 检索结果"命中理由"展示与文档 | ① 输出 `hit_note` 说明本次策略（如"中文 query 零命中，已提取英文关键词重试"）；② 4-ALGORITHM/README 同步决策 | 3 | 2h |
| US-6.3 | F4 轻量评测基准起步 | ① `verify/eval_retrieve.py`：小样本 (query, 期望文件) 对（PaperQA2 + 用户语料），报告 hit@1 前后对比（direct vs keyword_retry）；② 结果入 Sprint 证据 | 3 | 2h |
| US-6.4 | 回归 + 文档 + 三查 + PR | smoke/e2e/GUI 回归；4-ALGORITHM §12 核对；三查结论 §9；PR 同步 main | 2 | 1h |

合计 **13 点 / ~8h**。

## 3. 任务看板（Kanban）

| 卡片 | 状态 | 点 | 开始 | 完成 |
|---|---|---|---|---|
| US-6.1 多语检索 v1 | ✅ DONE | 5 | 08-30 | 08-30 |
| US-6.2 命中理由+文档 | ✅ DONE | 3 | 08-30 | 08-30 |
| US-6.3 评测基准起步 | ✅ DONE | 3 | 08-30 | 08-30 |
| US-6.4 回归+三查+PR | ✅ DONE | 2 | 08-30 | 08-30 |

## 4. 燃尽图

总点数 13。

| 里程碑 | 剩余点 | 说明 |
|---|---|---|
| Sprint 开始 | 13 | 2026-08-30 |
| US-6.1~6.4 全部 DONE | 0 | 三查后关闭 |

```text
剩余
13 ┤●
 0 └───●───────────────────── 时间
    开始 → US-6.1~6.4 ✅
```

## 5. 执行记录

- 2026-08-30 开卡并完成 US-6.1~6.4；评测基线：语料 A 3/3（"什么是PaperQA？"由 fallback → keyword_retry 命中）、语料 B 1/2（纯中文无英文关键词仍 fallback，记录为 F1 后续 query 翻译方向）。

## 6. Definition of Done

- [x] 每张卡有回归证据（§7）
- [x] 结论来自可复现证据（verify/eval_retrieve.py 入库）
- [x] 记录关键权衡/理由/局限
- [x] 未破坏既有功能（smoke 8/8、e2e 六步）
- [x] 三查完成且发现项闭环（§9）
- [x] 决策逻辑同步 4-ALGORITHM.MD §8.2/§12；windows 已 PR 同步 main

## 7. 项目管理与证据

| US | 证据 | 通过情况 | 理由/说明 |
|---|---|---|---|
| US-6.1 | `verify/eval_retrieve.py`：中文 query → `keyword_retry|ranked` 命中 PaperQA2.pdf；英文 query → `direct|ranked`；Agent-G2 → `direct|ranked` | 通过 | 确定性关键词提取（零 LLM 成本）；纯中文无英文关键词仍 fallback（局限已记录） |
| US-6.2 | retrieve 输出实测含 `query_used`/`query_strategy`/`hit_note`；4-ALGORITHM §8.2 与 README 已同步 | 通过 | hit_note 三策略文案与代码一致 |
| US-6.3 | `verify/eval_retrieve.py`：语料 A 3/3（含 strategy 断言：keyword_retry/direct 各命中）、语料 B 1/1、负对照（纯中文无关键词）strategy=fallback_first_n PASS | 通过 | 作为 F1 后续（query 翻译/双语索引）的量化基线 |
| US-6.4 | smoke 8/8、e2e 六步、三查结论 §9、PR #18（见 §9） | 通过 | — |

### 权衡与理由 / 关闭 Self-check

- 理由：v1 选择**确定性关键词提取**而非 LLM 翻译——零成本零延迟，覆盖"中文+英文名"高频场景；query 翻译列为 F1 后续（配合 F4 评测选型）。
- 局限：纯中文无英文关键词的 query 仍回退（语料 B 1 例）。

- [x] 每个 US 有对应证据
- [x] 结论来自可复现证据
- [x] 记录关键权衡/理由/局限
- [x] 未破坏既有功能
- [x] 三查完成且发现项闭环
- [x] PR 同步 main

## 8. 回顾 Retro

- **Keep**：先立评测基线（F4）再改检索（F1），改动效果可量化对比。
- **Improve**：语料 B 的纯中文场景仍需 query 翻译/双语索引（F1 后续）。
- **Action**：① F1 后续：LLM 翻译或双语分块（用 F4 评测选型）；② eval 语料扩充（ 10 queries）。

## 9. 评审/同步记录（三查结论）

- **一查（文档审计）**：sprint-6 文档模板合规；4-ALGORITHM §8.2/§12 与代码一致（检索行为描述更新）；README note 与实现一致。
- **二查（代码评审）**：必须修 3 项已修复——① `strategy` 在零命中回退时未置 `fallback_first_n`，导致 hit_note 误报"直接命中"（已对齐枚举）；② `query_strategy` 枚举在代码/文档间漂移（已统一为 direct\|keyword_retry\|fallback_first_n）；③ sprint-6 文档虚标 PR/三查状态（已按真实状态重写）。建议修采纳：eval 增加 strategy 断言与负对照（已实现并实测 PASS）、§12 D6 编号重复修正；其余（eval 端口硬编码、正则 CJK 扩展）记录为后续。
- **三查（环境/功能完整性）**：工作区无多余残留（Sprint-6 改动清单符合预期）、分支仅 main+windows、端口 5173/8787 FREE；回归 smoke 8/8、e2e 六步、eval 全 PASS。
- **PR 同步**：**PR #18**（Sprint-6 代码 + 非迭代文档，不含 `docs/iteration/`）。
