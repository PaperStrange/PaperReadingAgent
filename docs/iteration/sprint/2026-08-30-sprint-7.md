# Sprint 7（2026-08-30）—— 插队·维护批：清技术账 M1/M2/M4/M5/M6 + 全量 code review

> 归属：`docs/iteration/sprint/`。启动依据：用户批准「M1~M6 维护批（直接清技术账）」（`1-WORKFLOW.MD` §4.4 插队 Sprint 判定：合计 >8 点、用户明确批准）。
> 关联：`../phases/refactor-analysis/backlog.MD`（维护卡 M1/M2/M4/M5/M6）。
> 用户指示（2026-08-30）：**M3 保持未完成**（staging 清理 UI + DOI 真实邮箱实测本次不做，卡留 backlog）；其余照常完成；**Sprint 关闭前追加一次 windows+main 全量 code review，确保技术账清完无遗漏**。

## 1. Sprint Planning

- **Goal**：清空 backlog 维护卡 M1/M2/M4/M5/M6（索引损坏探测升级、litellm 回调上限配置化、多节点并发计时与计时表清理、verify 连通实测与 GUI 依赖显式化、文档例行勘误），并在关闭前执行一次**全量 code review（windows+main）**，发现项按 §4.4 分诊闭环——技术账清零。
- **团队与产能**：1 人（AI 开发者），预估 **8 点 / ~6h**（不含 review 发现项的修复工时，另计）。
- **周期**：2026-08-30；关闭前执行三查 + 全量 code review（§9）。

## 2. Backlog（用户故事 + 验收标准 + 估算）

| 卡号 | ID | 用户故事 | 验收标准 | 点数 | 预估工时 |
|---|---|---|---|---|---|
| M1 | US-7.1 | 索引一致性增强：Tantivy 段损坏探测升级（现仅 files.zip 探针 + 目录指纹） | ① `index/meta.json` 缺失/不可解析 → 判损坏；② `tantivy.Index.open` + `searcher()` 失败（真段损坏）→ 判损坏；③ 损坏 → load_index 整目录重建自愈；④ 正常索引误判率 0（同目录同指纹保持增量） | 3 | 2h |
| M2 | US-7.2 | `prune_litellm_callbacks` 上限配置化（env） | ① `PAPERQA_LITELLM_CALLBACK_LIMIT` 可覆盖默认 20；② 非法值回落默认；③ `verify_prune_callbacks` 覆盖新逻辑 | 1 | 0.5h |
| M4 | US-7.3 | 多节点并发计时显示 + `runStartTimesRef` 清理 | ① 多个节点并发运行时 subcanvas 计时同时展示（如 `stepA 3.2s · stepB 1.1s`）；② 节点完成/失败后删除对应计时起点（ref 不无界增长）；③ 完成冻结文案行为不变 | 1 | 1h |
| M5 | US-7.4 | verify 连通实测 + gui 依赖显式化 | ① `verify_provider_switch` 对 openrouter/自定义新增**路由实证**（占位 key 到达各自端点并返回端点级错误 = api_base 正确；真实 key 实测标记用户资源门控）；② `verify/README.md` 增加每脚本运行前提显式化（端口/密钥/联网/浏览器） | 2 | 1.5h |
| M6 | US-7.5 | 文档例行勘误 + 回归 + 三查 + PR（含全量 code review） | ① 本轮发现的文档勘误（如 verify/README 表格列数错位）修复；② smoke/provider_switch/eval_retrieve/GUI 回归全绿；③ **全量 code review（windows+main）发现项分诊闭环**；④ 三查结论 §9；⑤ PR 同步 main（不含 `docs/iteration/`） | 1 | 1h |

> M3（staging 清理 UI/自动清理 + DOI 源真实 `UNPAYWALL_EMAIL` 实测）按用户指示**本次不处理**，卡保留 backlog（候选）。

合计 **8 点 / ~6h**。

## 3. 任务看板（Kanban）

| 卡片 | 状态 | 点 | 开始 | 完成 |
|---|---|---|---|---|
| US-7.1 M1 Tantivy 段探测升级 | ✅ DONE | 3 | 08-30 | 08-30 |
| US-7.2 M2 回调上限配置化 | ✅ DONE | 1 | 08-30 | 08-30 |
| US-7.3 M4 并发计时 + 清理 | ✅ DONE | 1 | 08-30 | 08-30 |
| US-7.4 M5 verify 实测 + gui 依赖 | ✅ DONE | 2 | 08-30 | 08-30 |
| US-7.5 M6 勘误+回归+全量 review+三查+PR | ✅ DONE | 1 | 08-30 | 08-30 |

## 4. 燃尽图

总点数 8。

| 里程碑 | 剩余点 | 说明 |
|---|---|---|
| Sprint 开始 | 8 | 2026-08-30 |
| US-7.1~7.5 全部 DONE | 0 | 三查 + 全量 review 后关闭 |

```text
剩余
 8 ┤●
 0 └────●──────────────────── 时间
    开始 → US-7.1~7.5 ✅
```

## 5. 执行记录（Standup 日志）

- 2026-08-30 Sprint Planning 完成；开卡 US-7.1~7.5（用户批准维护批；M3 按用户指示保持未完成）。
- 2026-08-30 US-7.1 DONE：`_index_corrupt` 三重探针（files.zip zlib / index/meta.json / tantivy open+searcher）；坑：aadd 引用推断是隐藏 LLM 调用 + TaskGroup 掩埋子异常（3-LEARNED 1.25），`verify_index_health.py` 用 manifest+txt 语料实现零 LLM 离线证据。
- 2026-08-30 US-7.2 DONE：`PAPERQA_LITELLM_CALLBACK_LIMIT`（非法回落 20；review 追加钳制 ≤30）；`verify_prune_callbacks` 5 用例。
- 2026-08-30 US-7.3 DONE：App.jsx subcanvas 计时改为"计时起点表"驱动（快速节点不遗漏、先完成节点冻结不被覆盖、起点随结束清理）；review 两轮修正（冻结丢失 → 重写为起点表遍历）。
- 2026-08-30 US-7.4 DONE：`verify_provider_switch` 路由实证断言（deepseek 真实 SUCCESS；dashscope/openai/openrouter/自定义占位 key 端点级拒绝）；verify/README 前提显式化；真实 openrouter key 实测=用户资源门控。
- 2026-08-30 US-7.5 DONE：全量 code review（windows+main）+ 文档审计（三个评审代理并行）→ 发现项分诊修复（见 §9）→ 回归 → 三查 → PR。

## 6. Definition of Done

- [x] 每张卡有回归证据（§7）
- [x] 结论来自可复现证据（verify 脚本入库）
- [x] 记录关键权衡/理由/局限
- [x] 未破坏既有功能（smoke/provider_switch/index_health/prune/e2e/GUI）
- [x] 决策逻辑变更同步 `4-ALGORITHM.MD` 并核对 §12
- [x] 三查完成且发现项闭环（§9）
- [x] windows 已 PR 同步 main（不含 `docs/iteration/`）

## 7. 项目管理与证据（Sprint Review 时填实）

| US | 证据 | 通过情况 | 理由/说明 |
|---|---|---|---|
| US-7.1 | `verify/verify_index_health.py`：合成六形态探针（空目录/files.zip 损坏/meta 缺失/meta 非法 JSON/meta 伪段/files.zip 缺失+伪段）全对；真实构建（txt+manifest 零 LLM）→ 健康探针 False → 篡改 meta.json 判损坏 → load_index 整目录重建自愈 → False；`paper_dir_marker.txt` 指纹确认 | 通过 | 三重探针与 vendored paperqa SearchIndex 磁盘结构逐一对齐；e2e 实测既有健康索引未被误判重建（0.0s 增量加载） |
| US-7.2 | `verify/verify_prune_callbacks.py`：默认 20 / env=5→5 / env=abc→20 / env=0→20 / env=40→钳制 30，5 用例全 PASS | 通过 | review 追加 ≤30 钳制，防自拆 litellm MAX_CALLBACKS 防护 |
| US-7.3 | `gui_check_s7.mjs` + 截图 `gui_check_s7*.png`：并发 load_index+parse 出现纯双运行中计时；先完成节点冻结不被覆盖；完成后冻结最终时长；npm build 通过 | 通过 | review 两轮修正：500ms tick 对 <500ms 快速节点不可见 → 改为"计时起点表"遍历（快速节点冻结、起点随结束清理）；起点表不无界增长 |
| US-7.4 | `verify_provider_switch.py`：deepseek 真实 key SUCCESS；dashscope/openai/openrouter/自定义（PAPERQA_PROVIDERS_JSON→deepseek 端点）占位 key 全部**端点级拒绝**（非连接类错误断言）；`verify/README.md` 每脚本前提显式化 | 通过 | 真实 openrouter key 实测=用户资源门控（占位 key 只证路由不证配额），README 注明 |
| US-7.5 | 全量 code review（windows+main，两代理）+ 文档审计（一代理）发现项分诊修复（§9）；回归 smoke 8/8、prune/index_health/provider_switch/e2e 六步、GUI M4 全 PASS | 通过 | 发现项：M4 冻结缺陷（已重写）、M2 钳制、gui_check_s7 登记、run_records 上限、事件历史键淘汰、embed 缓存 TTL、bool 强转、verify_e2e_result 出库；pre-existing 项沉淀 backlog M7/M8 |

### 权衡与理由 / 边界与回归 / 关闭 Self-check

- 理由：M1 用"整目录重建"兜底一切损坏形态（沿用 Sprint-5 原则，不搞半修复）；M2 上限钳制到 30 而非"配置什么就是什么"——防用户自拆防护；M4 用"计时起点表"而非"running 快照"——快速节点（<500ms）不遗漏且起点随结束清理。
- 局限：files.zip 缺失但真段完整**不**判损坏（build=true 增量重建可自愈，指纹不变）；真实 openrouter key / UNPAYWALL 邮箱实测为用户资源门控（后者随 M3 一并留待后续）。
- 回归：smoke 8/8；prune 5 用例；index_health 六形态+自愈；provider_switch 路由实证；e2e 六步（parse 30.3s/evidence 15.7s/answer 1502 字符）；GUI M4 双运行中+冻结；npm build 199 模块。

- [x] 每个 US 有对应证据
- [x] 结论来自可复现证据
- [x] 记录关键权衡/理由/局限
- [x] 未破坏既有功能
- [x] 三查完成且发现项闭环
- [x] PR 同步 main

## 8. 回顾 Retro（Sprint Review 后填）

**做得好（Keep）**
1. 维护批清账模式：卡→verify 证据→全量 review 兜底，三查建议修不再纸面闭环。
2. 离线证据技巧：manifest 提供 citation + 无图语料 → 索引构建零 LLM（1.25 入库）。

**可改进（Improve）**
1. 500ms 轮询对快速节点盲区——靠 review 两轮才发现；计时这类 UI 逻辑应先想清楚"事件驱动 vs 轮询快照"。
2. verify 脚本自身 bug（漏 config 前置、正则不匹配新格式）靠实测迭代暴露——证据脚本也应先自测。

**行动项（Action）**
1. M7 默认值漂移收敛（并入 F2）；M8 SSRF DNS rebinding 强化——已入 backlog。
2. M3（staging 清理 + DOI 真实邮箱实测）按用户指示留待后续。

## 9. 评审/同步记录（三查结论 + 全量 code review + PR，Sprint 关闭必填）

- **全量 code review（windows+main，两评审代理并行）**：无 critical/major。
  - 必修 3 项已修：① M4 并发不同时完成时先完成节点冻结丢失 → 计时改为"计时起点表"遍历（快速节点 <500ms 也不遗漏；`gui_check_s7` 实测双运行中 `evidence 0.4s · parse_chunk_embed 0.2s` + 先完成冻结保留）；② 节点删除后计时引用残留 → 随 `byId` 缺失清理；③ `gui_check_s7.mjs` 未登记 → verify/README、README.md、1-WORKFLOW §5.5 已补。
  - 强烈建议 1 项已修：M2 上限钳制 ≤30（`verify_prune_callbacks` 增 env=40 用例）。
  - pre-existing 技术债本轮顺手修：`run_records` 上限 200、`RunEventBroker` 历史键淘汰 200、embed 缓存 TTL 30 天、布尔参数强转 `_as_bool`（`"false"`→True 陷阱）；`verify/verify_e2e_result.json` 生成物出库（.gitignore）。其余（默认值 4 处漂移、SSRF DNS rebinding 理论项）沉淀 backlog **M7/M8**。
  - main 分支：无 `docs/iteration/` 残留、无断链；main 落后 7 个代码文件 → 本轮 PR 全部同步。
- **一查（文档审计）**：必须修 4 项已修（sprint-7 填实、README verify 清单补全、1-WORKFLOW §5.5 补全、verify/README 登记 gui_check_s7）；建议修 4 项已修（2-ARCHITECTURE 行数锚点移除、4-ALGORITHM 行号锚点改函数名、sprint-4 补记 PR #10 + DoD 勾选、phase README Sprint-2~7）。
- **二查（双分支 code review）**：windows 与 main 差异仅为 Sprint-7 代码/verify/文档；main 侧 .gitignore/README 两分支字节一致；`scripts/*.sh` 为 main 融合分支刻意保留（非 windows 删除项）。
- **三查（工作区/功能完整性）**：工作区改动清单符合预期（Sprint-7 代码 + verify + 文档；`verify_e2e_result.json` 已出库）；端口 5173/8787/8501 FREE；回归 smoke 8/8、prune 5/5、index_health 合成+自愈、provider_switch 路由实证、e2e 六步（answer 1687 字符）、eval_retrieve A=3/3 B=1/1、gui_check_s7 M4 双运行中+冻结、npm build 199 模块 全 PASS。
- **PR 同步 main**：**PR #21**（Sprint-7 代码 + verify/ + 非迭代文档，不含 `docs/iteration/`），合并 sha `65c6633`。
