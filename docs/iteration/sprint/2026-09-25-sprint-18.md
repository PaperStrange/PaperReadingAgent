# Sprint 18（治理批 G2）· 2026-09-25 ~ 2026-10-09 · 闸门可信化收尾（判定域 / 反空转 / 棘轮完整性 / 读数派生）

- `sprint`: 18（治理批 G2）
- 计划书：[`2026-09-25-governance-batch-plan-G2.MD`](2026-09-25-governance-batch-plan-G2.MD)（**卡集/点数/日程/口径的唯一真源**；其 §0 写明了与 G1 计划书 §3 的取代关系）
- 阶段卡索引：[`../phases/testing-governance/backlog.MD`](../phases/testing-governance/backlog.MD)、[`../phases/agents-infra/backlog.MD`](../phases/agents-infra/backlog.MD)
- 上限：**≤14 点**（本批规划 **12 点** ＋ 插入预留 2）；关闭税另计

## 0. 开工基线（G2 kickoff 〇查）

- **〇查 run**：`run-2026-09-25-impact-assessment-081`（role `impact-assessment`，`succeeded`；其 `recommended_scope` 是本 Sprint 下游评审 scope 的**唯一合法来源**）。
- **范围口径**：`windows` 单血统；窗口 = `(b6198f51…, G2 关闭锚点]`；排除 `mac`/`main` 血统、`F-AC13/14`、`TG-16`、以及 G2 不含的 `TG-12` / `MM-1/2/3` / 多语言 lint（依据＝计划书 §0/§8.2 的 D4 裁定）。
- **本次开工同时修掉的机制缺口（`TG-19` M-A）**：〇查 081 一登记，Sprint-17 的关闭读数即由 1 项变 99 项（98 项假红）——G2 的第一个交付物就是让判定域由"被判定物"派生。
- **窗口纪律（M-A 修好之前的临时兜底）**：本 Sprint 的**关闭〇查登记之后，关闭期内不得再登记任何 scope 步 run**。

## 1. Sprint Planning

- **目标**：治"检查在说假话"的四类形态（判定域错、空转、上限自证、读数手抄），使 **M1 关闭税**这个头号指标在 G2 关闭期**可比、可复算**。
- **头号指标（M1）**：关闭期 `code major ≤ 2` 且 `文档项 ≤ 3`，三查一次通过、无需重跑；读数**由工具派生**（`TG-19` M-E），不得手抄。G1 的输入读数 = `7 / 11`（未达标，见 [`2026-09-21-sprint-17.md` §9.5](2026-09-21-sprint-17.md)）。
- **时间盒（有效工作日 6 天）**：`09-25 / 09-28 / 09-29 / 09-30 / 10-08 / 10-09`（09-26、09-27 为休息日；10-01~10-07 国庆不计入有效工作日）。日历跨度 `09-25 ~ 10-09`，燃尽以**有效工作日**为准。
- **日程（每张卡说得出"哪一天"）**：D1 `TG-19`① → D2 `A-M13`＋`TG-18` → D3 `TG-19`② → D4 `TG-17`①＋`A-M12`① → D5 `TG-17`②＋`A-M12`② → D6 `A-M11`＋预留。

## 2. Backlog 卡集 + 范围 + 排除

| 卡 | 点 | 来源（立项依据） |
|---|---|---|
| `TG-19` 关闭税机制化（新） | 2 | 用户 B4② ＋ 〇查 §4 ＋ 本日实测的跨 Sprint 窗口翻转 |
| `A-M13` 实现类 role（新） | 1 | `TG-17` 证据⑤ ＋ 〇查 §2.7 |
| `TG-18` 闸门债（历史表格缺陷清零） | 1 | 棘轮基线 ＋ 二查 R3 |
| `TG-17` 剩余条目（1/2/3/5/6/7/8） | 1 | 用户前移裁定后的剩余（2 点已在 G1 支付） |
| `A-M12` 编辑边界事故族 | 2 | 用户 2026-09-23 成卡（结构守卫"不可跳过"） |
| `A-M11` workflow/提示词治理 | 3 | 用户 2026-09-21 成卡（先跑 tech-research，见计划书 D3） |
| `TG-20` 决策登记册与提问纪律（新，**用户插入**） | 2 | 用户 2026-09-25 当场要求（原文见 [`docs/6-DECISIONS.md` §0](../../6-DECISIONS.md)） |
| ~~插入预留~~（**已兑现为 `TG-20`**） | 0 | — |

- **点数**：规划内 **12 点**（上表 2+1+1+1+2+3+2），距上限 14 **余 2 点**（再插入须用该余量或走"降范围/延时/换批次"三选一）。

- **排除（明文）**：`TG-12`、`MM-1/2/3`、多语言 lint（G2 不含，去向见计划书 §0）；`F-AC13/14` 冻结；`TG-16` 等 G2 关闭后定稿。
- **验收 case（ATDD）**：本批为**机制卡**，其"验收 case"= 各机制的反向对照（逐条写在卡内），不另立功能 case；这一口径与 G1 的 `TG-3` 规则不冲突（无用户可见功能）。

## 3. 看板（Kanban：待办 / 进行中 / 完成）

| 卡 | 点 | 状态 | 哪天 | 交付物（一句话） |
|---|---|---|---|---|
| `TG-19` | 2 | 进行中 | D1、D3 | M-A 判定域绑定＋M-B 反空转（D1）；M-C 棘轮完整性＋M-E 读数派生（D3） |
| `A-M13` | 1 | 进行中 | D2 | `implementation` spec ＋ role↔spec 校验 ＋ §9 在飞宽限（**已交付**：spec ＋ C2 封闭世界按全账本 ＋ 在飞宽限 ＋ 6 条机检；首次真实使用 `run-2026-09-25-implementation-082`） |
| `TG-18` | 1 | 进行中 | D3 | 15 处历史表格缺陷逐处清零（**已交付**：4 个文件问题 0；上限 1/12/1/1 → 0） |
| `TG-17` | 1 | 进行中 | D4、D5 | 条目 1（倒挂窗口判据）**D4 交付**、2/4/5/6/8 核验为已闭环；**条目 3/7 D5 交付**（条件根 git 证据 / 死键直接证据）；**C1 判定域定稿**随关闭批 |
| `A-M12` | 2 | 进行中 | D4、D5 | ① 结构守卫"不可跳过"**D4 交付**（git 基线档 ＋ pre-commit 钩子 ＋ CI 接线 ＋ 接线判据）；② 判据 ④ **口径在位 D5 核验**（现跑读数 = 3，可复算）；最终读数待 Sprint-18 关闭时统计 |
| `A-M11` | 3 | 候选 | D6 | ①外部调研 ②规则↔闸门清单 ③选项 ④判据接线 |
| `TG-20` | 2 | 进行中 | D1、D6 | `docs/6-DECISIONS.md` 登记册（原文逐字/状态/证据/时间戳）＋ 提问纪律条文（D1）；机检判据＋反向对照＋实战复算（D6） |
| ~~插入预留~~ | 0 | — | — | 已兑现为 `TG-20` |

## 4. 燃尽（Burn-down）

> **口径澄清（2026-09-25 当天补，防记录失真）**：本表的 `D1/D2/D3…` 是**交付批次标签**（每一批 = 计划里那天的卡片组），**不等于日历年日**。用户 D2 裁定"从今天开始开工"，而主代理可连续执行 ⇒ D1~D3 三批都在 **2026-09-25** 落地。真实交付时刻以**提交时间**为准（当日 G2 段：17:07 → 20:31 UTC+8，见 `git log`），点数按**批次**累计；计划书 §4 的 09-28/09-30/10-08… 是**容量与休息**的口径（有效工作日），不是"每批必须占满一天"。若后续批次也同日落地，本表照旧按批次记，并在 §5 写明实际时刻。

| 有效工作日 | 日期 | 当日完成点 | 累计完成 | 剩余 | 备注 |
|---|---|---|---|---|---|
| D1 | 2026-09-25 | 2 | 2 | 10 | 开工：Sprint 文档 + 看板；`TG-19` ① **M-A 判定域绑定 ＋ M-B 反空转（第 1 批）交付**（Sprint-17 读数 99 项 → 1 项且为真项；SKIP 档不再打印 PASS/EVIDENCE）；`TG-20` ① 登记册 v1 |
| D2 | 2026-09-28 | 2 | 4 | 8 | `TG-19` **M-A 收尾**：`register --sprint` ＋ `set-sprint` 受控回填 ＋ **20 条**身份回填 ＋ UC-22 机检（含锁覆盖断言）；`A-M13` **交付**：`implementation` spec ＋ C2 封闭世界（全账本）＋ §9 在飞宽限 ＋ 机检，首次真实使用 `run-2026-09-25-implementation-082`（并在使用中当场修掉 M-A 域右端缺陷） |
| D3 | 2026-09-29 | 3 | 7 | 5 | `TG-19` ② **M-C 棘轮完整性**（棘轮名单补齐 4→**5** 条并各补 `measured_value`/`measured_at`；提交态放宽必须带 `rebased_at` + 理由 + `rebased_from`；自检 19 断言）＋ **M-E 关闭税读数机器派生**（新闸门 `verify_close_tax_reading.py`；Sprint-17 真数据 `PASS`，工具派生 `7/3/11` 与 §9.5 更正一致）；`TG-18` **15 处清零**（4 文件问题 0；上限 1/12/1/1 → 0，`rebased_at` + 理由入政策）；**用户插入中途独立复核**（`D-250925-12`，`run-…-083`：major 5 全整改） |
| D4 | 2026-09-30 | 1.5 | 8.5 | 3.5 | `TG-17` ①（0.5）：**条目 1 倒挂窗口具名判据交付**（自检 91→**94**）；条目 2/4/5/6/8 核验为**已由 M-C/A-M13/M-B/M-A 闭环**（逐条给证据，不重复实现）；条目 3/7 转 D5。`A-M12` ①（1）：**结构守卫"不可跳过层"交付**——`verify --from-git <rev>`（git 当基线，坏修订 fail-closed；`Structure-Removal:` 具名署名，阈值入政策）＋ `scripts/hooks/pre-commit` 与 `scripts/install-hooks.py`（`core.hooksPath`，零写盘）＋ CI 接线 ＋ `verify_gate_integrity.py` 的**接线判据**（自检 19→**23**）。**过程事故 2 起（我自己造成，均 R1 同型）**：`edit` 行尾换行收边吞并一行（`py_compile` 抓到）；自动折行脚本用**过期行号表**重跑把注释切碎（AST 检查看不见注释 ⇒ 加固脚本后重做）。 |
| D5 | 2026-10-08 | 1.5 | 10 | 2 | `TG-17` ②（0.5）：**条目 3 交付**（`md_table_coverage.conditional_roots` ＋ git 直接证据：HEAD 里有而工作区没有 ⇒ FAIL「被删」；HEAD 里也没有 ⇒ **具名 SKIP**，判决行 `MD-TABLE SKIP（…不是通过）`；三形态真跑：windows `PASS 171`／删根 `rc=1`／main 形态 `SKIP 8`）＋ **条目 7 交付**（`verify_derived_numbers.py` 的死键判据换成 `git ls-tree HEAD` 直接证据：被删 ⇒ FAIL；旧键分支差异 ⇒ NOTE；**新键在树里不存在 ⇒ FAIL**＝复核指出的第二个洞；反向对照 10→**14**）。`A-M12` ②（1）：**判据 ④ 的口径与读数方式在位**＋**当前读数可复算**（卡片里的命令现跑 = **2**，含本批记入的两次 R1 同型；**目标 ≤1 ⇒ 当前不达标**，如实记账）。**过程事故 1 起（我自己造成，非 R1 族）**：验证 CI 时轮询脚本的 `git rev-parse HEAD` 取自**另一个 cwd 的仓库** ⇒ 拿别人的 run 当自己的证据（按 run id 复查才纠正）⇒ 留档反例档**案例 21**，规则补进 §6 第 1 条的对偶"不猜标识符"。 |

## 5. 工作日志（Standup）

- **2026-09-25（D1）**：G2 计划书定稿（含 §0 取代关系与 D1~D5 裁定落地）、`TG-19`/`A-M13` 开卡、机制缺口反例档建立、Sprint 文档与看板建立；**用户插入卡 `TG-20`（决策登记册＋提问纪律）**：登记册 v1（[`docs/6-DECISIONS.md`](../../6-DECISIONS.md)）＋ 提问纪律条文（`1-WORKFLOW.MD` §6 第 9 条）落盘，历史裁决的逐字征集进行中；随后开工 `TG-19`①（M-A/M-B）。
- **2026-09-25（D1 续）**：**`TG-19` ① 的 M-A（判定域绑定）交付**——`verify/verify_close_readiness.py` 增 Sprint 身份派生、域/右端派生、`domain-unavailable` 退出码（4）、需求侧窗口过滤、C2 全账本存在性；自检断言数以其 `EVIDENCE:` 行为准（本批 +13）；**在原始事故上验证**：Sprint-17 的关闭读数由 **99 项 → 1 项**（且该 1 项是真实缺口：其 doc-audit run 的 `task_id` 不符 `fanout.json` 声明的 target，此前由更早 Sprint 的 run 借位满足）；Sprint-18 自身读数 = 5 项、全部真实。落地同时暴露 4 条数据缺口（见 `TG-19` 卡「交付落地」节），均已登记、**不追改历史数据**。机制反例档新增案例 11/12。
- **2026-09-25（D1 续 2）**：**`TG-19` ① 的 M-B 第 1 批交付**（判决行单一真源：SKIP 档不得打印 PASS 横幅、不得打印 `EVIDENCE:` 行）——在**无 `docs/iteration/` 的真实树**与**账本缺失档真子进程**两条真实入口上验证；`verify_card_index.py` 自检 +3、`verify_close_readiness.py` 自检 +1。**`TG-20` ① 完成全量分层并入**（§4.1 逐字 103 条 / §4.3 附录 282 条，三路只读征集 → 合并稿 385 条）。**过程事故 1 起（我自己造成）**：改 `VERIFY_META` 后未重跑 `verify_matrix.py derive` ⇒ CI step 7 红（run `36124069452`），已修（重新 derive）并按用户定规留档为机制反例档**案例 13**。
- **2026-09-28（D2）**：`TG-19` **M-A 收尾**——`register --sprint`（归一化 + 非法即 fail-closed）与 `set-sprint`（受控回填：≥10 字符理由、同值无操作、`sprint_backfills` 五件套留痕）落地；真账本回填 **20 条**关闭流水线 run 的 Sprint 身份（`053`/`060` → 16，其余 → 17），回填后 Sprint-17 仍为 **1 项真项**、Sprint-18 **4 项全真**、Sprint-16 的域右端不再被 Sprint-17 的 `080` 借位；`verify_agentops.py` 新增 **UC-22**（6 条断言，含"写命令必须仍在锁内"）。**过程事故 1 起（我自己造成）**：新增 `_norm_sprint()` 被插在 `@_with_registry_lock` 与 `cmd_register` 之间 ⇒ **`register` 丢锁**（竞态回归）＋ `_norm_sprint` 自锁超时；已修并做成机检（反例档**案例 14**）。
- **2026-09-28（D2 续）**：`A-M13` **交付**——新增 `agents/functions/implementation.md`（`scope_required: false` ＋ `coverage_window: none`）＋ `agents/README.md` 同步；判据侧：C2 封闭世界改为**按全账本**、§9 新增**在飞宽限**（取值同 `ledger_measurement.in_flight.terminal_writeback_grace_minutes`）、新增 `_contributes_coverage()`；自检断言以其 `EVIDENCE:` 行为准（本批 +9，其中 3 条读**真政策**）。**首次真实使用**：`run-2026-09-25-implementation-082`（不进 §9 要求、不贡献 C3 覆盖）。**真实使用当场暴露并修掉的 M-A 缺陷**：实现类 run 在 `finish` 时自动记下**空窗口**，`domain_right_boundary()` 原先把"最新一条带 `covers_through`"当域右端 ⇒ 把 G2 的域右端拉到该 run 的提交上；已改为只认**承担内容覆盖**的 run，并补两条反向对照。
- **2026-09-28（D2 收尾）**：**`TG-18` 改期到 D3**（顺延理由写进计划书 §4：它与 M-C 同改 `agents/policy.json` 的棘轮键，**先 M-C 再 TG-18**，否则要改两遍——这是 〇查给的三条硬次序之一）。D2 实际完成点 = **2**（`TG-19` M-A 收尾 1 ＋ `A-M13` 1）。
- **2026-09-29（D3）**：`TG-19` ② **M-C 棘轮完整性交付**——棘轮名单补齐 4→**5** 条（复核 major 后补入 `artifact_paths.dynamic_ratchet`），全部补 `measured_value` + `measured_at`；`verify_gate_integrity.py` 新增三条"实测值"判据 + `committed_rebase_problems()`（**提交态放宽必须具名**：`rebased_at` + ≥10 字符理由 + `rebased_from` 逐键绑定父提交数值），真数据 `PASS`（断言数以现跑 `EVIDENCE:` 行为准）。
- **2026-09-29（D3 续）**：`TG-19` ② **M-E 关闭税读数机器派生交付**——新闸门 `verify/verify_close_tax_reading.py`（从报告派生 `code_major`/`code_critical`/`doc_items`，与 Sprint 文档的机读读数行逐字比较）；Sprint-17 文档补机读行后真跑 **`CLOSE-TAX PASS`**（`7 / 3 / 11`，**与 §9.5 的人工更正一致 ⇒ 该更正升格为机器复核**）；真数据反向对照（改 1 个数字 ⇒ rc=1）＋ 自检断言；规则条文入 `1-WORKFLOW.MD` §6 第 10 条；新闸门已进 `gate_integrity.guard_required`（`-O` 探针覆盖）。
- **2026-09-29（D3 续 2）**：**`TG-18` 交付**——15 处历史表格缺陷**逐处清零**（`verify_md_tables.py`：4 个文件全部 `问题 0`）：sprint-6 的裸管道转义、sprint-15 两张表各按"补列/并格"修、sprint-16 与验收指南的 `.md|.markdown` 转义；`md_table_legacy_files` 上限 1/12/1/1 → **0**（保留键与 `review_by`/`measured_value`，等价于删表但更紧；`rebased_at` + 理由入政策——M-C 的 rebase 字段首次真实使用）。反向对照：向受保护文件的既有表块内注入 1 行 ⇒ rc=1 逐处点名（还原 rc=0）。**过程如实记录**：① 我第一次注入的行追加在文件末尾、自成新表块 ⇒ 判 PASS（设计边界，已写进卡）；② 修完表格后我自己在 TG-18 卡的表格里又写了裸管道，被 `md_tables` 当场拦下（同类缺陷、当场修）——闸门比人可靠。
- **2026-09-25（D3 续 3 · 用户裁定插入）**：**中途独立复核**（用户 `D-250925-12`："先插一次'中途独立复核'"）——`run-2026-09-25-code-review-083`（`code-review` 子代理，只读；scope = D1~D3 落地 diff，`scope_lower_bound` `b6198f5` → HEAD `a89b2821`，20 提交/36 文件）：**critical 0 / major 5 / minor 4 / nit 1**（38 处 `file:line`）。**5 条 major 全部本轮整改**（M1 机读读数行可被正文冒充／M2 缺读数行即 SKIP／M3 域右端按时序而非覆盖／M4 在飞宽限无下界／M5 `artifact_paths.dynamic_ratchet` 漏出棘轮名单），minor/nit 亦逐条整改（含 `6-DECISIONS` 6 处漂移指针、`sprint-18` 一处过期现值）。**新执行点**：`verify_agentops.py` **UC-23**（5 条断言）＋ 受控回填子命令 `set-result-files`（我自己的事故：`finish` 漏带 `--result-file` ⇒ 账本报"做过"而产物未归档，且二次收尾被 fail-closed 拒绝 ⇒ 该问题**不可修**=永久红）。整改后 `verify_lint.py` 的 E501 由 2521 → **2493**（上限 2494 **未动**，本次新引入的 34 条全部折行）。机制反例档新增 5 例。**复核自身的覆盖边界如实记账**（未读卡片/计划书/两本反例档/TEST-MATRIX、未跑套件与 lint），不得当作"全查过"。
- **2026-09-25（D4）**：`TG-17` ① 与 `A-M12` ① 同日落地。① **倒挂窗口判据**（G2 条目 1）：`covers_through` 早于 `coverage_anchor` ⇒ 窗口对任何提交都不成立（`covers()` 恒 False）却**一条旧判据都不报**；现具名 + 修复指引，**与"空区间"分族**（空区间=覆盖声称问题、按 run 类别分流；倒挂=数据自相矛盾、与短 sha 同族不豁免任何 role）；自检 94 断言、真数据 0 例。条目 2/4/5/6/8 复核为**已闭环**（M-C / A-M13 / M-B / M-A），只补证据不重复实现；条目 3/7 转 D5。② **结构守卫"不可跳过层"**（A-M12 ①）：基线从"我记得拍快照"换成 **git**（`verify --from-git <rev>`，坏修订 rc=2 而非静默 PASS）、便利层 = `scripts/hooks/pre-commit` ＋ `install-hooks.py`（`core.hooksPath scripts/hooks`，**零写盘**）、不可跳过层 = CI 按 push 范围跑同一条判据、**接线本身有判据**（`verify_gate_integrity.py` 自检 23）。**过程事故 2 起（我自己造成，均 R1 同型 ⇒ 已按 ④ 登记义务落 §10）**：`edit` 行尾换行收边把两行并成一行（`py_compile` 抓到）；自动折行脚本用**过期行号表**重跑把注释切成碎片（AST 等价检查看不见注释）⇒ 脚本加"当前行确实超宽"前置条件 + 逐字拼回等价断言后重做，三轮折行 37 行、E501 2537 → **2492**。
- **2026-09-25（D5）**：`TG-17` ② 与 `A-M12` ② 同日落地。③ **条件根判据**（G2 条目 3）：`md_table_coverage.conditional_roots` 声明"按分支可能不存在的子树"（`docs/iteration`），判据改用 `git ls-tree HEAD` 的**直接证据**——HEAD 里有而工作区没有 ⇒ **FAIL「被删」**；HEAD 里也没有 ⇒ **具名 SKIP** 且判决行改打 `MD-TABLE SKIP（…不是通过）`（旧行为在 main 形态上是 `MD-TABLE PASS（8 个文件）`：少审 156 个文件在读数里不可见）。三形态在 `%TEMP%` 临时 worktree 上真跑（测后已 `worktree remove`）。④ **死键判据换直接证据**（条目 7）：`verify_derived_numbers.py` 的"父目录不存在 ⇒ 分支差异"改成 `git ls-tree HEAD`——被删 ⇒ FAIL、旧键分支差异 ⇒ NOTE、**新键在树里根本不存在 ⇒ FAIL**（复核指出的"静默预留豁免"洞）；反向对照 10→**14**。⑤ `A-M12` ②（判据 ④ 口径与读数方式）**核验在位**，现跑读数 = **3**（含本批记入的两次 R1 同型）。**过程事故 1 起（我自己造成，非 R1 族）**：查 CI 时轮询脚本的 `git rev-parse HEAD` 取自**另一个 cwd 的仓库** ⇒ 拿别人的 run 当自己的证据（按 **run id** 复查才发现并纠正）⇒ 反例档**案例 21** ＋ 规则补"**不猜标识符**"（§6 第 1 条的对偶）。
- **当日（D1~D3 三批）实际时刻**：`b6198f5` 17:07 → `5e0d8c2` 20:31（UTC+8，`git log` 可核）——三批落在同一日，见 §4 的口径澄清。

## 6. Definition of Done

沿用计划书 §6：① 每卡按 6 项标准（证据／根因／机制／反向对照／**在原始事故上验证**／复用面）交付，缺项即退回；② M1 门槛 `code major ≤ 2` 且 `文档项 ≤ 3`、三查一次通过；③ 读数由工具派生并与记录逐字一致；④ `A-M11`/`A-M12` 的判据 ④ 属跨 Sprint 读数 ⇒ G2 只交付"判据可执行并接线"，读数顺延 G3；⑤ 关闭税不进 ≤14。

## 7. 验收演示（Sprint Review 证据）

> 关闭期填写。要求：每卡一行证据（命令 + 现跑输出 + 反向对照结果），派生数字一律给"复现命令 + 以输出为准"。

| 卡 | 证据（命令 / 输出） | 反向对照 |
|---|---|---|
| `TG-19` ① M-A | `.venv\Scripts\python.exe verify\verify_close_readiness.py --sprint docs\iteration\sprint\2026-09-21-sprint-17.md` → 改前 **99 项**（98 项假红）／改后 **1 项（真项）**；Sprint-18 **D1 时点 = 5 项**（3 条本期步骤缺 run ＋ 未登记 `081` ＋ 未声明锚点），**把 `081` 写进 §9.1 之后 = 4 项**（现跑为准：`CLOSE-READINESS FAIL（4 项）` rc=1）；自检断言数以其现跑 `EVIDENCE:` 行为准（本批新增 13 条 M-A 断言） | 自检内置 ①原始事故复现（追加下一个 Sprint 的作用域 run ⇒ 上一个 Sprint 的窗口一字不变）／②本期未跑 ⇒ 点名缺 run／③无作用域 run ⇒ 起点 None／④无身份的更晚 run 不得顶掉本期 |
| `TG-19` ① M-B（第 1 批） | **无 `docs/iteration/` 的树**（worktree 删该目录后跑 `verify_card_index.py`）→ 判决行 `CARD-INDEX SKIP（…）`，**`EVIDENCE:` 0 行 / `CARD-INDEX PASS` 0 行**，rc=0；账本缺失档（真子进程 + `AGENT_OPS_DIR` 空目录）→ `CLOSE-READINESS SKIP（账本缺失档，不是通过）`，同样无证据行 | 自检内置 3 条判决行对照（SKIP 档不含 `PASS`／无问题才 PASS／有问题优先 FAIL）＋ 1 条真子进程 SKIP 对照（`verify_close_readiness.py` 自检断言数以其 `EVIDENCE:` 行为准） |
| `TG-20` ① 登记册 | [`docs/6-DECISIONS.md`](../../6-DECISIONS.md)：§0 原文逐字、§1 字段定义、§2 提问纪律、§3 本会话 11 条（逐字）、§4.0 首批已核 8 条、**§4.1 逐字原话 103 条（全收）＋ §4.3 附录 282 条**（用户裁定 R1 分层并入）、§5 违背事故索引、§6 待逐字核对；历史素材由**三路只读征集**取得（原始 519 条 → 合并稿 385 条，逐段字符级回源校验） | 机检判据与 4 条反向对照＝`TG-20` ③④（D6 交付，**未完成**——本行不声称闭环）；§4.2「二手转述中仍生效者」按批次回填 |
| `TG-19` M-A 收尾（D2） | `scripts/agent-ops.py register --sprint` / `set-sprint`（真 CLI 实测：归一化、拒绝非法身份、回填+四件套留痕、同值无操作）；真账本 **20 条** `sprint_backfills`；`verify_agentops.py` **UC-22**（断言数以该脚本 `EVIDENCE:` 末行为准） | UC-22 反向对照：非法身份不留半条 run／过短理由拒绝／**锁覆盖断言**（8 条写命令必须都带 `__wrapped__`——对应本批我自己的装饰器挂错对象事故，反例档案例 14） |
| `A-M13`（D2） | `agents/functions/implementation.md`（`validate-spec` PASS）＋ `agents/README.md` 职能表；`verify_close_readiness.py`：C2 封闭世界按**全账本**、§9 **在飞宽限**（取值同 `ledger_measurement.in_flight`）、`_contributes_coverage()`；首次真实使用 `run-2026-09-25-implementation-082` | 反向对照：域外野 role ⇒ FAIL／刚登记 ⇒ 只提示／超期 ⇒ FAIL／`produced_only` 与 `coverage_window:none` 不得界定**域右端**（这两条是真实使用中当场暴露的缺陷——实现类 run 的空窗口曾把 G2 的域右端拉到自己那一刻） |
| `TG-19` ② M-C（D3） | `agents/policy.json`：**五条**棘轮全部补 `measured_value`（逐键实测快照）＋ `measured_at`（`derived_numbers` 的 32 键由脚本生成，非手抄；第 5 条 `artifact_paths.dynamic_ratchet` 为复核 major 后补入）；`verify_gate_integrity.py` 真跑 `PASS … 5 个棘轮…都带 measured_value/measured_at；提交态无未署名的放宽`（断言数以 `EVIDENCE:` 行为准，现 **19**） | 自检：缺快照／缺测量日期／`cap > 实测`／快照多键／**提交态上调而无声 ⇒ FAIL**／同样的上调带 `rebased_at` + 理由 + **`rebased_from`** ⇒ PASS／**先降后升 + 复用旧署名 ⇒ FAIL（N4）**／提交态不变不误报 |
| `TG-19` ② M-E（D3） | 新闸门 `verify/verify_close_tax_reading.py`（offline；已进 `gate_integrity.guard_required`，`guard_required=9`）；Sprint-17 文档新增机读读数行 ⇒ 真跑 `CLOSE-TAX PASS：声明与报告派生逐字一致：code_major=7, code_critical=3, doc_items=11` | 真数据反向对照：把 `doc_items=11` 改成 `12`（副本逐字比对确认唯一差异）⇒ **rc=1** 并点名"差 1"；自检断言数以其 `EVIDENCE:` 末行为准（缺键／报告增删 1 条／节外行不计／说明行不计／报告缺失具名／**行首锚定：正文同形叙述不算读数行**／**两行读数 ⇒ 命中 2 行**／**自称关闭却缺读数行 ⇒ FAIL**） |
| `D-250925-12` 中途独立复核（D3 后，用户插入） | `agents/runs/run-2026-09-25-code-review-083/code-review.report.md`（18058 字符；账本 `result_files` 经受控回填补登，留痕 `result_files_backfills`）；结论 **critical 0 / major 5 / minor 4 / nit 1**；5 条 major 逐条整改见 [`TG-19` 卡「独立复核整改」节](../phases/testing-governance/cards/TG-19.md) | 整改后现跑：`verify_lint.py` `E501=2493/2494`（整改新引入的 34 条全部折行，**上限未动**）、`verify_gate_integrity.py` 19 断言、`verify_close_tax_reading.py` 13 断言、`verify_close_readiness.py` 91 断言、`verify_agentops.py` 120 断言（含新 **UC-23** 5 条）；反例侧：`set-result-files` 四类反向对照（产物不存在／仓库根相对路径／过短理由 ⇒ 拒绝且不改库；同值 ⇒ 无操作不留痕） |
| `TG-18`（D3） | `verify/verify_md_tables.py` ⇒ 4 个历史文件均 **`问题 0`**、全库 `MD-TABLE PASS`；`agents/policy.json::md_table_legacy_files` 上限 1/12/1/1 → **0/0/0/0**（+ `rebased_at` + `rebased_reason`） | 反向对照：往受保护文件的**既有表格块内部**注入 1 行（2 格 vs 众数 4）⇒ rc=1 逐处点名，还原 rc=0；**边界如实记录**：同样一行追加到文件末尾（自成新表块）时判 PASS（新表头自定义列数）。结构纪律：`structure-guard snapshot` → 改 → `verify` 拦下 5 条列数变化（本次为**有意**变更）→ 人工确认后重做基线 |
| `TG-17` ①（D4） | 倒挂窗口判据：`verify/verify_close_readiness.py` 自检 **91→94**（`EVIDENCE:` 末行为准）；真数据 17 窗口 **0 例** ⇒ 判据是**前置**的；条目 2/4/5/6/8 的核验证据见 [`TG-17` 卡「G2 条目交付状态」表](../phases/testing-governance/cards/TG-17.md) | 反向对照 3 条：倒挂 ⇒ 具名 + `set-anchor` 指引／**不按 role 豁免**（作用域类与内容评审类都判）／同端点方向正确 ⇒ 零问题（防假红） |
| `A-M12` ①（D4） | `scripts/structure-guard.py verify --from-git HEAD` 真跑 ⇒ `STRUCTURE PASS（171 文件无丢失/变形）`，**不需要任何本地快照**；`--replay-git` 自检 **9 条**全绿（含真 git 仓库里的具名署名对照）；钩子实测**拦下**删除标题的提交（`docs/1-WORKFLOW.MD:299 标题被删：## 6. …`，提交未产生）；CI 接线判据 `verify_gate_integrity.py` 自检 **19→23**、真数据 rc=0（`ratchets=5 guard_required=9`） | 反向对照：`--replay-git` 五件事（干净放行／删标题必拦／**具名署名**降级为可见 `[署名删除]`／无署名仍拦／坏署名与 3 种坏修订 rc=2）；接线判据 4 条（原样零问题／拿掉 CI 调用 FAIL／钩子空转 FAIL／安装器缺失 FAIL） |
| `TG-17` ②（D5） | 条目 3：`verify/verify_md_tables.py` 三形态真跑（临时 worktree）——windows `MD-TABLE PASS（171 文件）`／删条件根 ⇒ `rc=1` 点名『在 `HEAD` 的提交树里存在、工作区却缺失 ⇒ 被删』／main 形态 ⇒ `MD-TABLE SKIP（8 个文件…**不是通过**）` rc=0；条目 7：`verify/verify_derived_numbers.py` 自检 **10→14**（J/K/L/M 四条），真数据 rc=0 | 条目 3 反向对照 = 上面三形态（含"修前在 main 形态打印 PASS"这一**旧行为对照**）；条目 7 反向对照 = J 被删 ⇒ FAIL／K 目录在而文件不在 ⇒ FAIL／L **新增**键而树里不存在 ⇒ FAIL（静默预留豁免）／M 旧键真分支差异 ⇒ 仅 NOTE 不误报 |
| `A-M12` ②（D5） | 判据 ④ 的**可复算读数**现跑 = **2**（命令见 [`A-M12` 卡 §证据 6](../phases/agents-infra/cards/A-M12.md)；数据源 = `sprint-18` §10 带 `R1` ＋ `编辑边界`/`同型` 标记的事故行；**D5 修正**：命令限定在 §10 之内——初版扫全文，把 §7 证据行里提到的 "R1 同型" 也算进分子，现跑 5 ≠ 真值 2） | 该命令**只依赖 §10 表格行与标记**（不依赖卡片措辞）⇒ 关闭时任何人可独立复跑；本批两次 R1 同型已按登记义务落行，**当前读数 2 > 目标 1 ⇒ 判据 ④ 不达标**，如实记账 |

## 8. Retro（Sprint Review 之后）

> 关闭期填写：M1~M3 读数、计划点/实际点、插入卡占用、关闭税点数、红线事件。

## 9. 关闭三查与抽查记录（关闭时写）

> 顺序纪律：`scope → doc-audit → code-review ×2 → lessons-learned → workspace-check`；**修复验证复核与关闭三查分节记录**（复核不计入三查覆盖）。

### 9.1 结构化 run 表

| run_id | role | target | scope_source | coverage | deviation |
|---|---|---|---|---|---|
| run-2026-09-25-impact-assessment-081 | impact-assessment | Sprint-18 G2 kickoff scope |  | (b6198f51, b6198f51] 空区间——作用域类 run 按 B3 语义不承担内容覆盖，空窗口不判问题 | 无需 deviation |
| run-2026-09-25-code-review-083 | code-review | 中途独立复核（用户 `D-250925-12`；scope = D1~D3 落地 diff） | self-chosen（范围与理由见报告 §scope 与账本 `scope_deviation`） | (b6198f51, a89b2821] = 报告自述的 `scope_lower_bound` → HEAD，20 提交 | 自选范围：报告逐条声明了**复核边界**（未读卡片/计划书/两本反例档/TEST-MATRIX、未跑套件与 lint） |

三查锚点: 待填（G2 关闭时回填；填真锚点前 CI 的关闭前置块对本文件不生效）

## 10. 红线事件与过程事故

| 时间 | 红线 | 事实（可核） | 告警→闭环 | 根因 | 是否 ≥2 次/Sprint |
|---|---|---|---|---|---|
| 2026-09-25（D4） | **R1 编辑边界（同型）** | `edit` 的 `old_string` **以行尾换行收边**、`new_string` 未补回 ⇒ `scripts/structure-guard.py` 的两条语句被并成一行（`return 2    baseline_doc = json.loads(…)`）。**发现者 = `python -m py_compile`（机器，非肉眼）**；修法 = 补回换行；提交前 `--replay` / `--replay-git` / 全闸门复跑均绿 | `py_compile` 报 `SyntaxError` → 立即修 → 复跑编译与全部闸门（rc=0） | 与 §7 记的第 6 次**完全同型**：把"我以为的边界"（行尾换行）当成内容边界。**这是 R1 家族第 7 次** | **是**（本 Sprint 已记 3 次：本条 + 下条 + 表格裸管道被 `md_tables` 拦下那次） |
| 2026-09-25（D4） | **R1 编辑边界（同型，工具放大）** | **自动折行脚本**用**过期行号表**重跑 ⇒ 行号已指向"折过之后"的短行，脚本在最后一个空格处继续切，把注释切成 `# 恒` / `# False，` 这类碎片（`verify_close_readiness.py` 的倒挂判据注释块）。**没有闸门抓到**（注释不进 AST，当时的等价性检查只有 AST）——由我自己重读发现 | 从备份还原 5 个文件（`%TEMP%\*_backup.py`）→ 脚本加**前置条件**（只处理当前确实超宽的行）+ **逐字拼回等价**断言（注释/文档字符串/字符串三种还原规则）→ 重做折行（三轮 37 行，E501 2537→2492，上限未动） → 全闸门复跑绿 | 两条：① 脚本对输入**没有前置条件**（过期行号表也能跑）；② **等价性判据选错了**——AST 看不见注释，而注释正是被切的对象。**留档：机制反例档案例 20** | 同上（**是**） |
| 2026-09-25（D4） | 非红线（恢复流程） | `git add` 之后用 `git checkout -- <文件>` 恢复被故意损坏的文档 ⇒ 从**索引**恢复，拿到的是**损坏版本**（差点把"未还原"当成"已还原"，靠再跑一次结构守卫发现） | 改 `git checkout HEAD -- <文件>` 恢复 → `structure-guard verify --from-git HEAD` rc=0 确认 | 恢复动作的**来源**没说清：`checkout -- <file>` 取索引、`checkout HEAD -- <file>` 取提交 | 否（新类型，首次） |
