# Sprint 13（2026-08-31）—— F2 配置面板深化 + SSOT 驱动前端（阶段 C：完全 SSOT + 收口）

> 归属：`docs/iteration/sprint/`。启动依据：用户指示"继续阶段 C，完成后全盘验收"（2026-08-31）；`1-WORKFLOW.MD` §4.4 判定：命中问题 1（前端默认值来源变更）+ 问题 3（跨层）+ 问题 5（roadmap 主题 F2 收口）→ 新 Sprint。
> 关联卡：F2（阶段 C，收口后 F2 整体转用户验收）。

## 0. F2 阶段总计划（回顾）

| 阶段 | Sprint | 内容 | 状态 |
|---|---|---|---|
| A SSOT 地基 | Sprint-11 | 端点 + M7 收敛锁 + 只读清单 | ✅ |
| B 全字段交互 | Sprint-12 | SchemaForm 全字段表单 + 字段级校验 + impacts + provider 联动 | ✅ |
| C 完全 SSOT + 收口 | **Sprint-13（本 Sprint）** | 节点默认值 schema 派生 + Settings 升级同步脚本/CI 检查 + 三查收口 + F2 用户验收 | 🟢 进行中 |

## 1. Sprint Planning

- **Goal**：① **手写字面量退役**——App.jsx Config 节点初始参数改为空对象，页面挂载时从 `/api/config_schema` 派生默认值应用（仅当用户未改动时）；M7 收敛锁改为"前端零硬编码断言"；② **Settings 升级护栏**——基线快照（Settings 全字段路径）+ verify 比对，升级后新字段触发 CI 告警 → 强制策展；③ 三查 + PR + **F2 全盘验收材料**（阶段 A/B/C 证据汇总 + 验收清单）。
- **团队与产能**：1 人（AI 开发者），预估 **5 点 / ~5h**。
- **周期（时间盒，必填）**：**2026-08-31（网络时间 UTC+8，1 个工作日）**；超时预案：与用户确认后把验收材料整理拆到下一轮（三选一）。
- **验收演示**：页面加载后 Config 节点 params 自动为 schema 默认值（无手写）；`verify_config_schema.py` 全 PASS（含"前端零硬编码"与"Settings 基线一致"新断言）；基线重生成流程演示；F2 验收清单随 §7。

## 2. Backlog（用户故事 + 验收标准 + 估算）

| 卡号 | ID | 用户故事 | 验收标准 | 点数 | 预估工时 |
|---|---|---|---|---|---|
| F2 | US-13.1 | 节点默认值 schema 派生：App.jsx n1 手写字面量清空 → 挂载时按 schema defaults 填充（仅当未改动）；M7 锁改为"前端零硬编码"断言 | ① n1 无任何配置键字面量；② 加载后 params 与 SSOT 默认一致；③ verify_config_schema 新断言 PASS；④ gui_check_s4 行为断言仍 PASS | 2 | 2h |
| F2 | US-13.2 | Settings 升级同步护栏：基线快照（全字段路径）+ verify 比对（mismatch → FAIL 并提示重新策展/重生成基线） | ① `verify/settings_baseline.json` 生成入库；② 人为改动基线或升级后 verify FAIL 且给出 diff；③ `--regen-baseline` 可重建 | 2 | 2h |
| F2 | US-13.3 | 三查 + PR 同步 + F2 全盘验收材料（阶段 A/B/C 证据汇总 + 验收清单） | ① 三查闭环；② PR 合并 main；③ §7 附"F2 用户验收清单" | 1 | 1h |

合计 **5 点 / ~5h**。

## 3. 任务看板（Kanban：开卡/推进/完成）

| 卡片 | 状态 | 点 | 开始 | 完成 |
|---|---|---|---|---|
| US-13.1 节点默认值 schema 派生 | ✅ DONE | 2 | 08-31 | 08-31 |
| US-13.2 Settings 升级护栏 | ✅ DONE | 2 | 08-31 | 08-31 |
| US-13.3 三查 + 验收材料 | ✅ DONE | 1 | 08-31 | 08-31 |

## 4. 燃尽图（Burn-down，随每张卡推进更新）

总点数 5。

| 里程碑 | 剩余点 | 说明 |
|---|---|---|
| Sprint 开始 | 5 | 2026-08-31（网络时间 UTC+8） |
| US-13.1 DONE | 3 | App.jsx n1 手写字面量清空 → 挂载时 schema 派生默认值（仅当未改动，readonly 不注入）；M7 锁改"前端零硬编码"（23 键断言，守卫集 schema 派生）；gui_check_config_schema 增 defaults-derived 断言 PASS |
| US-13.2 DONE | 1 | verify/settings_baseline.json（77 字段路径）入库；verify 比对 mismatch → FAIL + diff；--regen-baseline 重建；CI 离线套件天然覆盖 |
| US-13.3 DONE | 0 | 三查 fan-out 闭环 + PR 同步 + F2 全盘验收清单（§7） |

```text
剩余
 5 ┤●
 3 ┤ ●
 1 ┤  ●
 0 ┤   ●
 0 └─────────────────────────────── 时间
    08-31 开始 → US-13.1 → 13.2 → 13.3（阶段 C 完成）
```

## 5. 执行记录（Standup 日志）

- 2026-08-31 环境预检通过后完成 Sprint Planning；开卡 US-13.1。
- 2026-08-31 US-13.1/13.2/13.3 全部 DONE（详见 §7）：verify_config_schema 114 断言（零硬编码 + Settings 基线）、gui_check_config_schema defaults-derived PASS、gui_check_s4 全 PASS；进入三查 fan-out。

## 6. Definition of Done

- [ ] 每张卡有验收证据（截图 + 命令输出，§7）
- [ ] 结论来自可复现证据
- [ ] 记录关键权衡/理由/局限
- [ ] 未破坏既有功能（三查按 fanout 五步；gui_check_s4/verify 全绿）
- [ ] 三查完成且发现项闭环（§9）
- [ ] windows 已 PR 同步 main（不含 docs/iteration/）；main CI 绿
- [ ] F2 三阶段完成，验收清单交付用户

## 7. 项目管理与证据（Sprint Review 时填实）

| US | 证据 | 通过情况 | 理由/说明 |
|---|---|---|---|
| US-13.1 | App.jsx n1 块零配置键字面量（23 键断言，守卫集 schema 派生，114 断言集内）；gui_check_config_schema `defaults-derived-from-schema: PASS`（加载后 JSON 含 st- 模型与 chunk_chars 5000）；前端 build 通过（324KB）；gui_check_s4 全 PASS | ✅ | 默认值唯一真源 = schema；手写字面量退役；readonly 字段不注入 params（032 加固）；provider 联动契约不受影响 |
| US-13.2 | `verify/settings_baseline.json` 生成入库（77 字段路径）；verify 比对逻辑（added/removed → FAIL + diff + 重建指引）；`--regen-baseline` 实测重建成功 | ✅ | 升级 paperqa 后新增/删除字段 → CI FAIL → 强制重新策展 GROUPS；CI 离线套件已含本脚本 |
| US-13.3 | 三查 fan-out（§9）+ PR 同步 + 下方 F2 用户验收清单 | ✅ | 验收材料随本表交付用户 |

### F2 用户验收清单（阶段 A/B/C 汇总，供全盘验收）

**自动化证据（可直接复跑）**
- [ ] `python verify/verify_config_schema.py` → 114 断言全 PASS（schema 结构/默认值/pydantic 一致性/validate 行为/**M7 前端零硬编码（23 键，schema 派生守卫集）**/**Settings 升级基线**）
- [ ] `python verify/verify_smoke.py` → 8/8；spec 校验（PowerShell 逐个循环：`foreach ($f in Get-ChildItem agents\functions\*.md) { python scripts\agent-ops.py validate-spec $f.FullName }`）→ 7/7
- [ ] 前端 `npm run build` 通过
- [ ] `node verify/gui_check_s4.mjs` → 光标三断言 + provider 联动四断言全 PASS（后端 8787/前端 5173 启动后）
- [ ] `node verify/gui_check_config_schema.mjs` → defaults-derived-from-schema PASS + 截图 ×2

**交互验收（启动后端 8787 + 前端 5173，浏览器 http://127.0.0.1:5173）**
- [ ] Config 节点 = **全字段表单**（7 分组 23 字段：LLM/Embedding/Parsing/Agent/Answer/数据源/Index；readonly 字段带 🔒）
- [ ] 页面加载后 JSON 编辑区自动含 schema 默认值（无手写来源）
- [ ] 改 temperature=5 → 字段红框 + 红色错误文案（防抖 ~1s）；清空该字段 → 错误消失
- [ ] 切换 provider → api_base/model/vision/embedding 自动带出；deepseek（无 embedding API）→ embedding_model 被删除、顶部 hints 出现"多语言"自动选择说明
- [ ] impacts 影响提示（⚠ 重建索引/重下载模型/需联网）在未切换时即常驻可见

**阶段成果映射**
- 阶段 A（Sprint-11）：`/api/config_schema` + `/api/config/validate` 端点、M7 默认值收敛锁、只读清单
- 阶段 B（Sprint-12）：SchemaForm 全字段交互（类型控件/字段级校验/impacts/provider 联动），取代三个手写面板
- 阶段 C（Sprint-13）：手写字面量退役（schema 派生默认值）、Settings 升级同步护栏（基线快照 + CI 检查）

### 权衡与理由 / 边界与回归 / 关闭 Self-check

- <填实>

- [ ] 每个 US 有对应证据
- [ ] 结论来自可复现证据
- [ ] 记录关键权衡/理由/局限
- [ ] 未破坏既有功能
- [ ] 三查完成且发现项闭环
- [ ] PR 同步 main

## 8. 回顾 Retro（Sprint Review 后填）

**做得好（Keep）** / **可改进（Improve）** / **行动项（Action）**

## 9. 评审/同步记录（三查结论 + PR，Sprint 关闭必填）

- 〇查范围（impact-assessment）：<填实>
- 一查（文档审计）结论与修复映射；
- 二查（双分支 code review）合并完整性；
- 三查（工作区/功能完整性）输出摘要；
- PR 同步 main 结果（PR 号/合并 sha，不含 `docs/iteration/`）。
