# 阶段 backlog：testing-governance（测试治理）

> 归属：`docs/iteration/phases/testing-governance/`。卡状态流转：`候选 → 计划中 → 完成 / 取消`；
> 进入 Sprint 后由 Sprint 文档引用卡号，本表仅维护卡级信息。
> 预研与决策基线：`docs/iteration/pre-research/tech/2026-08-31-testing-governance.MD`（规划时强制注入，见 `1-WORKFLOW.MD` §4.4）。
> 启动判定引用 `1-WORKFLOW.MD` §4.4。
> **卡片来源与时间口径（2026-09-20 用户要求）**：来源带类型前缀 `[规划]`/`[用户插入]`/`[Bug]`/`[走查]`/`[三查]`/`[评审]`；时间字段一律用**网络时间 UTC+8**（`YYYY-MM-DD（UTC+8）`），不得用本机时区（本机 UTC+9）。细则见 `1-WORKFLOW.MD` §3。

## 1. 功能卡

| 卡号 | 主题 | 规模(点) | 状态 | 所属 Sprint | 来源/备注 | 证据/卡内待产出 |
|---|---|---|---|---|---|---|
| TG-1 | P1 成文规则：DoD+三查增补"用户可见功能必须同 PR 交付 verify case + 矩阵登记"；反例演练留证据 | 1 | ✅ 完成 | Sprint-14（2026-08-31，PR #48） | 用户 2026-08-31（D1=A + 底座批次） |  |
| TG-2 | P2 覆盖矩阵 SSOT：脚本头部元数据 + `verify_matrix.py` 生成/校验 + `verify/TEST-MATRIX.MD` + CI 离线断言 + 全量脚本登记 | 4 | ✅ 完成 | Sprint-14（2026-08-31，PR #48） | 用户 2026-08-31（D1=A + 底座批次） |  |
| TG-3 | P4 ATDD case-first：功能卡模板增"验收 case"节（实现前先写步骤+断言），下一张新功能卡实测；**走查补充（2026-09-10）**：验收 case 须覆盖**完整测试链路 + 边界情况**（≥1 异常/边界输入；不适用须注明理由） | 1 | ✅ 完成（Sprint-15：§2.1 九卡 case 先行实测 + 边界覆盖规则落盘） | Sprint-15 | 用户 2026-08-31（D1=A）+ 走查 2026-09-10 |  |
| TG-4 | provider e2e 三脚本去漂移：参数化共享基座，修复 verify_e2e_openai 潜在 1.46 bug（D2：本次不做，单独成卡） | 2 | ✅ 完成 | Sprint-14（2026-08-31，PR #48） | 用户 2026-08-31（D2 + 底座批次） |  |
| TG-5 | P3 分层 runner + cron 夜间全量联网套件（fail-closed）。**参数已预决（D3）**：每周一次、单轮预算上限 ¥10、**上限必须可配置（config/env 驱动，不得写死文件）** | 3 | ✅ **完成（Sprint-16）** | Sprint-16 | 用户 2026-08-31（D3 预决，设计约束入卡）；调研结论 run-047（三态预算闸门）｜**终态证据**：`verify/verify_runner.py` **42 断言**、`run_suite.py` 三档（offline **14** / gui 21 / network 10）、离线套件 **SUITE PASSED（14 脚本）**、`scheduled-tasks.py` 三态预算闸门（超限拒跑 3 / 成本 unknown 拒放行 4 / 自动回填解除）+ `register-scheduled-tasks.ps1` |  |
| TG-6 | `[用户插入]` **验收方式改进（DoD 补"人机交互验收面" + 过程可见证据 + 规范执行可核查）**：① DoD 现偏代码执行层，需增补"用户可感行为"验收面（交互/呈现/感知）；② 验收指南每条须给**过程可见**的操作与留证（可回放命令 + 原始输出/截图/日志路径，不接受只给结论或"读文档确认"）；③ 机器回归项与人工可感项**分栏标注**（避免把纯脚本断言当人工用例）；④ 建立"Sprint 内规范条目 ↔ 执行记录"抽查机制，治理"规范写了但未执行"的先例；⑤ **反向对照规则（用户 2026-09-21 拍板，称"倒过来试试"）**：新增的交互/GUI 检查**必须先在未修复/旧实现上跑出 FAIL**（证明该检查确实能触发目标失效模式），再在修复后转 PASS——反例：`gui_check_s5/s7` 用程序化 `.click()` 结构上无法触发 F-AC13 | 3 | 候选 | Sprint-17（治理批 G1 = 规则落地） | **用户走查 2026-09-20（UTC+8）**：用户明确要求"做个 backlog 卡记录先"；**2026-09-21** 追加反向对照规则；与 `phases/mental-models/` 的 MM-6 联动 |  |
| TG-7 | `[Bug]` **预算闸门 fail-open**：`scripts/scheduled-tasks.py` 的 `load_state()` 吞掉所有异常并返回默认空状态（line 76–82）→ 状态文件损坏/带 UTF-8 BOM/手工编辑出错时，`last_run` 与成本三态被**静默重置**，预算闸门不再拦截，带预算的任务被放行（与 3-LEARNED 1.54 的 fail-closed 口径直接冲突）。修法：解析失败即 **fail-closed**（非零退出 + 明确文案 + 保留损坏文件副本），并给 `verify_runner.py` 增"损坏 state → 拒绝"断言；顺带标注"本机 PowerShell 5.1 的 `Set-Content -Encoding utf8` 会写 BOM"这一操作陷阱 | 2 | ✅ 完成（2026-09-20 当日闭环） | Sprint-16（走查修复） | **走查演示发现 2026-09-20（UTC+8）**（事故实证：BOM 状态文件使 `nightly-suite` 被放行并真实启动，估算损失约 ¥1.0） | **证据**：`load_state` fail-closed（`utf-8-sig` 容忍 BOM + `.corrupt` 副本 + 退出 2）；`verify_runner.py` **24 断言全 PASS**（⑩a 截断→拒 2 + 留副本 / ⑩b 带 BOM 合法状态照常读到并 REFUSED / ⑩c 顶层非对象→拒 2）；BOM 反证由 RUN 转 REFUSED(4)；教训入 `3-LEARNED` 1.58 |
| TG-8 | `[用户插入]` **自举后端前端口自检 + 离线开关统一注入**（Retro 行动项 ② 升级为必修）：`verify/e2e_common.py` 的 `wait_healthy` **只探测端口、不校验"服务是不是自己启的"** → dev 后端在跑时脚本会**静默复用**它并施加 parse/embed 负载；反之套件也会抢占/顶掉 dev 后端（2026-09-20 走查实证：后端两次消失）。修法：① 自举前探测端口占用 → 占用即 **fail-fast** 并提示"请先停 dev 服务或改用其他端口"（**不复用**）；② 退出时只回收自己拉起的进程；③ 统一注入 `HF_HUB_OFFLINE=1`/`TRANSFORMERS_OFFLINE=1`（避免 HEAD 重试阻塞）；④ 回归：占用端口下脚本必须明确失败而非静默复用 | 2 | **计划中（Sprint-17，用户 2026-09-21 确认）** | Sprint-17（治理批 G1） | **Retro 行动项 ②（Sprint-16）+ 用户 2026-09-21 确认进 S17** | 事故记录见 Sprint-16 §10 与验收指南 §13.5；教训 `3-LEARNED` 1.55 / 1.60 |
| TG-9 | `[用户插入]` **新脚本产物目录约定**（Retro 行动项 ④）：新脚本产物默认写**已忽略目录**（运行态 JSON → `agents/runtime/`、日志/截图 → 已 gitignore 的 `verify/*.log\|png`、临时结果 → `%TEMP%`），并在 `verify/README.md` 与 `1-WORKFLOW.MD` 写明"新增脚本前先确认产物路径已忽略"；配套：`.gitignore` 增补运行态文件清单 | 1 | **计划中（Sprint-17，用户 2026-09-21 确认）** | Sprint-17（治理批 G1） | **Retro 行动项 ④（Sprint-16）+ 用户 2026-09-21 确认进 S17** | 本轮两次误入库（`verify_checkpoint_run.log`、`ci_fail_log.txt`）已被清理并 gitignore |
| TG-10 | `[用户插入]` **多轮次 agent 的账本记录失真（可观测性缺口）**：同一 run 被追加多轮复核（run-053 共 5 轮）时账本只留**首轮**（53 秒 / 12800 字符，实际跨约 4h53m），因 `finish` 状态机拒绝 `succeeded→succeeded` → 看板严重低估时长与产出、看不出被中断。 | 2 | **v1 ✅ 完成（2026-09-21）**；③ 看板 UI 展示待排期 | Sprint-16（v1 已交付）／③ 看板 UI → Sprint-18（G2） | **用户 2026-09-21（UTC+8）**："每个 agent 运行时间相比之前怎么短了很多？而且有的是直接中途被停掉了，这些信息让我不太安心"；同日批准"做掉" | **v1 证据**：`agent-ops.py` 新增 `round`（终态 run 可追加：首轮快照 `rounds[0]` + 产出累加 + `ended_at` 前移 → 累计时长）与 `interrupt`（何时/谁/原因/影响）；`list` 增 `rounds=/dur=/int=`；回归 `verify_ledger_rounds.py` **13 断言**；**已回填 run-053** → `rounds=5 dur=241.3m int=2`（历史轮次字符数未采集，note 中如实标注）。**剩余 ③**：看板 UI 展示 rounds/dur/int。 |
| TG-11 | `[用户插入]` **规则绕过复盘 + 可执行闸门（用户 2026-09-21 定性为"很严重"）**：2026-09-20/21 我以"修复验证复核"名义**自选窄范围**跑了 5 轮子代理复核（run-053 R1~R5），而 `agents/functions/code-review.md:26/:34` 明文 `scope` **必须来自〇查 impact-assessment**（`never a self-chosen narrow scope`）、`1-WORKFLOW.MD` §4.2 要求**先跑〇查**且"偏离须在 §9 记理由"——**两处都被绕过，且我是在用户追问后才发现**。后果：① 二查覆盖缺口（`2761eb2`/`cce060a`/`2b4f354`/`502065e`/`bb80ea3`/`bdc669b` 六个提交无按范围评审）；② 用户需靠追问才识破；③ Sprint-16 HOLD。**用户的关键判断：再往 workflow 写规则没用——规则被绕过时没有任何东西会拦下来 → 本卡交付必须含"可执行闸门"，不是文档。** | 3 | **进行中（Sprint-17）**：③(a) **CLI 闸门已落地**（`register --scope-source impact-assessment:<run_id>` / 自选须 `--deviation "<≥10 字符>"`，账本落库 `scope_source`/`scope_deviation`；`verify_agentops.py` **UC-14 八条断言** + **反向对照实测**：旧实现放行无 scope 的评审 run（rc=0）且不认 `--scope-source`）；复盘与 ④ 关闭闸门待做 | Sprint-17（治理批 G1） | **用户 2026-09-21（UTC+8）**："这是一个很严重的问题，规则直接被绕过，在 workflow 记再多也没用，需要单独开卡复盘下" | **卡内待产出（复盘，不是写规则）**：<br>① **事实与根因**：区分"**未读到规则**"vs"**判断越界**"——可核事实：本轮我引用过 §4.2 的 Timebox 纪律（说明读过该节片段），但**三查表的 scope/〇查前置条款直到用户追问才第一次读**；两种根因对应不同对策，须在复盘里定论。<br>② **可检测性分析**：当时是否存在任何检查能发现？现状=**没有任何执行点**（三查是否跑过只在 §9 被人工阅读时暴露；CI 不查；账本不记 scope 来源；PR 前置也不查）→ 这是"绕过"的结构性原因。<br>③ **机制候选（必须可执行）**：(a) 评审 run 的 scope 来源机器可验——`agent-ops register --scope-source impact-assessment:<run_id>`；自选 scope 必须显式 `--deviation "<理由>"`，否则 **CLI 拒绝**或标记 `scope_source=self-chosen` 并在 `list` 高亮；(b) **关闭前置闸门脚本**（offline `verify/verify_close_readiness.py`）：校验本 Sprint ① 存在〇查 run 且**早于**二查 ② 二查含 **windows+main 两个 run** ③ 一查 doc-audit 与 lessons 各一 run ④ §9 的 run 表与账本**交叉一致**（防"写了没跑"）⑤ 每个自选 scope 的 run 都有 deviation 声明 → 任一缺失**非零退出**，并接入 PR 前置/CI；(c) **§9 结构化 run 表**（run_id/role/scope_source/覆盖范围/偏离声明）供 (b) 解析。<br>④ **闸门自身的验收判据**：必须有**反向对照**——故意省掉〇查、或只跑 windows 单分支、或 §9 写了但账本无 run 时，闸门必须 FAIL（沿用 `TG-6` ⑤"倒过来试"规则）。<br>⑤ 复盘结论**可否决或修改**我在 `1-WORKFLOW.MD` §4.2 新加的"修复验证复核"定义（文档不是对策，但它是否该存在/如何写由复盘定）。<br>📄 **证据台账（2026-09-21；⚠️ 这不是复盘结论——用户明确"开卡以后复盘，不是现在就做"，正式复盘在卡执行时进行）**：`docs/iteration/phases/testing-governance/2026-09-21-review-scope-incident-evidence.MD` —— 事实时间线（证据：registry `run-048~052` 五步 task_id 均为 `Sprint-16 close`；`run-053` 自述 `user walkthrough pre-check`；`origin/windows..HEAD` 22 提交中 **20 条 committer 时间晚于三查结束 2026-09-12T23:57:06+08**）；**根因五条**：**R1 规范无"三查失效/锚点"概念**（全文 grep 无命中，主因）、**R2 无任何执行点**（CI/套件/矩阵/账本/PR 前置全不校验覆盖关系，唯一途径是人读 §9）、**R3 scope 规则不可执行**（账本无 scope 字段、无"无〇查则拒派"装置）、**R4 §9 把关闭三查与走查期复核写在同一节 → 结构在替违规背书**、**R5 规则检索未发生**（"任务分类先于规则检索"：规则挂在「关闭」语境，违规发生在「修复后复核」动作语境 → 不命中）；**对策**：M1 scope 来源入账本（`--scope-source`，`self-chosen` 必须 `--deviation`）、M2 `verify/verify_close_readiness.py` 关闭前置闸门（含**三查锚点**校验：锚点之后不得有未评审提交）、M3 §9 结构分区 + 结构化 run 表、M4 接入 CI offline / PR 前置；§4 给 6 个**反向对照**场景。**待拍板/修订**：**D1 用户 2026-09-21 修订 = 方案保留、S16 期间不做任何代码改动**（"刚刚你建议的方案可以保留，但是不要做任何代码改动"→ 实现待正式复盘后另开）、D2 = 两者结合（默认拒绝 + `--force` 强制 `--deviation`，作为方案参数保留）、D3 锚点是否入 §6 DoD 必填、D4 §9 分区是否只对新文档生效。**本轮代码零改动**（曾起草 `scripts/agent-ops.py` 的 `--scope-source` 闸门与 `verify_agentops.py` 断言，**已全部回退**，仅保留本卡与证据台账）。 |
| TG-12 | `[用户插入]` **文档内容漂移复盘 + 派生事实机制（用户 2026-09-21："sprint17 和以前的开发过程发现的文档内容漂移问题也足够开个卡做复盘了"）**：**同一类"数字/状态/结论副本过期"问题已反复发生 ≥4 波，每波都靠人工发现+手工改数字**——Sprint-7 起就有 `M6`（文档例行勘误：`verify/README` 表格列数错位、清单漏项、`4-ALGORITHM` 行号锚点漂移）；此后 doc-audit 026 抓出 `4-ALGORITHM` §9.2 DSL 描述旧控件；Sprint-15 记录**日期整体漂移**（全标 09-07，实际 09-10）；Sprint-16 内**修了三轮**：`6ecee2a`（指南矩阵 40→41 / offline 10→11 / runner 20→26）→ Round-5 `bdc669b`（指南 41→44 / offline 11→13 / `4-ALGORITHM` 19→29 / `CK-1` 答复"前端零展示"过期）→ 本次 `2e1d20f`+`4e17ddd`（**11 处**：44→**45** 脚本、offline 11→**14**、runner 26→**42**、usage 21→**27**、`N-1`/`N-2` 估算 0.05/0.5→**0.02/1.1**、已知局限第 1/3 条已闭环仍写未解决、**TG-5 卡状态"计划中"实为 DONE**、F-AC8 备注"待用户确认"实已决策、Sprint §3/§7 与 **PR 正文要点**旧数字）。**共性**：同一事实被**抄进多处**（指南/§3/§7/PR 文案/卡状态/算法文档），源头变更后**无重生成机制、无检测点**，且修一次只修"当时看见的那几处" → 下一次必然再漂移。 | 3 | **候选（用户 2026-09-21 要求开卡复盘；同类问题已足够成卡）** | Sprint-18（治理批 G2） | **用户 2026-09-21（UTC+8）**："我想从 sprint17 和以前的开发过程发现的文档内容漂移问题也足够开个卡做复盘了，把类似问题一并加到 backlog 中吧" | **卡内待产出（复盘 + 机制，不新增条文）**：<br>① **卡内事实清单**（术语见 `1-WORKFLOW.MD` §3：`台账` 仅指独立文件；**本卡无独立台账文件**）：汇总历次漂移波次（**第 5 波 = 2026-09-21 关闭三查修复批**：㈠ 我修漂移时把 `est_cost_cny` 的**加系数前旧值 0.02/1.1** 从 Sprint §7 转抄进 3 处文档（SSOT 实为 **0.03/1.4**，一查独立复现抓出 → 教训 1.63）；㈡ "三查后提交数"两种口径并存（20 vs **26**＝`1389738..HEAD`），被一查/二查各自纠正；㈢ **修复批本身改动了断言数**（runner 42→**46**、usage 27→**29**、checkpoint_index 29→**31**、freshness 22→**25**、ledger_rounds 13→**15**、agentops 42→**43**）→ 所有引用断言数的文档**当场过期**、只能同批重写；㈣ `verify_smoke` 的"12 路由"此前**只是元数据声明、无任何断言在守**（本轮补钉桩）；㈤ `verify/README.md` 表格漏登 8 脚本，违反该文件自身第 10 行规则。㈥ **断言的环境鲁棒性也是"未验证面"**：`verify_runner` ⑩a 用裸子串 `RUN` 判定"未放行"，而 GitHub runner 的临时路径含 `RUNNER~1` → **断言在 CI 上恒假**（PR #51 首轮 CI 失败）；本地路径不含该子串、且该断言自 TG-7 引入后**从未在 CI 跑过** → 环境依赖长期不可见（已改 `_has_marker()` 行首标记判定，10 处统一）。**这六条共同证明：手抄型"事实"与"只在本地验证过的判定"都会随环境/改动过期，SSOT + 环境无关的断言才是解**。上表另列 4 波；建议把每波的"发现者/发现方式/修复提交/漏改处"补齐，作为复盘输入）。<br>② **根因假设（待复盘定论）**：**D1 派生事实无 SSOT**——脚本数有矩阵作 SSOT，但**文档里的是副本**；断言数**根本没有 SSOT**（只在脚本运行时输出）；卡状态纯人工维护。**D2 无"变更→重生成"触发**——加脚本/改断言/改决策后没有任何装置去刷新引用处（`verify_matrix.py check` 只管矩阵自身）。**D3 无检测点**——一查 doc-audit 有"陈旧事实"项，但**无"计数与状态一致性"的可操作判据**，关闭闸门也不查文档数字。**D4 修正方式加剧问题**——每次手工改数字而非消除副本，必然复发。**D5 历史与现状混排**——Sprint §5 日志是"当时读数"、§3/§7/指南是"当前状态"，同文档无标注规范，读者与 doc-audit 都分不清哪处该最新。<br>③ **机制候选（可执行）**：**N1 派生事实登记**（每条 = 事实名/生成命令/当前值/生成时间；文档引用占位符或"见命令输出"）；**N2 漂移闸门** `verify/verify_doc_drift.py`（offline，零成本）：扫文档中的可生成数字（`N 脚本`/`N 断言`/`CHECK OK: N`/`SUITE PASSED (N`/`est ¥X`）与实测比对 + 校验 backlog 卡状态 vs Sprint 文档 ✅/DONE；**N3 生成式状态报告**（本次手写的汇总报告改为脚本生成，避免报告本身成为新漂移源）；**N4 doc-audit spec 增补固定检查项**（"计数与状态一致性 + 历史/现状标注规范"，spec 正文改动按 §4.2 流程）；**N5 写作规范**（凡引可生成事实必须附"生成命令 + 生成时间"，历史读数必须显式标"（当时读数）"，否则一查判 FAIL）；**N6 与 TG-11 合流**（scope/漂移/关闭就绪三类闸门统一接入 CI offline 与 PR 前置）。<br>**N7【用户 2026-09-21 提出的结构性问题·优先候选】一卡一文档 + backlog 瘦索引 + 卡片索引 lint**：用户原话——"`phases` 下的所有文件夹，单个卡片应该对应且唯一对应一个独立文档。现在卡片内容全都记录在 backlog 中，有的卡片内容散落在 sprint 文档中到处都是。一旦卡片内容更新了其他几处万一有的地方漏了就直接造成文档内容漂移"（另追问"你经常说实例台账我咋没找到是啥文档"）。**现状（如实）**：卡片定义/根因/机制/实例**全部写在 `backlog.MD` 的表格单元格里**；Sprint 文档另存过程记录；本卡所谓"实例台账"实为**单元格内的一段文字**——全仓**仅 `TG-11` 有真正的独立文件**（`2026-09-21-review-scope-incident-evidence.MD`），**术语含混须统一**。**建议结构**：`phases/<phase>/cards/<卡号>.md` **一卡一文件**（卡内 = 状态/规模/来源/Sprint/正文/证据/台账）；`backlog.MD` 退化为**瘦索引**（卡号｜一句话｜状态｜Sprint｜文件链接）；Sprint 文档**只按卡号引用**、不复制卡正文。**配套 lint**：`verify/verify_card_index.py`（offline，纳入套件）校验 ① 索引行 ↔ 卡片文件一一对应（无孤儿/无重复）② 文件名与卡号一致 ③ 状态字段与 Sprint 文档引用一致 ④ Sprint 文档不得复制卡正文（按小标题特征扫描）⑤ **反向对照**：删一份卡文件 / 改一处状态 / 在 Sprint 文档塞入卡正文 → 必须 FAIL。**术语约定（须写入 `1-WORKFLOW.MD` §3）**：`台账` **仅指独立文件**（如 TG-11 证据台账）；卡片单元格内的清单一律称"**卡内实例清单**"，不得混称"台账"。**迁移成本提示**：全仓现有卡约 60+（refactor-analysis ~30 / agents-infra ~25 / testing-governance 12 / mental-models 6），建议**先在一个阶段试点**（建议 `testing-governance`，卡数适中且本卡就在其中），验证形态后再批量迁移。**✅ 用户 2026-09-21 决定：N7 归入本卡（TG-12）正式执行时一并做**（不在 Sprint-17 之前单独立项）；**术语约定已即时生效**——`台账` 仅指独立文件、单元格内清单一律称"卡内实例清单"，并连同引用纪律写入 `1-WORKFLOW.MD` §3（2026-09-21）。<br>④ **反向对照判据（"倒过来试"）**：把 `TEST-MATRIX` 脚本数改一位 / 把指南"14 脚本"改回"11" / 把某卡改成"计划中"而 Sprint 标 DONE / 把 est 数字改一位 → **闸门必须 FAIL**；全绿状态 → PASS。<br>⑤ **与既有卡的关系**：**建议复盘时决定 `M6`（refactor-analysis，文档例行勘误，持续）是并入本卡还是保留作执行项**；与 `TG-2`（矩阵 SSOT）、`TG-6`④（规范条目↔执行记录抽查）、`M18`（报告时效）联动。<br>**⤴ 2026-09-21 用户决定：机制候选 `N7`（一卡一文档 + backlog 瘦索引 + 索引 lint）已提升为独立卡 `TG-14`**——本卡机制清单收窄为 **N1~N6**（派生事实/漂移闸门/生成式状态报告/doc-audit 增补/写作规范/TG-11 合流），N7 的 4 类证据与迁移方案随卡移交 `TG-14`，避免两卡重复拥有同一机制。 |
| TG-13 | `[用户插入]` **账本"测量化"：时长/轮次/账本↔产物一致性**（用户 2026-09-21 拍板"开新卡"，而非并入 TG-10）：**TG-10 解决的是"多轮次没被记录"，本卡解决"记录了但不是测量值"**。实测证据（2026-09-21，来源 run 报告 + 主代理实测）：① **6 组 run 的 `started_at`/`ended_at` 逐字节相同**（含 3 条 0.00 min、3 条恰好 10.00 min）→ 时长是**写入时刻的巧合**而非墙钟测量；② **`rounds` 与实际轮次不符**——run-060 台账记 `rounds=5` 而报告只有 **3 轮**（高估）、run-053 曾少记 **4 轮**（低估，已由 TG-10 回填）；③ **账本 ↔ 产物目录双向漂移**：**主代理实测 60 个 `run-*` 目录 vs 账本 59 条记录**（B 路报"63 vs 59"系口径差异，引用时须注明量法）；④ **部分 run 类型从未入账本**（`provider-refresh` 两类 run 有目录无记录）、**账本里唯一的 `failed` run 恰恰没有产物目录**；⑤ `doc-audit-009` 终态未写回（账本滞留 `status=running`）；⑥ **中文 `task_id` 落库即乱码**（2026-09-21 〇查 `run-2026-09-21-impact-assessment-061` 实测：`pwsh` → `python` 传参把中文转成 mojibake，`python -c "print('中文测试')"` 同样乱码；ASCII 描述的历史 run 正常）→ 账本写入路径须统一编码（或改走文件/JSON 输入），并加"字段可读性"断言。 | 2 | **候选（用户 2026-09-21 拍板开卡）** | Sprint-17（治理批 G1） | **用户 2026-09-21（UTC+8）**："账本测量化……做成新卡 TG-13"（原提议：并入 TG-10 作补充 → 用户选择新卡） | **卡内待产出**：① **测量口径定义**——`dur` 是"墙钟实测/首末轮时间"哪一种？`rounds` 的真源是"报告轮次"还是"CLI 声明"？**缺值必须显式 `unknown`，禁止以 0.00 / 整十分钟等退化值冒充**；② **`verify/verify_ledger_measurement.py`（offline，纳入套件）**：账本↔`agents/runs/**` 目录**双向**一致（含"有目录无记录"与"有记录无目录"）、时间戳非退化（相同/0.00/整值告警）、`rounds` 与报告轮次一致（可核对者）、终态已写回；③ `agent-ops.py` 侧：`finish`/`round` 写入时检测时间戳退化并**标注或拒绝**，新增"测量来源"字段（wall-clock / declared / unknown）；④ **反向对照**：注入"相同时间戳 / 缺目录 / rounds 不符 / 终态未写回"四类样本 → 断言必须 FAIL；⑤ 与 `TG-10` ③（看板 UI 展示 `rounds=/dur=/int=`）合流：**看板展示前必须先保证数字可信**，否则是把失真数据可视化。关联教训 `3-LEARNED` 1.59（可观测性必须自证）。 |
| TG-14 | `[用户插入]` **一卡一文档：卡片正文独立成文 + backlog 瘦索引 + 索引 lint**（**由 `TG-12` 机制候选 N7 提升为独立卡**，用户 2026-09-21 拍板）：用户原话——"`phases` 下的所有文件夹，**单个卡片应该对应且唯一对应一个独立文档**。现在卡片内容全都记录在 backlog 中，有的卡片内容散落在 sprint 文档中到处都是。一旦卡片内容更新了其他几处万一有的地方漏了就直接造成文档内容漂移"（另追问"你经常说实例台账我咋没找到是啥文档"）。**现状证据**：卡片正文全在 backlog 单元格内，体积已失控——**`A-M11` 单格 7.9 KB、`TG-12` 4.1 KB、`TG-11` 2.7 KB**；本轮穷举不得不把明细外置成两份分析文档才没让它继续膨胀；同一事实散在"backlog 单元格 / Sprint §3/§7/§9 / 验收指南 / 状态报告 / PR 文案 / 卡状态"多处 → Sprint-16 内实测 **5 波、11+ 处漂移**。**与 TG-12 不同族**：TG-12 的 N1/N2 解决"**事实从哪来**"（派生事实 SSOT），本卡解决"**卡与文档的拓扑**"。 | 3 | **候选（用户 2026-09-21 拍板"提升为独立卡"）** | Sprint-17（治理批 G1） | **用户 2026-09-21（UTC+8）**："N7 是否提升为独立卡：可以" | **卡内待产出**：① `phases/<阶段>/cards/<卡号>.md` **一卡一文件**（状态/规模/来源/Sprint/正文/证据/台账各自成节）；② `backlog.MD` 退化为**瘦索引**（卡号｜一句话｜状态｜Sprint｜文件链接）；③ Sprint 文档/roadmap **只按卡号引用**，不得复制卡正文；④ **`verify/verify_card_index.py`（offline，纳入套件）**：索引行 ↔ 卡文件**一一对应**（无孤儿/无重复）、卡号 ↔ 文件名一致、状态与 Sprint 引用一致、**Sprint 文档出现卡正文特征即 FAIL**、**表格表头↔各行单元格数一致（`markdownlint` MD056 形态；2026-09-21 实测 3 处已修，见计划 §2.3 实测实例）**；⑤ **反向对照**：删一份卡文件 / 改一处状态 / 在 Sprint 文档塞入卡正文 → 必须 FAIL；⑥ **迁移**：先试点 `testing-governance`（**16 卡**，卡数适中）→ 验证形态后批量（**全仓 94 张卡**（2026-09-23 实测：按各 `phases/*/backlog.MD` 表的**有效卡行**计数；agents-infra 另有 1 行 `—` 占位不计。**本行是手抄事实，开/关卡后必须同步**——正是 TG-12 要治的形态）：refactor-analysis 47 / agents-infra 25 / **testing-governance 16**（TG-15 / TG-16 于 2026-09-23 新开）/ mental-models 6）；⑦ 术语已即时生效（`台账` 仅指独立文件，见 `1-WORKFLOW.MD` §3）。 |
| TG-15 | `[用户插入]` **闸门泛化：三不变式 + 数据驱动（替换"逐场景加条件"式硬编码）**（用户 2026-09-23 批准 P1="可以"，并追加要求"复查下类似场景的硬编码问题"）：**问题**——闸门现在是"遇到一个场景加一条判据"：`scripts/agent-ops.py:67` 硬编码 `_REVIEW_ROLES={"code-review","doc-audit"}`、`:258` 认死 `role=="impact-assessment"`、`--deviation ≥10 字符`、状态白名单；`verify/verify_close_readiness.py:37,87-119` 硬编码 `CLOSE_ROLES` 与 6 条按角色的判据，且要求人**手抄** `三查锚点` 进 Sprint 文档；`verify/verify_lint.py:35` 硬编码覆盖路径清单 `DEFAULT_PATHS`；`scripts/report-freshness.py:197` 认死 `startswith("tech-research")`；`verify/verify_agentops.py:40` 又存一份角色集合。→ 新角色/新分支/新步骤/新失效形态**都要改代码或手改文档**。 | 2.5 | **候选（用户 2026-09-23 批准开工；Sprint-17 内实现）** | Sprint-17（治理批 G1） | **用户 2026-09-23（UTC+8）**："P1：可以，建议复查下类似场景的硬编码问题"；"P2：C3-T，但是有个担心，**是否归属表未及时更新同样导致 C3 闸门失效**" | **卡内待产出（①~⑧）**：① **三条不变式替换现有判据**——**C1 声明完备**（凡产出评审结论的 run 必须有 scope 声明：外部引用或自选理由）、**C2 指涉可核**（任何外部引用必须解析到"存在且处于可用终态"的对象，不认角色名）、**C3 覆盖闭环**（`git rev-list <锚点>..HEAD` 的**每个提交**必须有归属）。② **需求只来自两个数据源**：`agents/fanout.json` 的 `sprint_close_pipeline`（哪些步骤/角色/target 必填）＋ 各角色 spec frontmatter 的 `scope_required: true`（复用 `validate-spec` 既有 frontmatter 解析）→ 加步骤/加角色**只改数据**。③ **锚点自动化**：run 在 register/finish 自动记 `coverage_anchor` / `covers_through`（`--coverage-anchor <sha>` 可显式覆盖），Sprint 文档**不再手抄锚点**；`covered(run) = (anchor, covers_through]`。④ **C3-T 例外表（用户选型）＋防"表过期"三件套**：(a) 覆盖**默认由 run 数据计算**，表**只登记例外**；(b) 例外**sha 钉死**、禁止模式/路径通配 → 不可能覆盖未来提交；(c) **表落后 = 默认 FAIL 并逐条点名未归属 sha**（失效方向反转：不是静默变绿）。⑤ `agent-ops close-sync` 自动生成候选行（未归属提交 + 改动文件 + `doc-only` 自动归类；`doc_only_globs` 规则放**数据文件**），人只做"挑类别/写理由"。⑥ **push 时即检**：覆盖检查进 CI（条件由数据推导），漂移在**一次 push 内**暴露，而非关闭时才发现。⑦ **硬编码政策闸门** `verify/verify_no_policy_hardcode.py`（offline）：扫 `scripts/**`+`verify/**`（豁免 fixture/选择器白名单，白名单为数据文件且每条须写理由），发现"角色名/覆盖路径清单/计数常量/阈值"类政策硬编码即 FAIL；**反向对照**＝注入 `_ROLES={"code-review"}` 必须 FAIL。⑧ **规则入 `1-WORKFLOW.MD` §3**：脚本内不得把政策写成常量，政策必须在数据文件；并改造上列 6 处存量。**反向对照要求**：变异用例**从 `fanout.json` 自动生成**（每个必填步骤/字段各抽掉一次 → 必须 FAIL），不逐场景手写。 |
| TG-16 | `[用户插入]` **审查流程与策略内容"整体过一遍" → 图文并茂 HTML 汇报**（用户 2026-09-23："我想需要对现有审查流程和具体策略内容找时间整体过一遍，记张卡把这些内容规则流程梳理作为一个 backlog，**输出图文并茂容易理解的 html 汇报文件**"）：**用途**＝让用户**一次看懂**"我们到底有哪些审查动作、各挂在什么触发点、哪些是真闸门、哪些只是文字、策略参数放在哪、红线怎么生效"，作为后续治理决策的输入。**与其他卡不同族**：TG-11/TG-12/TG-14/TG-15 治"闸门/漂移/拓扑/泛化"，**本卡只治"全景可读性"，不改任何规则**（改规则一律另开卡）。 | 3 | **候选（用户 2026-09-23 要求开卡）** | 待排期（建议：素材可在 G1 期间并行收集，**HTML 定稿放 G1 关闭后**——届时的流程图/闸门矩阵才包含 G1 全部落地物；若用户要先看"现状版"，可提前单独出一版并在文件头标注快照日期） | **用户 2026-09-23（UTC+8）**："先这样做吧，我想需要对现有审查流程和具体策略内容找时间整体过一遍 记张卡把这些内容规则流程梳理作为一个backlog，输出图文并茂容易理解的html汇报文件" | **卡内待产出**：① **内容清单（逐条可核）**：审查动作全景（开工〇查 → 一查 doc-audit / 二查 code-review×2 → lessons → workspace-check）、**修复验证复核 vs 关闭三查**的区别与记录分节、`agents/fanout.json` 两条流水线、**每条规则的执行点**、闸门清单（`verify/*.py` 各自判据）、策略参数位置（`fanout.json` / spec 的 Configurable Parameters / `providers/*.json` / env / `prices.json`）、红线三段（S0 观测 / S1 仅 R1 硬停 / S2 全量）与升级条款、指标 M1~M3、卡↔Sprint↔文档拓扑（含 TG-14 落地后的 `cards/` 形态）、账本字段与证据链（`scope_ref`/`coverage_anchor`/`rounds`/`int`）。② **每条规则必须标注 `执行点=`**：有闸门（写脚本名+判据）／**无执行点**（显式标红）——**这是本卡最重要的判据**，直接服务用户"策略内容过一遍"的目的。③ **输出：单文件 HTML**（离线可开、内联 CSS + 内联 SVG 图形，**零 CDN/零外链**）：五步 fan-out 时序图、红线 S0/S1/S2 决策树、**规则×执行点矩阵表**、闸门×判据表、术语表、开放问题页。④ **一致性**：术语依 `1-WORKFLOW.MD` §3（`台账`=独立文件）；数字**引用 SSOT 或标注实测日期**，不手抄派生事实（TG-12 纪律）。⑤ 附"待用户裁定的开放问题"页（已答项不列）。**验收/反向对照**：a) 断网打开必须完整渲染（无外链请求，可用 DevTools/`--disable-network` 实测留证）；b) 清单自检：每条规则行**必须**含 `执行点=` 字段，缺一条即失败；c) 随机抽 3 条规则，从 HTML 回溯到**实际代码/配置位置**必须命中（可核性）。 |
| TG-17 | `[用户插入]` **闸门可信化收尾：覆盖窗口语义 + C1 判定域**（来源＝`TG-15` 独立复核批的两条 finding，用户 2026-09-23 批准开卡）：**问题① 覆盖窗口结构性为空**——`TG-15` 的"锚点自动化"让每个 run 记 `(coverage_anchor, covers_through]`，但 `register` 记登记时 HEAD、`finish` 记收尾时 HEAD，而关闭三查各步常在**同一 HEAD** 登记/收尾 ⇒ 窗口恒为 `(X, X]` = 空集。实测证据：账本 `run-…-impact-assessment-062` 的 `coverage_anchor == covers_through == e6ccd257…`；`092a2f7..HEAD` 的 9 个提交里 **6 个 UNOWNED**，自动窗口贡献 **0 覆盖**，C3 压力全落到 C3-T 例外表（而例外表 sha 钉死、按设计只能登记例外，不该承担主路径）。**问题② C1 判定域＝全账本**——账本 60/63 条历史 run 无 `scope_source` 键（`TG-11` 之前的 run 本就没有该字段），现判据把"凡 `scope_required: true` 的 run 都要有 scope 声明"施加到**全部历史** ⇒ 真数据上 `verify_close_readiness.py` **永久 FAIL 34 条**，闸门退化为噪音；且账本有完整性哈希（`_integrity`），无法手工补字段回填历史。**共同根因**：判据写成"无条件的全域断言"，而语境是"本次关闭窗口"——空输入与历史数据都会让判定失去意义。 | 3 | **候选（用户 2026-09-23 批准开卡）** | Sprint-18（治理批 G2），与 `TG-15`⑥（CI push 即检）同批 | **用户 2026-09-23（UTC+8 22:47）**："1+2 合成一张新卡（比如 TG-17「闸门可信化收尾」）排进 G：可以，开进backlog吧"；上游证据＝`code-review` run `run-2026-09-23-code-review-064` 的 major #3 与 major #5，及 `TG-15` 二查结论"建议开卡，不要在本轮半改" | **卡内待产出（①~④）**：① **窗口语义定稿并落地**——候选 A：锚点取"本 run 开工前的 HEAD"（登记时记 `HEAD~1` 或由调用方显式传上一锚点，语义＝"本 run 覆盖它之后产生的提交"）；候选 B：区间改**左闭** `[anchor, through]`（登记提交本身也算被覆盖，与 `§9.1` 填法一致）。二选一需给出**取舍理由**，并说明对已有历史 run 的兼容策略（旧窗口按新语义重算还是标记 deprecated）。② **C1 判定域收窄**——限定为"本次关闭窗口内"（§9 表最早 run 起的 run），历史 run 不卷入；**反向对照必须包含**：窗口内缺 scope 声明仍须 FAIL（防"收窄成静默放行"）、且历史缺字段**不得**遮蔽当期缺口。③ **反向对照／变异用例**——从 `agents/fanout.json` 自动生成的既有变异用例之外，新增：空窗口必须 FAIL（当前已落检测）、窗口边界（相邻 run 首尾相接不得漏提交）、`HEAD~1` 锚点在"本 run 无新提交"时的行为显式判定。④ **与 `TG-15`⑥ 合并排期**——覆盖检查进 CI 的触发条件、锚点填充方式（`§9.1` 是否仍需人工填 `三查锚点`）、以及 `close-sync` 输出格式（供人只挑类别）三者必须一次对齐，避免"工具一个口径、闸门另一个口径"（二查已实测该不一致：`cmd_close_sync` 曾以不传 `policy=` 的方式构造窗口）。**验收判据**：`verify_close_readiness.py --sprint <Sprint-17 文档>` 在**补全 §9 表与锚点后能真的 PASS**（现在是"无论怎么填都 FAIL"）；且历史 run 的缺失字段不再产生 FAIL，但**当期**缺 scope 声明仍 FAIL（两条都要有反向对照实测）。 |

## 2. 启动条件

- **前置闸门**：F2 用户全盘验收通过（验收发现 8 项已另立 F-AC 批次，见 `2026-08-31-f2-acceptance-findings.MD`）。
- 底座批次（TG-1/TG-2/TG-4）已完成于 Sprint-14；TG-3（case-first）完成于 Sprint-15；TG-5（cron 回归/分层 runner）完成于 Sprint-16；走查期新增 TG-6（验收方式改进，候选）、TG-7（预算闸门 fail-closed，✅）、TG-8/TG-9（Sprint-17 计划中）、TG-10（账本多轮次，v1 ✅）、TG-11（规则绕过复盘+闸门，候选）、TG-12（文档内容漂移复盘+派生事实机制，候选，**其机制候选 N7 已于 2026-09-21 提升为独立卡 TG-14**）、TG-13（账本测量化，候选，2026-09-21 用户拍板开卡）、TG-14（一卡一文档结构，候选，2026-09-21 用户拍板）。
- 开 sprint 时：planning_pipeline 注入预研笔记（`tech/2026-08-31-testing-governance.MD`），既定决策（D1–D4）为基线，不得重议（翻案建议除外）。
