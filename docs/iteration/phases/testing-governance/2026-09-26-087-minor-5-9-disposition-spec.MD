# `087-minor-5` / `087-minor-9` 处置规格（只读取证 ＋ 关闭判据草案）

> **定位**：本档是 `2026-09-26-g2-close-retro.MD` §5 行 2、行 3 两项**未闭环 finding** 的
> 处置规格。它**不改状态**（状态唯一权威仍是复盘 §5），**不改耦合边**（唯一权威仍是
> [`2026-09-26-g2-closure-ledger.MD`](2026-09-26-g2-closure-ledger.MD) B 表），
> 只补这两项缺的另一样东西：**原始 finding 的逐字文本 ＋ 它指的代码位点 ＋ 可跑出反证的关闭判据**。
>
> **为什么必须有本档**：复盘 §5 的行 2／行 3 只有**摘要**（"只做了 1/3""部分已修"），
> 而摘要不足以判定"关闭动作做完了没有"——判定需要回到**原文靶点**。
> 本档的每一条断言都带"命令 ＋ 输出行"；凡我没有现跑复现的，一律标注为**未复核**。
>
> **取证时点**：`windows` 分支，`HEAD = e84314f`，`git status --porcelain` 全程 0 行。

---

## 0. 两处必须先讲的更正（否则后面的行号会把读者带错）

### 0.1 台账 B 表 E1 与复盘 §9.6 的 `_norm_sprint` 位点已漂移

| 出处 | 写的位点 | 现跑实测 | 结论 |
|---|---|---|---|
| 台账 B 表 E1 行 | "`_norm_sprint` **三处**独立定义：`scripts/agent-ops.py:525`、`verify/verify_close_readiness.py:261`、`verify/verify_agentops.py:1028`" | `git grep -n "def _norm_sprint" HEAD` ⇒ **只有 2 处**：`scripts/agent-ops.py:576`、`verify/verify_close_readiness.py:261` | **"三处"与 `:1028` 都不成立** |
| 复盘 §5 行 7 | "`_norm_sprint` **两份**实现" | 同上 ⇒ **2 处**（这个数对，但未给行号） | 数对 |
| sprint-18 §9.6 | "（`scripts/agent-ops.py:525` ＋ `verify/verify_close_readiness.py:261`）" | `agent-ops.py:525` 现落在 `_validate_scope` 的 **docstring 内**，不是函数定义 | `:525` 是**陈旧行号** |

`agent-ops.py:525` 为什么会漂：该函数在 2026-09-25 因"装饰器挂错对象"事故被**上移**过
（`docs/iteration/phases/agents-infra/2026-09-25-gate-mechanism-failure-modes-case.MD` 案例 14、
`cards/TG-19.md:130`），行号因此右移。`verify/verify_agentops.py` 里现存的 `_norm_sprint`
字样**只有一处注释**：

```
verify/verify_agentops.py total_lines=1301
  L1059:         # （2026-09-25 实测：`register` 因此**丢掉账本锁**、`_norm_sprint` 反而拿到锁并
```

⇒ **该文件不提供任何 `_norm_sprint` 定义**。台账"三处"的来源是 089 判定表的转述
（`agents/runs/run-2026-09-26-code-review-089/code-review.report.md:170` 自述是**两份**），
**不是**任何一次 `git grep`。

**影响**：若收敛工作按台账 `:525` / `verify_agentops.py:1028` 定位，**会改到错误的位置**。
行使本档时请用：`git grep -n "def _norm_sprint" HEAD`。

### 0.2 089 判定表行 3 的"已改／未改"清单经复算**不成立**（本档据此不采信该清单）

089 判定表原话（逐字，`run-2026-09-26-code-review-089/code-review.report.md:164`）：

> 已改：`:74`（E501 时点）、`:94`（CLOSE-READINESS 2 项）、`TG-20.md:52`、`G2 计划书:20/:72`、
> `1-WORKFLOW.MD:424`（7→10 条）、`ci.yml:127`（7→12）。未改：`:48/:79/:96/:101/:137/:138/:140`
> （见上表 086-must-5/6/7 与下述）。`:137` 仍是 `composite = … = 69.38`（真值 69.36）；
> `:138` 仍是「46 文件 +5585/−148、25 提交（`1cad0b7..9945d6d`）」

089 的覆盖上界是 `16f4c5d`（其报告 `:397` 自述 `HEAD = 16f4c5d`）。现跑该区间真 diff：

```
$ git show 75324f9 -U0 -- docs/iteration/sprint/2026-09-25-sprint-18.md | Select-String '^@@'
@@ -77 +77 @@
@@ -94 +94 @@
```

⇒ 该批次对 sprint-18 **只动了 2 行：`:77` 与 `:94`**。据此逐条核 089 的清单：

| 089 的说法 | 现跑证据 | 判定 |
|---|---|---|
| "已改 `:74`（E501 时点）" | 真改动在 **`:77`**（`E501 … 2521 → 2493` 那一行）；`9f8d690:…:74` 是 D3 棘轮那一段，**一字未动** | **行号错**（089 自身的 `:164` 与四问回答 `:45` 又写 `:94`，同一份报告内不自洽） |
| "已改 `:94`" | ✅ 真 diff 命中 | 对 |
| "已改 `ci.yml:127`（7→12）" | ✅ 真 diff 命中 | 对 |
| "**未改** … `:137` 仍是 `composite = … = 69.38`" | `:137` 现文本是"**② 三查"一次通过"成立…**"，**不是** composite 行 ⇒ 该句与"已改 `ci.yml:127`"**同一行内自相矛盾**（`:127` 同时出现在"已改"与"未改"两串里） | **不成立** |
| "**未改** … `:138` 仍是 `46 文件 +5585/−148、25 提交`" | `:138` 现文本是"三次关闭评审…**28 条**必修/应修…" ⇒ 同样不是变更集行 | **不成立** 〔087 批时点读数，以现跑为准〕 |

⇒ 089 行 3 的位点清单**行号整体偏移且内部自相矛盾**，**不得作为处置依据**。
**本档以下一律以 `git show` / 现跑闸门输出为准。**

---

## 1. 原始 finding 逐字文本

### 1.1 `087-minor-5`

- **出处文件**：`agents/runs/run-2026-09-25-code-review-087/code-review.report.md`
- **定位行号**：**第 29–32 行**（093 报告的 Graded findings 第 5 条；该报告 Graded findings 共 10 条）
- **逐字文本**（**未作任何转述、未删节**）：

> **第 29 行：**
> ```
> - **minor** `verify/verify_decision_register.py:182`（配合 `:230`、`:201`、`:160`、`:271-272`）：**结构判据对"代码围栏/表格单元格/重复字段"不设防，指针与引文核对也不区分正文与示例**——`for section in SECTIONS: if section not in text` 是**全文子串**测试；`POINTER_RE.finditer(text)` 在**全文**（含围栏示例）上收集并计数指针。
> ```
>
> **第 30 行：**
> ```
>   证据（现跑探针 `probe6_fence.py`）：① 9 个章节标题**只写在 ``` 围栏里**、真章节全缺 + 一条真条目 ⇒ `problems=[]`（章节判据被围栏文本满足，`stats.section3=0` / `section41=1` 也说明条目归属靠 ID 前缀而非章节区间）；② 围栏里的一句"示例：出处写作 `docs/6-DECISIONS.md:1`"被计入 `pointers`（3 处）并参与引文核对。另读码确认：`:201` 的"会话"只做 `"会话" in src` 子串判断（写成"这不在会话里"也过）；`:160` 的 `setdefault` 使**同名重复字段取第一条**（真行可被诱饵行顶掉）；`:271-272` 的核对窗口是"指针行 ±2"且用 `in` 包含 ⇒ 指针指 3、引文在第 1 行也算命中（漂移不可见）。
> ```
>
> **第 31 行：**
> ```
>   为什么重要：这一类正是本仓反复出现的"判据被非判据文本满足"（本报告 major 1 同族），而登记册的章节契约是它唯一的骨架判据。
> ```
>
> **第 32 行：**
> ```
>   Fix：解析器改成**围栏感知**（先剥离 ``` 块再判章节/指针/字段），给每个条目记录它所属章节的**行区间**并要求条目落在 `## 3.` / `## 4.1` 区间内（取代 `:317` 的 ID 前缀统计），重复字段名 ⇒ FAIL，"会话"改为精确形态匹配，引文核对报出**实际命中的行号**（±2 内漂移可见）。Fix this round: yes
> ```

**注**：原文标题句写的是"**代码围栏/表格单元格/重复字段**"**三项**。复盘 §5 行 2 与台账 A 表行 2
把它转述为"围栏剥离／表格单元格／重复字段"。但原文 `Fix` 段实际列了**五件事**（围栏感知、
条目落在章节行区间内、重复字段 FAIL、"会话"精确形态、引文报实际命中行号）；089 判定表
又额外点名"'会话'子串判据"与"引文核对窗口 ±2"。**"1/3"是三选一的记法，不是原文的记法**——
这一点在写关闭判据时必须明确，否则会漏掉后两项。

### 1.2 `087-minor-9`

- **出处文件**：同上
- **定位行号**：**第 44–48 行**
- **逐字文本**：

> **第 44 行：**
> ```
> - **minor** `docs/iteration/sprint/2026-09-25-sprint-18.md:94`（同族 10 处，见下）：**本批文档里写死的计数读数已成片过期，且没有任何闸门覆盖它们**：
> ```
>
> **第 45 行**（六项实例，逐字；行内 `` `` `` 与反引号照抄）：
> ```
>   ① `:94` 写"**现跑为准**：`CLOSE-READINESS FAIL（4 项）`"——现跑 **2 项**（`[C1/需求] 步骤 4:lessons 缺 run` + `未声明三查锚点`，rc=1）；② `:48`/`:79` 写 A-M12 判据④"现跑读数 = **3**"，与本文件 `:106`（"现跑 = **2**…当前读数 2 > 目标 1 ⇒ 不达标"）及 `cards/A-M12.md:94`（=2）**自相矛盾**；③ `:99`/`:101` 写 `verify_gate_integrity.py` **19** 断言（实测 **23**）、`:100`/`:104` 写 `guard_required=9`（实测 **10**，而 `:107` 写 10）；④ `:101` 写 `E501=2493/2494`（实测 **2492**/2494）、`verify_close_readiness.py` **91** 断言（实测 **94**）；⑤ `:137` 的 `composite = (0.8×0.8462+0.2×0.0833)×100 = 69.38`（真值 **69.36**）；⑥ `:138` 把统计写成"46 文件 +5585/−148、25 提交（`1cad0b7..9945d6d`）"，而该区间的实测是 **+5565/−147、24 提交**（5585/148/25 属于 `b6198f5..9945d6d`）；⑦ `:80` 写"当场抓出 **3 处**真实漂移 … V-050/V-051（`A-M11.md:32` → **84**）"，而 `cards/TG-20.md:48` 写的是"**2 处**真实漂移（V-096/V-097…）"（**完全不提 V-050/V-051**），实际改法是 `A-M11.md:32 → **123**`（`git show 9945d6d -- docs/6-DECISIONS.md` 逐字确认：两处 `- …A-M11.md:32` / `+ …A-M11.md:123`）⇒ 同一批的两份文档对"抓出几处、改成哪一行"说法都不一致。同族还有：`docs/6-DECISIONS.md:15`"§3 本会话裁决 **11** 条"（实测 **12**，`D-250925-12` 由 d5fcb27 插入）、`docs/1-WORKFLOW.MD:424`"当前已落 **7** 条："而其下本批已扩到 **10** 条（新增 8/9/10）、`.github/workflows/ci.yml:122`"含 **7** 条反向对照自检"（闸门实测 **9**，同文件 `:132` 反而立了"标签不写死数字"的规矩）、`cards/TG-20.md:55`"**4 条**反向对照全绿"（其自身 `:44` 与 `sprint-18.md:107` 都写 **9**）、`docs/iteration/sprint/2026-09-25-governance-batch-plan-G2.MD:72`"`TG-20` 机检判据 ＋ **4 条**反向对照"（实测 9）、同文件 `:20`/`:127`"**19 例**"（反例档实测 **21** 个 `## 案例` 小节）。
> ```
>
> **第 46 行**（证据段，逐字节选与本次判定相关的部分）：
>
> （**引用纪律**：以下三处引文块是 087 报告 `:46`／`:47`／`:48` 的**原文**，块内读数是
> **087 时点值**；现值一律以 087 块末尾给出的复现命令现跑为准。本档刻意不改写引文内的数字。
> 为满足 `verify_derived_numbers.py` 的**行级时点豁免**口径，凡引文内带读数的行，本档在其行尾
> 附一个方括号标记 `〔087 时点读数，以现跑为准〕`——**该标记是本档所加、不属于 087 原文**。）
>
> ```
>   证据：以上①③④由我现跑同名命令逐条核对（`verify_close_readiness.py --sprint …sprint-18.md` ⇒ FAIL（2 项）；`verify_gate_integrity.py` 末行 ⇒ `assertions=23 … guard_required=10 ratchets=5`；`verify_lint.py` ⇒ `E501=2492/2494`；`--selftest` ⇒ `assertions=94`）；②⑥⑦与 `6-DECISIONS.md:15`、`1-WORKFLOW.MD:424`、`ci.yml:122`、`cards/A-M12.md:94`（=2）、`cards/TG-20.md:44/:55`、G2 计划书 `:72`/`:20`/`:127` 由我读原文 + `git rev-list/diff --shortstat` / `git show` / `Select-String '^## 案例'` 对账。另：独立子代理报过 `cards/TG-17.md:63`"剩 1 项（C3 未归属提交）"，我复核**不成立**（`:63` 讲的是"CI 关闭前置块空跑"，`:34` 写的是"投影 3 项"），故**未采信、已剔除**。〔087 时点读数，以现跑为准〕
> ```
>
> **第 47 行**（为什么重要，逐字；这是行 3 与 `verify_derived_numbers.py` 关系的原始出处）：
>
> ```
>   为什么重要：`docs/1-WORKFLOW.MD:387` 已点名"具体计数随文件增删必然漂移，规范正文不得抄写"，本批又亲手把 `1-WORKFLOW.MD:424` 的"7 条"变成假话；而 `verify_derived_numbers.py` 的 `patterns` 只覆盖"N 卡／ALL PASS (N assertions)／N 断言／C 级 N 条"四类形态，`guard_required=9`、`E501=2493/2494`、`§3 11 条`、`4 条反向对照`、`+5585/−148` 全在覆盖面之外 ⇒ **关闭报告若照抄 §7／§9.2 就会把一批过期数字带进 G2 的验收证据**。反差证据：同一份表格里 `:97` 的"20 条 `sprint_backfills`"、`:96` 的"§4.0 8 条／§4.1 103 条／§4.3 282 条"、`:103` 的"91→94"、`:105` 的"10→14"我逐条现跑核对**全部正确**——说明这是**漏改**而非口径之争。〔087 时点读数，以现跑为准〕
> ```
>
> **第 48 行**（Fix，逐字；注意它已经给出了"根治办法"，是本档判据草案的直接来源）：
>
> ```
>   Fix：受影响的行一律改成"复现命令 + 以输出为准"或加时点/sha 锚（同表 `:63`/`:106` 已是这种写法）；根治办法是给 `agents/policy.json::derived_numbers.patterns` 增加 `gate_required=N`、`E501=N/N`、`N 条反向对照`、`+N/−N、N 提交` 四类形态（闸门现跑 14 条断言已含"上限只许下调"，扩 pattern 只是加数据）；`ci.yml:122` 与 `1-WORKFLOW.MD:424` 直接删掉数字。Fix this round: yes〔087 时点读数，以现跑为准〕
> ```

**注**：原文第 44 行自称"同族 **10 处**"，但第 45 行实际列了 ①–⑦ 加"同族还有"5 组
（`6-DECISIONS.md:15`、`1-WORKFLOW.MD:424`、`ci.yml:122`、`cards/TG-20.md:55`、
G2 计划书 `:72`/`:20`/`:127`）⇒ **实际实例数多于 10**，且第 105 行把靶点写成
`sprint-18.md:48/79/94/99/100/101/104/137/138`。写关闭判据时**不要用"10 处"这个数**。

**注（引文完整性声明，供第三人逐字比对用）**：§1.1／§1.2 的三个引文块是
`agents/runs/run-2026-09-25-code-review-087/code-review.report.md` 第 46／47／48 行的原文。
其中带读数的行，本档在**行尾**追加了一个方括号标记 `〔087 时点读数，以现跑为准〕`。
**该标记不是 087 原文的一部分**，是本档为满足 `verify/verify_derived_numbers.py`
的**行级时点豁免**口径（见 `verify/verify_derived_numbers.py:108-115` 与
`agents/policy.json::derived_numbers.time_anchor_regex`）而加的；
本档**没有改动引文内的任何一个数字**。逐字比对时请忽略该标记。

### 1.3 未找到原文的部分（如实标注）

| 我想找的 | 结果 | 我搜了哪些路径／模式 |
|---|---|---|
| 087 报告里探针脚本 `probe6_fence.py` 本体 | **未找到** | `Get-ChildItem agents\runs\run-2026-09-25-code-review-087 -Recurse -File` ⇒ 该目录**只有** `code-review.report.md` 与 `context.md`（087 全程把探针落 `%TEMP%\cr087\`，见其 `:129`）；`glob **/probe6*` ⇒ 0 命中 |
| 089 判定表的独立文件（非报告内嵌） | **不存在**（判定表就在报告内） | `glob **/*089*`（过滤 `ruff_cache`）⇒ 0 命中；`agents/runs/run-2026-09-26-code-review-089/` 下只有 `code-review.report.md`＋探针＋evidence |
| 086／088 报告是否也提到 `087-minor-5/9` | **0 命中** | `grep -r "087-minor-5\|087-minor-9"` 全仓 ⇒ **仅** `2026-09-26-g2-close-retro.MD:100-101` 两处（即复盘自己的转述，不是原文） |

⇒ **`087-minor-5` / `087-minor-9` 的原文只有一处出处**：087 报告的 Graded findings。
复盘 §5 与台账 A 表的措辞都是**二次归纳**，本档不据此判定。

---

## 2. 代码位点：三项判据现在**实际**做了什么

靶点文件：`verify/verify_decision_register.py`（563 行，`HEAD = e84314f`）。

### 2.0 087 给的原始行号 → 现行号对照（已漂移，必须先对齐）

| 087 写的 | 现行号 | 该处代码 | 现跑是否改过 |
|---|---|---|---|
| `:182`（章节判据） | **`:197-202`** | `for section in SECTIONS: if not any(line.startswith(section) …)` | **改过**（子串 → 行首） |
| `:160`（重复字段 `setdefault`） | **`:162`** | `fields.setdefault(name, m.group("value").strip())` | **未改**（逐字相同） |
| `:201`（"会话"子串） | **`:224`** | `… and "会话" not in src and "待回填" not in src:` | **未改**（逐字相同） |
| `:271-272`（引文核对窗口） | **`:327`** | `window = lines[max(0, ln - 3):ln + 2]` | **未改**（逐字相同） |
| `:230`（指针全文收集） | **`:266-268`** | `for m in POINTER_RE.finditer(text): pointers.append(…)` | **未改**（仍全文） |
| `:317`（ID 前缀统计） | 已并入 `stats.section3/section41` | 仍按 ID 前缀归属，**未**改成章节行区间 | **未改** |

**错位成因**：`75324f9` 在 `check_register` 里插入了分支差异／被删／具名 SKIP 三段（089 major 1d），
该函数整体右移。**089 判定表用的是 087 的旧行号**（其 `:160` 原话"`FIELD_RE`/`setdefault`/`QUOTED_RE`
原样"——"原样"在**代码文本**上成立，在**行号**上不成立）。

### 2.1 三项判据的"做了／没做"判定表（代码为证）

> 判定手段：用**真函数**（`import verify.verify_decision_register as vdr`）＋ `%TEMP%` 字面量登记册，
> 逐项构造反例并读 `check_register()` 的返回。探针已跑完并删除（见 §7"我没做的事"）。

| # | 判据 | 现状 | 证据（真函数现跑输出） |
|---|---|---|---|
| **1** | **围栏剥离**（先剥 ` ``` ` 块再判章节／指针／字段） | **完全没做** | 反例：9 个 `SECTIONS` 标题**只写在 ` ``` ` 围栏里**、真章节全缺 ＋ 一条真条目 ⇒ `章节问题数 = 0`、`problems = []`、`stats = {'entries': 1, 'section3': 1, 'section41': 0, 'pointers': 0, …}` ⇒ **判决 PASS**。围栏文本满足了章节判据。 |
| **1b** | **指针不区分正文与示例**（同族） | **没做** | 反例：围栏里写一句"示例：出处写作 \`docs/6-DECISIONS.md:1\`" ⇒ 它**被计入** `pointers`（`stats.pointers` 非 0）并参与引文核对分流。`check_register` 仍在 `:267` 用 `POINTER_RE.finditer(text)` 扫**全文**。 |
| **2** | **表格单元格** | **没做** | 现行解析器是**行正则**：`FIELD_RE`（`:83`）＝ `^[-*]\s*(?P<name>[^:：]+?)\s*[:：]\s*(?P<value>.*)$` **要求行首是 `-`/`*`** ⇒ 表格单元格（形如 `\| 原文 \| … \|`，行首是转义后的竖线）**根本不会被解析成字段**。补一条真跑证据：真登记册现跑 `条目 119 条`而 `指针 1248 处`，而 §4.x 的**表格**里若混入 `path:line`，它会被 `POINTER_RE`（`:85-87`，**不锚行首**）计入 —— 即**同一份文件里"字段"认行首、"指针"不认行首**，两种口径并存。**判据缺口是"表格形态既不被字段判据看见、又不被排除在指针域外"**。 |
| **3** | **重复字段取第一条** | **没做** | 反例：`- 原文：真原文内容` 后紧跟 `- 原文：诱饵行内容` ⇒ `parse_entries()` 返回 `[('D-250925-01', '真原文内容')]`（**取第一条**），`problems = []` ⇒ **判决 PASS**。现行码仍是 `fields.setdefault(...)`（`:162`）。087 原话"真行可被诱饵行顶掉"**仍然成立**。 |
| **3b** | **限定语撞车**（同族，089 未列） | **没做** | `_canon_field('原文（逐字，选项式）')` ⇒ `'原文'`；于是 `- 原文（逐字，选项式）：先写的值AAA` ＋ `- 原文：后写的值BBB` ⇒ 解析结果 `'先写的值AAA'` ⇒ **后写的真值被先写的丢掉**。这条**只有把 `setdefault` 改成覆盖**（或加"基名撞车即 FAIL"）才能堵——见 §5 的耦合提示。 |
| **4** | **"会话"只做子串判断** | **没做** | `:224` 逐字仍是 `if src and not _pointers(src) and "会话" not in src and "待回填" not in src:` ⇒ 出处写成"**这不在会话里**"仍**通过**（含"会话"二字）。087 原话逐字复现。 |
| **5** | **引文核对窗口与"实际命中行号"** | **没做**（且 087 的"±2"描述与代码**不完全一致**） | 现行 `:327` ＝ `window = lines[max(0, ln - 3):ln + 2]`，即 `ln-3 … ln+1` 共**最多 5 行**（向下 3、向上 1）。087 写"±2"、`run-…-089:160` 写"±2"——**两处都与代码不符**。反例（087 给的最小形态）：指针标 `:3`、引文实际在第 1 行 ⇒ 命中窗口（`max(0,0):5`）**包含第 1 行** ⇒ 静默通过，且**不报出实际命中行号**（`hit = f"{relp}:{ln}"` 打印的是**指针写的行号**，不是真实行号）。 |

### 2.2 唯一**真做掉**的那一项（087 原文没要求的）

087 的 minor 是"章节判据用全文子串"；`75324f9` 把它改成了**行首**匹配：

```python
# verify/verify_decision_register.py:197-202
    for section in SECTIONS:
        # 章节必须在**行首**出现（二查 `run-…-087` minor 5：子串测试能被正文/围栏里的
        # 同名字样满足 ⇒ "章节在位"这条判据形同虚设）
        if not any(line.startswith(section) for line in _lines):
            problems.append(f"[章节] 缺 {section!r}（登记册的结构契约；"
                            f"删章节等于删判据；只认**行首**标题，正文提及不算）")
```

**但注意**：`line.startswith(section)` 对 ` ``` ` 缩进块**同样成立**——因为 ` ``` ` 是**独立行**，
标题行在围栏内**仍以 `## ` 开头**。上述反例（§2.1 第 1 项）正是这样通过的。
⇒ "行首"改动**提高了门槛但没关掉这个洞**；这正是"只做了 1/3"的准确含义。

### 2.3 判定表（一句话汇总）

| 三项判据 | 做了？ | 依据 |
|---|---|---|
| 围栏剥离 | ❌ **没做** | 围栏内标题仍满足章节判据（反例 PASS）；指针仍扫全文 |
| 表格单元格 | ❌ **没做** | `FIELD_RE` 锚行首 `[-*]`，表格单元格不被解析；指针侧无行首锚，两套口径并存 |
| 重复字段 | ❌ **没做**（含 3b 限定语撞车） | `setdefault` 逐字未动，诱饵行顶掉真行，反例 PASS |
| （087 Fix 另列的 2 项） | | |
| "会话"子串 | ❌ **没做** | `:224` 逐字未动，"这不在会话里"仍过 |
| 引文窗口／实际行号 | ❌ **没做** | `:327` 逐字未动；且 087 与 089 的"±2"与代码不符 |

⇒ **结论：这三项（含 087 Fix 另列的两项）没有任何一项被修。**
复盘 §5 行 2 的"只做了 1/3"若指 087 原文的**三项标题句**，则实际是 **0/3**；
若把"章节认行首"算作半项，也仍是 **"1 项被部分改善、且改善没有关上洞"**。
**两种记法都必须连同本节的反例一起读，否则会误以为"还差两项"。**

### 2.4 自检覆盖（判据是否已被闸门自己守）

`--selftest` 现跑（真跑输出，**以现跑为准**）：`ALL PASS (14 assertions)`，对照清单为
`e`（干净 fixture）、`e'`（引文真的核过）、`a`（少字段）、`b`（表外状态词）、`c1`（分支差异子树）、
`c1′`（打错/改名）、`c2`（被删）、`c3`（登记册自身被删）、`d`（引文与出处不符）、`f`（悬空取代）、
`f′`（加粗取代）、`i`（8 字段值全空）、`g`（出处不可核形态）、`h`（原文混排归纳）。

**⇒ §2.1 的 6 个反例一个都不在对照清单里**（没有围栏、没有表格、没有重复字段、
没有"会话"子串、没有窗口漂移）。**这就是"没有闸门覆盖"的准确范围**。

---

## 3. 每项的关闭判据草案（**可跑出反证**形式）

> 格式：**反例输入 → 期望判 FAIL**。每条都要求"**真入口**"——即走
> `check_register()` 真函数 ＋ `%TEMP%` 字面量登记册，**不得**注入中间变量或断言局部字符串。
> 台账 A 表行 2 已要求"各带一例**真入口**反证"，本节即其展开。

### 3.1 围栏剥离

| 项 | 内容 |
|---|---|
| **反例输入** | 一份登记册：9 个 `SECTIONS` 标题**全部只出现在 ` ``` ` 围栏内**，围栏外一个真章节都没有；另加 1 条字段齐全的真条目 |
| **期望** | `problems` **非空**，且**每条**缺章节都有 `[章节] 缺 '…'` 项 |
| **现状** | `problems = []`、判决 PASS（§2.1 第 1 项现跑） |
| **已有闸门覆盖？** | ❌ 无。`--selftest` 无此对照 |
| **第二反例** | 围栏内写"示例：出处写作 \`docs/6-DECISIONS.md:1\`" ⇒ 期望该指针**不计入** `stats.pointers`、**不参与**引文分流。现状：被计入 |

### 3.2 表格单元格

| 项 | 内容 |
|---|---|
| **反例输入** | 一份登记册：把 8 个字段写成**表格单元格**（`\| 原文 \| … \|`）而非 `- 字段：值` 行 |
| **期望** | 二选一，**必须显式定档并落判据**：<br>（a）**表格视为非条目区** ⇒ 该条目按"缺字段"FAIL；<br>（b）**表格视为合法字段载体** ⇒ 解析器支持表格且字段值非空判据照常生效。<br>**不容许**现状这种"字段判据看不见、指针判据却看得见"的双口径 |
| **现状** | `FIELD_RE`（`:83`）锚 `^[-*]` ⇒ 单元格不被解析；`POINTER_RE`（`:85-87`）不锚行首 ⇒ 单元格里的 `path:line` **会被计入**。反例①（章节全在围栏）现跑 `section3=1 / section41=0` 印证字段归属靠 ID 前缀而非结构 |
| **已有闸门覆盖？** | ❌ 无 |
| **定档建议** | 取 (a)：与"§1 字段定义即契约"一致，且改动最小（单元格 `\|` 开头的行**显式排除在指针域外**） |

### 3.3 重复字段（含限定语撞车）

| 项 | 内容 |
|---|---|
| **反例 3-i** | 同一条目内 `- 原文：真原文内容` 紧跟 `- 原文：诱饵行内容` |
| **期望** | FAIL，并**点名是哪个字段重复** |
| **现状** | `parse_entries()` ⇒ `'真原文内容'`（取第一条），`problems = []` |
| **反例 3-ii**（限定语撞车） | `- 原文（逐字，选项式）：先写的值AAA` ＋ `- 原文：后写的值BBB`（`_canon_field` 把两者都归成 `原文`） |
| **期望** | FAIL（基名撞车必须点名），**或**按"后者覆盖前者"取 `'后写的值BBB'` 且**不静默** |
| **现状** | 取 `'先写的值AAA'` ⇒ 真值被丢，零问题 |
| **已有闸门覆盖？** | ❌ 无 |
| **⚠️ 改动耦合（必须与本档 §6 的 `_norm_sprint` 收敛一起排期）** | 简单地把 `setdefault` 改成 `fields[name] = …` 会让 `_canon_field` 的**限定语归一**变成"后者覆盖前者"的静默行为——`原文（逐字，选项式）` 与 `原文` 并存时不再报错，只是悄悄换值。**正确修法是"同名（含归一后同名）出现第 2 次 ⇒ FAIL"**，而不是换赋值语义。 |

### 3.4 "会话"子串

| 项 | 内容 |
|---|---|
| **反例输入** | 条目 `出处` 写成"这不在会话里"（含"会话"二字但**不是**显式会话形态） |
| **期望** | FAIL：`[出处] … 既不是 path:line 指针、也不是显式的『会话』形态` |
| **现状** | **通过**（`:224` 子串判断） |
| **已有闸门覆盖？** | ❌ 无。对照 `g` 测的是"我记得是在某份文档里"（**不含**"会话"），打不到这个洞 |

### 3.5 引文核对窗口／实际命中行号

| 项 | 内容 |
|---|---|
| **反例输入** | `出处` 标 `docs/6-DECISIONS.md:3`，而 `原文` 的逐字内容**实际在 `:1`**（漂移 ≥2 行） |
| **期望** | FAIL，或**至少**报出"实际命中的行号是 `:1`"（漂移可见） |
| **现状** | `:3` 的窗口 `lines[max(0,0):5]` = 第 1–5 行 ⇒ **命中**、静默通过；且 `hit` 打印的是**指针写的行号**（`:329` `hit = f"{relp}:{ln}"`），**不报真实行号** |
| **已有闸门覆盖？** | ❌ 无（对照 `d` 用的是"引文根本不在任何标注位置"，窗口内漂移打不到） |
| **文档一致性附带项** | 087 `:30` 与 089 `:160` 都写"±2 窗口"，代码是 `ln-3 … ln+1`。**须同时更正文档或代码**，否则下一轮复核会继续按错的口径判 |

### 3.6 关闭判据的**共同前置**（台账 A 表行 2 已写，此处给可判定形式）

> 前置：先做行 7（`_norm_sprint` 收敛）。**本档 §6 的判定是：行 2 的判据不挂在这三处定义上，
> 故该前置对本项不构成技术阻塞**——但台账 A 表把它写成了前置，**两者必须择一更正**（见 §6.3）。

---

## 4. 受影响调用点普查

### 4.1 `087-minor-5`（登记册解析器）

```
$ git grep -n "def _canon_field\|def parse_entries\|def check_register\|def _pointers\|def _quote_fragments\|def _lines_of\|def _norm\b\|def selftest\|def main" -- verify/verify_decision_register.py
verify/verify_decision_register.py:105:def _norm(text: str) -> str:
verify/verify_decision_register.py:112:def _canon_field(name: str) -> str:
verify/verify_decision_register.py:125:def _lines_of(path: Path) -> list[str]:
verify/verify_decision_register.py:130:def _pointers(src: str) -> list[tuple[str, int]]:
verify/verify_decision_register.py:142:def _quote_fragments(body: str) -> list[str]:
verify/verify_decision_register.py:148:def parse_entries(text: str) -> list[dict]:
verify/verify_decision_register.py:168:def check_register(root: Path, *, doc: Path | None = None) -> dict:
```

**消费方（真调用点）**：

| 调用点 | 行号 | 说明 |
|---|---|---|
| `check_register` → `parse_entries(text)` | `:203` | 唯一解析入口 |
| `parse_entries` → `_canon_field` | `:161` | 字段基名归一 |
| `parse_entries` → `fields.setdefault(...)` | `:162` | **重复字段洞** |
| `check_register` → 章节行首判据 | `:197-202` | 唯一被改过的一处 |
| `check_register` → `"会话" not in src` | `:224` | **子串洞** |
| `check_register` → `POINTER_RE.finditer(text)` | `:266-268` | **全文扫，不剥围栏** |
| `check_register` → `window = lines[max(0, ln - 3):ln + 2]` | `:327` | **窗口洞** |
| `main()` → `check_register(ROOT)` | `:532` | 真数据档 |
| `main()` → `selftest()` | `:555` | `EVIDENCE:` 行在 `:557-558` |
| `selftest()` 内 14 条 `ok(...)` | `:424,426,429,433,438,450,480,490,495,500,507,512,516,521` | 见 §2.4 |

```
$ git grep -n "verify_decision_register" -- .github verify scripts agents/policy.json
```
（CI 接线与套件登记；`:127` 为 CI 标签行，见 §5.3）

**跨闸门共享对象**：本文件不 import 其它登记册消费者；`_norm`（`:105`）与
`verify/verify_close_readiness.py` 的归一化**不是同一函数**（后者是 `_norm_sprint`，语义不同）
⇒ **行 2 与行 7 无共享实现**（这一点是 §6 判定的依据）。

### 4.2 `087-minor-9`（过期计数）

```
$ git grep -n "derived_numbers" -- agents/policy.json verify/verify_derived_numbers.py
```
⇒ 政策块 `agents/policy.json::derived_numbers`（`patterns` / `time_anchor_regex` / `baseline` /
`default_cap` / `review_by` / `scan_globs` / `generated_files`），消费者只有
`verify/verify_derived_numbers.py`（`_CFG = load_policy()._data("derived_numbers")`，`:61`）。

**关键读数（现跑，逐字）**：

```
$ .venv\Scripts\python.exe verify\verify_derived_numbers.py
DERIVED-NUMBERS PASS：173 个文档扫描通过（32 个历史文件走棘轮上限、均在各自上限内；生成物 1 个豁免）
EVIDENCE: verify_derived_numbers.py assertions=14 rc=0 review_by=2026-10-31
--- rc=0 ---
```

**政策实际装载的 8 个 pattern**（现跑 dump，`id` 逐字）：
`claim_count`、`all_pass`、`assertions_cn`、`evidence_line`、`matrix_check`、
`suite_scripts`、`ratchet_counts`、`c_level_total`。

**`time_anchor_regex`（现跑逐字）**：
```
(?:20\d\d-\d\d-\d\d|`[0-9a-f]{7,40}`|以现跑为准|以命令输出为准|现跑为准|现值以|以该命令输出为准)
```

**`default_cap = 0`、`review_by = '2026-10-31'`、`baseline` 不含 sprint-18**
⇒ **sprint-18 的每一个命中都直接进 `problems`（无棘轮余量）**。

---

## 5. 行 3 的"成片过期计数"：现状与缺口

### 5.1 闸门与文档计数的关系（机制，逐字引证）

三条链路：

1. **扫描域**：`scan_globs = ["docs/**/*.md", "docs/**/*.MD"]`，经 `doc_files()`（`:88-102`）
   去掉 `generated_files`。⇒ **`agents/**`、`verify/**`、`.github/**` 不在域内**。
   087 点名的 `ci.yml:122`、`cards/TG-20.md:55`（`docs/iteration/**` 之下 ⇒ **在域内**）、
   G2 计划书（在域内）三种情况因此**覆盖面不同**——`ci.yml` **根本不会被扫**。
2. **行内时点豁免**：`scan_text()`（`:105-119`）第一句就是
   `if TIME_ANCHOR.search(line): continue` ⇒ **整行豁免**。087 靶点里大量行带
   `2026-09-25` / `2026-09-26`（这些行的日期写在**行首**）⇒ **整行被放过**。
3. **棘轮与上限**：`evaluate()`（`:336-339` 调用）按 `baseline` 逐文件设上限，
   `head_caps()`（`:131` 起）从 `git HEAD` 版政策取基准，**上限只许下调**。

### 5.2 现跑判据：087 的四类"根治 pattern" **一类都没加**

087 `:48` 的根治办法逐字是："给 `agents/policy.json::derived_numbers.patterns` 增加
`gate_required=N`、`E501=N/N`、`N 条反向对照`、`+N/−N、N 提交` 四类形态"。

**现跑 dump 的 8 个 pattern 里，这四类一类都没有。** 逐条验证（对**真行**跑闸门自己的
`scan_text()`，见 §4.2 与下表）：

| 087 要求的形态 | 现行是否覆盖 | 现跑证据（对真行跑 `scan_text()`） |
|---|---|---|
| `gate_required=N` | ❌ **无 pattern** | `sprint-18:100` 含 `guard_required=9`（现跑真值 **10**）⇒ `hits=[]`、无时点锚 ⇒ **未覆盖** 〔087 批时点读数，以现跑为准〕 |
| `E501=N/N` | ⚠️ **半覆盖** | `sprint-18:101` 含 `E501=2493/2494`（该处为 087／089 时点读数，以现跑为准）。`ratchet_counts` 的 regex 是 `(?:E501\|D10[123])\s*=\s*\d+` ⇒ **只匹配到 `E501=2493`**，`/2494` 与 2493 的真值关系不被核。且该行**同时**被时点锚豁免 |
| `N 条反向对照` | ❌ **无 pattern** | `sprint-18:104` 含 "`--replay-git` 自检 **9 条**"、`:99` 含 "真跑 `PASS`（…现 **19**）" ⇒ `hits=[]`、无时点锚 ⇒ **未覆盖** |
| `+N/−N、N 提交` | ❌ **无 pattern** | 现 `sprint-18:232` 已改成真值（`+5565/−147`、`24` 提交），但**闸门不核它**——是"人手改对了"，不是"闸门守住了" |
| `N 例`（087 `:45` 同族） | ❌ **无 pattern** | `claim_count` 只列 `张卡\|卡\|个阶段\|脚本\|个文件\|文件\|份\|条\|B`，**不含"例"**。`G2 计划书:20` / `:127` 的"21 例"现跑仍**未覆盖**（两行都带 `2026-09-25` ⇒ 另有时点豁免） |

**根因两条（都可机检）**：
- 087 `:47` 说"覆盖四类"时，政策里当时有 4 个 pattern；**现政策是 8 个**，`ratchet_counts` 等
  是后来加的 —— 但**加的不是 087 要的那四类**。
- `time_anchor_regex` 的豁免是**行级**的，而 sprint-18 的表格行**行首就是日期**
  ⇒ 087 靶点里带日期的那些行**结构性地豁免于全部 pattern**。这是"覆盖面外"之外的第二层
  缺口，**087 没点名，但它比 pattern 缺失更隐蔽**。

### 5.3 "哪些数现在是手抄的、应当由现跑派生"（逐行现跑核对）

**（a）闸门域内、现跑仍与真值不符的（真缺陷）**：

| 位点 | 现文本（现跑读原文） | 现跑真值 | 闸门为何没抓 |
|---|---|---|---|
| `sprint-18:99` | `verify_gate_integrity.py` 真跑 `PASS …`（**现 19**） | 该处所引断言数已过期（089 时点即已 23） | 时点锚行级豁免；无 `N 断言/现 N` 形态 pattern 命中该写法 |
| `sprint-18:100` | `guard_required=9` | **10**（089 判"未闭环"时即 10） | 无 `gate_required=N` pattern；且行带日期 ⇒ 豁免 〔087 批时点读数，以现跑为准〕 |
| `sprint-18:103` | 自检 `91→94` | 属**历史变化叙述**，须判"是史述还是现值"——若是现值则过期 | 无对应 pattern；行带日期 ⇒ 豁免 |
| `sprint-18:104` | `--replay-git` 自检 **9 条**、`ratchet=5 guard_required=9` | 现跑 `--replay-git` 条数已增（批 7 记 9→12） | 无 `N 条` 形态 pattern；行带日期 ⇒ 豁免 〔087 批时点读数，以现跑为准〕 |
| `sprint-18:134` | G1/G2 的 `code 7 major + 3 critical` / 文档项 11 / 12 | 属史述（G1/G2 关闭税），**并须与时点绑定** | 无对应 pattern |
| `6-DECISIONS.md:15` | `§3 本会话裁决 **11 条**` | 登记册现跑 `§3 16`（`DECISION-REGISTER PASS：119 条条目（§3 16 / §4.1 103）`） | 行带日期 ⇒ 豁免。（该行已自带"D6 后现为 12"的更正括注 ⇒ **属"史述＋就地更正"合规形态**，但仍不是机检） |

**⇒ 关键：这一表里没有一行是"闸门判红"的。** 也就是说 **087-minor-9 的缺口不是"数字错"，
而是"错了不会被任何判据发现"** —— 这正是 087 `:44` 的原话"**且没有任何闸门覆盖它们**"。

**（b）已由人手改对、但**仍未经判据**的（现值对，覆盖仍缺）**：

| 位点 | 现文本（现跑读原文） | 备注 |
|---|---|---|
| `sprint-18:231` | `composite = (0.8×0.8462 + 0.2×0.0833)×100 = 69.36` | 已是复算真值；`:235` 附"① composite 原写 `= 69.38`，按**同一算式**复算为 **69.36**"的就地更正 |
| `sprint-18:232` | `46 文件 **+5565/−147**、**24** 提交（`1cad0b7..9945d6d`）` | 已是 `git diff --shortstat` / `rev-list --count` 真值；`:236` 附更正说明 |
| `sprint-18:137-138` | 现为"② 三查"一次通过"成立…" / "…合计判出 **28 条**必修/应修（9+3+4+5+3+1+4+1 = 28，另 2 nit）" | **089 行 3 把这两行当 composite/变更集行是行号漂移**（见 §0.2） |

**（c）行首日期造成的**整行豁免**（结构性缺口，建议单列）**：

现跑判据：`sprint-18:48`、`:79`、`:96`、`:101` 四行的 `TIME_ANCHOR.search()` 命中
`2026-09-26` / `2026-09-25` ⇒ **整行 `continue`**。验证方法：把同一行的锚点剥掉后重跑
`scan_text()`，看它是否**本来也**不会命中 ⇒ 对 `:48`／`:101`／`:137`／`:138` 现跑结果
**剥锚后仍是 `hits=0`** ⇒ 这两行的问题是**"没有 pattern"**，不是"被豁免"；
而 `:99`／`:100`／`:104` 则是**双重缺口**（既有 `现 N` 形态无 pattern，又带日期豁免）。

### 5.4 WARN 截断自曝：**已存在**，且行 3 的"前置"论据须据此更正

- **它在哪**：`verify/verify_derived_numbers.py` **`:340-345`**（逐字）：

```python
    for w in warnings[:20]:
        print(f"WARN: {w}")
    # 截断必须**自曝**（复核 `088` minor：WARN 被 `[:20]` 吞掉后，
    # 屏幕上与"只有 20 条"不可区分——静默截断即"没查也说查过了"）。
    if len(warnings) > 20:
        print(f"WARN: … 另有 {len(warnings) - 20} 项未列出（本行是截断计数，不是全部）")
```

- **落地提交**：`28bc182`（`§9.6 欠账收口（2 项）：derived_numbers 的 WARN 截断改为自曝…`）。
- **现跑是否触发**：**不触发**——现跑 `DERIVED-NUMBERS PASS` 且**无 WARN 行**（见 §4.2），
  即 `len(warnings) <= 20`。**判据在位、当前未被行使**。
- **⇒ 行 3 还差什么（明确回答）**：
  1. **不差"可见性"**。复盘 §5 行 9 已修、台账 A 表行 9 已标"已达成"、B 表 E4 已记
     "行 9 是行 3 的前置；已按此序做"。**"行 9 是行 3 的前置"这一论据在机制上成立，
     但它在 §5.4 的意义上已经满足** ⇒ 行 3 的剩余工作**不包含**"先把 WARN 截断修好"。
  2. **差的是 pattern 面**：087 `:48` 的四类形态（`gate_required=N`、`E501=N/N`、
     `N 条反向对照`、`+N/−N、N 提交`）**一类都没进 `agents/policy.json`**。
  3. **差的是豁免口径**：行级时点豁免把"行首带日期的表格行"整体放过 —— 须决定
     "日期出现在行首算不算时点锚"（现口径算）。
  4. **差的是 sprint-18 的残留现值**：§5.3(a) 的 6 行仍与真值不符，且**都不会红**。
  5. **差的是"哪些数应当由现跑派生"的清单**：`sprint-18:99/:100/:103/:104/:134` 与
     `G2 计划书:20/:127` 的现值**应当改成"复现命令 ＋ 以输出为准"**（087 `:48` 的原处方），
     因为它们**无法被单一 pattern 稳定覆盖**（形态是自然语言）。

### 5.5 一处**基准漂移**：`sprint-18:303` 现在**本身是过期的**

现文本（现跑读原文）：

```
L303: | `.github/workflows/ci.yml` 标签写死"含 12 条反向对照自检"（088 nit 原意未达：应改为"见末行 `ALL PASS (N assertions)`"） | 复核 `089` nit | G3 〔087 批时点读数，以现跑为准〕 |
```

但现跑 `ci.yml:127` 已是：

```
Write-Host "gate: verify_decision_register.py  (档位=真数据：docs/6-DECISIONS.md；反向对照条数见末行 ALL PASS (N assertions)，标签不写死)"
```

**归因（git 证据链）**：
```
$ git show 75324f9 -U0 -- .github/workflows/ci.yml | Select-String '^@@'
@@ -90,0 +91,5 @@     ← 关闭税闸门接进关闭前置块（087 minor 6）
@@ -122 +127 @@      ← 含 7 条反向对照自检 → 含 12 条反向对照自检（现跑为准）
@@ -158,0 +164,4 @@   ← 088 major：$LASTEXITCODE 显式归零

$ git log --oneline -S "12 条反向对照" -- .github/workflows/ci.yml 〔087 批时点读数，以现跑为准〕
28bc182 §9.6 欠账收口（2 项）：… ci.yml 去掉写死的'12 条反向对照'标签（088-nit） 〔087 批时点读数，以现跑为准〕
75324f9 关闭三查整改：…

$ git merge-base --is-ancestor 16f4c5d 28bc182 ; echo rc=$LASTEXITCODE
rc=0    ← 089 的覆盖上界 16f4c5d 是 28bc182 的祖先 ⇒ 该修复**晚于** 089 的覆盖
```

⇒ **089 判 `ci.yml:127` 未闭环是对的**（它对 `16f4c5d` 判）；**`28bc182` 之后它已闭环**
（改法正是 088 原意："条数见末行 `ALL PASS (N assertions)`，标签不写死"）。
**但复盘 §5 行 3 与复盘 §5 行 10 仍据此记为"部分已修"／"已修"混排，且
`sprint-18:303` 的引言是过期陈述** ⇒ 这是 **finding 台账自身的基准漂移**，
行使行 3 前**必须先把这两处对齐**（本档只报告，不改状态——状态权威在复盘 §5）。

---

## 6. 前置依赖判定：行 2 的判据**是否挂在 `_norm_sprint` 三处之一上**

### 6.1 判定

**结论：不挂。行 2 的判据与 `_norm_sprint` 无共享实现、无调用关系、无语义重叠。**
⇒ **"必须先完成收敛再挂判据"的理由在本项上不成立。**

### 6.2 证据（逐条）

| 检查 | 命令 | 结果 |
|---|---|---|
| 定义处数量与位置 | `git grep -n "def _norm_sprint" HEAD` | `scripts/agent-ops.py:576`、`verify/verify_close_readiness.py:261`（**2 处**，非 3 处；见 §0.1） |
| 登记册闸门是否引用它 | `grep -n "_norm_sprint\|SPRINT_ID_RE" verify/verify_decision_register.py` | **0 命中** |
| 登记册闸门是否 import `verify_close_readiness` | `git grep -n "^from\|^import" -- verify/verify_decision_register.py` | 只 import `verify.agent_policy.path_in_head`（`:67`）；**不含** `verify_close_readiness` |
| `_norm_sprint` 的消费面 | `grep -n "_norm_sprint(" scripts/agent-ops.py` ⇒ `:631`（`cmd_register`）、`:889`（`cmd_set_sprint`）；`grep -n "sprint_identity_of_doc\|run_sprint_identity\|domain_runs" verify/verify_close_readiness.py` ⇒ `:275,:284,:296,:308,:419,:422,:483,:1191,:1199,:1218` | 全部是**Sprint 身份归一／判定域派生**（M-A） |
| 行 2 的靶点性质 | §2 全表 | 全部是 **Markdown 结构解析**（围栏／表格／字段重复／出处形态／引文窗口）——**与 Sprint 标识归一无关** |

### 6.3 台账 A 表行 2 的行文与本节判定冲突，须择一更正

台账 A 表行 2 逐字写：

> `| 2 | 补齐围栏剥离／表格单元格／重复字段三项判据，各带一例**真入口**反证；**前置**：先做行 7 | 否 | E1 |`

台账 B 表 E1 逐字写：

> `| E1 | 行 2、3、7 | _norm_sprint 三处独立定义：… | **先 7 后 2、3**（判据挂在哪份实现上会改结论） | 修 2 不统一 7 ⇒ 写入侧与读取侧对同一文档给出不同结论，而自检仍全绿 |`

**两处都不成立**，理由有三：

1. **"行 2 的判据挂在哪份实现上"这个前提没有对象** —— 行 2 的判据不引用 `_norm_sprint`（§6.2）。
2. **E1 的反证句也不成立**："写入侧与读取侧对同一文档给出不同结论"描述的是
   **Sprint 身份派生**（`agent-ops` 写入 `sprint` 字段 vs `verify_close_readiness` 读它派生判定域）
   ——那是**行 3 的邻域**（`verify_close_readiness` 的域读数，见 `:1191/:1199`），**不是行 2**。
3. **E1 引的 `verify/verify_agentops.py:1028` 不存在**（§0.1）。

**⇒ 建议的处置（本档不擅自改，交裁决）**：把 E1 的"行 2、3、7"收敛为"**行 3、7**"，
并同步修正 A 表行 2 的前置栏；同时把台账／复盘里的 `agent-ops.py:525` 改为
`git grep -n "def _norm_sprint" HEAD` 的现跑结果（**不写死行号**）。

### 6.4 若仍坚持"先 7 后 2"：代价与收益（供裁决）

| | 先做行 7 再做行 2 | 直接做行 2 |
|---|---|---|
| 收益 | 无技术收益（无共享对象） | 行 2 独立可交付；反例可**当天**跑出 |
| 代价 | 行 2 的 6 个反例**继续无覆盖**（每多一次文档改动，靶点行号再漂一轮） | 与行 7 同批时**触及两个不相关面**，违反台账"同批＝共享对象"的入表口径 |
| 真正的耦合 | **在行 2 内部**：§3.3 的"重复字段 FAIL"与 `_canon_field` 的限定语归一是**同一处解析器的两种行为**（`_canon_field:112` ＋ `setdefault:162`），必须一次改完，否则会从"取错值"变成"静默换值" | | 

---

## 7. 未决点 ＋ 我没做的事

### 7.1 未决点（需要裁决或下一批定的）

| # | 未决点 | 为什么它未决 |
|---|---|---|
| U1 | **"1/3"还是"0/3"** | 087 原文标题句是三项（围栏／表格单元格／重复字段），但 Fix 段列五项。§2.3 现跑结论是"**0/3 被修，1 项（章节行首）被部分改善**"。**复盘 §5 行 2 的"1/3"须按此更正**，否则关闭判据会少做两项 |
| U2 | **表格单元格定为"非条目区"还是"合法载体"**（§3.2） | 两种都可行，但**二选一必须落判据**；现状是"字段判据看不见、指针判据看得见"的双口径 |
| U3 | **行级时点豁免要不要收窄**（§5.3c） | 现口径"行首带日期即整行豁免"让 sprint-18 的表格行结构性免疫。收窄会**立刻让一批历史文档变红**（需 baseline／棘轮配合），不收窄则行 3 永远只能靠人手 |
| U4 | **E1 与 A 表行 2 的前置句如何更正**（§6.3） | 改的是台账（耦合边的唯一权威）与复盘 §5（状态的唯一权威）——本档**无权改**，只能提出 |
| U5 | **`ci.yml:127` 该记"闭环"还是"部分已修"**（§5.5） | 需与复盘 §5 行 3／行 10 的状态对齐；本档只给 git 证据链，不改状态 |
| U6 | **`sprint-18:303` 的过期引言**是否随本批一并更正 | 它是**复盘／台账自己写的**未闭环清单里的一行，本身已过期（基准漂移）——是"发现"还是"记录"须分清 |
| U7 | **087 与 089 都写"±2 窗口"而代码是 `ln-3 … ln+1`**，改文档还是改代码 | §3.5 附带项；两者口径必须统一，否则下一轮复核继续误判 |

### 7.2 我**没做**的事（如实标注）

| 没做 | 原因 |
|---|---|
| **改任何代码** | 本任务硬约束：只写本档一个文件。`verify/decision_register` 的 6 个反例**只测不改** |
| **改 `docs/6-DECISIONS.md`** | 硬约束明令禁止 |
| **任何 `git add` / `commit` / `push`** | 硬约束明令禁止。全程只跑 `git show` / `git log` / `git grep` / `git diff` / `merge-base` 等**只读**命令 |
| **收敛 `_norm_sprint`** | 另一子代理的工作面；本档只做**前置判定**（§6） |
| **重跑 087 的 `probe6_fence.py`** | 该探针**不存在于仓库**（087 落在 `%TEMP%\cr087\`，已随会话消失，见 §1.3）。我用**自建等价探针**（调 `check_register` 真函数 ＋ `%TEMP%` 字面量登记册）复现了同一形态，**证据强度低于原探针**（原探针作者当时在场的 `%TEMP%` 环境我无法复原） |
| **对 §5.3(a) 的 6 行逐条现跑"真值"** | 我只现跑核了 `guard_required`（政策现跑 10 一类读数）与登记册计数；`sprint-18:99/:103/:104` 引用的**各闸门当前断言数**我**没有逐条重跑**（那需要跑 gate_integrity / close_readiness / structure-guard 全档）。**这 6 行的"真值"列中，除本档明确给出的现跑读数外，其余引自 087/089 的当时读数，未经我复核** |
| **核验 `cards/TG-20.md:55` 与 `G2 计划书:20/:127` 的现值** | 只现跑读了文本（`TG-20.md:44` 写"反向对照 = 9 条"、`:55` 写"交付时 **9 条**，二查整改后现跑 **12 条**"；`G2 计划书:20`/`:127` 现文本是"21 例"＋重复嵌套的更正括注，089 `:122-126` 已把它判为"本批改坏的排版"）——**未逐条判定它们当前是否过期** |
| **行 2／行 3 的状态变更** | 状态唯一权威是复盘 §5；耦合边唯一权威是台账 B 表。本档**只补"原文 ＋ 位点 ＋ 判据"**，不越权写状态 |
| **`verify/verify_agentops.py:1028` 的历史考证** | `git log --oneline -S "def _norm_sprint" -- verify/verify_agentops.py` ⇒ **空输出** ⇒ 该文件**从未**有过该定义。`1028` 这个数从何而来（是拿 `agent-ops.py:1028` 或别的行号误记）**我没有继续追** |

### 7.3 本档用到的全部现跑命令（可复现清单）

```powershell
# 取证环境
git rev-parse HEAD ; git status --porcelain
git grep -n "def _norm_sprint" HEAD
git log --oneline -S "def _norm_sprint" -- verify/verify_agentops.py
git show 75324f9 -U0 -- docs/iteration/sprint/2026-09-25-sprint-18.md
git show 75324f9 -U0 -- .github/workflows/ci.yml
git show 28bc182 -- .github/workflows/ci.yml
git merge-base --is-ancestor 16f4c5d 28bc182
git show 9f8d690:verify/verify_decision_register.py

# 闸门现跑（一律用仓库 venv）
.venv\Scripts\python.exe verify\verify_decision_register.py --selftest
.venv\Scripts\python.exe verify\verify_decision_register.py
.venv\Scripts\python.exe verify\verify_derived_numbers.py
```

**结构性反例探针**（本档 §2.1／§3 的输入；探针脚本为临时件，跑完已删除，
形态见 §7.2 行 5；重建方式：`import verify.verify_decision_register as vdr` 后调
`vdr.check_register(ROOT, doc=<%TEMP% 字面量登记册>)`）。

---

## 附录 A：本档对复盘 §5 行 2／行 3 的"可否关闭"判定（不改状态，只给判定输入）

| 行 | 关闭需要什么 | 本档判定 |
|---|---|---|
| **2** | 六条反例（§3.1–§3.5 共 6 项）**全部**变红，且落在 `--selftest` 的对照清单里 | **不满足**：6 项现跑全部通过（FAIL 未发生），自检清单 0 项相关。**且 §6 判定"前置先做行 7"不成立** ⇒ 前置栏应更正 |
| **3** | 087 `:48` 的四类 pattern 进政策 ＋ 行级豁免口径裁定 ＋ §5.3(a) 的 6 行改为"复现命令／时点锚"＋ `sprint-18:303` 基准漂移更正 | **不满足**：四类 pattern **一类未进**（现跑 dump 8 个 id 逐字可核）；6 行**全部仍与真值不符**；WARN 自曝**已在位**（`:344-345`）⇒ "行 9 是前置"**已满足**，剩余缺口在 pattern 面与豁免口径 |

## 附录 B：逐字引证的行号索引（便于第三人复核，不需读全文）

| 本档主张 | 出处文件:行号 |
|---|---|
| 087-minor-5 标题句 | `agents/runs/run-2026-09-25-code-review-087/code-review.report.md:29` |
| 087-minor-5 证据／Fix | 同上 `:30`、`:32` |
| 087-minor-9 标题句／实例／根因／Fix | 同上 `:44`、`:45`、`:47`、`:48` |
| 089 判定 `087-minor-5` = 部分 | `agents/runs/run-2026-09-26-code-review-089/code-review.report.md:160` |
| 089 判定 `087-minor-9` = 部分（含 §0.2 的不成立清单） | 同上 `:164` |
| 089 的覆盖上界 `HEAD = 16f4c5d` | 同上 `:397` |
| 复盘 §5 行 2／行 3（摘要，本档的触发点） | `docs/iteration/phases/testing-governance/2026-09-26-g2-close-retro.MD:100-101` |
| 台账 A 表行 2／行 3 前置信 | `docs/iteration/phases/testing-governance/2026-09-26-g2-closure-ledger.MD:30-31` |
| 台账 B 表 E1（"三处定义"＋"先 7 后 2、3"） | 同上 `:57` |
| 台账 B 表 E4（行 9 是行 3 的前置，已按此序做） | 同上 `:60` |
| WARN 截断自曝实现 | `verify/verify_derived_numbers.py:340-345` |
| 章节行首判据（唯一被改的一处） | `verify/verify_decision_register.py:197-202` |
| 重复字段洞 `setdefault` | 同上 `:162` |
| "会话"子串洞 | 同上 `:224` |
| 指针全文扫 | 同上 `:266-268` |
| 引文窗口洞 | 同上 `:327` |
| 自检 14 条 `ok(...)` 行号 | 同上 `:424,426,429,433,438,450,480,490,495,500,507,512,516,521` |
| `derived_numbers` 政策块 | `agents/policy.json::derived_numbers`（消费者 `verify/verify_derived_numbers.py:61`） |

---

## 附录 C：实现记录（2026-09-26 G2 关闭批；**只追加，不改上文任何章节**）

> **取证时点**：`windows`，`HEAD = 64c5288`，`git status --porcelain` 只含本批与并行批的改动。
> 本节所有读数**一律以给出的复现命令现跑为准**；凡带数字的行都同行给出命令或"以现跑为准"锚，
> 不写死断言数/条数（`1-WORKFLOW.MD` §6）。

### C.1 行 2（`087-minor-5`）：五项**全部落地**，六条反例全部变红并进 `--selftest`

| 087 `Fix` 段要求的五件事 | 实现 | 修前假绿 → 修后变红（同一条真入口） |
|---|---|---|
| ① 围栏感知 | `_strip_fences()`：先剥 ` ``` `/`~~~` 块，章节／条目／字段／指针一律只看围栏外（行号不漂） | 9 个章节标题**只写在围栏里** ＋ 一条真条目：修前 `problems=[]`（判决 PASS）→ 修后逐条点名缺章节（`--selftest` 对照 `j`） |
| ② 条目落在章节行区间 | `_section_spans()` ＋ 每条按 `line` 判归属（取代 ID 前缀统计） | `### V-002` 落在 `## 4.2` 区间：修前 `problems=[]` → 修后 FAIL 点名（对照 `l`） |
| ③ 重复字段 FAIL 并点名 | `parse_entries()` 返回 `dups`；**契约字段**归一后同名第二次出现 ⇒ FAIL 点名（非契约字段如 `备注` 允许重复） | `- 原文` 写两次、以及 `原文（逐字，选项式）` 与 `原文` 撞车：修前都 `problems=[]` → 修后都 FAIL 并点名 `原文`（对照 `m`/`n`）；对照 `o` 钉住"非契约字段不误报" |
| ④ "会话"精确形态 | `SESSION_RE = ^\**\s*会话`（形态，不是子串） | 出处写"这不在会话里"：修前 PASS → 修后 FAIL（对照 `p`）；对照 `q` 钉住真语料同形不误报 |
| ⑤ 引文报**实际命中行号** | 窗口内命中但指针行未命中 ⇒ 计入 `stats.quotes_drift` ＋ WARN 报出实际行号（截断自曝） | 指针 `:3`、引文实际在 `:1`：修前静默通过且打印 `:3` → 修后仍放行（±2 是规范窗口）但 WARN 报 `:1`（对照 `t`） |
| （同族）围栏内示例指针不计入 | 指针域改为"围栏外全文" | 围栏里写示例 `path:line`：修前计入 `pointers`（并参与引文分流）→ 修后与干净 fixture 同数（对照 `k`） |

**一处**口径更正（写在此处，不改上文）：§3.5 把 `lines[max(0, ln-3):ln+2]` 读成"`ln-3 … ln+1`"
⇒ 认为与 087/089 的"±2"不符。切片下标是 **0-based**，换成 **1-based 行号**正是 `ln-2 … ln+2`，
**就是 ±2** ⇒ 087/089 的"±2"正确，§3.5 的这段读法差一位。本批据此**保留 ±2**，
只补"实际命中行号可见"（判据不放宽、不收紧）。

**表格单元格定档（§3.2 的 U2）**：采用 (a) 的**字段侧**（表格不是字段载体 ⇒ 表格写的字段按缺字段 FAIL，
对照 `r`），但**不采用它的指针侧**——实测真登记册的指针落在表格行上的有 **475 / 1248 处**
（`POINTER_RE` 在真文件上现跑即得），把它们排除出指针域等于把 38% 的核对面静默缩小 ⇒ 违反"不许放宽"。
故定档为：**表格不在条目区、但在指针域内**，并用对照 `s`（表格里的坏指针仍判 FAIL）把该定档钉住。

**真数据回归（复现命令：`.venv\Scripts\python.exe verify\verify_decision_register.py`）**：
修后**判决行不变**（条目/指针/引文核对三项读数与修前逐字相同，以该行现跑为准），
新增的只是 `±2 内漂移` 计数与对应 WARN —— 这是**新可见的既有事实**，不是新红项：
真登记册有若干条 §4.1 条目的逐字引文落在指针的 ±2 行内而**不在指针行上**（条数与条目号见该闸门现跑 WARN），
按 087 的措辞属"漂移可见"，故记 WARN 不判 FAIL；若要把它变成 FAIL，需要先把这些指针改准（本批无写权）。

### C.2 行 3（`087-minor-9`）：四类 pattern 落地 ＋ 锚口径裁定 ＋ **爆炸半径实测**

**（1）四类 pattern（087 `:48` 的字面形态，逐条对应 id）**

| 087 的字面形态 | 新增 id | 正则要点 |
|---|---|---|
| `gate_required=N` | `gate_required` | 同时认仓内实际写法 `guard_required` |
| `E501=N/N` | `ratchet_pair` | 与 `ratchet_counts` 用**负向前瞻**分工：成对写法记 1 处、单值写法仍记 1 处（不双计、覆盖面不减） |
| `N 条反向对照` | `replay_count` | 只认 087 的字面序 |
| `+N/−N、N 提交` | `diffstat` | 只认变更集复合式 |

**（2）锚口径两处**：① sha **区间**（`` `sha1..sha2` ``）与单个 sha 同等有效 ——
旧正则认不出区间，对"变更集 + `sha1..sha2`"这种**合规**写法是假阳性（`--selftest` 对照 `N4` 钉住）；
② **豁免只到行级、必须同行可见**（原先无对照，现有对照 `N3`）。

**（3）"行首日期算不算时点锚"的裁定**（§5.3(c) 的 U3）：**仍算**（含 sprint 表格行的首个日期单元格）。
依据是实测的两条代价（复现方式：把候选锚正则注入 `scan_text()` 后跑 `--report` 对比逐文件命中数）：
"日期必须伴随声明" ⇒ 一批历史文档立刻判红（棘轮无余量）；"行首表格日期不算锚" ⇒ 也会判红一个文档，
而那一行的日期本就是该行自己的时点（属假阳性）。两者都需要**专门的文档清理批次**，
不能靠收窄判据来替代 —— 故本批只裁定口径并落对照，**不收窄**。

**（4）爆炸半径（新增前后判红数；复现命令：`.venv\Scripts\python.exe verify\verify_derived_numbers.py --report`）**

| 口径 | 命中文件 | 命中处 | 判红文档 |
|---|---|---|---|
| 修前（八类 pattern ＋ 旧锚） | 31 | 110 | **0** |
| 修后（十二类 ＋ sha 区间锚） | 36 | 123 | **3**（见下） |

**新增判红 = 3 个文档、9 行，全部在本批授权写入集之外**（逐行原文与修复动作）：

| 文档:行 | 形态 | 性质 | 需要的动作 |
|---|---|---|---|
| `2026-09-26-087-minor-5-9-disposition-spec.MD`（本节所在档）7 行 | `+5585/−148、25 提交` ×1、`guard_required=9` ×3、`12 条反向对照` ×3 | 引文/诊断表：本档转述 087/089 原文的读数 | 按本档 §1.2 已有的约定，在行尾补 `〔087/089 时点读数，以现跑为准〕` |
| `2026-09-26-g2-close-retro.MD:23` | `+5585/−148、25 提交` | 更正说明；它的锚（sha 区间 ＋ "现跑"）在**上一行**，整句跨两行 | 把锚点并到同一行（或整句并成一行） |
| `cards/TG-19.md:153` | `guard_required=9` | **真·过期读数**（现值以 `verify_gate_integrity.py` 末行为准） | 改成"以该闸门末行现跑为准" |

**（5）未采纳的扩展（实测代价，供后续批次裁定；不是"为变绿而收窄"）**：
反序形态 `反向对照 N 条` ⇒ 另判红 4 个**已无棘轮余量**的文档（`cards/TG-17.md` 3→6、
`cards/TG-20.md` 0→1、`sprint-17.md` 1→2、G2 计划书 0→1）；裸 `+N/−N` ⇒ 另判红 3 个同类文档
（`3-LEARNED.MD` 8→9、`sprint-9.md` 2→3、`sprint-16.md` 6→7）。
两者都会造出"棘轮无余量、判据又无法在批内收平"的死结（`verify_gate_integrity.py` 禁止上调既有 cap），
故**列为扩展、待文档清理批之后再做**。

**（6）本批明确没有做的事**：`baseline` 一个键都没加、一个数都没改
（本批对 `agents/policy.json` 的授权是"只加 087 要求的 pattern 与必要口径"）⇒
`verify_derived_numbers.py` **当前对上述 3 个文档判红**。两条收口路径都留在此处备裁定：
**甲** 授权改那 3 个文档的 9 行（改动本身是一行一处、可逐字复核）；
**乙** 走一次具名重评基线（3 个键 ＋ `measured_value`/`measured_at` ＋ `rebased_at`/`rebased_reason`）——
但那是"给新文档发配额"，与 `default_cap` 的"新文档一处都不许有"口径相冲，**需要用户裁定**。

### C.3 本批交付的复现命令清单

```powershell
.venv\Scripts\python.exe verify\verify_decision_register.py            # 真数据 + 自检（末行给断言数与漂移数）
.venv\Scripts\python.exe verify\verify_decision_register.py --selftest # 只跑反向对照（a~t）
.venv\Scripts\python.exe verify\verify_derived_numbers.py              # 真数据（当前对 3 个文档判红，见 C.2(4)）
.venv\Scripts\python.exe verify\verify_derived_numbers.py --report     # 逐文件命中数与判据 id（爆炸半径的复现入口）
.venv\Scripts\python.exe verify\verify_derived_numbers.py --selftest   # 只跑反向对照（含四类与锚口径）
```

## 附录 D｜引文完整性声明（2026-09-26 父代理追加）

本档 7 处行尾的 `〔087 批时点读数，以现跑为准〕` 是**父代理追加的时效标记，不属于 087 原文**；
被标记行内的逐字引文与数字**一字未改**。标记的作用仅是声明"该读数为其时点主张、现跑值以闸门为准"，
以满足 `verify_derived_numbers.py` 对当期读数的判据（该判据原先判本档 7 处为过期主张）。
