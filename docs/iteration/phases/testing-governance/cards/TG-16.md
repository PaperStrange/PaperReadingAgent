# TG-16 **审查流程与策略内容"整体过一遍" → 图文并茂 HTML 汇报**（用户 2026-09-23："我想需要对现有审查

- `card`: TG-16
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**候选（用户 2026-09-23 要求开卡）**

## 规模
3

## 来源
**用户 2026-09-23（UTC+8）**："先这样做吧，我想需要对现有审查流程和具体策略内容找时间整体过一遍 记张卡把这些内容规则流程梳理作为一个backlog，输出图文并茂容易理解的html汇报文件"

## Sprint
待排期（建议：素材可在 G1 期间并行收集，**HTML 定稿放 G1 关闭后**——届时的流程图/闸门矩阵才包含 G1 全部落地物；若用户要先看"现状版"，可提前单独出一版并在文件头标注快照日期）

## 正文
`[用户插入]` **审查流程与策略内容"整体过一遍" → 图文并茂 HTML 汇报**（用户 2026-09-23："我想需要对现有审查流程和具体策略内容找时间整体过一遍，记张卡把这些内容规则流程梳理作为一个 backlog，**输出图文并茂容易理解的 html 汇报文件**"）：**用途**＝让用户**一次看懂**"我们到底有哪些审查动作、各挂在什么触发点、哪些是真闸门、哪些只是文字、策略参数放在哪、红线怎么生效"，作为后续治理决策的输入。**与其他卡不同族**：TG-11/TG-12/TG-14/TG-15 治"闸门/漂移/拓扑/泛化"，**本卡只治"全景可读性"，不改任何规则**（改规则一律另开卡）。

## 证据
**卡内待产出**：① **内容清单（逐条可核）**：审查动作全景（开工〇查 → 一查 doc-audit / 二查 code-review×2 → lessons → workspace-check）、**修复验证复核 vs 关闭三查**的区别与记录分节、`agents/fanout.json` 两条流水线、**每条规则的执行点**、闸门清单（`verify/*.py` 各自判据）、策略参数位置（`fanout.json` / spec 的 Configurable Parameters / `providers/*.json` / env / `prices.json`）、红线三段（S0 观测 / S1 仅 R1 硬停 / S2 全量）与升级条款、指标 M1~M3、卡↔Sprint↔文档拓扑（含 TG-14 落地后的 `cards/` 形态）、账本字段与证据链（`scope_ref`/`coverage_anchor`/`rounds`/`int`）。② **每条规则必须标注 `执行点=`**：有闸门（写脚本名+判据）／**无执行点**（显式标红）——**这是本卡最重要的判据**，直接服务用户"策略内容过一遍"的目的。③ **输出：单文件 HTML**（离线可开、内联 CSS + 内联 SVG 图形，**零 CDN/零外链**）：五步 fan-out 时序图、红线 S0/S1/S2 决策树、**规则×执行点矩阵表**、闸门×判据表、术语表、开放问题页。④ **一致性**：术语依 `1-WORKFLOW.MD` §6 文档规范（`台账`=独立文件）；数字**引用 SSOT 或标注实测日期**，不手抄派生事实（TG-12 纪律）。⑤ 附"待用户裁定的开放问题"页（已答项不列）。**验收/反向对照**：a) 断网打开必须完整渲染（无外链请求，可用 DevTools/`--disable-network` 实测留证）；b) 清单自检：每条规则行**必须**含 `执行点=` 字段，缺一条即失败；c) 随机抽 3 条规则，从 HTML 回溯到**实际代码/配置位置**必须命中（可核性）。
