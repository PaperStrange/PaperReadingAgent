# TG-10 **多轮次 agent 的账本记录失真（可观测性缺口）**：同一 run 被追加多轮复核（run-053 共 5 轮）时

- `card`: TG-10
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**v1 ✅ 完成（2026-09-21）**；③ 看板 UI 展示待排期

## 规模
2

## 来源
**用户 2026-09-21（UTC+8）**："每个 agent 运行时间相比之前怎么短了很多？而且有的是直接中途被停掉了，这些信息让我不太安心"；同日批准"做掉"

## Sprint
Sprint-16（v1 已交付）／③ 看板 UI → Sprint-18（G2）

## 正文
`[用户插入]` **多轮次 agent 的账本记录失真（可观测性缺口）**：同一 run 被追加多轮复核（run-053 共 5 轮）时账本只留**首轮**（53 秒 / 12800 字符，实际跨约 4h53m），因 `finish` 状态机拒绝 `succeeded→succeeded` → 看板严重低估时长与产出、看不出被中断。

## 证据
**v1 证据**：`agent-ops.py` 新增 `round`（终态 run 可追加：首轮快照 `rounds[0]` + 产出累加 + `ended_at` 前移 → 累计时长）与 `interrupt`（何时/谁/原因/影响）；`list` 增 `rounds=/dur=/int=`；回归 `verify_ledger_rounds.py` **13 断言**；**已回填 run-053** → `rounds=5 dur=241.3m int=2`（历史轮次字符数未采集，note 中如实标注）。**剩余 ③**：看板 UI 展示 rounds/dur/int。
