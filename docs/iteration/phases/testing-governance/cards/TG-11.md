# TG-11 **规则绕过复盘 + 可执行闸门（用户 2026-09-21 定性为"很严重"）**：2026-09-20/21 我以"

- `card`: TG-11
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**进行中（Sprint-17）**：③(a) **CLI 闸门已落地**（`register --scope-source impact-assessment:<run_id>` / 自选须 `--deviation "<≥10 字符>"`，账本落库 `scope_source`/`scope_deviation`；`verify_agentops.py` **UC-14 八条断言** + **反向对照实测**：旧实现放行无 scope 的评审 run（rc=0）且不认 `--scope-source`）；复盘与 ④ 关闭闸门待做

## 规模
3

## 来源
**用户 2026-09-21（UTC+8）**："这是一个很严重的问题，规则直接被绕过，在 workflow 记再多也没用，需要单独开卡复盘下"

## Sprint
Sprint-17（治理批 G1）

## 正文
`[用户插入]` **规则绕过复盘 + 可执行闸门（用户 2026-09-21 定性为"很严重"）**：2026-09-20/21 我以"修复验证复核"名义**自选窄范围**跑了 5 轮子代理复核（run-053 R1~R5），而 `agents/functions/code-review.md:26/:34` 明文 `scope` **必须来自〇查 impact-assessment**（`never a self-chosen narrow scope`）、`1-WORKFLOW.MD` §4.2 要求**先跑〇查**且"偏离须在 §9 记理由"——**两处都被绕过，且我是在用户追问后才发现**。后果：① 二查覆盖缺口（`2761eb2`/`cce060a`/`2b4f354`/`502065e`/`bb80ea3`/`bdc669b` 六个提交无按范围评审）；② 用户需靠追问才识破；③ Sprint-16 HOLD。**用户的关键判断：再往 workflow 写规则没用——规则被绕过时没有任何东西会拦下来 → 本卡交付必须含"可执行闸门"，不是文档。**

## 证据
**卡内待产出（复盘，不是写规则）**：<br>① **事实与根因**：区分"**未读到规则**"vs"**判断越界**"——可核事实：本轮我引用过 §4.2 的 Timebox 纪律（说明读过该节片段），但**三查表的 scope/〇查前置条款直到用户追问才第一次读**；两种根因对应不同对策，须在复盘里定论。<br>② **可检测性分析**：当时是否存在任何检查能发现？现状=**没有任何执行点**（三查是否跑过只在 §9 被人工阅读时暴露；CI 不查；账本不记 scope 来源；PR 前置也不查）→ 这是"绕过"的结构性原因。<br>③ **机制候选（必须可执行）**：(a) 评审 run 的 scope 来源机器可验——`agent-ops register --scope-source impact-assessment:<run_id>`；自选 scope 必须显式 `--deviation "<理由>"`，否则 **CLI 拒绝**或标记 `scope_source=self-chosen` 并在 `list` 高亮；(b) **关闭前置闸门脚本**（offline `verify/verify_close_readiness.py`）：校验本 Sprint ① 存在〇查 run 且**早于**二查 ② 二查含 **windows+main 两个 run** ③ 一查 doc-audit 与 lessons 各一 run ④ §9 的 run 表与账本**交叉一致**（防"写了没跑"）⑤ 每个自选 scope 的 run 都有 deviation 声明 → 任一缺失**非零退出**，并接入 PR 前置/CI；(c) **§9 结构化 run 表**（run_id/role/scope_source/覆盖范围/偏离声明）供 (b) 解析。<br>④ **闸门自身的验收判据**：必须有**反向对照**——故意省掉〇查、或只跑 windows 单分支、或 §9 写了但账本无 run 时，闸门必须 FAIL（沿用 `TG-6` ⑤"倒过来试"规则）。<br>⑤ 复盘结论**可否决或修改**我在 `1-WORKFLOW.MD` §4.2 新加的"修复验证复核"定义（文档不是对策，但它是否该存在/如何写由复盘定）。<br>📄 **证据台账（2026-09-21；⚠️ 这不是复盘结论——用户明确"开卡以后复盘，不是现在就做"，正式复盘在卡执行时进行）**：`docs/iteration/phases/testing-governance/2026-09-21-review-scope-incident-evidence.MD` —— 事实时间线（证据：registry `run-048~052` 五步 task_id 均为 `Sprint-16 close`；`run-053` 自述 `user walkthrough pre-check`；`origin/windows..HEAD` 22 提交中 **20 条 committer 时间晚于三查结束 2026-09-12T23:57:06+08**）；**根因五条**：**R1 规范无"三查失效/锚点"概念**（全文 grep 无命中，主因）、**R2 无任何执行点**（CI/套件/矩阵/账本/PR 前置全不校验覆盖关系，唯一途径是人读 §9）、**R3 scope 规则不可执行**（账本无 scope 字段、无"无〇查则拒派"装置）、**R4 §9 把关闭三查与走查期复核写在同一节 → 结构在替违规背书**、**R5 规则检索未发生**（"任务分类先于规则检索"：规则挂在「关闭」语境，违规发生在「修复后复核」动作语境 → 不命中）；**对策**：M1 scope 来源入账本（`--scope-source`，`self-chosen` 必须 `--deviation`）、M2 `verify/verify_close_readiness.py` 关闭前置闸门（含**三查锚点**校验：锚点之后不得有未评审提交）、M3 §9 结构分区 + 结构化 run 表、M4 接入 CI offline / PR 前置；§4 给 6 个**反向对照**场景。**待拍板/修订**：**D1 用户 2026-09-21 修订 = 方案保留、S16 期间不做任何代码改动**（"刚刚你建议的方案可以保留，但是不要做任何代码改动"→ 实现待正式复盘后另开）、D2 = 两者结合（默认拒绝 + `--force` 强制 `--deviation`，作为方案参数保留）、D3 锚点是否入 §6 DoD 必填、D4 §9 分区是否只对新文档生效。**本轮代码零改动**（曾起草 `scripts/agent-ops.py` 的 `--scope-source` 闸门与 `verify_agentops.py` 断言，**已全部回退**，仅保留本卡与证据台账）。
