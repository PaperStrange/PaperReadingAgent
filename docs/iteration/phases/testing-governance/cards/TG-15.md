# TG-15 **闸门泛化：三不变式 + 数据驱动（替换"逐场景加条件"式硬编码）**（用户 2026-09-23 批准 P1="可以

- `card`: TG-15
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**候选（用户 2026-09-23 批准开工；Sprint-17 内实现）**

## 规模
2.5

## 来源
**用户 2026-09-23（UTC+8）**："P1：可以，建议复查下类似场景的硬编码问题"；"P2：C3-T，但是有个担心，**是否归属表未及时更新同样导致 C3 闸门失效**"

## Sprint
Sprint-17（治理批 G1）

## 正文
`[用户插入]` **闸门泛化：三不变式 + 数据驱动（替换"逐场景加条件"式硬编码）**（用户 2026-09-23 批准 P1="可以"，并追加要求"复查下类似场景的硬编码问题"）：**问题**——闸门现在是"遇到一个场景加一条判据"：`scripts/agent-ops.py:67` 硬编码 `_REVIEW_ROLES={"code-review","doc-audit"}`、`:258` 认死 `role=="impact-assessment"`、`--deviation ≥10 字符`、状态白名单；`verify/verify_close_readiness.py:37,87-119` 硬编码 `CLOSE_ROLES` 与 6 条按角色的判据，且要求人**手抄** `三查锚点` 进 Sprint 文档；`verify/verify_lint.py:35` 硬编码覆盖路径清单 `DEFAULT_PATHS`；`scripts/report-freshness.py:197` 认死 `startswith("tech-research")`；`verify/verify_agentops.py:40` 又存一份角色集合。→ 新角色/新分支/新步骤/新失效形态**都要改代码或手改文档**。

## 证据
**卡内待产出（①~⑧）**：① **三条不变式替换现有判据**——**C1 声明完备**（凡产出评审结论的 run 必须有 scope 声明：外部引用或自选理由）、**C2 指涉可核**（任何外部引用必须解析到"存在且处于可用终态"的对象，不认角色名）、**C3 覆盖闭环**（`git rev-list <锚点>..HEAD` 的**每个提交**必须有归属）。② **需求只来自两个数据源**：`agents/fanout.json` 的 `sprint_close_pipeline`（哪些步骤/角色/target 必填）＋ 各角色 spec frontmatter 的 `scope_required: true`（复用 `validate-spec` 既有 frontmatter 解析）→ 加步骤/加角色**只改数据**。③ **锚点自动化**：run 在 register/finish 自动记 `coverage_anchor` / `covers_through`（`--coverage-anchor <sha>` 可显式覆盖），Sprint 文档**不再手抄锚点**；`covered(run) = (anchor, covers_through]`。④ **C3-T 例外表（用户选型）＋防"表过期"三件套**：(a) 覆盖**默认由 run 数据计算**，表**只登记例外**；(b) 例外**sha 钉死**、禁止模式/路径通配 → 不可能覆盖未来提交；(c) **表落后 = 默认 FAIL 并逐条点名未归属 sha**（失效方向反转：不是静默变绿）。⑤ `agent-ops close-sync` 自动生成候选行（未归属提交 + 改动文件 + `doc-only` 自动归类；`doc_only_globs` 规则放**数据文件**），人只做"挑类别/写理由"。⑥ **push 时即检**：覆盖检查进 CI（条件由数据推导），漂移在**一次 push 内**暴露，而非关闭时才发现。⑦ **硬编码政策闸门** `verify/verify_no_policy_hardcode.py`（offline）：扫 `scripts/**`+`verify/**`（豁免 fixture/选择器白名单，白名单为数据文件且每条须写理由），发现"角色名/覆盖路径清单/计数常量/阈值"类政策硬编码即 FAIL；**反向对照**＝注入 `_ROLES={"code-review"}` 必须 FAIL。⑧ **规则入 `1-WORKFLOW.MD` §6（政策数据化）**：脚本内不得把政策写成常量，政策必须在数据文件；并改造上列 6 处存量。**反向对照要求**：变异用例**从 `fanout.json` 自动生成**（每个必填步骤/字段各抽掉一次 → 必须 FAIL），不逐场景手写。
**结算结论（2026-09-25，D-prep 批复核）**：`open majors = 0`（`N1`~`N7` 逐条可核闭环）⇒ 按 [`2026-09-21-sprint-17.md`](../../../sprint/2026-09-21-sprint-17.md) §4 的扣分条件"审核若判 critical/major 且未闭环 → 按级别继续扣"**无适用项 → 2.5 点不扣**（算式 `2.5 − 0.5×(棘轮未闭环=0) − (未闭环 critical/major=0) = 2.5`），逐条闭环依据见 [`2026-09-25-open-items-check.MD`](../2026-09-25-open-items-check.MD) §O5。⚠️ 该结论有一处**已知未裁定口径**（同文件 §O5 末）：⑥ 的状态是"已裁定顺延"，若按"⑥ 未交付 = 本卡未闭环"计则上面的 0 分不成立——但那样也**缺扣率**（空洞记录见 [`2026-09-21-governance-batch-plan.MD`](../../../sprint/2026-09-21-governance-batch-plan.MD) §2.1.1，待用户裁定）。
