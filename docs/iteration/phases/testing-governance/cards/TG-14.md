# TG-14 **一卡一文档：卡片正文独立成文 + backlog 瘦索引 + 索引 lint**（**由 `TG-12` 机制候选 

- `card`: TG-14
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**✅ 完成（2026-09-23，D3）**：95 卡全部迁完（4 阶段 × 一卡一文件）+ 4 份 backlog 瘦索引化；`verify_card_index.py` 宽松档与严格档**双 PASS**；`verify_md_tables.py` 对迁移产物 PASS。
（原状态：**候选**——用户 2026-09-21 拍板"提升为独立卡"。**状态由本文件承载，瘦索引行同步**。）
**迁移后卡数仍在增长**（迁移只保证"当时那 95 张一张不丢"）：同日新开 `A-M12`（编辑边界事故族治理）→ 当前 **96 卡**；基线 `agents/runtime/tg14-baseline.json` 已同步刷新并注明用途变更（迁移凭据 → 卡库存当前真值）。

## 规模
3

## 来源
**用户 2026-09-21（UTC+8）**："N7 是否提升为独立卡：可以"

## Sprint
Sprint-17（治理批 G1）

## 正文
`[用户插入]` **一卡一文档：卡片正文独立成文 + backlog 瘦索引 + 索引 lint**（**由 `TG-12` 机制候选 N7 提升为独立卡**，用户 2026-09-21 拍板）：用户原话——"`phases` 下的所有文件夹，**单个卡片应该对应且唯一对应一个独立文档**。现在卡片内容全都记录在 backlog 中，有的卡片内容散落在 sprint 文档中到处都是。一旦卡片内容更新了其他几处万一有的地方漏了就直接造成文档内容漂移"（另追问"你经常说实例台账我咋没找到是啥文档"）。**现状证据**：卡片正文全在 backlog 单元格内，体积已失控——**`A-M11` 单格 7.9 KB、`TG-12` 4.1 KB、`TG-11` 2.7 KB**；本轮穷举不得不把明细外置成两份分析文档才没让它继续膨胀；同一事实散在"backlog 单元格 / Sprint §3/§7/§9 / 验收指南 / 状态报告 / PR 文案 / 卡状态"多处 → Sprint-16 内实测 **5 波、11+ 处漂移**。**与 TG-12 不同族**：TG-12 的 N1/N2 解决"**事实从哪来**"（派生事实 SSOT），本卡解决"**卡与文档的拓扑**"。

## 证据
**卡内待产出**：① `phases/<阶段>/cards/<卡号>.md` **一卡一文件**（状态/规模/来源/Sprint/正文/证据/台账各自成节）；② `backlog.MD` 退化为**瘦索引**（卡号｜一句话｜状态｜Sprint｜文件链接）；③ Sprint 文档/roadmap **只按卡号引用**，不得复制卡正文；④ **`verify/verify_card_index.py`（offline，纳入套件）**：索引行 ↔ 卡文件**一一对应**（无孤儿/无重复）、卡号 ↔ 文件名一致、状态与 Sprint 引用一致、**Sprint 文档出现卡正文特征即 FAIL**、**表格表头↔各行单元格数一致（`markdownlint` MD056 形态；2026-09-21 实测 3 处已修，见计划 §2.3 实测实例）**；⑤ **反向对照**：删一份卡文件 / 改一处状态 / 在 Sprint 文档塞入卡正文 → 必须 FAIL；⑥ **迁移**：先试点 `testing-governance`（**17 卡**，卡数适中）→ 验证形态后批量（**全仓 95 张卡**）；⑦ 术语已即时生效（`台账` 仅指独立文件，见 `1-WORKFLOW.MD` §6 文档规范）。<br>**卡数口径（2026-09-23 D3 起改为可复现实测，并当场抓出前值漂移）**：命令 `python scripts/card-inventory.py` → **95 张**（`refactor-analysis 47` / `agents-infra 25` / **`testing-governance 17`** / `mental-models 6`；`agents-infra` 另有 1 行 `—` 占位不计）；基线落 `agents/runtime/tg14-baseline.json`（迁移前后 diff 用，证明"一张卡都没丢"）。**原写"94 张 / TG 16 卡"是手抄事实，`TG-17` 开出后即过期**——正是本卡引述的那类漂移。
