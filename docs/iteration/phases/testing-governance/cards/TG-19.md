# TG-19 **关闭税机制化：判定域绑定 / 反空转不变式 / 棘轮完整性 / 关闭税读数派生**（来源＝Sprint-17 关闭期 6 轮独立复核的机制缺口 ＋ G2 kickoff 〇查 `run-2026-09-25-impact-assessment-081` 的四条提案 ＋ 主代理 2026-09-25 实测的"跨 Sprint 窗口翻转"事故）

- `card`: TG-19
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**进行中（2026-09-25 D1 起交付 M-A/M-B；用户 B4② 排进 G2，本卡为 G2 头号机制卡）**

## 规模
2

## 来源
- **用户 2026-09-25 裁定 B4②**：`TG-18` ＋ 复核给出的机制缺口排进 G2（口径真源见 [`2026-09-21-governance-batch-plan.MD` §6](../../../sprint/2026-09-21-governance-batch-plan.MD)）。
- 输入证据：Sprint-17 关闭期独立复核 `run-2026-09-25-code-review-074`~`-079` 末尾的 backlog 小节，＋ `-080` 的 F1（SKIP 后仍印 PASS 横幅）/F2（死键放宽过宽）——F1/F2 已登记为 [`TG-17`](TG-17.md) 条目 6/7，本卡承接**其余机制缺口**。
- 〇查 [`run-2026-09-25-impact-assessment-081`](../../../../../agents/runs/run-2026-09-25-impact-assessment-081/impact-assessment.report.md) §4：机制提案 M-A/M-B/M-C/M-D ＋ 根因族 A~G。
- 主代理 **2026-09-25 实测**（本卡 M-E 的来源）：Sprint-17 的关闭税读数把**上一个 Sprint** 的数字抄进了本行（文档项 `12` 应为 `11`、"19 条"应为 `21`），且算术自洽到连一查都没抓到——口径与复现命令见 [`2026-09-21-sprint-17.md` §9.5](../../../sprint/2026-09-21-sprint-17.md)。

## Sprint
Sprint-18（治理批 G2）——**硬次序**：M-A/M-B 必须在 G2 的**任何评审 run 之前**落地；否则 G2 自己的关闭读数会被同一形态的假红淹没（M1 是"关闭期产出"，机制若在关闭期才落，就无法被 G2 自己复测）。

## 正文
**为什么要成卡**：关闭税（关闭期判出的必修/应修条数）是 M1 的头号复测指标，但**产生它的闸门本身**有四类系统性失真——2026-09-25 一天之内，这四类在真数据上全部实测到，且每一类都有"检查在说假话"的形态（不是"没查到"，而是"报了错的话"）：

| 族 | 形态 | 本日实测实例 |
|---|---|---|
| **D 判定域** | 闸门按窗口过滤时取**全局最新**的作用域 run，无 Sprint 身份 ⇒ 下一个 Sprint 的 kickoff 〇查一登记，上一个 Sprint 的窗口即作废、判定域退回全域 | Sprint-17 关闭读数 **1 项 → 99 项**（其中 98 项假红） |
| **C 空转** | 空扫描集、`-O` 下裸 `assert` 被剥离、SKIP 后仍打印 PASS 横幅 ⇒ 闸门在"什么都没查"时仍然绿 | 同一份政策下，无 `docs/iteration/` 的树里 `md_tables` **应扫 8 / 实扫 8 PASS**（windows 为 164） |
| **B 上限** | 棘轮 `cap` 只与 `git HEAD` 比较 ⇒ 一次**已提交**的放宽即成为新基线；`cap` 高于实测值等于给新增违规发静默配额 | 机制缺口 2；`docs/3-LEARNED.MD` 1.66 |
| **A 派生数字** | 关闭税读数靠手抄 ⇒ 抄成上一个 Sprint 的值，且因两个 Sprint 的 must-fix 数相同而**算术自洽** | 文档项 `12→11`、合计"19 条"`→21`（§9.5） |

**治的是"闸门可信化"的最后一层**：`TG-15` 解决了"逐场景硬编码"，`TG-17` 解决了覆盖窗口语义与 C1 判定域，本卡解决**判定域的来源、空转的不可见、上限的自证、读数的派生**——四者共同决定"关闭税这个数字能不能信"。

## 证据 / 交付物（4 个机制；每条都必须按计划书 §2.2.2 的 6 项标准交付：证据／根因／机制／反向对照／**在原始事故上验证**／复用面，**缺项即退回**）

**M-A 判定域绑定**（治族 D；本卡必做、D1 交付）
- **检查什么**：任何按"窗口/域"过滤的闸门，其域**必须由被判定物派生**——Sprint 文档自己的 `三查锚点` ＋ **该 Sprint 自己的**关闭流水线 run；**不得**取"全账本最新一条作用域 run"。域不可派生时 **fail-closed 且报 distinct exit code（`domain-unavailable`）**，不得退化成 finding 清单；一旦退化为全域，必须单独打印 `[domain-fallback]` 行（可被机检）。
- **数据在哪**：`agents/policy.json::close_gate` ＋ Sprint 文档的 `三查锚点: <sha>` ＋ 账本 `started_at`/`role`/`target`。
- **反向对照**：① 在流水线之后追加一条作用域 run（= 本日 081 的原始事故形态）→ 域仍可派生、**不得**产生 99 项；② **域的两个端点都必须来自该 Sprint 自己的记录**——左端 = 该 Sprint 的作用域 run，右端 = 该 Sprint 关闭期的最后一条覆盖 run；**关闭锚点之后**新增的提交属于**下一个 Sprint**，不得计入本 Sprint 的 C3（否则"恢复"会变成"把 G2 的提交算进 Sprint-17 的缺口"）；③ 窗口有效但 §9 漏登记一行 → 仍 FAIL；④ 域真的不可派生 → rc 为 `domain-unavailable` 且**不打印** PASS。
- **通过标准（写法要防自欺）**：不是"项数变少"，而是"**恢复后的项数 = 该 Sprint 的真实缺口**"。因此实施第一步是**先测出恢复后的逐项清单**（当前预期至少含：G2 kickoff 〇查未登记 §9 ＋ 关闭锚点后非 doc-only 的提交），再逐条判定"真缺口 / 假红"，**不接受**只报总数。
- **在原始事故上验证**：修好后重跑 Sprint-17 的关闭闸门，读数回到"仅剩真实项"（当前反事实 = 1 项：081 自身未登记 §9）。
- **能预防**：本日 98 项假红、`TG-17` ②（34 条永久 FAIL）、`TG-17` 条目 8。

**M-B 反空转不变式**（治族 C；D1 交付）
- **检查什么**：每条闸门必须 (i) `SKIP` 生效时**抑制**任何 `PASS` 横幅（含"缺数据即跳过"的四个闸门各自的横幅文案）；(ii) 打印"应扫 N / 实扫 M"，`N ≠ M` ⇒ 具名 WARN/FAIL；(iii) **断言"政策声明的扫描根 ⊆ 实存根"**，且对**整体缺席的子树**（如无 `docs/iteration/` 的树）给出具名 SKIP 而不是自洽缩水；(iv) `-O` / `PYTHONOPTIMIZE=1` 下必须非零退出。
- **数据在哪**：政策 `*_coverage.roots`、`verify/*.py` 的 `VERIFY_META`、`verify/run_suite.py` 的 `suite_result.json`、`policy.json::gate_integrity.guard_required`。
- **反向对照**：注入缺失扫描根/子树 → 具名；`PYTHONOPTIMIZE=1` 跑带守卫的闸门 → 非零（`verify_gate_integrity.py` 已落一条，本卡把它推广到"子树缺席"形态）。
- **能预防**：`TG-17` 条目 3/6/7、`-080` F1/F2、`docs/3-LEARNED.MD` 1.65。

**M-C 棘轮完整性（实测值钉死）**（治族 B；D3 交付）
- **检查什么**：每条棘轮在政策里除 `caps` 外必须存 **`measured_value` ＋ `measured_at`**；闸门重测后要求 `实测 ≤ measured_value` **且** `cap ≤ measured_value`；**改 `cap` 或改 `measured_value` 都必须带 `rebased_at` ＋ 理由**，无理由即 FAIL（把"提交态放宽"从盲区变成具名事件）。
- **数据在哪**：`agents/policy.json::{lint_readability_ratchet, md_table_legacy_files, derived_numbers.baseline, ledger_measurement, artifact_paths}` ＋ `review_by`。
- **反向对照**：**提交态**上调 `caps.E501` → 必须 FAIL（今日只对 `git HEAD` 比 ⇒ 提交后即通过）；`measured_total ≠ Σcaps` → FAIL。
- **能预防**：`TG-17` 条目 2、`TG-18` 的棘轮键改造、`docs/3-LEARNED.MD` 1.66。

**M-E 关闭税读数机器派生**（治族 A；D3 交付）
- **检查什么**：关闭税读数（`code major` / `code critical` / `文档项`）**必须由评审报告派生**并由工具输出，关闭记录里的数字必须与工具输出**逐字一致**；禁止手抄。计数口径即 [`sprint-17.md` §9.5](../../../sprint/2026-09-21-sprint-17.md) 已定的两条（code 侧＝`## Graded findings` 的 graded 行数、文档侧＝`## Must fix` + `## Should fix` 的编号条数）。
- **数据在哪**：`agents/runs/*/*.report.md`（含 `agents/functions/doc-audit.md` 的 `Finding cap`）。
- **反向对照**：把关闭记录里的数字改 1（如 `11`→`12`）→ 必须 FAIL；在报告里增删 1 条 finding → 工具读数必须跟着变（防"工具读死值"）。
- **口径必须区分"计数主张"与"标识符＋单位词"**（2026-09-25 实测的假阳性形态）：现存的同类判据 `verify/verify_derived_numbers.py` 用 `\d+\s*断言` 匹配手抄的断言数，于是**步骤标识 `C2 断言` 也会被当成计数**（本次改计划书时当场命中，被迫改写句子才过闸）。本机制的判据必须带"前邻字符不是字母数字"或等价的消歧规则，并**各自带一条反向对照**（`11 断言` 仍须 FAIL、`C2 断言` 须放行）。
- **能预防**：本日实测的误抄（族 A 的活体实例）、`-074` 的 claim-precision 项。

## 交付落地 · M-A 判定域绑定（2026-09-25 D1；用户 D2 裁定"从今天开始开工"）

> 改动**只收窄判定域，不放松任何判据**；每条都带反向对照与"在原始事故上验证"。

**改了什么**（`verify/verify_close_readiness.py`）
1. `sprint_identity_of_doc()` / `run_sprint_identity()`：Sprint 身份取自**被判定物**文件名里的 `sprint-<N>`；run 的身份**优先取显式 `sprint` 字段**，否则从 `task_id` 派生（legacy 兼容）。
2. `scope_step_runs()` / `scope_step_run()` / `scope_window_problems()` / `derive_close_window()` 增 `sprint_id`：作用域 run 只在**同一 Sprint** 内挑，次序判据也只在同一 Sprint 内比较。两类 run 的处理**故意不对称**——候选作用域 run 里"无身份"的**保留**（否则换个 `task_id` 就能让窗口消失），比较用的后续步骤 run 里"无身份"的**排除**（否则每个新 Sprint 的 kickoff 都会被判"晚于整条流水线"）。
3. `run_real_data()`：域 = 该 Sprint 的 run（+ 窗口内的无身份 run）；**文件名给不出 Sprint 身份**、**域内没有作用域 run**、**窗口被次序/时序判据弃用** 三种情形一律打印具名原因 + 返回 **退出码 4（domain-unavailable）**，**不产出判定结论**（findings 即使上屏也标 `[domain-fallback]` 只作诊断）。
4. **右端**：`domain_right_boundary()` 取域内**覆盖最远**一条覆盖 run 的 `covers_through` 作为覆盖扫描上界（不再用移动靶 `HEAD`）；`check_c3` 的失败文案随之写成"锚点→**域右端**"。**（2026-09-25 独立复核 `run-…083` major 后修正）**：初版取的是"`started_at` **最新**那条"——后登记一条覆盖更少的 run 就能**静默缩小**判定域；现改为**真 git 祖先关系**比较（`X` 更远 ⇔ `X.covers_through` 是 `Y.covers_through` 的后代），**不可比（分叉）⇒ 具名问题**（fail-closed，不猜）。
5. `check_requirements()` 增 `window_start` 过滤：本期步骤必须**在本期窗口内**跑过。
6. `check_c2()` 增 `ledger`：**"引用对象是否存在"按全账本判定**——收窄判定域 ≠ 收窄存在性判据（实测：域内查存在性会给 Sprint-16 / Sprint-18 各多出 1 条假红）。

**在原始事故上验证（6 项标准第 5 条）**

| 读数 | 改前 | 改后 |
|---|---|---|
| `--sprint docs/iteration/sprint/2026-09-21-sprint-17.md` | **99 项**（`[linkage]` 65 + `[C1]` 33 + `[窗口/次序]` 1；其中 98 项假红） | **1 项**，且该项是**真实缺口**（见下） |
| 域与窗口 | 全域（081 一登记即作废） | `Sprint-17；域内 13 条；作用域 run=…-069；窗口起点=2026-09-25T04:48:10+00:00；右端=25c8a8cc081d…（由 …-080 的 covers_through 界定）` |
| `--sprint docs/iteration/sprint/2026-09-25-sprint-18.md`（新 Sprint、流水线未跑） | 同一机制下的全域噪音 | **5 项、全部真实**：3 条"本期步骤缺 run" ＋ 1 条"账本有 run 但 §9.1 未登记（081）"＋ 1 条"未声明三查锚点" |

**反向对照（自检内置；`verify_close_readiness.py` 自检断言数以其**现跑 `EVIDENCE:` 末行**为准——本批新增 13 条 M-A 断言）**
- ① **原始事故复现**：Sprint-16 流水线跑完后追加一条 Sprint-17 的作用域 run ⇒ Sprint-16 的窗口与问题清单**一字不变**；①b 同一条注入在**它自己**那个 Sprint 上仍是合法起点（证明注入没被当空气）。
- ② 本期流水线未跑 ⇒ 逐条点名缺 run（**不是** PASS）。
- ③ 该 Sprint 没有作用域 run ⇒ 起点 `None`（fail-closed，不退回全域清单）。
- ④ 无身份的**更晚**作用域 run 不得顶掉本期（显式身份优先）；④b 一个带身份的候选都没有时才回退到无身份候选。
- 域右端 a/b/c（**夹具模式**：`root=None`、无 git）：退回"登记时间最新"取右端并**在问题里点名该口径**；空 `covers_through` 不参与；无覆盖窗口 ⇒ `None`（调用方退回仓库 HEAD）且上屏说明。
- 域右端 d/e（**复核后补**）：d = 用两个**真实提交**（`HEAD` 与 `HEAD~1`）构造"后登记但覆盖更少"的 run ⇒ 必须选中覆盖更远者（原实现会选错的那个）；e = 两条覆盖端点**互不为祖先** ⇒ 具名问题而不是猜。

**新发现的数据缺口（M-A 落地当场暴露；**均为真项，不追改历史数据**）**
1. `run-2026-09-25-doc-audit-070` 的 `task_id` = `Sprint-17 close`，**不等于** `fanout.json` 为 doc-audit 步骤声明的 target `working-tree` ⇒ 这就是 Sprint-17 恢复后剩下的那 1 项。**它此前是被更早 Sprint 的同 role run"借位"满足的**（原 `check_requirements` 在全账本里找 target，历史的 `working-tree` run 一直在替本期过关）——典型的跨期假绿，已入机制反例档**案例 11**。
2. `run-2026-09-25-impact-assessment-065` 的 `task_id` **为空** ⇒ 身份不可派生（B1 记录里它是 Sprint-17 的作用域 run）。
3. `run-…-code-review-074`~`-080`（修复验证复核）的 `task_id` 不含 Sprint 身份 ⇒ 按"窗口内保留"规则进入**上一个 Sprint** 的域（实测把 Sprint-16 的域右端拉到了 Sprint-17 的 080）。
4. `run-2026-09-25-impact-assessment-081` 的 `scope_source` 指向 `run-…-069`：全账本口径下**合法**（对象存在），但语义上是跨 Sprint 引用（历史遗留）。

**残留（本批不声称闭环）**：`register --sprint` 尚未落地（下一批随 `TG-19` M-A 收尾 + `A-M13` 一起做）；上述 2/3/4 条数据缺口的受控回填随它一起做。

## 交付落地 · M-B 反空转不变式（第 1 批：SKIP 不得冒充通过；2026-09-25 D1）

> 治的是 `-080` **F1**（`TG-17` 条目 6）那一族：**闸门跳过判据之后，仍然打印成功横幅**——"没查"与"查过了"在输出上无法区分。

**改了什么**
1. `verify/verify_card_index.py`：新增 `verdict_line(problems, skipped)` 作为**判决行单一真源**（三类互斥：有问题 → FAIL；有 SKIP → **SKIP 行且不含 `PASS` 字样**；两者皆无 → PASS 横幅）；`check_phase` 在"本分支没有 `docs/iteration/**`"时把具名原因写入模块级 `SKIPPED`；`main()` 改为先打判决行；**SKIP 档不再打印机读 `EVIDENCE:` 行**（证据行只属于"判据真的跑过且通过"）。
2. `verify/verify_close_readiness.py`：账本缺失档的判决行由 `CLOSE-READINESS SKIP-PASS` 改为 `CLOSE-READINESS SKIP（…，**不是通过**）`；新增 `_RUN_STATE["skipped"]`，`main()` 据此**不再打印 `EVIDENCE:` 行**。
3. 自检断言：`verify_card_index.py` 14 → **17**（判决行三条：SKIP 档不含 PASS／无问题才 PASS／有问题优先 FAIL）；`verify_close_readiness.py` 79 → **80**（新增真子进程 + 真 SKIP 分支的反向对照）。

**在真实入口上验证（不是函数注入）**

| 环境 | 命令 | 结果 |
|---|---|---|
| **无 `docs/iteration/` 的树**（`git worktree add --detach HEAD` 后删除该目录，= `main` 分支/CI 的真实形态） | `verify_card_index.py` | 输出 `SKIP[no-iteration-docs] …` ＋ 判决行 `CARD-INDEX SKIP（…）——真数据判据未执行…（不是通过）`；**`EVIDENCE:` 0 行、`CARD-INDEX PASS` 0 行**，rc=0 |
| 账本缺失档（真子进程 + `AGENT_OPS_DIR` 指向空目录） | `verify_close_readiness.py --sprint …sprint-17.md` | 输出 `SKIP[ledger-absent]` ＋ `CLOSE-READINESS SKIP（账本缺失档，不是通过）`；**无 `EVIDENCE:`、无 `CLOSE-READINESS PASS`**，rc=0（自检内置，改前会打印证据行 ⇒ 属"跳过冒充通过"） |
| 本机（有账本 / 有 iteration 文档） | 两闸门 | 判决行仍为 PASS 横幅 + `EVIDENCE:` 行（**不误伤正常路径**） |

**本批未覆盖（M-B 剩余，下一批）**：① `md_tables` 的"声明子树缺席 ⇒ 具名 SKIP"（`TG-17` 条目 3 的修法：`docs/iteration` 未独立声明为 root，缺席时仍打印 `MD-TABLE PASS`——本批只登记，未改）；② `verify_ledger_measurement.py` / `verify_agentops.py` 的 SKIP 文案与判决行已自查（均不打印 PASS），但**未加机检断言**；③ "应扫 N / 实扫 M" 尚未推广到 `card_index` / `derived_numbers`。

## 交付落地 · M-A 收尾：Sprint 身份的显式登记 ＋ 受控回填（2026-09-25 D2）

> 目标：让判定域从"**猜**（`task_id` 正则）"升级为"**读显式字段**"——身份写错会让一条 run 静默掉进**别的** Sprint 的域，比当场报错难查得多。

**改了什么**（`scripts/agent-ops.py`）
1. `register --sprint <Sprint-N | N>`：归一到纯编号（与闸门 `run_sprint_identity()` **同一口径**）；归不出编号 ⇒ `SPRINT-ID-ERROR` **fail-closed**（宁可留空，不写错）。
2. `set-sprint <run_id> --sprint … --reason …`：**受控回填**（与 `set-scope`/`set-anchor` 同构）——必须给 ≥10 字符理由；**同值 = 无操作**（不改库、不留痕、报"无变化"）；每次实际变更追加 `sprint_backfills`（`field`/`from`/`to`/`reason`/`by` 五件套）。
3. 机检：`verify/verify_agentops.py` 新增 **UC-22**（新增断言数以该脚本 `EVIDENCE:` 末行为准）——归一化、非法身份拒绝且不留半条 run、回填 + 四件套留痕、同值幂等、过短理由拒绝、**锁覆盖**（8 条写命令必须都带守卫）。

**数据回填（真账本，逐条带理由；`sprint_backfills` 可复核）**：给 **20 条**关闭流水线 run 补了 Sprint 身份——`062`/`065(impact)`/`063`/`064`/`065(code-review)`/`066`/`067`/`068`/`071`/`072`/`073`/`074`~`080` → **17**；`053`/`060` → **16**。每条理由都写明依据（`sprint-17` §9.1/§9.2 run 表行、`sprint-16` §9 与状态报告）。**未回填 19 条**（以现跑为准）：`2026-08` 的目标名/测试名 run（如 `verify-case-high`、`migration-audit`）**无法在不逐条判断的情况下归属** ⇒ 由 M-A 的"窗口内保留"兜底（fail-closed：宁多勿少）。

**回填后实测（`--sprint` 真跑）**：Sprint-17 = **1 项（真项，不变）**；Sprint-18 = **4 项全真**（3 条本期步骤缺 run ＋ 1 条未声明锚点；`081` 已登记 §9.1 后，原先那条 linkage 项消失）；Sprint-16 = **13 项**，且其**右端不再被 Sprint-17 的 `080` 借位**（这是身份回填的直接效果）。

**过程事故（我自己造成，已按用户定规留档）**：加 `_norm_sprint()` 时把它插在 `@_with_registry_lock` 与 `def cmd_register` **之间** ⇒ 装饰器挂错对象——`register` **丢掉账本锁**（登记重新变成 last-writer-wins 竞态），`_norm_sprint` 反被加锁并在 `cmd_set_sprint` 内**自锁超时**（9.1s）。发现路径：CLI 报"锁获取超时" → 裸锁/其他写命令均正常 → `__wrapped__` 绕过成功 → patch `_file_lock` 打栈定位到装饰器。修法＝函数上移 + **UC-22 锁覆盖断言**（机检，不靠记性）。留档：机制反例档**案例 14**。

## 交付落地 · M-C 棘轮完整性（2026-09-25 D3）

> 治的是复核机制缺口 2（`3-LEARNED` 1.66）：**上限是自声明数据**——`cap` 记在实测之上 = 给新增违规发静默配额；而"与 `git HEAD` 比较"只拦得住**未提交**的上调，一次**已提交**的放宽立刻成为新基线，事后看不出它被放宽过。

**改了什么**
1. `agents/policy.json`：**五条**棘轮（`lint_readability_ratchet` / `md_table_legacy_files` / `derived_numbers` / `ledger_measurement.legacy_ratchet` / `artifact_paths.dynamic_ratchet`）**全部补上 `measured_value`（重评那一刻的逐键实测快照）＋ `measured_at`**，并各带一段 `_measured_value_why` 说明判据（`derived_numbers` 的 32 条快照由脚本按 baseline 逐键生成——**不是手抄**）。**第 5 条（`artifact_paths.dynamic_ratchet`）是 2026-09-25 独立复核 `run-…083` major 后补入的**：它当时**不在** `gate_integrity.ratchets` 名单里，且带 2 个单位空余 ⇒ 一张被漏掉的棘轮可被静默抬到实测之上（现补入名单并把上限收到实测值，`measured_total` 同步重算）。
2. `verify/verify_gate_integrity.py`：
   - `ratchet_problems`（工作区 vs HEAD）新增三条：缺 `measured_value`/`measured_at` ⇒ FAIL（fail-closed）；`cap > measured_value` ⇒ FAIL 且点名"**静默配额**"；快照与上限表**键集不等**（缺键/多键）⇒ FAIL。
   - 新增 `committed_rebase_problems(HEAD, HEAD~1)`：**提交态**的任何一个 `cap`（或 `measured_value`）被抬高时，该棘轮块必须带 `rebased_at` + `rebased_reason`（≥10 字符）⇒ 放宽**必须是具名事件**（谁、何时、依据哪次实测）。它不判"放宽是否合理"，只消除盲区。**（复核后加固）**：光有署名不够——"先降后升"可以复用上一次的旧署名，故现要求 `rebased_from` **逐键等于父提交里被抬高那些键的值**（对不上 ⇒ FAIL 且点名"署名不是这一次的"）。
   - 三处串起来即 `活读数 ≤ cap ≤ 重评时实测`（活读数由守门闸门各自测）。

**反向对照**（自检断言数以 `verify_gate_integrity.py` 的 `EVIDENCE:` 末行为准，本批 ＋复核加固后 **19**）：J 缺 `measured_value` ⇒ FAIL；K 缺 `measured_at` ⇒ FAIL；L `cap=12 > 实测 10` ⇒ FAIL 且点名静默配额；M 快照多出键 ⇒ FAIL（死数据）；**N 提交态上调而无声 ⇒ FAIL**；N2 同样的上调**带 `rebased_at` + 理由 + `rebased_from`** ⇒ PASS（成为具名事件，不是放行）；**N4 先降后升 + 复用旧署名 ⇒ FAIL**（复核 major 的原始形态）；N3 提交态不变 ⇒ 不误报。

**在原始事故上验证**：真数据跑 `verify/verify_gate_integrity.py` ⇒ `PASS …；5 个棘轮的上限与 HEAD 逐项比较无上调、且都带 measured_value/measured_at；提交态（HEAD vs HEAD~1）无未署名的放宽`（rc=0；断言数以该脚本 `EVIDENCE:` 末行为准）。**反例侧**：把"提交态抬高"喂给同一函数（自检 N）必红——这正是上一版看不见的形态。

**残留**：`TG-18` 接下来要**删掉** `md_table_legacy_files` 这整张上限表（15 处历史缺陷清零）——删键本身也要带 `rebased_at` + 理由（M-C 已把这条变成机检）。

## 交付落地 · M-E 关闭税读数机器派生（2026-09-25 D3）

> 治的是**案例 9**（手抄读数）：`M1 关闭税`是头号指标，而它的读数此前只能手抄；Sprint-17 因此抄进了**上一个 Sprint** 的数字，且两个 Sprint 的 must-fix 数恰好都是 8 ⇒ 抄错后算术自洽、无人抓到。

**新闸门**：`verify/verify_close_tax_reading.py`（offline；已进 `gate_integrity.guard_required` ⇒ 真实 `-O` 探针会跑它，`guard_required` 以该闸门末行现跑为准）。
**判据**：Sprint 文档 §8 必须带一行**机读读数**：

```
关闭税读数: code_major=<N> code_critical=<N> doc_items=<N> 来源=<run_id>,<run_id>,…
```

闸门**从这些 run 的报告文件重新派生**同一组数字并**逐字比较**；缺 `来源=` ⇒ FAIL；来源 run 的报告不存在 ⇒ **具名问题**（不把"查不到"算成 0）。
**（2026-09-25 独立复核 `run-…083` major 后加固）**：读数行必须**行首锚定**（`^[ \t]*关闭税读数…`）且**全文恰好一行**——原实现的正则无行首锚点、且只认第一处匹配，实测在真行**之前**写一句同形叙述即可让闸门 `PASS`（正文冒充机读行）。同时"缺读数行 ⇒ 显式 `SKIP`"的旧口径被收紧：**文档自称已关闭**（声明真锚点 或 §9 有 run 表行）却缺读数行 ⇒ **FAIL**（否则删掉读数行就能拿到 rc=0，等于给"没写读数"开后门）；只有"文档没自称关闭"或"报告目录不存在"（全新 checkout）才是 SKIP。命中 ≥2 行 ⇒ FAIL（两套口径）。
**派生口径**（= `sprint-17.md` §9.5 定稿口径，机器版）：`code_major`/`code_critical` = `code-review` 报告 `## Graded findings` 节内 `^- **<级别>**` 的**行数**（"本轮未发现 critical"这类说明行**不计**、**节外行不计**）；`doc_items` = `doc-audit` 报告 `## Must fix` + `## Should fix` 的**编号条数**。

**独立复核价值（本批最硬的一条证据）**：工具派生的 Sprint-17 读数 = **`code_major=7 / code_critical=3 / doc_items=11`**，与 §9.5 的人工更正**完全一致** ⇒ 那次"抄错 Sprint"的更正从"人工复核"升级为**机器复核**；且该行已写入 Sprint-17 文档，真跑 `CLOSE-TAX PASS`。
**反向对照**（自检断言数以该脚本 `EVIDENCE:` 末行为准，复核加固后 **13**）：① 真数据上把 `doc_items=11` 改成 `12` ⇒ **rc=1** 且点名"差 1"（副本与原文逐字比对：唯一差异就是那个数字）；② 缺 `code_critical=` 键 ⇒ FAIL；③ 报告**新增 1 条 major** ⇒ 派生 2→3、原声明随即 FAIL（证明工具不读死值）；④ graded **节外**的 `- **major**` 不计（该边界来自我第一版反向对照写错位置时暴露的形态，已固化为断言）；⑤ `本轮未发现 critical` 说明行不计；⑥ 来源 run 报告缺失 ⇒ 具名问题；⑦ **正文里的同形叙述不算读数行**（行首锚定）且**两行读数 ⇒ 命中 2 行**（调用方据此 FAIL）；⑧ **自称已关闭却缺读数行 ⇒ FAIL**（`claims_closure` 的两条判据各一例）、**没自称关闭 ⇒ SKIP**。

**规则条文**：`docs/1-WORKFLOW.MD` §6 第 10 条（关闭税读数必须由工具派生，禁止手抄）。
**残留**：G2 自己的读数行在关闭期由**同一工具**生成后填入（届时三个数字应显著低于 G1 的 7/11，这正是 M1 的复测读数）。

## 独立复核整改 · `run-2026-09-25-code-review-083`（2026-09-25 D3 收尾后；用户 `D-250925-12` 裁定"先插一次中途独立复核"）

> 触发：用户裁定**不等到 Sprint 关闭才复核**，在 D4 之前先插一次。对象 = D1~D3 已落地的代码/文档/闸门（不是只看某个文件）。产出：**critical 0 / major 5 / minor 4 / nit 1**，38 处 `file:line`。

**5 条 major 全部本轮整改（逐条：原形态 → 修法 → 反向对照）**

| # | 原形态（复核原话要点） | 修法 | 反向对照 |
|---|---|---|---|
| M1 | `verify_close_tax_reading.py` 的 `TAX_RE` **无行首锚点且只认第一处匹配** ⇒ 正文里写一句同形叙述就能让闸门 `PASS`，而真读数行从未被读 | 正则改**行首锚定** + "命中必须恰好 1 行"；调用方对 ≥2 行 FAIL | 自检 ⑦（同形叙述不算 + 两行 ⇒ 2 命中） |
| M2 | 同一脚本**缺读数行 ⇒ `SKIP`** ⇒ 删掉读数行即可拿到 rc=0（"没写读数"被静默放行） | 新增 `claims_closure()`：**自称已关闭却缺行 ⇒ FAIL**（真锚点 或 §9 run 行两条判据）；只有"没自称关闭"才 SKIP | 自检 ⑧（两判据各一例 + 负例） |
| M3 | `domain_right_boundary()` 取"`started_at` 最新" ⇒ **后登记但覆盖更少**的 run 可**静默缩小**判定域 | 改**真 git 祖先关系**取覆盖最远者；**不可比 ⇒ 具名问题**（fail-closed） | M-A 域右端 d（两个真实提交构造）+ e（分叉） |
| M4 | 在飞宽限**没有下界** ⇒ `started_at` 在**未来**的 run 永久落在宽限内、其 §9 登记要求被**无限期豁免** | 宽限加下界（`0 <= age < grace`）；未来时间戳 ⇒ **具名问题**并照旧要求登记 | M-A 宽限反向对照（`age_min < 0`） |
| M5 | `artifact_paths.dynamic_ratchet` **不在** `gate_integrity.ratchets` 名单里，且带 2 个单位空余 ⇒ 漏掉的棘轮可被静默抬到实测之上 | 补入名单（`ratchets=5`）并把上限收到实测值、`measured_total` 重算；`rebased_from` **逐键绑定父提交数值**（先降后升 + 复用旧署名 ⇒ FAIL） | M-C 反向对照 N4 |

**已整改的 minor/nit**：① `docs/6-DECISIONS.md` 6 处漂移的 `path:line` 指针（逐条核对后改正，全文件重扫）；② `sprint-18.md` 一处**过期现值**（"现测 5 项"→ 时间锚定的 4 项）；③ `policy.json` 的 `_remeasure` 说明里残留的旧数字（2552→**2494**）与已知局限句；④ `md_table_legacy_files._comment` 的过期句；⑤ `verify_close_tax_reading.main()` 的死代码；⑥ 本次整改自己新引入的 **34 条 E501** 全部折行（不是抬上限——上限 2494 未动）。
**新增执行点**：`verify_agentops.py` **UC-23**（断言数以该脚本现跑 `EVIDENCE:`/`ALL PASS` 末行为准：复现病根／受控回填 + 六件套留痕／同值幂等／产物不存在则拒绝且不改库／**只认 agents 根一种口径**；并把 `cmd_set_result_files` 纳入 UC-22 的锁覆盖探针）——对应我自己的事故：`finish` 漏带 `--result-file` 后**无法修复**（二次收尾被 fail-closed 拒绝），只能新增受控回填口（`result_files_backfills` 留痕、文件必须真实存在、`--reason` ≥10 字符）。

**留档（`D-250925-10` 的机制缺口规则）**：新增机制反例档案例——"**正文可冒充机读行**"（M1/M2）、"**判定域右端按时序而非覆盖**"（M3）、"**宽限无下界 = 永久豁免**"（M4）、"**棘轮名单缺项 = 静默配额**"（M5）、"**收尾时才可写的字段漏写即永久红**"（UC-23 那条病根）。
**未采纳但已记账（复核自身的 `Fix this round: no`）**：`_norm_sprint()` 在 `scripts/agent-ops.py` 与 `verify/verify_close_readiness.py` **各有一份实现**——复核建议的廉价修法是加一条 UC 断言"两份实现结论一致"；本轮**未做**（转入 `TG-17`/backlog 复核口径），理由与替代控制见本节末与 sprint-18 §5。
**复核自身的覆盖边界（不得当作"全查过"）**：未读各卡正文（`TG-18`/`TG-19`/`TG-20`/`A-M13`）、`TEST-MATRIX.MD`、G2 计划书、两本反例档、15 处表格修复的 diff 与约 15 个提交的 hunk；未跑 `run_suite`/`lint`/`card_index`。

## 验收判据
① 4 个机制全部落地且**都带反向对照**（缺一条即不算交付）；
② **在原始事故上验证**：M-A 用"081 形态"复现、M-B 用"无 `docs/iteration/` 的树"复现、M-C 用"提交态放宽"复现、M-E 用"改 1 个数"复现；
③ 四个机制各自接入 `verify/run_suite.py --tier offline`（或明确写出为何不进套件并给出替代执行点）；
④ **不引入新的"应扫/实扫"自洽式判据**（判据必须是"声明的范围 vs 实存"的跨源比较，不得两侧同源）。

## 边界 / 不在本卡
- 关闭期发现的**逐条缺陷**（`-074`~`-080` 的具体 finding）由 [`TG-17`](TG-17.md) 条目 1~8 承接，不在本卡重复立案。
- 实现类 run 的账本 role（`implementation` spec ＋ 在飞宽限）单独成卡：[`A-M13`](../../agents-infra/cards/A-M13.md)（改动 role 集合，必须先于 C1 判定域定稿）。
- 历史表格缺陷清零 = [`TG-18`](TG-18.md)（与 M-C 同改 `agents/policy.json` 的棘轮键，**先 M-C 再 TG-18**）。
