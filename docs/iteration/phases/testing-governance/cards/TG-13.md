# TG-13 **账本"测量化"：时长/轮次/账本↔产物一致性**（用户 2026-09-21 拍板"开新卡"，而非并入 TG-10）

- `card`: TG-13
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**候选（用户 2026-09-21 拍板开卡）**

## 规模
2

## 来源
**用户 2026-09-21（UTC+8）**："账本测量化……做成新卡 TG-13"（原提议：并入 TG-10 作补充 → 用户选择新卡）

## Sprint
Sprint-17（治理批 G1）

## 正文
`[用户插入]` **账本"测量化"：时长/轮次/账本↔产物一致性**（用户 2026-09-21 拍板"开新卡"，而非并入 TG-10）：**TG-10 解决的是"多轮次没被记录"，本卡解决"记录了但不是测量值"**。实测证据（2026-09-21，来源 run 报告 + 主代理实测）：① **6 组 run 的 `started_at`/`ended_at` 逐字节相同**（含 3 条 0.00 min、3 条恰好 10.00 min）→ 时长是**写入时刻的巧合**而非墙钟测量；② **`rounds` 与实际轮次不符**——run-060 台账记 `rounds=5` 而报告只有 **3 轮**（高估）、run-053 曾少记 **4 轮**（低估，已由 TG-10 回填）；③ **账本 ↔ 产物目录双向漂移**：**主代理实测 60 个 `run-*` 目录 vs 账本 59 条记录**（B 路报"63 vs 59"系口径差异，引用时须注明量法）；④ **部分 run 类型从未入账本**（`provider-refresh` 两类 run 有目录无记录）、**账本里唯一的 `failed` run 恰恰没有产物目录**；⑤ `doc-audit-009` 终态未写回（账本滞留 `status=running`）；⑥ **中文 `task_id` 落库即乱码**（2026-09-21 〇查 `run-2026-09-21-impact-assessment-061` 实测：`pwsh` → `python` 传参把中文转成 mojibake，`python -c "print('中文测试')"` 同样乱码；ASCII 描述的历史 run 正常）→ 账本写入路径须统一编码（或改走文件/JSON 输入），并加"字段可读性"断言。

## 证据
**卡内待产出**：① **测量口径定义**——`dur` 是"墙钟实测/首末轮时间"哪一种？`rounds` 的真源是"报告轮次"还是"CLI 声明"？**缺值必须显式 `unknown`，禁止以 0.00 / 整十分钟等退化值冒充**；② **`verify/verify_ledger_measurement.py`（offline，纳入套件）**：账本↔`agents/runs/**` 目录**双向**一致（含"有目录无记录"与"有记录无目录"）、时间戳非退化（相同/0.00/整值告警）、`rounds` 与报告轮次一致（可核对者）、终态已写回；③ `agent-ops.py` 侧：`finish`/`round` 写入时检测时间戳退化并**标注或拒绝**，新增"测量来源"字段（wall-clock / declared / unknown）；④ **反向对照**：注入"相同时间戳 / 缺目录 / rounds 不符 / 终态未写回"四类样本 → 断言必须 FAIL；⑤ 与 `TG-10` ③（看板 UI 展示 `rounds=/dur=/int=`）合流：**看板展示前必须先保证数字可信**，否则是把失真数据可视化。关联教训 `3-LEARNED` 1.59（可观测性必须自证）。
