# TG-17 **闸门可信化收尾：覆盖窗口语义 + C1 判定域**（来源＝`TG-15` 独立复核批的两条 finding，用户 2

- `card`: TG-17
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**进行中（关闭阻塞项已修复，2026-09-25 close-blocker 批；用户裁定从 Sprint-18 前移为 Sprint-17 关闭阻塞）**

## 规模
3

## 来源
**用户 2026-09-23（UTC+8 22:47）**："1+2 合成一张新卡（比如 TG-17「闸门可信化收尾」）排进 G：可以，开进backlog吧"；上游证据＝`code-review` run `run-2026-09-23-code-review-064` 的 major #3 与 major #5，及 `TG-15` 二查结论"建议开卡，不要在本轮半改"

## Sprint
Sprint-18（治理批 G2），与 `TG-15`⑥（CI push 即检）同批

## 正文
`[用户插入]` **闸门可信化收尾：覆盖窗口语义 + C1 判定域**（来源＝`TG-15` 独立复核批的两条 finding，用户 2026-09-23 批准开卡）：**问题① 覆盖窗口结构性为空**——`TG-15` 的"锚点自动化"让每个 run 记 `(coverage_anchor, covers_through]`，但 `register` 记登记时 HEAD、`finish` 记收尾时 HEAD，而关闭三查各步常在**同一 HEAD** 登记/收尾 ⇒ 窗口恒为 `(X, X]` = 空集。实测证据：账本 `run-…-impact-assessment-062` 的 `coverage_anchor == covers_through == e6ccd257…`；`092a2f7..HEAD` 的 9 个提交里 **6 个 UNOWNED**，自动窗口贡献 **0 覆盖**，C3 压力全落到 C3-T 例外表（而例外表 sha 钉死、按设计只能登记例外，不该承担主路径）。**问题② C1 判定域＝全账本**——账本 60/63 条历史 run 无 `scope_source` 键（`TG-11` 之前的 run 本就没有该字段），现判据把"凡 `scope_required: true` 的 run 都要有 scope 声明"施加到**全部历史** ⇒ 真数据上 `verify_close_readiness.py` **永久 FAIL 34 条**，闸门退化为噪音；且账本有完整性哈希（`_integrity`），无法手工补字段回填历史。**共同根因**：判据写成"无条件的全域断言"，而语境是"本次关闭窗口"——空输入与历史数据都会让判定失去意义。

## 证据
**卡内待产出（①~⑤）**：① **窗口语义定稿并落地**——候选 A：锚点取"本 run 开工前的 HEAD"（登记时记 `HEAD~1` 或由调用方显式传上一锚点，语义＝"本 run 覆盖它之后产生的提交"）；候选 B：区间改**左闭** `[anchor, through]`（登记提交本身也算被覆盖，与 `§9.1` 填法一致）。二选一需给出**取舍理由**，并说明对已有历史 run 的兼容策略（旧窗口按新语义重算还是标记 deprecated）。② **C1 判定域收窄**——限定为"本次关闭窗口内"，历史 run 不卷入；**反向对照必须包含**：窗口内缺 scope 声明仍须 FAIL（防"收窄成静默放行"）、且历史缺字段**不得**遮蔽当期缺口。③ **反向对照／变异用例**——从 `agents/fanout.json` 自动生成的既有变异用例之外，新增：空窗口必须 FAIL（当前已落检测）、窗口边界（相邻 run 首尾相接不得漏提交）、`HEAD~1` 锚点在"本 run 无新提交"时的行为显式判定。④ **与 `TG-15`⑥ 合并排期**——覆盖检查进 CI 的触发条件、锚点填充方式（`§9.1` 是否仍需人工填 `三查锚点`）、以及 `close-sync` 输出格式（供人只挑类别）三者必须一次对齐，避免"工具一个口径、闸门另一个口径"（二查已实测该不一致：`cmd_close_sync` 曾以不传 `policy=` 的方式构造窗口）。
⑤ **实现类 run 无账本 role**（输入来源：独立审核文档 [`2026-09-25-d2d3-independent-review.MD:139`](../2026-09-25-d2d3-independent-review.MD)，原文口径"强行用 code-review role 会让关闭闸门多点名'§9 未登记'"）——**账本无实现类 role** ⇒ 执行实现工作的子代理**无法在不污染 C1 判定域的前提下登记 run**（实测：`run-2026-09-25-code-review-068` 在真数据关闭闸门里被判 `[linkage] §9 未登记`）⇒ 需新增 **implementation spec**（`scope_required: false` + `coverage_window` 取值必填）**或**显式例外口径。实测补充（2026-09-25 D-prep 批，可复核）：`agents/functions/` 8 个 spec 中仅 3 个是评审类（`scope_required: true`），其余 5 个是评审/研究/主代理手册；C1 判定域收窄后，窗口内唯一 C1 项正是这类"实现工作挂在评审 role 下"的 run（`run-2026-09-21-code-review-060`，`task_id` = `fix-verification after close-3check…`）。
**验收判据**：`verify_close_readiness.py --sprint <Sprint-17 文档>` 在**补全 §9 表与锚点后能真的 PASS**（现在是"无论怎么填都 FAIL"）；且历史 run 的缺失字段不再产生 FAIL，但**当期**缺 scope 声明仍 FAIL（两条都要有反向对照实测）。

## 交付落地（2026-09-25 close-blocker 批，B1~B4；用户裁定本卡从 Sprint-18 **前移为 Sprint-17 关闭阻塞项**）

> 前移理由（可核）：不修则关闭闸门**不可达**——真数据 13 项里 1 项是 C1（历史域）、11 项 linkage 全由"窗口被历史步骤 run 拖宽"造成；而 CI 的关闭前置块只对**声明了真锚点**的 Sprint 文档执行（选择器实测 19 份命中 0 份 ⇒ 当前空跑），**填上真锚点的记录那一刻**该闸门首次实跑即报错 ⇒ 属关闭阻塞。改动全部**不放宽任何断言**——B1~B4 只收窄判定域、补数据、修口径分裂；每项都带反向对照。

**B1 关闭窗口起点改为"本次关闭的作用域 run"**（`verify/verify_close_readiness.py::ledger_close_window` + 新增 `scope_step_run`）
- 改前口径 = "各必填步骤最新 run 的 `started_at` 最小值"，被 Sprint-16 补跑的 `lessons-learned-058`（`2026-09-20T17:34:57`）拖宽约 4 天；改后 = `close_gate.scope_ref_step`（`scope` 步）最新 run `impact-assessment-065` 的 `started_at` = **`2026-09-24T16:46:18+00:00`**；作用域 run 缺席 → 返回 `None`（**fail-closed 不过滤**，不退回旧口径）。
- 口径来源是**数据**：`close_gate.scope_ref_step` 指向哪一步就是哪一步；`Policy.close_gate` 现在**装载即校验**该步骤名存在于 `fanout.json` 且 `close_ledger=true`（拼错会让窗口恒不可推导、判定域静默退回全域 ⇒ 必须当场抛错）。
- 实测（真数据，§9 仍未填）：**13 项 → 6 项**。改前构成 = `[C1]`1（`run-…-code-review-060`）+ `[linkage]`11（058/060/061/062/063/064/065(impact)/065(code-review)/066/067/068）+ `[C3]`1（未声明锚点）；改后 = `[C1]`**0** + `[linkage]`5（065(impact)/065(code-review)/066/067/068）+ `[C3]`1。消失的 7 项 = C1 那条（其 `started_at` 落在新窗口之前）+ linkage 058/060/061/062/063/064（同因）。**收窄不等于放宽**：窗口内的 5 条 linkage 与 C3 全部照旧判，且 `check_requirements`（按 `fanout.json` 必填步骤）**不看窗口**，仍是"本期没跑过就 FAIL"的兜底。
- **填好之后的实测投影**（§9 用真实账本行填 5 行 + 真锚点，样本在 `%TEMP%`）：**3 项**（不是 0）——`[C3-窗口]`2（066 短 sha / 068 空窗口，见下"仍未闭环"）+ `[C3]` 未归属提交（锚点 `9ffc108d` ⇒ 15 个；锚点 `092a2f78` ⇒ 30 个）。**本卡不声称"填完即 PASS"**：残余全是 C3 覆盖宽度问题，B1~B4 都不改变任何 run 的 `covers_through`。
- 反向对照 5 条（自检 `B1 正向/a/b/c/d`）：作用域 run 缺席 → 窗口 `None` 且不过滤；同 role 更早 run 出现 → 仍取最新（不取最小值）；非作用域步骤最早 run 不得定义窗口；`scope_ref_step` 拼错 → 装载即 `PolicyError`。

**B2 `covers_through` 的受控回填**（`scripts/agent-ops.py::cmd_set_anchor` 增 `--covers-through`）
- 与 `--anchor` **逐条同构**：短 sha 规范化成完整 40 位、解析失败 fail-closed、必须 `--reason`（≥10 字符）、两个端点**全部解析成功后才落库**（不留半改状态）、每次实际变更追加 `anchor_backfills` 记录（新增 `field` 键以标明改的是哪个端点）。
- 顺带修掉一处**假留痕**：改前同值回填也会追加 `anchor_backfills`，审计据此会以为窗口被改过；现同值 → 不改库、不留痕、报"无变化"（实测 UC-17/UC-17b）。
- **已回填**：`run-2026-09-23-impact-assessment-062` 的 `covers_through` `e6ccd257`（8 位）→ `e6ccd257b8bd8cb69dfd8ed1cbe7b306f2053000`；账本实读 `anchor_backfills[0] = {field: "covers_through", from: "e6ccd257", to: "e6ccd257b8bd…", reason: "B2: short covers_through silently dropped…", by: "main-agent"}`。
- **诚实标注**：062 的 `coverage_anchor` 也是同一个 sha ⇒ 回填后该窗口是 `(X, X]`（仍贡献 0 覆盖）。B2 修的是"短 sha 让窗口**静默**消失"这条**不可修的红**，不是覆盖广度。残留见下"仍未闭环"。

**B3 空窗口语义按 run 类别分流**（`verify/agent_policy.py::Attribution.window_problems` + `Policy.close_gate` 新键 `coverage.scope_run_window`）
- 语义定稿：**作用域类 run 不承担内容覆盖**（= `scope_ref_step` 那一步的 role，本仓 `impact-assessment`；以及 spec 声明 `coverage_window: none` 的 role）⇒ 其空区间**不判问题**——它只在 T0 界定本次关闭覆盖哪些变更，用"量内容覆盖的尺子"量它是**结构性假红**。**内容评审类 run**（spec 声明 `coverage_window: self` 的评审 role：`code-review` / `doc-audit` / `agent-onboarding-review`）声称覆盖内容却贡献 0 覆盖 ⇒ **仍判 FAIL**，并给出修复指引（`set-anchor <run_id> --covers-through <sha> --reason <理由>` 或重跑）。
- **短 sha 不在此豁免内**：它是另一族缺陷（数据非法），任何 run 都判——否则等于多开一条"短 sha 免检"后门。
- 实测判定（新语义）：`run-…-impact-assessment-065`（`9ffc108d..9ffc108d`）**不再报**；`run-…-code-review-068`（`08dbea94..08dbea94`，内容评审类）**仍报**并附修复指引。
- 反向对照 7 条（自检 `B3 *`）：作用域类空窗口不报；3 个内容评审类 role 各自仍 FAIL 且带 `set-anchor` 指引；把 `scope_ref_step` 改指 `doc-audit` → 同一 role 判定**翻转**（证明"作用域类"来自政策而非代码认角色名）；作用域类的**短 sha** 仍报。

**B4 `run-060` 的 C1：选 (a) 受控回填 `set-scope`，不用例外清单**
- 选择理由：`run-060` 的缺口是**可修的账本数据缺口**（`1-WORKFLOW.MD` §4.2 与 [`2026-09-23-tg11-retro.MD:80`](../../testing-governance/2026-09-23-tg11-retro.MD) 明确要求"修复验证复核的 scope 也必须声明来源：`--scope-source self-chosen --deviation "<fix blast radius 与理由>"`"），而例外清单是把该缺口固化成**长期放行规则**（且它是闸门自己的白名单）——"改数据 vs 改闸门"里错的那一边；例外清单还会随 run 增长而变长，正好复现 D1 要治的"永久噪音"。
- 做法：`scripts/agent-ops.py` 新增 `set-scope <run_id> --source self-chosen --deviation "<理由>" --reason "<为什么回填>"`，**校验完全复用 `_validate_scope()`**（`register` 的唯一实现）⇒ 回填口径与登记口径不会再分叉；`role` **取自账本行自身**（不由命令行传，否则又开一条"换个角色换套判据"的路）；同值 = 无操作；每次实际变更追加 `scope_backfills`（含 `field`/`from`/`to`/`reason`/`by`）。
- **已回填**：`run-2026-09-21-code-review-060` 的 `scope_source` `None` → `self-chosen`、`scope_deviation` `None` → `"…fix-verification re-check (NON-second-check, does not replace close 3-check); scope = fix blast radius, cap 5 / 15min per 1-WORKFLOW 4.2 and 2026-09-23-tg11-retro.MD:80"`（≥10 字符阈值）；账本实读两条 `scope_backfills` 记录。
- **独立于 B1 的证据**（这条很关键）：回填后 `check_c1` 在**新窗口**与**旧窗口**（`2026-09-20T17:34:57`）下**都返回空**⇒ 修的确实是数据，不是靠 B1 把问题挤出判定域。

## 仍未闭环（本批**不**声称已闭环；诚实标注）

1. **C3 未归属提交（真数据 15~30 条，随锚点取值）**——结构性原因：内容评审类 run 的 `covers_through` **都早于 HEAD**（最新一条 = `code-review-067` 的 `4f2fcc1551`），而锚点之后必须"每个提交都有归属"。以锚点 `092a2f78` 实测：58 提交 / **30 UNOWNED**；以 `9ffc108d` 实测：28 提交 / **15 UNOWNED**；以 `08dbea94` 实测：9 提交 / **6 UNOWNED**。B1~B4 **不消除**这一类（B1 只收窄"窗口起点"口径，不扩大任何 run 的覆盖宽度）。
   - **D0 批复测（2026-09-25）**：锚点 `9ffc108d` → `9ffc108d..HEAD` **31 提交 / 16 UNOWNED**；锚点 `4f2fcc1551` → 25 提交 / **同样这 16 个**（多出的 6 个在 `9ffc108d..4f2fcc1551` 区间内、全是 `doc-only` 或落在 067 窗口内）。15→16 的原因可核：TG-17 测量之后新增 3 个提交，其中 `d4fef2a` / `058d71b` 只改计划文档（自动归 `doc-only`），`c5407844`（关闭阻塞项批本身）改了 `agents/policy.json` / `scripts/agent-ops.py` / `verify/agent_policy.py` ⇒ 非 doc-only ⇒ 计入 UNOWNED。**16 个一个都不是 doc-only**（逐个实测非文档文件数 ≥1：policy.json / scripts / verify / `.github/workflows/ci.yml`）。
   - 合法出路（都属**关闭期动作**）：① 二查/一查**重跑或补跑**，使最新内容评审 run 的 `covers_through` 覆盖到 HEAD——**注意这需要同时给 `--coverage-anchor <上一轮覆盖上界>`**：`register` 默认把锚点记成"登记那一刻的 HEAD"，按默认登记的新 run 窗口只有 `(登记时 HEAD, 收尾时 HEAD]`（通常为空），**16 条一条都不会消失**；要如实地把它们纳入覆盖，必须显式声明本次评审覆盖"上一轮上界之后的全部提交"。② 按 C3-T 规则对确属 `doc-only` 的收尾提交登记**逐条钉 sha + 写理由**的例外（注意 `doc_only_globs` 已含 `docs/**`，但改动混合文件（如 `docs/**` + `agents/policy.json`）不会被自动归类）——本批实测这 16 条**都不适用**（全是代码/政策/CI 改动），把它们登记为例外等于用例外表承担主路径。
   - **不得**用 `set-anchor --covers-through HEAD` 伪造覆盖：那等于声称这些提交被评审过，而实际没有——属于"改数据以换绿"，本卡明令禁止。
2. **`run-…-doc-audit-066` 的 `covers_through="9ffc108"`（7 位短 sha）**——与 062 同族。**已处置（D0-2，2026-09-25）**：`set-anchor run-2026-09-24-doc-audit-066 --covers-through 9ffc108d69113883b99bb23162be38d1fde5d0b8 --reason "…"`。**只规范化、不改范围**：该 run 自己的报告明写"审核对象: 本地提交 `092a2f78..9ffc108`"，两者是**同一个提交**（`git rev-parse 9ffc108` = 完整 sha）。账本实读：`covers_through` `'9ffc108'`(7) → `'9ffc108d69113883b99bb23162be38d1fde5d0b8'`(40)，`anchor_backfills[1] = {field, from, to, reason, by}`。
3. **`run-…-code-review-068` 的空窗口**——**已处置（D0-3，2026-09-25，选(a)）**：它不是内容评审，而是"实现类工作补登记挂评审 role"的实例（卡内 ⑤；其 `task_id` 与 `scope_deviation` 已自述"非分支评审"）。B3 对它报"内容评审类空窗口"是**结构性假红**。处置 = 新增 `scripts/agent-ops.py mark-produced <run_id> --reason <理由>`，把"它不是内容评审"落成**账本数据**（`produced_only: true` + 理由 + `produced_only_marks[]` 留痕数组），B3 据此**只对已标注 run 跳过空窗口这一条判据**。**没有伪造覆盖**：`coverage_anchor`/`covers_through` **一个字节没动**（仍 `08dbea94..08dbea94`），C3 未归属提交一条不少。为什么不选 (b)：例外清单会把"账本缺口"固化成长期放行规则，且它是闸门自己的白名单（"改数据 vs 改闸门"里错的那一边，同 B4 的取舍）。反向对照 4 条 + CLI 用例 10 条见卡内"实现与自检状态"。
4. **CI 关闭前置块当前是空跑**（本批实测，比 D-prep 批的口径更准）：选择器正则（`ci.yml:73`，逐字 `三查锚点\s*[:：]\s*`?[0-9a-fA-F]{7,40}`）遍历 `docs/iteration/sprint/*sprint-*.md`，实测**19 份文档命中 0 份**（sprint-17 的锚点是 `待填（…）`，非十六进制 ⇒ 不匹配）。故该步骤**现在被跳过**，不是"必红"；**填上真锚点的那一刻**它才首次实跑——而按上面投影，那一刻它会报 3 项 ⇒ 仍是**关闭阻塞**，只是机制更精确。**D0 批复测修正**：上面 3 项中的 2 项 `[C3-窗口]` 已由 D0-2/D0-3 清掉，**填好 §9 + 真锚点后实测剩 1 项**（C3 未归属提交，构成见第 1 条）。

## 实现与自检状态（本批终态，2026-09-25）

- **断言数（如实报，均增不减）**：`verify_close_readiness.py` 自检 **40 → 51 → 55**（+11：B1 五条 + B3 七条 − 其中重复计 1；**+4：D0-3(a) 的四条边界**——未标注仍 FAIL / 标注后不报 / 标注可见 / 短 sha 不因标注豁免）；`verify_agentops.py` **84 → 91 → 101**（+7：UC-17 拆条 + UC-17b 空操作 + UC-17c 五条；**+10：UC-20 `mark-produced` 全路径**）。其余闸门断言数不变；新增闸门 `verify_deduction_rates.py`（§2.1.1 扣率口径，**34 条**）。
- **闸门终态（全部 rc=0）**：`close_readiness` 自检 55 / `deduction_rates` 34 / `md_tables` 163 文件 PASS / `card_index` 默认+strict 各 9 / `no_policy_hardcode` 22 / `ledger_measurement` 30 / `artifact_paths` 10 / `agentops` 101 / `lint` 11 / `structure-guard --replay` 全通过 / `verify_matrix.py check` 「53 脚本一致」/ `run_suite.py --tier offline` **SUITE PASSED (22 scripts), status=ok**。
- **一处口径澄清**：`verify_lint.py` 用**显示宽度**判 E501（CJK = 2 列），与 Python `len()` 不同——本批新增中文行曾因此多出 E501（一度 2683 > cap 2631）；已逐行折行修掉，**未上调 cap**（棘轮"只许下调"）。

## G2 追加条目（2026-09-25 用户裁定 B4②：复核给出的 5 条机制缺口排进 G2）

> 来源：关闭期 6 轮独立复核（`run-…-074`~`-079`）末尾的 "Sprint-18 backlog" 小节。**均非假绿**（当时如实标注为"非阻塞"），但都属"闸门可信化"同族，故并入本卡。

1. **倒挂窗口缺具名判据**：`anchor ∈ 序表` 而 `covers_through` **早于** `coverage_anchor`（`through < anchor`）时，窗口既不点名也不贡献覆盖 ⇒ **静默 0 覆盖**。真数据 17 个窗口里 0 例，且 `register`/`finish` 路径产不出该形态（`covers_through` 在 `finish` 时记、必 ≥ 登记时 HEAD），**只能由 `set-anchor --covers-through` 造成**。修法：加一条 `through < anchor` 的具名判据 + 反向对照（构造倒挂窗口 → 必须点名）。
2. **棘轮单调性只对 `git HEAD` 比较**：`verify_gate_integrity.py` 能拦住**未提交**的上调；一次**已提交**的放宽会成为新基线（`measured_total` 也可同步抬）。修法：把"实测值"也钉进政策（或让闸门接受一个只在重评时更新的 `rebased_at` 锚）。
3. **`md_tables` 的"应扫/实扫"在缺整棵子树时同步缩水**（原前提已更正，2026-09-25 G2 规划期实测；原文的数字与路径均为重建值）：原文写"文档集由政策 glob 派生、含 `agents/runs/**` ⇒ 本机 163 文件、CI 只解析到 1 个文件却仍 rc=0"——**实测三条都不成立**：政策实读 `md_table_globs` = `docs/**/*.md` / `docs/**/*.MD` / `verify/**/*.md` / `verify/**/*.MD` / `agents/README.md`（**无 `agents/runs/**`**）、`md_table_docs` = 8 个显式文件、`md_table_coverage.roots` = `['docs','verify']`；windows 实跑 **应扫 164 / 实扫 164**。**真实形态（可复跑）**：在**没有 `docs/iteration/` 的树**里（`git worktree add --detach %TEMP%\g2-main-check origin/main`）同一政策实跑 = **应扫 8 / 实扫 8，rc=0，打印 `MD-TABLE PASS（8 个文件解析通过）`** ⇒ 同一闸门在"少 156 个待审文件"的环境里读数**完全自洽**。根因：`docs/iteration/**` 只经 **glob** 进入扫描集，而"应扫"（roots + include_files）与"实扫"（docs + globs）**都由同一份政策声明派生** ⇒ 整棵子树缺席时两侧**同步缩水、等式仍成立**；`roots` 层的"目录缺失即 fail-closed"只管 `docs`/`verify` 这一层，管不到子树。修法：给"本次环境里该子树**必须存在**"一条具名判据——把 `docs/iteration` 提升为独立声明的 root 并按分支给**显式 SKIP**，或加"实扫文件数 ≥ 声明下限"的判据；**不接受**仅靠"应扫 == 实扫"（它是自洽式，不是覆盖式）。
4. **关闭闸门的 `[linkage]` 在任何复核 run 期间结构性红**：复核 run 一登记就出现"账本有 run 但 §9 未登记"，直到主代理补登记为止。修法：给 §9 登记一个**在飞宽限**（口径同 `ledger_measurement` 的 `terminal_writeback_grace_minutes`）。
5. **"全新 checkout 模拟"的方法要写进流程**：必须 `git clone`（含 `.git`、全历史），`git archive` 会剥掉 `.git` 导致两脚本假失败（实测 `SUITE FAILED (2/24)` vs clone 全绿）。已写入 sprint §9.3 与 `1-WORKFLOW.MD` §6 派单纪律第 5 条，**此处登记为卡片条目以便 G2 验收时对照**。
6. **F1：跳过时仍打印无限定的成功横幅**（复核 `run-2026-09-25-code-review-080` finding F1，minor）：`verify_card_index.py` 在打印 `SKIP[no-iteration-docs]` 之后，紧接着仍输出 `CARD-INDEX PASS：索引↔卡文件一一对应（含必备节 / 正文分离 / 状态三处一致 / 卡库存基线一致）` —— 那条横幅把**四条被跳过的判据**说成通过了。修法：SKIP 生效时抑制/替换该横幅（改为 `CARD-INDEX SKIP（分支无该目录）`）。**同族检查**：其余"缺数据即跳过"的闸门（`verify_ledger_measurement`、`verify_agentops` M-g、`verify_close_readiness`）也应按同一口径自查一遍横幅文案。
7. **F2：死键放宽过宽**（同批 finding F2，medium）：`verify_derived_numbers.py` 的"父目录不存在 ⇒ 分支差异"判据，把"分支本就没有"与"**本分支上被整目录删除**"混为一谈——复核实测在 windows 提交的临时 worktree 里 `rm -r docs/iteration/pre-research` → 闸门 **rc=0**（只报 2 条 NOTE），而修前那是硬 FAIL；`docs/iteration/sprint/**` 同理。还重新打开了该判据本要堵的口子：**在不存在目录下新增一个基线键 = 静默预留豁免**。边界：仅 windows-only 子树（共享 `docs/*.MD` 缺失仍 FAIL）、整 phase 删除仍被 `verify_card_index` ⑦ 抓住、改动在 diff 里很显眼。修法（复核建议）：用 `git ls-tree HEAD -- <dir>` 区分"分支本就没有"，或把放宽**限定在 `docs/iteration/**`**。
8. **跨 Sprint 窗口翻转（本节唯一"关闭后当场发生"的机制缺口，2026-09-25 实测）**：`verify_close_readiness.py::scope_step_run()` 取 `close_gate.scope_ref_step`（本仓＝`impact-assessment`）**全局最新**的那条 run，**没有任何 sprint 身份** ⇒ **下一个 Sprint 的 kickoff 〇查一登记，上一个 Sprint 的窗口必然作废**。实测：`run-2026-09-25-impact-assessment-081` 登记后，Sprint-17 的关闭读数由 **1 项 → 99 项**，其中 **98 项是假红**（`[linkage]` 65 + `[C1]` 33 + `[窗口/次序]` 1）；根因链与反事实见 [`sprint-17.md` §9.4](../../../sprint/2026-09-21-sprint-17.md)。该事故**同时推翻本闸门 docstring 的"真数据零假阳性"自述**，故不能按"文档措辞"处理。修法：给作用域 run **sprint 身份**（账本记 `--sprint`，或由任务名/scope 声明派生），闸门按"**被校验 Sprint 自己的**作用域 run"选窗口。**反向对照 3 条（缺一条即不算交付）**：① 登记一条更晚的作用域 run **不得**破坏上一个 Sprint 的窗口（= 在本条原始事故上复现）；② 指向错的/更晚的作用域 run 仍要具名 FAIL（**不得**把"找不到自己那条"静默成"无窗口 ⇒ 退回全域"）；③ G2 自己的关闭窗口不受将来 G3 kickoff 影响。**修好之前**的兜底纪律（已写进 `sprint-17.md` §9.4）：G2 的关闭 〇查登记后，关闭期内**不得**再登记任何 scope 步 run。
   - **状态（2026-09-25 D2 已闭环）**：`register --sprint` + `set-sprint` 受控回填 + 真账本 20 条身份回填；
     实测 Sprint-17 = 1 项真项、Sprint-18 = 4 项全真、Sprint-16 的域右端不再被 Sprint-17 的 `080` 借位（三场景逐条见 [`TG-19`](TG-19.md) 的 M-A 收尾节）。

## G2 条目交付状态（2026-09-25 D5 收尾；逐条给证据，未完成的写"待"）

| 条目 | 状态 | 证据（可复跑） |
|---|---|---|
| 1 倒挂窗口具名判据 | ✅ **已交付（D4）** | `verify/agent_policy.py::Attribution.window_problems` 新增倒挂判据（`order[through] > order[anchor]` ⇒ 具名 + `set-anchor` 修复指引）；**与"空区间"分族**：空区间是**覆盖声称**问题（按 run 类别分流），倒挂是**数据自相矛盾**（与短 sha 同族，任何 role 都不豁免）。反向对照 3 条（`verify_close_readiness.py` 自检：倒挂 ⇒ 具名 e；不按 role 豁免；同端点方向正确 ⇒ 零问题防假红）。**真数据 17 个窗口 0 例** ⇒ 本判据是**前置**的，不是事后补的 |
| 2 棘轮单调性只比 `git HEAD` | ✅ **已由 `TG-19` M-C 闭环**（2026-09-25 D3，本批只做证据核验） | `verify_gate_integrity.py::committed_rebase_problems`（提交态放宽必须 `rebased_at` + `rebased_reason` ≥10 字符 + **`rebased_from` 逐键等于父提交被抬高键的值**）；真跑 19→**23** 断言中含 N/N2/N4 三例（N4 = 复核 major 的"先降后升 + 复用旧署名"）。**本批无新改动**——只补本行证据，不重复实现 |
| 3 `md_tables` 缺整棵子树时两侧同步缩水 | ✅ **已交付（D5）** | `agents/policy.json::md_table_coverage.conditional_roots`（`docs/iteration` ＝ windows-only）＋ `verify/verify_md_tables.py`：把"分支本来就没有"与"**本分支上被删**"用 `git ls-tree HEAD -- <路径>` 的**直接证据**分开——① HEAD 里有、工作区没有 ⇒ **FAIL（被删）**；② HEAD 里也没有 ⇒ **具名 SKIP**，判决行改打 `MD-TABLE SKIP（…不是通过）`（`PASS` 横幅不再出现）。三种形态**真跑实测**（`%TEMP%` 的临时 worktree，测后已清理）：windows 形态 `MD-TABLE PASS（171 文件）`／删掉条件根 ⇒ `rc=1` 且点名『在 HEAD 的提交树里存在、工作区却缺失 ⇒ 被删』／main 形态（`origin/main` 检出、分支从没有该树）⇒ `MD-TABLE SKIP（8 个文件…不是通过）` rc=0——**旧行为在 main 形态上正是 `MD-TABLE PASS（8 个文件）`**（案例 3 的"少审 156 个文件在读数里不可见"）。`structure-guard.py` 的覆盖报告同步按四元组取值 |
| 5 "全新 checkout 模拟"方法入流程 | ✅ **已在位**（本批只做证据核验） | `docs/1-WORKFLOW.MD` §6 派单纪律第 5 条（必须 `git clone`，`git archive` 剥 `.git` 会致两脚本假失败）；`sprint-17.md` §9.3 同款记录。**本批无新改动** |
| 6 F1 跳过时仍打印成功横幅 | ✅ **已由 `TG-19` M-B 闭环**（2026-09-25 D1） | `verdict_line()` 三类互斥判决行 + SKIP 档不打印 `EVIDENCE:`；真入口实测（无 `docs/iteration/` 的树、账本缺失档） |
| 7 F2 死键放宽过宽 | ✅ **已交付（D5）** | `verify/verify_derived_numbers.py`：判据由"**父目录**是否存在"（间接推断）换成 `git ls-tree HEAD` 的**直接证据**——① 该文件或其目录在 HEAD 里存在、工作区没有 ⇒ **FAIL（被删/死键）**；② 两处都没有 **且该键是 HEAD 政策里已有的旧键** ⇒ NOTE（真分支差异）；③ 两处都没有 **而键是新增的** ⇒ **FAIL**（'在不存在的地方新增上限 = 静默预留豁免'，即复核指出的第二个洞）。反向对照 **+4**（J/K/L/M，断言数以现跑 `EVIDENCE:` 行为准，现 14） |
| 4 `[linkage]` 在复核 run 期间结构性红 | ✅ **已由 `A-M13` 闭环**（2026-09-25 D2） | `verify_close_readiness.py::check_linkage` 的在飞宽限（取值同 `ledger_measurement.in_flight.terminal_writeback_grace_minutes`）；**并在 D3 后按复核 major 加固**：宽限加下界（`0 <= age < grace`），未来时间戳走具名 FAIL |
| 8 跨 Sprint 窗口翻转 | ✅ **已由 `TG-19` M-A（含 D2 收尾）闭环** | 见上文本条末的"状态"行（三场景实测） |
