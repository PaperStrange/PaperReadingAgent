# TG-12 **文档内容漂移复盘 + 派生事实机制（用户 2026-09-21："sprint17 和以前的开发过程发现的文档内容漂

- `card`: TG-12
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**候选（用户 2026-09-21 要求开卡复盘；同类问题已足够成卡）**

## 规模
3

## 来源
**用户 2026-09-21（UTC+8）**："我想从 sprint17 和以前的开发过程发现的文档内容漂移问题也足够开个卡做复盘了，把类似问题一并加到 backlog 中吧"

## Sprint
Sprint-18（治理批 G2）

## 正文
`[用户插入]` **文档内容漂移复盘 + 派生事实机制（用户 2026-09-21："sprint17 和以前的开发过程发现的文档内容漂移问题也足够开个卡做复盘了"）**：**同一类"数字/状态/结论副本过期"问题已反复发生 ≥4 波，每波都靠人工发现+手工改数字**——Sprint-7 起就有 `M6`（文档例行勘误：`verify/README` 表格列数错位、清单漏项、`4-ALGORITHM` 行号锚点漂移）；此后 doc-audit 026 抓出 `4-ALGORITHM` §9.2 DSL 描述旧控件；Sprint-15 记录**日期整体漂移**（全标 09-07，实际 09-10）；Sprint-16 内**修了三轮**：`6ecee2a`（指南矩阵 40→41 / offline 10→11 / runner 20→26）→ Round-5 `bdc669b`（指南 41→44 / offline 11→13 / `4-ALGORITHM` 19→29 / `CK-1` 答复"前端零展示"过期）→ 本次 `2e1d20f`+`4e17ddd`（**11 处**：44→**45** 脚本、offline 11→**14**、runner 26→**42**、usage 21→**27**、`N-1`/`N-2` 估算 0.05/0.5→**0.02/1.1**、已知局限第 1/3 条已闭环仍写未解决、**TG-5 卡状态"计划中"实为 DONE**、F-AC8 备注"待用户确认"实已决策、Sprint §3/§7 与 **PR 正文要点**旧数字）。**共性**：同一事实被**抄进多处**（指南/§3/§7/PR 文案/卡状态/算法文档），源头变更后**无重生成机制、无检测点**，且修一次只修"当时看见的那几处" → 下一次必然再漂移。

## 证据
**卡内待产出（复盘 + 机制，不新增条文）**：<br>① **卡内事实清单**（术语见 `1-WORKFLOW.MD` §3：`台账` 仅指独立文件；**本卡无独立台账文件**）：汇总历次漂移波次（**第 5 波 = 2026-09-21 关闭三查修复批**：㈠ 我修漂移时把 `est_cost_cny` 的**加系数前旧值 0.02/1.1** 从 Sprint §7 转抄进 3 处文档（SSOT 实为 **0.03/1.4**，一查独立复现抓出 → 教训 1.63）；㈡ "三查后提交数"两种口径并存（20 vs **26**＝`1389738..HEAD`），被一查/二查各自纠正；㈢ **修复批本身改动了断言数**（runner 42→**46**、usage 27→**29**、checkpoint_index 29→**31**、freshness 22→**25**、ledger_rounds 13→**15**、agentops 42→**43**）→ 所有引用断言数的文档**当场过期**、只能同批重写；㈣ `verify_smoke` 的"12 路由"此前**只是元数据声明、无任何断言在守**（本轮补钉桩）；㈤ `verify/README.md` 表格漏登 8 脚本，违反该文件自身第 10 行规则。㈥ **断言的环境鲁棒性也是"未验证面"**：`verify_runner` ⑩a 用裸子串 `RUN` 判定"未放行"，而 GitHub runner 的临时路径含 `RUNNER~1` → **断言在 CI 上恒假**（PR #51 首轮 CI 失败）；本地路径不含该子串、且该断言自 TG-7 引入后**从未在 CI 跑过** → 环境依赖长期不可见（已改 `_has_marker()` 行首标记判定，10 处统一）。**这六条共同证明：手抄型"事实"与"只在本地验证过的判定"都会随环境/改动过期，SSOT + 环境无关的断言才是解**。上表另列 4 波；建议把每波的"发现者/发现方式/修复提交/漏改处"补齐，作为复盘输入）。<br>② **根因假设（待复盘定论）**：**D1 派生事实无 SSOT**——脚本数有矩阵作 SSOT，但**文档里的是副本**；断言数**根本没有 SSOT**（只在脚本运行时输出）；卡状态纯人工维护。**D2 无"变更→重生成"触发**——加脚本/改断言/改决策后没有任何装置去刷新引用处（`verify_matrix.py check` 只管矩阵自身）。**D3 无检测点**——一查 doc-audit 有"陈旧事实"项，但**无"计数与状态一致性"的可操作判据**，关闭闸门也不查文档数字。**D4 修正方式加剧问题**——每次手工改数字而非消除副本，必然复发。**D5 历史与现状混排**——Sprint §5 日志是"当时读数"、§3/§7/指南是"当前状态"，同文档无标注规范，读者与 doc-audit 都分不清哪处该最新。<br>③ **机制候选（可执行）**：**N1 派生事实登记**（每条 = 事实名/生成命令/当前值/生成时间；文档引用占位符或"见命令输出"）；**N2 漂移闸门** `verify/verify_doc_drift.py`（offline，零成本）：扫文档中的可生成数字（`N 脚本`/`N 断言`/`CHECK OK: N`/`SUITE PASSED (N`/`est ¥X`）与实测比对 + 校验 backlog 卡状态 vs Sprint 文档 ✅/DONE；**N3 生成式状态报告**（本次手写的汇总报告改为脚本生成，避免报告本身成为新漂移源）；**N4 doc-audit spec 增补固定检查项**（"计数与状态一致性 + 历史/现状标注规范"，spec 正文改动按 §4.2 流程）；**N5 写作规范**（凡引可生成事实必须附"生成命令 + 生成时间"，历史读数必须显式标"（当时读数）"，否则一查判 FAIL）；**N6 与 TG-11 合流**（scope/漂移/关闭就绪三类闸门统一接入 CI offline 与 PR 前置）。<br>**N7【用户 2026-09-21 提出的结构性问题·优先候选】一卡一文档 + backlog 瘦索引 + 卡片索引 lint**：用户原话——"`phases` 下的所有文件夹，单个卡片应该对应且唯一对应一个独立文档。现在卡片内容全都记录在 backlog 中，有的卡片内容散落在 sprint 文档中到处都是。一旦卡片内容更新了其他几处万一有的地方漏了就直接造成文档内容漂移"（另追问"你经常说实例台账我咋没找到是啥文档"）。**现状（如实）**：卡片定义/根因/机制/实例**全部写在 `backlog.MD` 的表格单元格里**；Sprint 文档另存过程记录；本卡所谓"实例台账"实为**单元格内的一段文字**——全仓**仅 `TG-11` 有真正的独立文件**（`2026-09-21-review-scope-incident-evidence.MD`），**术语含混须统一**。**建议结构**：`phases/<phase>/cards/<卡号>.md` **一卡一文件**（卡内 = 状态/规模/来源/Sprint/正文/证据/台账）；`backlog.MD` 退化为**瘦索引**（卡号｜一句话｜状态｜Sprint｜文件链接）；Sprint 文档**只按卡号引用**、不复制卡正文。**配套 lint**：`verify/verify_card_index.py`（offline，纳入套件）校验 ① 索引行 ↔ 卡片文件一一对应（无孤儿/无重复）② 文件名与卡号一致 ③ 状态字段与 Sprint 文档引用一致 ④ Sprint 文档不得复制卡正文（按小标题特征扫描）⑤ **反向对照**：删一份卡文件 / 改一处状态 / 在 Sprint 文档塞入卡正文 → 必须 FAIL。**术语约定（须写入 `1-WORKFLOW.MD` §3）**：`台账` **仅指独立文件**（如 TG-11 证据台账）；卡片单元格内的清单一律称"**卡内实例清单**"，不得混称"台账"。**迁移成本提示**：全仓现有卡约 60+（refactor-analysis ~30 / agents-infra ~25 / testing-governance 12 / mental-models 6），建议**先在一个阶段试点**（建议 `testing-governance`，卡数适中且本卡就在其中），验证形态后再批量迁移。**✅ 用户 2026-09-21 决定：N7 归入本卡（TG-12）正式执行时一并做**（不在 Sprint-17 之前单独立项）；**术语约定已即时生效**——`台账` 仅指独立文件、单元格内清单一律称"卡内实例清单"，并连同引用纪律写入 `1-WORKFLOW.MD` §3（2026-09-21）。<br>④ **反向对照判据（"倒过来试"）**：把 `TEST-MATRIX` 脚本数改一位 / 把指南"14 脚本"改回"11" / 把某卡改成"计划中"而 Sprint 标 DONE / 把 est 数字改一位 → **闸门必须 FAIL**；全绿状态 → PASS。<br>⑤ **与既有卡的关系**：**建议复盘时决定 `M6`（refactor-analysis，文档例行勘误，持续）是并入本卡还是保留作执行项**；与 `TG-2`（矩阵 SSOT）、`TG-6`④（规范条目↔执行记录抽查）、`M18`（报告时效）联动。<br>**⤴ 2026-09-21 用户决定：机制候选 `N7`（一卡一文档 + backlog 瘦索引 + 索引 lint）已提升为独立卡 `TG-14`**——本卡机制清单收窄为 **N1~N6**（派生事实/漂移闸门/生成式状态报告/doc-audit 增补/写作规范/TG-11 合流），N7 的 4 类证据与迁移方案随卡移交 `TG-14`，避免两卡重复拥有同一机制。
