# F-AC13 节点运行按钮**点击 1~2 次无响应**（第 3 次才生效）——**根因已定位（2026-09-20 全量按钮审计）*

- `card`: F-AC13
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**计划中（Sprint-17 插队卡，用户 2026-09-21 拍板）**

## 规模
2

## 来源
**用户走查 2026-09-20（UTC+8）**：Sprint-16 走查新增问题①；**2026-09-21** 用户要求"影响范围评估下再交给我看看"→ 评估已出；同日拍板 **Sprint-17 插入**、**接受"不能从按钮行拖动节点"**、**要求新检查先在未修复代码上跑出 FAIL（反向对照，用户称"倒过来试试"）**

## Sprint
Sprint-17（治理批 G1 搭车）

## 正文
`[Bug]` 节点运行按钮**点击 1~2 次无响应**（第 3 次才生效）——**根因已定位（2026-09-20 全量按钮审计）**：`FlowNode.jsx` 中带 `nodrag nopan` 的只有文本区（:120）与 output 块（:177/:242/:249），而 **5 个运行按钮所在的 `.node-actions`（:133、:146）完全没有该 class** → mousedown 阶段被 ReactFlow 拖拽/选择吃掉首次点击。修法：给 `.node-actions`（或每个按钮）加 `nodrag nopan`（与既有两处一致）

## 证据
**影响评估（2026-09-21）**：见 [`2026-09-21-fac13-14-impact.MD`](../2026-09-21-fac13-14-impact.MD)——最小修复面=2 行 class；受影响检查 `gui_check_s5/s7` 用程序化点击**不会因此变红**；⚠ 关键发现：**现有 GUI 检查全用 `.click()`，结构上无法触发本 bug** → 修复必须同时新增**真实鼠标 down/up 序列**检查 + 反向对照。关联：运行按钮无 `disabled`、`runSingleNode` 无 `running` 早退 → 按用户 2026-09-21 口径"**允许运行中点击**"，重复点击语义未定义 → 只记 `F-AC17`，不实现
