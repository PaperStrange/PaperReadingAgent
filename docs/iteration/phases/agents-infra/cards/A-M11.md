# A-M11 **Agent workflow 与提示词治理迭代（对照 OpenAI/Anthropic 的 `AGENTS.MD` 

- `card`: A-M11
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**候选（用户 2026-09-21："这两个方案需要继续调研后再确认怎么做，先记张卡吧"——**本轮只记卡、不实现**）**

## 规模
3

## 来源
**用户插入 2026-09-21（UTC+8）**

## Sprint
Sprint-18（治理批 G2；含 MM 并行线的交叉用例）

## 正文
`[用户插入]` **Agent workflow 与提示词治理迭代（对照 OpenAI/Anthropic 的 `AGENTS.MD` 实践 + 评估 OpenAI codex harness）**：目标 = **让每个 Agent（含主代理自己）减少甚至不再出现"擅自行动 / 违规行为"**。用户给出的两条方向：① **现有 agent 框架结构对照 OpenAI 与 Anthropic 目前使用的 `AGENTS.MD` 提示词管理方式做更新迭代**；② **借用 OpenAI 开源的 codex harness 工具包管理 agent workflow**。

## 证据

### 派单纪律与反例留档（**用户 2026-09-25 定规**）
> 关系：derived-from=`docs/iteration/phases/agents-infra/2026-09-25-subagent-dispatch-failure-modes-case.MD`（一案一节，6 要素齐备）｜input-to=`docs/1-WORKFLOW.MD` §6「子代理派单纪律」（7 条条文真源）

**规则**：与子代理派单相关的**一切漏洞/问题/事故**，一律作为 `agents-infra` 的**反例留档**（写进上面那份 case 文件，一案一节，必须含 现象/证据/根因/机制/反向对照/复用面），并同步把派生规则写进 `1-WORKFLOW.MD` §6。**当日追加，不许只写在对话里。**

**已留档的 6 个案例（2026-09-25 首版）**：① 派单 prompt 里手抄短 sha `092a2f78` ⇒ 3 个 run 的覆盖窗口静默作废；② 三个修复批余额耗尽中断、未回执，留下未验证半成品（含"崩了仍打印成功行"）；③ 复核的对照走假路径（注入中间变量，躲过真实缺陷）；④ 子代理报告"已修" ≠ 提交里有（主张与事实错位）；⑤ "全新 checkout 模拟"用 `git archive` 剥掉 `.git` ⇒ 假失败（必须 `git clone`）；⑥ 派单前提未经复现（我假设"登记 run 即归属覆盖"，而 C3 引擎当时 14/14 窗口全失效）。

**本卡在 G2 的动作（据此新增）**：把上述 7 条与 ①~⑥ 的调研/盘点结论**一起**做——即"提示词/框架层治理"的输入不再只是外部实践对标，还包括**本仓自产的派单失效模式**；验收判据沿用 ④（可观测指标 + 逐条反向对照），其中"**prompt 里手抄标识符 = 0**"与"**派单前未复现前提 = 0**"为本轮新增指标。

**卡内待产出**：<br>① **调研（开卡即触发 `planning_pipeline` 的 tech-research，深度按决策影响取 expert 或 scholar）**：≥3 来源/论断 + 对比矩阵 + 结论建议，并按 M16 归档结构落 `context.md`/`evidence/`/`reasoning.md`：OpenAI 侧 `AGENTS.md` 的分层/作用域/优先级与 Codex 用法；Anthropic 侧 `CLAUDE.md` 与 subagent/技能约定的异同；**codex harness 工具包的定位、能力边界与可复用面**（它解决 workflow 编排的哪一层，与本仓 `agents/fanout.json` + `agent-ops` 账本重叠/互补在哪）。<br>② **现状盘点（规则 vs 闸门）**：把本仓 `agents/functions/*.md`（8 个 spec）、`agents/fanout.json`、`1-WORKFLOW.MD` §3/§4/§6 的条目分成"**只有文字**"与"**有可执行检查点**"两类 —— 直接对应 TG-11 的核心结论"有规则 ≠ 有执行点"。<br>③ **方案对比与建议**：A 仅引入 `AGENTS.md` 式**分层提示词治理**（根/子目录/角色三级 + 冲突优先级）；B 引入 **codex harness** 管理 workflow；C 混合；D 维持现状只补闸门。含成本、兼容性（DSH/多 IDE/CI）、迁移风险，以及"不引入"的正当理由。<br>④ **度量与验收**：把"擅自行动/违规"变成**可观测指标**并逐条给反向对照（示例：未跑〇查即派评审 = 0 次；三查锚点之后未评审提交 = 0；self-chosen scope 无 deviation 声明 = 0；§9 与账本交叉不一致 = 0；规范条目↔执行记录抽查不一致 = 0）。<br>⑤ **与既有卡的分工（避免重复建设）**：`TG-11` = scope 越权的**具体闸门**（证据台账已固化，复盘待做）；`TG-12` = 文档口径漂移的**检测机制**；本卡 = **提示词/框架层**的治理与外部实践对标 —— 建议在 TG-11/TG-12 复盘出结论后再定本卡方案，以免三张卡各自造一套。关联 `3-LEARNED` **1.29**（scope 规则）、**1.61**（部分修复比未修复更危险）、**1.62**（同语义定位须复用同一实现）。<br>⑥ **【一级子目标·用户 2026-09-21 追加】过程事故与"内容丢失/误删"防线（防患于未然）**：用户原话——"过程事故也是 A-M11 卡需要解决的问题之一，防范提前避免，尤其是这种可能会造成内容丢失的情况，和之前遇到的多次文件莫名被删除的情况等等"。**已发生的实例（复盘输入；卡执行时补全时间线）**：㈠ **2026-09-21**：`git commit -m` 传含**反引号**的中文消息 → PowerShell 解析破坏 → **提交根本没有发生**（且无任何提示），随后 `git switch -f` 把工作区未提交的修复**直接丢弃**（只能重做）——"以为提交了其实没有"＋"强切分支静默丢弃"是最高危组合；㈡ **2026-09-21（多次）**：文档编辑"**锚点行被吞**"——替换某行时连带吃掉下一行的标题/表格行（`## 附：证据索引`、`## 2. 启动条件`、`TG-10` 表行、"阶段 2 待确认"注记），均是事后自查才发现，**不报错、只静默少内容**；㈢ **分支操作致目录"凭空消失"**：建 PR 分支时 `git switch -c sync/sprint-16 origin/main` 使 `docs/iteration/**`（44 文件）从工作树消失，用户视角即"文件莫名被删除"；㈣ **远端丢件**：PR 若建在落后的 `origin/windows` 上会静默丢 28 个未推送提交（二查 main critical#1 实证）；㈤ **有意分叉的删除语义**：4 个 `scripts/*.sh` 相对 main 是 D，同步时若不 `git checkout origin/main --` 挡住就会真删到 main；㈥ 历史同类：`verify_checkpoint_run.log`/`ci_fail_log.txt` 误入库后清理、`agents/runs/_acceptance-scratch-2026-09-20/` 走查后清理（用户观感同样是"文件消失"）；㈦ **【2026-09-21 现场实证·用户批准补录】杀软 AMSI 导致的"命令级静默失败"三连**：(1) 后台任务（PR #52 轮询+合并脚本）**因 AMSI 编译期崩溃整条未执行**——没有轮询、也没有合并，只是打印了一篇 .NET 堆栈，**看起来像"命令失败"实为"命令从未开始"**，随后改用**纯 ASCII 短脚本**重做成功；(2) 重做 `_has_marker` 修复的长脚本在编译期崩溃 → 该次修改**根本没落盘**，改走编辑工具；(3) 提交"AMSI 规则"的长命令再次编译期崩溃 → **按新规则核对效果**发现 `HEAD` 未变、三个文件仍是未提交状态（连 `git add` 都没跑）→ 改用**短命令**重做成功（`c48089c`），措辞合规补丁为 `442d5e1`。**根因**：本机唯一 AMSI 提供者是火绒 `hramsi.dll`（Defender AMSI 关闭），**所有 pwsh 进程退出码恒为 `0xC0000005`**（含成功命令），脚本越大越复杂越容易在**编译期**崩溃。**该实例的教益**：① 退出码在本机**不可作为判据**，一律验效果（本条同时是 (b) "提交是否真的发生"的现实版）；② 命令体必须小、大文本走文件；(3) 崩溃信息是**噪声很高的长堆栈**，容易被误读为"命令执行到一半失败"，须以 `git log/status`、文件 mtime、产物存在性等**外部证据**复核。<br>**该子目标待产出**：(a) **破坏性操作前置闸门**——`git switch -f`/`checkout -f`/`reset --hard`/`clean -fd`/`rm -rf`/`branch -D`/删目录等，**必须先断言"工作树干净或已提交/已 stash"**，否则拒绝执行并给替代路径（建议 `scripts/safe-git.ps1` 包装器 + 供 agent 调用的检查函数）；(b) **"提交是否真的发生"必须验证**——提交后 `git log -1 --format=%H` 与预期对照、失败即大声报错；**含反引号/引号/换行的提交消息一律走 `-F <文件>`**（本次事故的直接对策）；(c) **文档编辑完整性校验**——每次编辑后比对"标题数/标题层级序列、表格行数、backlog 卡号无跳号、无空行断表"，例如 `verify/verify_doc_integrity.py` 并纳入离线套件，使"锚点行被吞"**当场变红**而非事后靠人发现；(d) **可恢复性**——`git reflog`/`git stash list`/补丁快照（高风险操作前 `git diff > %TEMP%\pre-op.patch`）与标准恢复手册；(e) **反向对照验收**——每个闸门先在违规输入上跑出 FAIL（脏工作树 + `switch -f` → 必须被拒；含反引号消息走 `-m` → 必须走 `-F` 或报错；被吞标题的文档 → 完整性校验必须 FAIL），再在正常输入上 PASS；(f) 与方向①②的关系——`AGENTS.md` 分层提示词与 codex harness **正是把这类纪律写进 agent 常驻上下文/工具层的载体**，目标是把"靠主代理记性"变成"**工具层默认拒绝 + 校验必红**"。<br>(g) **【环境级隐患·2026-09-21 实测】** 本机**唯一的 AMSI 提供者是火绒**（`D:\ArrangeTools\HuoRong\Sysdiag\bin\hramsi.dll` v6.0.0.0；Defender AMSI 关闭 `AMServiceEnabled=False`）→ 每条 PowerShell 命令的脚本正文都被它扫描，而该提供者有缺陷：**几乎所有 pwsh 进程的退出码都是 `0xC0000005`（=既有的 `[exit code: 3221225477]`），包括完全成功的命令**；脚本更大更复杂时，崩溃会提前到**编译阶段**（`AmsiScanBuffer` 栈、整条命令一条语句都没跑——2026-09-21 发生 3 次，均为数 KB here-string + 多处 `.Replace()`）。火绒自身在 2026-09-20 00:43 自动更新过（`SysDiag.exe` 等时间戳可核），与"今日开始频发"吻合（**现场实证三连见 ⑥ ㈦**）。**该子目标待产出**：① 约定"**大文本走文件 + 编辑工具，绝不塞进 shell 命令**"；② **禁止以退出码判定成败**（本机退出码恒为 0xC0000005）→ **一律以输出与效果验证**，并把这条并入 (b) 的"提交事实校验"与所有破坏性操作的前后置断言；③ 在 `agents/README.md`/`1-WORKFLOW` 记一条环境注记，避免后来的 agent 把该退出码误判为命令失败（或反复重试）。<br>⑦ **【用户 2026-09-21 指示·Sprint-16 关闭后执行，现在不动】目标清单的"穷举式补全"**：用户原话——"等当前任务完全结束后，把 LEARNED 文档内容完整过一遍，分门别类地补充下目标，除了我说的目标外肯定还有别的。其他文档内容也可以自主阅读查找类似事件的记录，比如 report agent runtime 下的文档什么的。" → 卡执行时须：**(1) 通读 `docs/3-LEARNED.MD` 全部 1.1–1.63 条目**，按八类（🖥️/🧩/📦/🎨/🛡️/🏗️/🔎/📋）逐条判断"是否属于本卡要根治的 agent 行为/流程缺陷"，命中的**分类汇总成本卡子目标**（含条目号引用，便于溯源）；**(2) 自主检索 `agents/runs/**` 下的报告（一查/二查/lessons/修复验证复核等）与 `agents/runtime/**` 状态文件**，抽取同类事件客观记录（如规则未执行、断言环境依赖、账本写入竞态、报告口径漂移、子代理无产出、误删/丢件等），补进 ⑥ 的**卡内实例清单**；**(3) 结论须回答"哪些目标属于本卡、哪些已由 TG-11/TG-12 覆盖、哪些应另立卡"**，避免重复建设。<br>⑧ **【穷举新增·最大空档】验证可信度（C2b = 24 条）**：③/④ 只覆盖"有没有检查点"与"违规是否可观测"，**"有检查点但检查点在骗人"这一格此前完全空缺**——实测 **C2b 24 条 vs C2a（压根没检查）8 条 ＝ 3:1**，即**本仓的问题不是"没检查"，而是"检查在骗人"**（假绿/形态漏检/负例不命中真实失效模式/只在本地验证过/断言环境依赖）。判据：**反向对照（旧实现须 FAIL）＋"唯一失败项名"＋抽取集 ⊇ 断言集同源＋负例成对＋跨平台口径两侧一致**；逐条清单见 `2026-09-21-a-m11-from-learned.MD`（§C2a/C2b）。**分工写死**：交互/GUI 验收面归 `TG-6`⑤、数字/状态一致性归 `TG-12` N2，**本卡只出通则与判据，不重复造闸门**。<br>⑨ **【穷举新增·因果上游】"接管"这一型（卡内原 7 条实例完全没有）**：实证链＝**子代理超时/零产出/不稳定 → 主代理接管 → 自选 scope → 覆盖边界失效**（四处独立记录：`run-2026-09-09-doc-audit-043`、`-lessons-learned-046`、`run-2026-09-12-impact-assessment-048`、`-doc-audit-049`、`-lessons-learned-052` 均记"前两轮不稳定/未产出"；09-12 因此〇查与一查**都由主代理自己跑** → 窄档 32.8 → 9 天后同批文档被全量档一查开出 must-fix 8 条）。**TG-11 只记了最后一环**，上游从未入册；更关键：`run-2026-09-12-lessons-learned-052:28` **早已写下行动项**"N 分钟无产出即主代理接管判据"——**写下之后仍以同样方式失败**。待产出：**接管判据 + 接管必须留痕（时间/原因/接管者/被接管 spec 的 Forbidden 边界是否继承）+ 事后落盘须标注**。**边界继承实证**：`run-2026-09-09-doc-audit-043:3` 明写"子代理超时未产出，由主代理按 doc-audit v1.3.0 全 9 步执行"，其 Must-fix 全是"已补/已更新"——主代理在执行 **Forbidden: Never modify any file** 的 spec 时改了文件（本次抽检确证）。<br>⑩ **【穷举新增】报告与 run 的绑定只靠目录名**：11 份报告正文**没有 run-id 行** → 待产出：模板硬字段（run-id / spec 版本 / scope 来源）+ 断言（现**无任何断言**）。<br>**⑥ 实例清单追加（B 路 `agents/runs/**` 实证）**：㈧ **"退出码不可信"是双向的**——除 ㈦ 的 AMSI 编译期崩溃外，13 次 pwsh 调用**全部**返回 `0xC0000005` 而输出正常，且该崩溃**污染过行数统计**（须 `ReadAllLines` 才得真值）；㈨ ~~**动作语义与标签不符**：看板配置页"校验"按钮实际无条件直写 `agents/fanout.json`，UI 却标"未写入"~~ **【2026-09-21 主代理复现后撤回】**：**代码是对的**——配置页 `app/fanout/page.tsx:64-69` 的"校验"按钮调 **POST**（`app/api/fanout/route.ts:62-73`，注释明写"校验（dry-run，不写入）"），反馈文案"结构校验通过（dry-run，未写入任何文件）"**准确**；写只发生在 PUT（`:88`）。该缺陷是 **Sprint-10 的 017 major 修复**之前的**历史状态**，B 路从旧教训记录转述时**把历史当成现状**（其自陈"83 条转述未逐条核对"的风险实证）→ **本条不是实例，而是又一个"历史与现状混排"的证据**（`TG-12` 形态 D5；也说明"证据必须核现状、不能只引旧记录"）。㈩ **只读归档被越界增改**：阶段已标"封存只读"，仍发生 `architecture.MD:285` 的 v1.4.0 增改（`doc-audit-021:7`）；(十一) **并发写同一状态文件静默丢段**：`fetch-prices.py --apply` 与 `agent-ops.py prices-derive` 对 `prices.json` 各自 load→merge→save 且无同一把锁（`lessons-037:10`）；(十二) **liveness ≠ responsiveness**：HF 不可达时对已缓存模型仍 HEAD 退避 5 次阻塞事件循环（`lessons-052:13`）、门禁 `rglob` 跟随 junction **挂死**（25s 被 kill；3 层 junction 拖到 5 分钟/CPU 105s/WS 632MB）；(十三) **验收命令与差分口径不可复现**：三点差分把 5 个真实分叉**放大成 94 个、险些触发 94 文件盲目批量同步**（`lessons-019:9`）；`git grep -- '*.md'` 大小写敏感致"0 悬空"为假、漏 14 处 `.MD`；(十四) **账本↔产物目录双向漂移**：**主代理实测 60 个 `run-*` 目录 vs 账本 59 条**（B 路报 63 系口径差异）＋ 账本时长/轮次**不是测量值**（6 组起止时间逐字节相同、含 0.00 min 与恰好 10.00 min；run-060 记 `rounds=5` 而报告只有 3 轮＝高估，run-053 少记 4 轮＝低估）→ 建议**另立卡**（或作 `TG-10` 补充）。<br>**⑤ 跨卡接口（穷举后写死，防四卡各造一套闸门）**：`C1 规则无执行点` → 本卡 ②（**升级为"规则→执行点→触发时机→责任者"四列映射表**，否则重演 1.26"纸面闭环"）；`C2b 验证可信度` → **本卡 ⑧ 出通则/判据** ＋ `TG-6`⑤ 交互面 ＋ `TG-12` N2 数字/状态；`C3 可观测性` → `TG-10`（＋本卡 ⑩）；`C4 破坏性/内容丢失` → 本卡 ⑥；`C5 文档口径漂移` → **`TG-12`（候选未落地；8 条覆盖状态已改标"待 TG-12 落地（当前未覆盖）"）**；`C6 规则检索不到` → **根因归 `TG-11`（其台账 R5 已官方定性"规则检索未发生——任务分类先于规则检索"）**，本卡只做**载体**（① 的 `AGENTS.md` 分层提示词＝把规则挂到动作语境）；`C8 成本/预算` → `TG-5`/`TG-7`（通则侧本卡 ②）。**⚠️ 该接口表目前是「提案·待研究」，不是已定稿的分工**——用户 2026-09-21 明确："**任何涉及写死的操作都需要注意下，这个需要再研究才能决定**"。故本卡执行时：① 先复核每条归属是否与 `TG-6`/`TG-11`/`TG-12`/`TG-13`/`TG-14` 卡正文一致（含"候选未落地"状态）；② 边界条款须**随复盘结论修订**，不得当作既定事实引用；③ 尤其"⑧ 只出通则、交互面归 TG-6⑤"这条**尚未获批**，执行前须重新论证。<br>**穷举明细（两份分析文档，本卡不复制正文）**：`2026-09-21-a-m11-from-learned.MD`（63/63 逐条；正式口径＝**修复动作类型**，本卡域 **49 条**＝37 强＋12 弱，技术类 14；类别计数 C1 16 / C2 32（C2a 8＋C2b 24）/ C3 6 / C4 7 / C5 12 / C6 22 / C7 3 / C8 5 / C9 3；另含八类索引三档改进建议）＋ `2026-09-21-a-m11-from-runs.MD`（34 份报告 / 198 条事件 / Top3 系统性问题 / 归属对账）。**⚠️ B 路自陈 198 条中 83 条为子代理转述、未逐条回原文核对 → 引用前须抽检**（本次已抽检 2 条：账本漂移口径、接管越界，后者确证）。
