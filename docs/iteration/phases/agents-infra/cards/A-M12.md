# A-M12 **编辑边界事故族治理：写入前体检 + 标题层检测**（由 5 次真实事故提炼，用户 2026-09-23 要求成卡）

- `card`: A-M12
- 索引: [../backlog.MD](../backlog.MD)

## 状态
**候选（用户 2026-09-23 要求：`Sprint-17` 内 5 次同型事故 → "没有卡就直接开卡"）**

## 规模
2

## Sprint
Sprint-18（治理批 G2，与 `A-M11`、`TG-17` 同批）

## 来源
**用户 2026-09-23（UTC+8）**："对于这一类事故问题的治理是否有卡，如果没有同样作为反例加入 agent infra，历史卡片也是做类似移动分类操作"。
**事实来源（可核）**：`phases/agents-infra/2026-09-23-edit-boundary-incidents-case.MD`（5 次 occurrence 的逐条证据）＋ Sprint-17 §10 的 R1-1~R1-5。

## 正文
**问题**：本 Sprint 内实测 **5 次**同型事故——`edit`（或自写替换脚本）的 `old_string` / 替换范围跨到了
"我以为是边界、其实是内容"的位置，把**标题行、表格行首、单元格分隔符**吃掉：

| # | 吞掉的东西 | 触发动作 |
|---|---|---|
| R1-1 | `## 2. 启动条件` 整行标题 | `old_string` 以锚点行作边界 |
| R1-2 | `1-WORKFLOW` 下一节的粗体标题 | 插入新节时取了**下一节首行** |
| R1-3 | §5 风险表某行的"处置"单元格 | 替换跨越表格行尾 |
| R1-4 | **两份 Sprint 文档整表被压成 1 格** | 转义脚本把**单元格边界**也转义 |
| R1-5 | Sprint-17 §5 第 10 行**行首被新行顶掉** | 插新行时取了被插入行的行首 |

**关键事实**：**5/5 全部由自检发现**（`verify_md_tables.py` 或一次性列数自检），**没有一次靠肉眼**。
⇒ 失效点不是注意力，而是**动作的假设**；因此"更小心"不是对策，**预防 + 检测**才是。

**与相邻卡的分族（避免重复开卡）**：
- `TG-11` 治"规则被绕过"（**人的判断**越过规则）；
- `TG-12` 治"内容漂移"（**事实**被抄成多份后过期）；
- `TG-14` 治"卡与文档拓扑"（**一卡一文档**）；
- **本卡治"执行动作本身的边界失效"**（工具层）——全仓扫描确认此前**0 卡覆盖**（2026-09-23 实测）。

**已有对策**：`verify/verify_md_tables.py`（离线档，含棘轮基线）已进套件，抓表格类 3/5。

## 证据
**卡内待产出**：
1. **预防机制（核心，比规则强）**：写入前对 `old_string` 做**边界体检**——命中行首 `|` / `#` / 表头分隔行 /
   下一节标题等"边界特征"即**拒绝并要求改用脚本**；落地形态二选一：(a) 独立校验脚本 + 调用纪律，
   (b) 若能挂到 harness 的工具前置钩子则优先 (b)；
2. **检测补盲**：把 `verify_md_tables.py` 的判据扩到**标题层级**（相邻标题缺失/重复/层级跳变）——
   补上 R1-1/R1-2 这类"标题被吞"的盲区（当前闸门只覆盖表格）；
3. **规则入 `1-WORKFLOW.MD` §4.2**：插入/替换 Markdown 结构时 `old_string` 的取值纪律；
4. **反向对照（硬要求）**：对 §1 的 **5 个形态逐一**给出"旧实现 FAIL → 新机制 PASS"的实测
   （用例素材现成：`2026-09-23-edit-boundary-incidents-case.MD` §1 即真实样本，不必编造）；
5. **度量**：下一个 Sprint 结束时统计同型事故次数（目标 **0**；当前基线 **5 次/Sprint**），
   并写入 Sprint Retro——这是本卡唯一可证伪的验收指标。

**验收判据**：① 5 个形态各有一条反向对照实测；② 标题层检测上线并进套件；③ 规则条目落 §4.2；
④ 下一 Sprint 同型事故 ≤1（目标 0）且有数据。
