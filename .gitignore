# --- generated / runtime leftovers ---
__pycache__/
*.pyc
.pytest_cache/
node_modules/
dist/
.venv/
venv/
.next/
.DS_Store
# 看板运行时数据（SQLite 派生索引，文件真相源在 agents/）
agents-dashboard/data/

# --- macOS AppleDouble junk ---
._*
__MACOSX/

# --- local data / logs ---
data/parsed/*.jsonl
# Sprint-3 远程源下载暂存（运行时生成，不入库）
data/remote/
# Sprint-4 用户自定义 provider（模板 providers.example.json 入库）
paper-qa-script/providers.json
# 用户本地论文目录（实测数据，不入库）
data/arxiv_pdf_sep/
tmp.log

# 本地 agent 框架参考目录（skills/notes，工具侧运行时内容，不入库）
.agents/
# TG-5 运行时状态与套件结果（本地运行时产物）
# TG-9（2026-09-25）：`agents/runtime/` 由**逐条列名**改为**目录级忽略 + `!` 白名单**。
# 理由：逐条列名（schedule.json / report_freshness.json / registry.json …）等于"约定靠人记"——
# 新脚本往该目录写**新文件名**时默认不被忽略，`git status` 直接多出未跟踪产物（正是 TG-9 的动因，
# 实测 `git check-ignore agents/runtime/probe.json` rc=1）。改目录级后新产物自动被覆盖。
# 形式取 `agents/runtime/*`（而非 `agents/runtime/`）：**父目录被整体排除时 git 无法用 `!` 重新包含
# 其中的文件**（实测：目录级写法下 `!agents/runtime/prices.json` 仍 rc=0=被忽略），而 `dir/*` 写法下
# 白名单生效（实测 prices.json rc=1=未忽略、新文件 rc=0=被忽略），故必须用 `dir/*` 才能表达白名单。
agents/runtime/*
!agents/runtime/prices.json
!agents/runtime/tg14-baseline.json
verify/suite_result.json

# --- secrets (use .env.example as template) ---
.env
**/.env
.env.local
*.pem
*.key

# --- IDE ---
.idea/
.vscode/
verify/*.log
# Sprint-7 review 修正：验收生成物不入库（每次运行都变，会污染 PR diff）
verify/verify_e2e_result.json
verify/verify_e2e_openai_result.json
verify/verify_e2e_dashscope_result.json

# --- 用户问题②（2026-08-30）：中间态/实时结果/存档留证不入库（本地保留）---
# AgentOps runtime 账本与报告存档（实时态与留证；账本真相源=本地文件，非 git 追踪）
# TG-9：registry.json / doc-structure.json / *.lock 已由上面的 `agents/runtime/*` 目录级规则覆盖
agents/runs/
# 静态分析中间产物（可由 analyze_paperqa_system.py 再生成）
paper-qa-script/paperqa_callgraph.json
paper-qa-script/paperqa_function_interfaces.json
paper-qa-script/paperqa_function_inventory.json
docs/iteration/sprint/config_schema.json
# 截图留证（本地留存，不入库）
docs/iteration/sprint/*.png
verify/*.png

