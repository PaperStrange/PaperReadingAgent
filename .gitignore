# --- generated / runtime leftovers ---
__pycache__/
*.pyc
.pytest_cache/
node_modules/
dist/
.venv/
venv/
.DS_Store

# --- macOS AppleDouble junk ---
._*
__MACOSX/

# --- local data / logs ---
data/parsed/*.jsonl
# Sprint-3 远程源下载暂存（运行时生成，不入库）
data/remote/
# Sprint-4 用户自定义 provider（模板 providers.example.json 入库）
paper-qa-script/providers.json
# 用户本地论文目录（实测数据，不入库）
data/arxiv_pdf_sep/
tmp.log

# --- secrets (use .env.example as template) ---
.env
**/.env
.env.local
*.pem
*.key

# --- IDE ---
.idea/
.vscode/
verify/*.log
# Sprint-7 review 修正：验收生成物不入库（每次运行都变，会污染 PR diff）
verify/verify_e2e_result.json
